# 활성 일감 백로그 (2026-09-19 개설) — 8세대

> **다음 스레드는 이 문서만 읽으면 된다.** 배경은 **색인**만 있다 — 전문은 키워드가 걸릴 때만 연다(규칙 ⑥).

## 이 문서의 성격

**구독을 켜서 내보낸 뒤**의 기능 고도화 트랙이다. 앞 세대(`260917-260919_SUBSCRIPTION_LAUNCH_AND_GAMEPLAY_FLOW.md`,
#150~#178)가 수익화를 정리하고 **프로덕션 909**까지 내보냈다 — 이제 **출시가 막는 일감은 없다.**

⚠️ **그래서 우선순위 기준이 바뀌었다.** 앞 세대는 *"무엇이 출시를 막는가"* 로 줄을 세웠지만, 여기서는
**"스토어가 이미 약속한 것부터"** 다. 등재문 부제가 *"오프라인 AI 대국·**복기**"* 이고 상세 설명이
「대국 기록」을 적어 두었는데 **보는 화면이 아직 없다** — #156이 맨 위인 이유다.

⚠️ **출시 트랙(콘솔 작업)은 여기 오지 않는다** — `work/plans/GOOGLE_PLAY_LAUNCH_PLAN.md` §0이 정본이다(규칙 ④).

## 이 문서의 규칙

① `예정사항` → `진행중` → `완료사항`, 한 방향. 끝난 항목을 예정사항에 `[완료]`로 두지 말 것.
② 완료는 **한 줄 + 커밋 해시.**
③ 살아 있는 항목(진행중·예정사항)은 **전문 유지** — 다음 작업의 입력이다.
④ 콘솔 조작 상태는 `GOOGLE_PLAY_LAUNCH_PLAN.md` §0이 정본. 콘솔이 시킨 **코드** 일감만 여기.
⑤ 우선순위는 사용자가 정한다(예외: 출시를 **막는** 것이 위).
⑥ **배경은 색인만 둔다.** 함정 전문은 봉인 문서에 동결 — **색인의 키워드가 지금 일에 걸릴 때만** 편다.
  새 함정(51~)은 여기 전문으로 쓰고, 봉인할 때 줄인다.

## 서두 — 착수 전에 알 것

- **계보** 7세대 — 세대별 문서·범위는 `work/roadmap/README.md` 표. 설계 각주는 `260823-260830_OFFLINE_ENGAGEMENT_FEATURES_KICKOFF_PLAN.md`.
- **출시 상태**는 §0이 정본. 프로덕션 **905 / v0.9.5**(2026-09-17 승인). 출시 노트는 직전 **프로덕션**본 대비로 쓴다.
- **제스처는 계측 테스트가 없다** → 소스 계약 + 실기(`adb shell input motionevent DOWN/MOVE/UP`). 실기로 밟지 않은 것을 "됐다"고 말하지 말 것.
- **개발자 섹션 2단** — 1차(버전 10탭)는 release에도 실림, 2차('빌드 정보' 10탭)는 debug 전용. 권한을 만드는 컨트롤은 반드시 2차.
- ⚠️ **수익화 기조가 2026-09-18에 좁혀졌다 — 팔 것은 「프리미엄 월간 구독 3,900원」 하나뿐이다.**
  캐릭터 개별 판매(#160·#161·#18)와 프로모션 코드(#166)는 **폐기**했다(사유는 「폐기」 절). 그 이전의
  번복 이력은 **참고하지 말 것** — 유효한 기조는 #150 하나다.
- ✅ **콘솔 쪽 결제 준비는 2026-09-18에 끝났다** — 수익창출 열림 확인 · 옛 `premium_lifetime` 비활성화 ·
  구독 `premium_basic`/`monthly`/**3,900원**/177개국 생성. ⏸️ **기본 요금제는 일부러 비활성이다**(#150 4번).
  **다시 조사하거나 다시 만들지 말 것.**

## 핵심 동작 기조

**엔진 "아직"과 "실패"는 다른 상태다** — 가르는 질문 *"시간이 지나면 저절로 풀리는가?"* 풀리면 조용한 안내(문제시·차단·
준비 화면 금지), 안 풀리면(스텁 폴백·기동 실패) 팝업. 엔진이 필요한 곳은 대국 설정 안·대국 시작부터. 코드:
`DeferredEngineCoreApi`·`engineNotReadyToStart`·`EngineUnavailableNoticeDialog`. 전문은 `260906-260911_RELEASE_AND_EARLY_FEEDBACK.md` 「부록 B」.

**스토어가 하는 말은 앱이 하는 일과 같아야 한다** — 2026-09-16 거부가 가르친 것이다(함정 51·52). 기능을 지우거나
게이팅을 바꾸는 커밋은 `store_listing.txt`를 **같은 커밋에서** 훑는다.

## 신규 스레드 착수 프로토콜

**고정 프롬프트**(그대로 복사):

> 이번 스레드에서는 `work/roadmap/260919-_ACTIVE_BACKLOG.md` 이 파일의 내용을 파악하여 '예정사항'의 첫번째 항목을 파악하여 수행 담당해야합니다. 먼저 항목을 이해하셨다면 진행중으로 변경하고 작업 착수하시고, 결과물을 사용자와 논의 후 사용자가 완료 승인을 하면 문서에 완료 업데이트하고 당신의 스레드 업무가 종료되었다고 명시해주시면 됩니다.
>
> - 그 항목에 **미결 사용자 결정(U-번호)** 이 걸려 있으면 코드보다 **그것부터 묻는다.** 추측으로 만들지 말 것.
> - **실기로 밟지 않은 것을 "됐다"고 말하지 않는다.** 기기가 둘 이상이면 `make … TARGET=emu|phone`으로 대상을 명시하고, **실기기에 임의로 설치하지 않는다.**
> - **작업 트리를 다른 세션과 공유한다.** `git add -A`·`git checkout` 금지 — **내가 만진 파일만** 지정해 커밋한다.
> - 승인을 청할 때는 **사용자가 판정해야 할 것을 채팅에 펼쳐서** 낸다(파일 경로만 주지 말 것).

1. **읽기** — 이 문서 + 맡을 항목. 작업이 **함정 색인의 키워드**에 걸리면 그 번호의 전문만 봉인 문서 부록에서.
   저장·계정·보상 일이면 `…KICKOFF_PLAN.md` 2·3장. ⚠️ 봉인 문서를 통째로 읽지 말 것.
2. **상태 전환** — 예정사항 첫 항목을 진행중으로, `[진행중]`. 번호·제목·AI 모델·노력정도 유지.
3. **작업** — 그 항목 범위만. 다음 항목 선착수 금지.
4. **결정** — 코드로 풀리는 건 스스로, **제품·설계 결정만** 사용자에게. ⚠️ *"막혔을 때"* 가 아니라
   **착수 직후**다 — 맡은 항목의 미결 U-번호는 「사용자가 정해 줘야 하는 것」 표에 있고, 그걸 모른 채
   만들면 다 만든 뒤에 방향이 뒤집힌다(#164가 U-17~19를 먼저 물어 그 값을 피했다).
5. **완료** — 사용자 승인 후 완료 표에 한 줄 + 해시, *"이 스레드의 업무가 종료되었습니다"*.
6. **안내** — 다음 항목 번호만. "이어서"면 2~5단계를 항목마다 다시.

참고: **JDK 17** 필수(`export JAVA_HOME=/Library/Java/JavaVirtualMachines/temurin-17.jdk/Contents/Home`) · 검증
`./gradlew :shared:testDebugUnitTest :app-android:testDebugUnitTest`(shared를 건드렸으면 `:shared:compileKotlinIosSimulatorArm64
-PenableIosTargets=true`) · 기기가 둘이면 `make … TARGET=emu|phone` · AI 모델 표기는 사람이 고르는 참고값 · 스레드는 한 번에
하나 · 커밋은 `main` 직접, `feat(scope): … (backlog #N)`.
## ⚠️ 함정 색인 — 키워드가 지금 일에 걸릴 때만 전문을 연다

> **전문은 `260906-260911_RELEASE_AND_EARLY_FEEDBACK.md` 「부록 A」에 동결돼 있다**(번호 그대로. 45~50은 `260911-260916_GAMEPLAY_UX_AND_POLICY_COMPLIANCE.md`, **51~57은 `260917-260919_SUBSCRIPTION_LAUNCH_AND_GAMEPLAY_FLOW.md`**). 여기는 **색인**이다 —
> 한 줄로 걸리는지만 판단하고, 걸리면 그 번호의 전문을 읽는다(2026-09-11 사용자 지시). 2026-09-11 재검토에서
> **낡은 서술은 색인에서 바로잡았다**(⚠️ 표시) — 전문과 색인이 어긋나면 **색인이 옳다.**

**저장·상태**
- **1** enum 이름 = 저장 포맷 — 개명하면 기존 사용자 값이 조용히 기본값으로. R8은 AGP 기본 keep이 지킴, 검증은 `dexdump -d`
- **2** 설정 스냅샷 새 필드 — 자동저장이 새로 만들어 미배선 필드는 조용히 초기화. `current`에서 필요한 축만 덮어쓸 것
- **6** 정식 릴리즈 초기화 — `ReleaseResetCoordinator`가 권한 저장소 넷을 지운다. 새 권한 저장소는 목록에 추가, `CurrentResetGeneration` 함부로 올리지 말 것
- **12** 출석 시계 — `AppClock` 미사용(`System.currentTimeMillis` 직접). 날짜 조작은 `lastCheckInUtcDay` 되감기로만
- **13** 저장소 직접 쓰기 → 화면 상태 미갱신 — 지급 경로마다 되읽기 콜백. 출석 다이얼로그는 어떻게 닫아도 지급된다
- **14** 같은 권한에 지급 경로 둘 — 쉬운 쪽이 설계를 무력화. 정책을 옮길 땐 언급한 문서·주석도 grep, 판정 경로는 닫지 말 것
- **37** 백업 — ⚠️ **2026-09-22에 뒤집혔다**(#186): 제외 규칙 두 벌로 엔진 모델(94MB)을 빼 백업이 **된다**(약 2MB, 실측 복원 확인). `minSdk 26`이라 `fullBackupContent`+`dataExtractionRules` **둘 다** 필요. 문구·방침도 함께 조건형이 됐다(`LocalOnlyDataNoticeContractTest`)

**화면·레이아웃·문구**
- **3** 셸 예산 — 조이는 건 줄 수가 아니라 **상태 훅(여유 0)**. 새 상태는 그 상태를 쓰는 역할의 파일로. 숫자 정본은 `LayeringContractTest`
- **7** 다이얼로그 z순서 — 별도 윈도우라 선언 순서로 위아래가 안 정해진다. 명시적으로 게이트
- **9** 고정 dp 높이 금지 — 글자 상자가 폰트 배율에 잘린다. `heightIn(min)`·`IntrinsicSize`, 캐러셀은 계산, 원 안 글자는 dp로
- **10·10-2** 문구 그물 사각지대 — 리플렉션 그물은 String 필드만(`fun` 문구는 손 그물). 쓰임 확인은 이름으로 전 저장소 grep(`strings::`·문구가 문구를 부름·data class 4개 언어)
- **21** 폭 부족 — 레이아웃 비율 말고 **4개 언어 문구 길이**부터(CJK=2로 세기)
- **26** 앱 이름 — 번역되는 서술. `AppNameContractTest`: 런처 = 앱 안, 스토어 제목은 런처 이름으로 **시작**(⚠️ #137에서 "같다"→"시작한다"로 바뀜). 그물 밖이던 셋(`RuntimeAppName`·리포트 제목·클립보드 라벨)도 이제 한국어 런처 이름에 묶였다(⚠️ #137이 셋을 놓쳐 2026-09-11에 고치며 그물을 달았다)
- **38** 겹침 — z순서는 `MainActivity`의 `Box`(나중이 위). `layout(w,0)` 안 `Text` 미표시 → `onSizeChanged`+`offset`. 글꼴 배율은 1.0·1.3뿐
- **39** 안내 문구 — "무엇이 있다" 말고 "무엇을 하면 된다". 인용 라벨은 그 화면 글자 그대로, 상태로 바뀌는 라벨은 인용 금지
- **40** 조용한 기본값·반쪽 판정기·하나만 세는 게이트 — 폴백 기본값은 거짓말(`requireNotNull`), 한글 판정기는 섞인 영어를 못 봄, 팝업 게이트는 `GuideBlockingOverlays`
- **42** 비활성 표현 — 같은 배경이면 enabled가 글자 농도로만 보인다. 다른 축으로도, 화면 성격이 정해졌으면 조건 판정을 쓰지 말 것
- **43** 버튼과 응답이 다른 화면 — 옮길 땐 응답이 그려지는 곳도. `engineMessage`·`candidateText`는 화면에 안 보인다

- **45** 스크롤 부모 안에서 `min(가로,세로)`는 **늘 가로폭**이다 — `maxHeight`가 무한. 넓은 화면(폴드)에서만 터진다. `wm size 1812x2176`로 재현하고 끝나면 **`wm size reset`**. 스크롤 안에서 잰 내용 높이는 거짓(최소 높이가 넘어온다)
- **46** `rememberUpdatedState`는 **구조적** 동등성 — 지역 함수 참조(`::f`)는 새 클로저여도 `==`라 갱신이 걸러지고 **첫 컴포지션의 콜백이 계속 불린다**. 처방은 `referentialEqualityPolicy()` 또는 람다. `clickable`은 참조 비교라 멀쩡해 보인다

**제스처·입력·기기**
- **17** 스크롤 안 길게 누르기 — 조용히 실패, 트리거는 탭 수·위치로. ⚠️ 전문의 *"`input swipe`는 MOVE를 안 만든다"* 는 2026-09-11 실측과 어긋난다(스와이프가 스크롤에 빼앗겼다 = MOVE가 있었다)
- **30** adb 입력 — ⚠️ **2026-09-11 실측(API 35 에뮬레이터): `input tap`이 Compose 버튼·반상에 모두 닿았다.** 안 닿는 기기면 `input swipe x y x y 120`. 좌표는 `uiautomator dump`의 bounds
- **41** 회전·크기 변화 — 세로 고정은 큰 화면(sw≥600dp, targetSdk 36)에서 무시, `configChanges` 필수. 프로세스 종료는 별개('이어 하기')
- **44** 판이 끌기를 가진다(의도) — 판에서 시작한 스와이프는 스크롤 안 됨, 빠른 튕김은 착수. 소비하면 Final 단계 검사 금지

**테스트·검증**
- **15** `@Test` 누락은 조용히 사라진다(`TestAnnotationContractTest`). shared만 바뀌면 app 테스트가 쉰다 → `--rerun-tasks`
- **16** 테스트 건수 — `build/test-results` 누적 XML을 세지 말 것. 비우고 한 번의 실행만
- **19** 아무도 컴파일 안 하는 소스셋 — `make test`에 `compileDebugAndroidTestKotlin`. 시그니처 바꿀 땐 세 트리
- **20** 저장소 밖 상태가 초록을 만든다 — "이름이 서로 맞나"를 계약으로(AGP는 `.gz`를 푼다). 산출물은 직접 열어 볼 것
- **22** 소스 계약은 식별자를 고정 — 이름을 바꾸기보다 되돌리는 게 싸다
- **23** 스텁 폴백 재현법 — 에셋 치우기·`make release` 금지·병합 산출물 삭제·`pm clear`·원복
- **24** 초록 ≠ 안전 — 검사가 그 표기를 실제로 보는지 일부러 깨서 확인
- **32** 미사용 import — 암묵 사용(`getValue` 등)과 KDoc `[Symbol]`도 사용으로 셀 것

- **47** `input tap`은 down→up이 몇 ms라 **"누르고 있는 동안"에 걸린 결함을 못 잡는다** — 지속시간이 얽히면 `input swipe x y x y <ms>`. tap으로 통과한 것은 통과가 아니라 **안 해 본 것**
- **48** 판 위를 픽셀로 잴 때 **격자선도 어둡다** — 빈 자리는 교차점 대각선 25% 지점으로 판정, 크기는 지름 대신 **넓이**로. 그래도 **절대 배율의 증거는 못 된다**
- **49** `screencap`으로 1초 미만 Compose 애니메이션은 **볼 수 없고**, 그 눈멂이 '측정값'처럼 보인다 — **On/Off A/B로 계측기부터 검증**할 것(#145가 그 수치를 적었다가 철회했다). `screenrecord`도 같은 경로
- **50** `pm clear`는 **엔진 모델까지 지운다**(앱 데이터 안) → 조용히 스텁 AI. 뒤에 **반드시 `make seed-engine TARGET=…`**. 엔진 `Unavailable` 팝업이 셸 게이트 **첫 항**이라 엉뚱한 검증까지 막는다

**빌드·릴리스·콘솔**
- **4** R8 release — 스모크만으로 끝내지 말 것(대국·출석·1회권). ⚠️ *"release 실행 기록 없음"* 은 낡았다(813·901·902 실기, §0 B-3)
- **5** 산출물(AAB·스크린샷)은 화면 변경이 멎은 뒤에
- **11** 개발자 섹션 1차는 **release에도 실린다** — 게이트는 런타임 boolean(`DeveloperModeStore`, 버전 10탭). 권한을 만드는 버튼은 `BuildConfig.DEBUG`로 한 겹 더(→ 2차)
- **25** 빌드타입 블록에서 던지지 말 것 — 검사는 `packageRelease`/`packageReleaseBundle`에. `make`는 검사가 `bump-version`보다 앞
- **31** `src/friend/assets`는 지우지 말 것 — release AAB의 엔진(98MB, gitignore)
- **33** 16KB — ⓐ 우리 `.so` ELF 정렬 + ⓑ 패키징, ⓑ만으론 통과 못 함. 확인은 `PT_LOAD p_align`
- **34** Play "이전 출시 버전"은 **트랙별** — 번들 둘을 워크트리로 대조하기 전엔 원인을 말하지 말 것
- **35** `setContent` 앞에 SDK를 깨우지 말 것 — 기동 크래시(`StartupOrderContractTest`)
- **36** (참고) #123 헛다리 — EPIPE는 잡음, n=30은 표본 부족. 지난 크래시 원문은 `dumpsys dropbox --print data_app_crash`

- **51** 등재문이 앱보다 늦게 늙는다 — 그물(`AppNameContractTest`)이 **이름 하나만** 본다. 기능을 지우면 `[주요 기능]`·`[요금 안내]`·`[차별점]`을 **대괄호 표제로** grep
- **52** 거부문의 줄이 둘이면 정책도 둘 — Families(연령 선언으로 소멸)와 일반 Ads(살아남는다). AdMob 광고등급 기본값은 **MA**, 콘솔 값은 **심사관에게 안 보인다**(코드에도 박을 것). 이의신청은 집행당 1회
- **53** 16KB 지적은 *"프로덕션에 낡은 번들이 남았다"* 는 뜻일 수 있다 — 고치려 들기 전에 `PT_LOAD p_align`부터 읽을 것
- **54** 콘솔 폼을 프로그램으로 만지면 **화면만 바뀌고 모델은 안 바뀐다** — 실제 클릭 + 저장 뒤 재방문 확인. 「게시 개요」의 미제출 출시가 함께 나갈 수 있다
- **55** 저장소를 prefs → `filesDir`로 옮기면 `wipeToFreshInstall`이 **조용히 지나친다**(테스트는 전부 초록). 목록/본문을 가르고 스키마는 **키만** 더할 것
- **56** Play 앱 서명 앱은 **사이드로드로 결제를 검증할 수 없다** — 그런데 `queryPurchasesAsync`만 성공해서 속는다. 내부 테스트 업로드 → Play 설치가 유일한 길
- **57** 진단 로그를 보려고 개발자 모드를 켜면 **그 순간 검증 대상이 지워진다**(3시간마다 초기화). 실기 결제는 **UI로 판정**할 관찰 지점을 먼저 정할 것
- **58** IARC **새 설문지는 이전 답을 하나도 안 불러온다** — 전부 다시 채운다. 문항은 **앞 문항에 답해야 나타난다**
- **59** 스토어 애셋은 **파일명 순으로 안 들어간다**(업로드 완료 순). 한 장씩 넣을 것 + 라이브러리가 **내용 해시로 중복을 합친다**
- **60** 방침과 데이터 보안 양식이 **어긋나면 그 자체가 위반** — 한쪽만 고치지 말 것. AdMob이 무엇을 보내는지는 **Google이 발행한 매핑**을 따른다

**광고·결제**
- **8** `AndroidBillingClient` 공유 — 상품 종류는 **호출부가 선언**(#136에서 기본값 제거)
- **28** UMP — 콘솔 메시지 게시 전엔 `canRequestAds()`가 늘 true. CMP 미비는 수익만 준다(급한 건 데이터 보안)
- **29** 동의 폼 실기 — `addTestDeviceHashedId` 필요, 거부해도 limited 광고는 정상, 테스트 폼은 우리 메시지가 아니다

**문서·에셋**
- **18** 이 문서 스크립트 편집 — `---`로 항목 경계를 잡지 말 것(다음 항목 머리글로). 편집 뒤 출현 수를 세고 **읽은 뒤** 커밋, 편집·커밋을 한 호출에 묶지 말 것
- **27** 원형 아바타 — `_transparent` 원화에서 출발, 배경은 연결성으로 제거
### 이번 세대에 새로 생긴 함정 (58~) — 전문은 여기

58. **⚠️ Play Console IARC 「새 설문지」는 이전 답을 하나도 안 불러온다** (2026-09-19, #162)
    - 답 하나만 바꾸려 해도 **카테고리부터 전부 다시** 채워야 한다. 바꾸려던 항목만 보고 들어가면 나머지를 빈칸으로 제출한다.
    - ⚠️ **문항이 순차로 열린다** — 앞 문항에 답해야 다음 문항이 DOM에 나타난다. 참조(ref)를 미리 모아
      한 번에 클릭하면 **첫 클릭만 먹고 나머지는 조용히 버려진다.** 한 문항씩 답하고 눈으로 확인할 것.
    - ⚠️ **카테고리가 스토어 설정과 다를 수 있다** — 이 앱은 스토어가 `게임 > 보드`인데 IARC는
      `다른 모든 앱 유형`으로 받아 놨다. 바꾸면 설문 축이 통째로 달라져 **177개국 연령등급이 움직인다.**
      출시 직전에는 건드리지 않는 쪽이 옳다(2026-09-19 사용자 결정) — **정리할 거리로 남아 있다.**

59. **⚠️ 스토어 애셋은 파일명 순으로 들어가지 않는다 — 그리고 같은 그림은 하나로 합쳐진다** (2026-09-19, #162)
    - 8장을 한 번에 올리면 **업로드 완료 순**으로 들어간다. 실측 순서가 `03,04,05,02,06,01,08,07`이었다.
      **한 장씩 검색 → 선택 → 추가**해야 `01→08`이 된다.
    - ⚠️ **애셋 라이브러리는 내용 해시로 중복을 합친다.** 휴대전화/7인치/10인치 폴더의 파일이 바이트 단위로
      같으면 뒤의 업로드가 **통째로 무시된다**(*"애셋이 중복 삭제되었습니다"*). 태블릿 칸에도 **같은 애셋을
      그대로** 넣으면 된다 — 애셋 이름은 스토어에 보이지 않는다.
    - ⚠️ **휴대전화 스크린샷 상한은 8장이다**(10장 아님). 최소는 2장이라, 전량 교체할 때는 **지우고 나서 넣는다.**

60. **⚠️ 개인정보처리방침과 데이터 보안 양식은 한 몸이다 — 한쪽만 고치면 그 자체가 위반** (2026-09-19, #162)
    - 콘솔에 들어 있던 답이 **「아무것도 수집하지 않음」** 이었다. AdMob을 싣고 `AD_ID`를 선언하는 앱에서
      그것은 **과소 선언**이다 — 코드를 grep해서 보이는 것만 적으면 이렇게 된다.
    - **SDK가 무엇을 보내는지는 코드에 안 보인다.** Google이 발행하는
      [AdMob 매핑 안내](https://developers.google.com/admob/android/play-data-disclosure)를 **그대로** 따를 것:
      **IP 주소**(대략적 위치 추정) · **사용자 제품 상호작용** · **진단 정보** · **기기/계정 식별자** 넷이다.
    - ⚠️ **「금융 정보 = 아니오」는 전제에 기댄다** — 콘솔 도움말이 *"Google Play 결제 시스템을 활용하는 경우
      결제 서비스가 수집하는 데이터는 선언할 필요가 없다"* 고 **이름을 짚어** 적고 있다(조건 둘: 앱이 그 정보에
      액세스하지 않을 것 · 결제 서비스가 사용자로부터 직접 수집할 것). **구매 내역**은 *"기기에서 로컬로만
      처리되고 기기 밖으로 안 나간 데이터"* 예외에 걸린다.
    - **판정법**: 데이터 보안을 고쳤으면 **같은 날 방침도 연다.** 방침이 좁으면 넓히고, 우리 로그와 SDK 진단은
      **문단을 갈라 적을 것** — 한 문단에 섞으면 *"이 앱은 진단이 밖으로 안 나간다"* 로 읽힌다.

61. **⚠️ `UiStrings` 생성자의 여유는 **정확히 0칸**이다 — 문구를 더하면 앱은 컴파일되고 테스트만 무더기로 죽는다** (2026-09-20, #163)
    - `internal data class UiStrings(...)`에 문구 **9줄**을 더했더니 `:app-android:compileDebugKotlin`은 **통과**하고
      단위 테스트만 **570개 중 29개**가 `java.lang.ClassFormatError: Too many arguments in method signature`로 죽었다.
      실패 메시지가 *"문구를 너무 많이 더했다"* 고 말해 주지 않아 원인을 엉뚱한 데서 찾게 된다.
    - **범인은 생성자가 아니라 data class가 자동으로 만드는 `copy$default`다.** 그 정적 메서드는
      `인스턴스 1 + 필드 N + 기본값 비트마스크 ceil(N/32) + 마커 1` 칸을 쓴다 — 필드가 245개인 지금
      `1+245+8+1 = **정확히 255칸**`(JVM 한도)이다. **한 칸만 더해도 깨진다.**
    - **처방**: 새 문구는 생성자에 더하지 말고 **곁표 + 멤버 함수**로 뺀다(`UiStringsStudyCategories.kt`,
      선례 `UiStringsStudyVideos.kt`·`UiStringsBotCharacters.kt`). 함수는 이 산수에 끼지 않는다.
    - **그물**: `StudyHubContractTest.theUiStringsConstructorStaysUnderTheJvmArgumentLimit`이 칸수를 센다.

62. **⚠️ 이 작업 트리는 세션 여럿이 공유한다 — `git checkout`으로 되돌리면 남의 미커밋 작업이 날아간다** (2026-09-20, #163)
    - 실제로 당했다. `UiStrings.kt` + 4개 언어 파일을 `git checkout`으로 되돌렸는데, 그 직전 다른 세션이
      넣어 둔 `scoreLeadEstimateLabel`/`scoreLeadEvenLabel`(형세 보기 우세 레이블)이 함께 사라졌다.
    - ⚠️ **증상이 내 코드와 무관한 파일에서 난다** — `GamePlaySection.kt`의 `Unresolved reference`로 나타나
      원인을 내 쪽에서 찾으면 영원히 못 찾는다.
    - ⚠️ **세션 시작 시점의 `git status`가 "clean"인 것은 아무 보장도 아니다** — 남들이 그 뒤에 쓴다.
      되돌리기 전에 `git status --short`로 **내가 건드리지 않은 파일이 `M`으로 떠 있는지** 먼저 볼 것.
    - **복구 경로**: `~/.claude/projects/-Users-ryan9kim-worksoc-go-ai-coach/*.jsonl`에 각 세션의 `Edit` 호출이
      `old_string`/`new_string` 그대로 남아 있다. 최근 mtime의 jsonl을 `grep`해 그 편집을 다시 적용하면 된다
      (이번에 그렇게 온전히 복구했다).

63. **⚠️ 마이페이지의 "구독 관리·해지"는 실제 Google Play 구독관리 화면으로 딥링크된다 — 에뮬레이터라도
    로그인된 계정이 실제 개인 계정이면 실수로 진짜 구독을 건드릴 수 있다** (2026-09-21, #182)
    - `openSubscriptionManagement(context)`가 여는 화면은 **우리 앱 안이 아니다.** adb로 화면을 훑다가
      뒤로가기 좌표를 잘못 짚어 그 Play 스토어 화면 위에서 다른 곳을 탭했더니 **"Subscription will be
      canceled"** 토스트가 떴다 — 실제로 무엇이 취소됐는지 앱 쪽 로그로는 전혀 알 수 없다.
    - ⚠️ **에뮬레이터도 예외가 아니다.** 이 작업에 쓴 에뮬레이터에 로그인된 계정이 Play Points 2,429점의
      실사용 흔적이 있는 진짜 개인 Google 계정이었다 — "에뮬레이터니까 안전하다"는 전제가 깨진다.
    - **처방**: 프리미엄 배지·구독 진입점을 실기로 확인할 때는 다이얼로그의 "구독하기"/"취소" 버튼까지만
      누르고 멈출 것. "구독 관리·해지"(Play 스토어로 나가는 버튼)는 **누르지 않거나**, 누르더라도 그 뒤
      모든 좌표를 `uiautomator dump`로 정확히 구해서만 탭한다 — 감으로 좌표를 짚지 않는다.
    - ⚠️ **물리 기기가 함께 연결돼 있으면 `adb`가 대상을 못 정한다** — `adb devices -l`로 몇 대가 잡히는지
      먼저 확인하고, 여러 대면 모든 명령에 `-s <serial>`을 명시할 것(빠뜨리면 `gradlew installDebug` 같은
      태스크가 **연결된 기기 전부**에 설치한다 — 이번엔 다행히 그 폰도 이미 같은 디버그 빌드였어서 무해했다).

64. **⚠️ 읽기 전용 도해에서 `tentativeMove`를 "여기를 보세요" 마커로 돌려쓰지 말 것 — 뜻이 하나뿐이다** (2026-09-22, #164)

    `GoBoard`는 `tentativeMove`를 **`gameState.nextPlayer` 색의 깜빡이는 가늠돌**로 그린다
    (`GoBoard.kt:442`). 대국에서 그 뜻은 *"여기 둘까?"* 하나뿐인데, 도해에서 주의를 끄는
    범용 마커가 없다 보니 **아무 자리나 가리키는 데** 쓰고 싶어진다.
    - ⚠️ **착수금지·패 단원이 정확히 그 유혹에 걸린다.** 「여기엔 둘 수 없습니다」라고 적어
      놓고 그 자리에 가늠돌을 찍으면 **판은 "놓을 수 있다"고 말한다** — 글과 그림이 정반대다.
      입문자용 화면이라 더 나쁘다. 그 사람은 판을 믿지 글을 믿지 않는다.
    - **처방**: 뜻을 **하나로 못박고 그물을 단다.** `GoRuleStep.marker`의 뜻은 「다음은 여기」
      뿐이고, `GoRuleLessonsTest.everyMarkerPointsAtALegalMove`가 모든 마커를
      `LegalMoveGenerator.isLegalPlay`로 되묻는다. 못 둘 자리는 `marker = null`로 두고 문구가 맡는다.
    - ⚠️ 표현을 새로 만들고 싶더라도 **`GoBoard`를 건드리는 값이 비싸다** — 이 저장소에서 가장
      많이 불리는 컴포넌트다(호출부 7곳). 뜻을 좁히는 쪽이 싸다.

65. **⚠️ 이 앱의 글꼴 배율 상한은 2.0이 아니라 1.3이고, 레이아웃은 **영어**에서 먼저 터진다** (2026-09-22, #164)

    함정 9와 그 밖의 여러 주석이 *"글꼴 배율 2.0"* 을 말하지만 **이 앱은 시스템 배율을 따르지
    않는다**(`AppFontScalePolicy` — 2026-09-04 사용자 결정). 고를 수 있는 값은
    `AppFontScales = [1.0, 1.3]` **둘뿐**이다. `adb shell settings put system font_scale 2.0`은
    **아무 일도 하지 않는다** — 그걸 모르면 "2.0에서 확인했다"는 거짓 검증을 하게 된다.
    - **실기 방법**: 설정 화면의 「글꼴 크기 → 크게」. 시스템 설정이 아니다.
    - ⚠️ **네 언어 중 먼저 터지는 것은 영어다.** 함정 21이 *"CJK=2로 세라"* 고 하지만 그것은
      **한 줄 폭** 이야기이고, **문단 높이**는 다르다 — 같은 내용을 한국어보다 영어가 훨씬 긴
      글자 수로 적는다. #164의 가장 긴 본문은 한국어 220칸 대 **영어 257칸**이었고, 한국어
      1.3배는 멀쩡한데 **영어 1.3배에서 마지막 줄이 잘렸다.**
    - **처방**: 세로 공간을 비율로 나눌 때 **가장 긴 본문을 영어 · 1.3배로 실기에서 밟을 것.**
      하나가 전부를 대표한다(길이 순위는 소스에서 바로 셀 수 있다). 모자라면 **양보하는 쪽은
      그림이다** — 판은 작아져도 읽히고 글은 잘리면 못 읽는다.


66. **⚠️ 표식으로 잘라 재는 소스 계약은, 그 표식이 사라지면 조용히 파일 전체를 잰다** (2026-09-22, #183)

    `source.substringAfter("private fun …(")`로 한 함수만 떼어 재는 그물이 이 저장소에 여럿 있다.
    **`substringAfter`는 표식을 못 찾으면 문자열 전체를 돌려준다** — 이름을 바꾼 날, 그물은
    죽지 않고 **다른 것을 재기 시작한다.**
    - #183에서 `StudyRuleLessonScreen` → `StudyLessonScreen` 개명으로 실제로 걸렸다. 그때는
      **빨개져서** 알았지만(다른 함수의 `verticalScroll`을 재게 되어 실패), 순서가 반대였다면
      **초록인 채로** 아무것도 안 지키는 그물이 됐을 것이다. 함정 24의 *"초록 ≠ 안전"* 이
      계약 테스트 자신에게 적용되는 경우다.
    - **처방**: 자르기 전에 **표식의 존재를 먼저 단언한다.**
      `assertTrue("표식을 못 찾았다", source.contains(marker))` 한 줄이면 된다. 함정 22가
      *"이름을 바꾸기보다 되돌리는 게 싸다"* 고 말하는 이유가 여기에도 있다.

## 완료사항

> **한 줄 + 커밋만 남긴다**(규칙 ②). 상세가 필요하면 커밋을 볼 것.
> 다음 작업이 밟을 함정은 위 「함정」 절로 끌어올린다. 순서는 **완료 순서**(위가 최신).
>
> ⚠️ **#178까지의 완료 이력은 여기 없다** — 앞 세대 문서들에 봉인돼 있다.
> **세대가 넘어갈 때 완료 항목을 앞 세대로 되돌리지 않는다**(2026-08-31 규칙).

| # | 무엇을 했는가 | 커밋 |
| --- | --- | --- |
| 190 | **대국 종료 화면 — 승리 진영을 테두리로 알아보게 한다** — #187이 결과를 **글자로만** 남겨서 읽어야 알았다. 종국이 되면 좌석 카드 둘은 `isActiveTurn`이 둘 다 false라 회색으로 똑같아져, 그 구간에서 승패를 **색으로** 말해 주는 것이 하나도 없었다. 배지에 승자 진영색 테두리(2.5dp) + 트로피 옆 진영 글리프. ⚠️⚠️ **금색을 안 쓴 것이 핵심 제약이다** — `PremiumTheme.kt`가 「금색 = 프리미엄 기능」이라고 못박았고(2026-09-18 ⓐ안) 대국 메뉴의 프리미엄 옵션 셋이 그 규칙을 쓴다. 트로피 옆이라 **가장 고르기 쉬우면서 고르면 안 되는 색**이고, 두르면 같은 화면에서 금색이 두 가지를 뜻한다. ⚠️ 프라이머리(초록)도 금지 — 그쪽은 「지금 차례」(`ActiveStateBorder`). ⭐ **새 색을 하나도 만들지 않았다** — 좌석 카드가 이미 쓰는 진영색(흑 `Color.Black` · 백 **`Color.Gray`**)을 그대로 쓴다. 백이 흰색이 아닌 것이 요점이고(밝은 `surfaceVariant` 바탕에서 흰 테두리는 있으나 마나), **한쪽만 고치면 같은 화면에서 백이 두 색이 된다.** ⚠️ 무승부는 테두리 없음(아무도 안 이겼다 — 트로피가 빠지는 것과 같은 이유) ⚠️ 글리프가 **두 번째 통로**다(색맹·저대비에서 테두리 색만으로는 흑·백이 안 갈린다). 그물 셋은 *"어울려 보여서"* 바뀌기 쉬운 두 규칙을 코드로 고정한다. 실기 — 백 승 회색+○ · 흑 승 검정+● · 무승부 테두리 없음 · **글꼴 1.3배에서 안 잘림**. 기권 종국으로 밟았고 계가 종국은 같은 `winner` 값을 받아 경로가 갈리지 않는다 | `d2f1ecdf` |
| 189 | **가늠돌을 손가락보다 1.5칸 위에 그린다** — #188과 짝이다(확대 창을 없앤 대신 가늠돌이 손가락을 더 벗어나게 한다). `PlayDragLiftCells` 1f → 1.5f **한 줄**이 전부고, 그 파일 KDoc이 *"재조정은 이 한 줄이다"* 라고 미리 적어 둔 그대로였다. ⚠️ 칸 단위 유지(같은 28dp가 판 크기에 따라 0.45~1.94칸으로 벌어진다) ⛔ 터치 면적(`touchMajor`) 기반은 일부러 안 골랐다(사용자 결정 「50%만 올린다」 — Compose가 터치 크기를 공개 API로 안 주고 판의 입력 경로가 가장 예민하다, 함정 44·47). ⭐ **맨 아랫줄이 예상과 반대였다** — 등재할 때는 *"1.5칸이면 여백이 모자라 끌기로 사실상 안 닿는다"* 고 봤는데, 끌기는 판 밖으로 나가도 계속 따라가고 **착수 판정은 조준점만 본다.** 손가락이 판 카드 아래(좌석 카드 위)까지 내려가면 맨 아랫줄에 그대로 놓인다 — 판 아래에는 좌석 카드와 버튼이 늘 있어 손가락이 갈 자리가 있다. 「나빠진다」는 여전히 참이지만 못 쓰게 되진 않았고, 되물을 상태가 아니어서 그대로 진행했다. 실기(Pixel 7, 9x9, 격자 118px) 셋 — 손가락 y=981 → 돌 y=804(차이 177px = 정확히 1.5칸) · 맨 아랫줄 착수됨(손가락 y=1571, 카드 밖) · **그냥 탭은 띄움 없이 누른 자리**(탈출구). 전부 `input swipe`로 밟았다(`input tap`은 끌기가 아예 안 생긴다, 함정 47) | `d2d20418` |
| 188 | **설정 간소화 — 넷을 기능째 걷어내고, 기본값이 기존 사용자에게도 미치게 한다** — 2026-09-22 사용자 지시 넷. `지연 착수`·`착수 돋보기`·`돋보기 창 크기`·`돋보기 확대 배율`을 **플래그 뒤로 숨기지 않고 삭제**했다(`BoardMagnifier.kt`·`MagnifierSettings.kt` 파일째 + 설정 필드 셋 — 이 저장소는 죽은 스위치를 남기지 않는다, #151 선례). 메뉴는 사용자가 준 표와 일치. ⭐ **이 항목의 본체는 ④였다** — 「탐색 시간 기본값 10초」(#19047906)가 **신규 설치에만 닿고 있었다.** 저장된 값이 늘 이기기 때문이다. `settingsSchemaGeneration`을 두고 읽을 때 세대가 낮으면 **그 세대에서 뜻이 바뀐 필드만** 기본값으로 덮고 세대를 올린다(`SettingsSchemaMigration.kt`). ⚠️ **전체 초기화가 아니다**(언어·글꼴 배율·판 크기는 지킨다) ⚠️ **`ReleaseResetCoordinator`와 다른 축**(함정 6 — 묶으면 설정 고칠 때마다 권한이 날아간다) ⚠️ **디코드 폴백 0은 의도다** — 현재 세대로 폴백하면 정작 대상인 기존 사용자가 전부 빠져나간다 ⚠️ 자동저장 `copy` 카나리아에 배선(함정 2). ⭐ **끌어서 두기는 살아남았다** — 돋보기와 한 몸이 아니었고 오히려 확대 창이 그 위를 가리고 있었다(이번 삭제의 유일한 실질 위험이었고 실기로 먼저 확인했다). `GoBoard`의 `holdLiftPx`가 이제 늘 `liftPx`다 — 확대 창이 뜨면 0으로 떨구던 U-9의 규칙이 창과 함께 사라졌다. 테스트는 **지울 것과 고칠 것을 갈랐다** — 기능이 사라진 넷은 삭제, 뜻이 뒤집힌 둘은 방향을 반대로 돌려 남겼다(`PlayControlsContractTest`: 「메뉴에 있어야」→「없어야」). 실기(Pixel 7 에뮬레이터) 셋 전부 확인 — 넷이 사라졌고 · 끌어서 두기가 여전히 되고 · **3초로 저장돼 있던 기기에서 10초로 올라왔다**(`settingsSchemaGeneration: 1`). 1,359개 통과. `APP_IA_AND_UI_SPEC.md`에 메뉴 표 + 「기본값을 고치는 사람은 세대를 함께 올려야 한다」 | `97655d83` |
| 187 | **대국 종료 상태 화면에 「누가 어떻게 이겼는가」를 남긴다** — 「판정 결과」 팝업을 닫으면 결과를 볼 곳이 없었다(사용자 제보). 판 아래 좌석 카드 **둘 사이 가운데에 겹쳐** 세 줄을 띄운다: 🏆 / `백 승` / `(+11.5집)`. ⭐ **칸을 만들지 않고 겹쳤다** — 가운데에 칸을 더 두면 흑·백 반반 배분(#143)이 깨지고 그 폭은 1.3배에서 이미 빠듯하다(#107이 같은 자리에서 `Captures: 0`을 잘라 먹었다). ⚠️ **기권 판에는 `FinalScoreJudgement`가 아예 없다** — `Move.Resign`의 **반대편**을 승자로 삼고(기록과 같은 규칙) 셋째 줄은 `(불계)`다(빈 괄호는 고장으로 읽힌다). ⚠️ 둘째 줄은 팝업과 **같은 낱말**(`winnerWithoutMarginLabel`, 함정 39) — 새로 만든 건 셋째 줄 하나이고 거기서 「승」을 다시 말하지 않는다. ⚠️ 무승부엔 트로피를 달지 않는다. ⏸️ **폰 배치에만 붙였다** — 넓은 배치 둘은 다른 좌석 컴포넌트를 쓰고 가운데를 `WideScoreSummary`가 차지해 같은 겹침이 성립하지 않는다(**남은 몫**). `APP_IA_AND_UI_SPEC.md` §2.3-1-1 신설. 실기: 계가승(흑 승/+8.5집) · 기권승(백 승/불계) · 글꼴 크게 | `52862d42` |
| 169 | **「로그인 없이」를 판매 문구에서 걷어낸다** — 2026-09-18 결정의 **앞 절반**이다(뒤 절반 「로그인을 더한다」는 #170). ⚠️ **걷어낸 사유가 「거짓이어서」가 아니다** — 로그인은 여전히 비활성이고 그 말은 지금도 참이다. **장점으로 파는 것을 그만둔** 것이고, #170이 켜는 날 정반대로 뒤집히는 사고를 미리 막았다. 등재정보 세 곳(간단한 설명 → 「인터넷 없이도 그대로」 · 주요 기능 → **기기 백업**(#186에서 실제로 동작하게 된 것) · 차별점은 앞 절만 제거) + 앱 문구 하나(마이 페이지 고지 제목 → 「내 기록은 어떻게 보관되나요」, 4언어 — 본문이 #186에서 복원 안내가 되어 제목이 따로 놀던 것도 함께 해소). ⚠️ **로그인을 약속하는 문구는 넣지 않았다**(켜기 전에 적으면 함정 51의 재판) ⚠️ **방침의 「회원가입・로그인 기능이 없으며」도 건드리지 않았다**(지금 참이고 #170이 켜는 날 함께 고친다 — 방침이 앱보다 앞서가면 그 자체로 위반, 함정 60). 그물 신설: `theListingNoLongerSellsTheAbsenceOfSignIn` — 되돌리기 쉽고 되돌려도 **아무 테스트도 빨개지지 않던** 자리였다 | `14756b1f` |
| 186 | **안드로이드 자동 백업을 설계로 되살린다** — 이 앱의 「되돌릴 수 없어요」는 **설계가 아니라 파일 크기 덕분에** 참이었다. 매니페스트는 이미 `allowBackup="true"`인데 `filesDir`의 KataGo 모델 **94MB**가 쿼터 25MB를 넘겨 백업이 **통째로 실패**하고 있었다. 제외 규칙 두 벌로 그 무게만 덜어냈다. ⚠️ **`minSdk = 26`이라 둘 다 필요**하다(`backup_rules.xml` API 30↓ · `data_extraction_rules.xml` API 31↑) — 하나만 두면 한쪽 대역에서 조용히 무효이고 **최신 에뮬레이터에서는 보이지 않는다.** `device-transfer`도 따로 뺐다(안 빼면 새 폰 직접 전송 때 94MB가 따라간다). `app_webview`는 `domain="root"`다(`file`로 적으면 아무것도 안 빠지고 조용하다). ⭐ **실측**: `bmgr backupnow`가 `Size quota exceeded` → **`Success` 약 2.0MB**, 앱을 지우고 다시 깐 뒤 **닉네임·소모품(29/38/2)·기보 8건 전부 복원**되고 엔진 모델만 빠졌다. 문구는 소실 고지 → **복원 안내**로 네 언어를 다시 썼고 ⚠️ **「안전합니다」라고 쓰지 않았다**(24시간 주기·Wi-Fi/충전/유휴·사용자가 끌 수 있음 → "대부분"·"마지막 백업 시점"). 트립와이어도 **방향을 뒤집어** ① 규칙 파일 둘 ② 모델이 실제로 빠지는가(경로가 틀려도 빌드는 통과한다) ③ 문구가 소실도 안전도 단정하지 않는가를 본다. 함정 37 색인 갱신. ⚠️⚠️ **방침이 먼저 배포돼야 한다** — `rezen.dev` 방침을 조건형으로 고치고 시행일자를 올렸다(그 저장소 커밋 `d0fe7c9`). 앱이 먼저 나가면 배포된 방침이 앱 동작과 어긋나 그 자체로 위반이다(함정 60) | `1fcbc9ea` |
| 165 | **마이페이지 — 내 아바타·닉네임** — 앱 어디에도 사용자 자신을 가리키는 이름·그림이 **0건**이던 것을 처음 만들었다. 2026-09-22 결정 둘: 아바타는 **닉네임 첫 글자 + 색**(U-20 — 새 그림 0장·새 권한 0개), 노출은 **마이 페이지 안에서만**(U-21). ⭐ **갤러리 사진을 고르지 않은 것이 요점이다** — 사진 권한·백업 제외 규칙(함정 37)·데이터 보안 양식(함정 60)까지 번져 **이미 출시된 앱의 정책 문서를 고쳐야** 했다. 봇 캐릭터 그림도 빌리지 않았다(좌석 카드에서 내 아바타와 상대가 같은 그림으로 둘 뜬다, #155). ⚠️ 저장소는 `UserPreferencesStore`가 아니라 독립 prefs다(함정 2) · 접두사 `go_ai_coach_` · **정식 릴리즈 초기화에는 판단해서 뺐다**(권한이 아니라 취향, 함정 6). ⚠️ 팝업은 `*Dialog.kt` 별도 파일(인라인이면 첫돌이 가이드 그물 밖) · 입력칸을 팝업에 둔 것도 같은 결(`imePadding()` 전 저장소 0건). ⚠️ 아바타 원은 dp에 묶고 원 안 글자도 지름에 비례(함정 9) · 첫 글자는 **코드 포인트로** 자른다(`first()`면 이모지가 반 토막) · 색은 이름 해시로 결정적(난수면 껐다 켤 때마다 바뀐다). ⚠️ **「나」 줄을 구독 카드 위로 올리며 2026-09-18(#159) 결정을 뒤집었다** — 그때 지키려던 것이 *맨 위*가 아니라 「해지 경로가 스크롤 없이 닿는다」였음을 확인하고, 계약 테스트를 「맨 위」에서 「출석 절보다 위」로 고쳤다. ⏸️ **같은 날 「나」 줄을 껐다** — 기능이 덜 돼서가 아니라 **쓸 데가 없어서**다(사용자 지적). 코드는 전부 남기고 `ShowUserProfileRow`만 `false`로 두었고, 켜는 조건은 **U-53**이 든다. `APP_IA_AND_UI_SPEC.md` §2.3-2 신설 | `3de28fe9` · `5571743e` · `3ece1819` |
| 185 | **대국 → 복기 → 다시 두기를 한 흐름으로 잇는다** — 새로 지은 것은 **길뿐이다**(다시보기 #156도 분기 대국 #172도 이미 있었고, 둘로 가는 길이 「대국 기록」 화면 하나뿐이었다). 계가 팝업의 「재 대국」을 **「복기 하기」로 교체**(버튼은 계속 둘, 2026-09-18 결정 유지) · 종료 상태 하단을 **재 대국 / 복기 하기 / 대국 설정** 셋으로 · 다시보기의 **나가는 문을 둘로**(목록에서 온 것은 목록으로, 대국에서 온 것은 그 대국 화면으로) · **방금 끝난 그 판에서 갈라질 때는 덮어쓰기 경고를 띄우지 않는다**(슬롯에 남은 것이 자기 자신이라 경고문이 거짓이 된다). **U-52 해소 — 하단 「대국 시작」을 「재 대국」으로**(둘은 같은 이벤트를 쏘고 그 라벨은 종국 후에만 보이며, 문구를 갈라 뒀던 사유 주석은 **이미 끊어진 포인터**였다). ⭐ **화면·칸을 복사하지 않았다** — 하단 갈래를 *칸을 만드는 곳*에만 뒀다(같은 다섯 칸을 세 배치가 그리고 **좌우 기둥은 순서가 뒤집혀 있어** 「통과 오른쪽」이 성립하지 않는다). ⚠️ **실기에서 둘이 잡혔다**: ① 복기에서 돌아오면 **판정 결과가 다시 떴다**(「복기 하기」가 대국 화면을 컴포지션에서 빼서 「닫았음」 기억이 새로 만들어졌다) → 그 기억을 화면 밖으로 옮겼다 ② 끝난 판의 **무르기가 기권을 되돌리고 있었다** → 그 길을 걷어내도 좋다고 사용자가 확인(되살리려면 그 확인부터 뒤집을 것). ⚠️ 셸은 이 흐름의 상태를 하나도 안 든다(훅 42/42 유지) · 자동으로 여는 다시보기는 **수순 개수로 그 판이 맞는지 확인한다**(기록 붙이기가 아직 안 돌았으면 최신 기록은 직전 대국이고, 확인 없이 열면 조용히 남의 판을 보여 준다). `APP_IA_AND_UI_SPEC.md` §2.3-1 신설 | `5db9c452` · `98f3b53c` |
| 183 | **학습 콘텐츠 — 「바둑 기초 행마」** — 학습 허브의 두 번째 갈래를 열고 단원 다섯(이음과 끊음 / 뻗어서 달아나기 / 호구 / 한 칸 뜀 / 날일자, 도해 17장)을 채웠다. #164와 같은 **ⓐ+**라 U-17을 다시 묻지 않았다. 「규칙」 여섯 단원을 읽었다는 전제로 쓰여 있고(활로·단수·따냄을 다시 설명하지 않는다), **축·장문 같은 잡는 기술은 넣지 않았다** — 분류 소개문이 「돌을 잇고 끊는 기본 모양」만 약속했고 성격도 전투 쪽이다. ⭐ **화면을 복사하지 않고 갈래로 갈랐다** — `StudyLessonTrack` 하나를 두고 **화면 하나가 둘을 다 그린다.** 분류를 갈래로 옮기는 자리는 `studyLessonTrackFor` 하나이고 계약 테스트가 그것을 못박는다(레이아웃 비율이 실기로 정해진 값이라 두 벌이 되면 한쪽만 고치는 날이 온다, 함정 65). 그래서 **#184가 ⓐ+로 결론 나면 콘텐츠만 쓰면 된다.** ⚠️ 문구 표를 갈래별로 나눠 합치므로 단계 키에 `rule.`·`shape.` 접두사를 달았다 — 열쇠가 겹치면 한쪽 본문이 **말없이 덮이고** 화면에는 다른 갈래의 멀쩡한 문장이 뜬다(빈칸보다 알아채기 어렵다). ⚠️ **새 함정 66을 여기서 팠다** — 개명 때문에 소스 계약 하나가 조용해질 뻔했다(위 「함정」 절). `StudyLessonsTest`는 행마 단원이 가르치려는 것을 판에 되묻는다(호구 자리는 정말 둘 수 있는가 · 뻗으면 활로가 1→3→2인가 · 가른 돌이 정말 먼저 단수인가). `APP_IA_AND_UI_SPEC.md` §2.5를 갈래 둘로 고쳐 썼다. 실기(Pixel 7 에뮬레이터) 확인: 다섯 단원 · 한국어/영어 · 글꼴 기본/크게 | `ba42a4b8` · `92ade6f7` |
| 164 | **학습 콘텐츠 — 첫 카테고리를 끝까지** — 학습 허브의 「바둑 규칙 배우기」를 열고 단원 여섯(바둑판과 첫 수 / 활로 / 단수와 따냄 / 둘 수 없는 자리 / 패 / 집과 계가, 도해 23장)을 채웠다. 2026-09-22 사용자 결정 넷을 그대로 따랐다 — 첫 분류는 **「바둑 규칙 배우기」**(U-16의 선언 순서), 형식은 **ⓐ+ 정적 문구 + 도해 넘김**(U-17 — 문제 데이터 모델·정오 판정·진행도 저장소는 만들지 않았다), 문구는 **내가 네 언어 초안 → 사용자가 한국어만 검수**(U-18), 콘텐츠는 **바이너리에 못박는다**(U-19). ⭐ **도해를 좌표 표로 적지 않았다** — 출발 국면에 수순을 얹어 `BoardRules`로 접는다(`GoRuleLesson.statesByStep`). 따냄으로 돌이 사라지는 것도 패의 금지점이 서는 것도 규칙이 직접 만들어 **도해가 규칙과 어긋날 수 없고**, 위법수를 적으면 테스트가 죽는다. `GoRuleLessonsTest`는 그 위에 단원이 가르치려는 규칙 자체를 되묻는다(E5는 정말 둘 수 없는가 · 패는 정말 되따낼 수 없는가 · 집은 정말 27 대 36인가). ⚠️ **새 함정 64·65를 여기서 팠다** — 가늠돌의 뜻은 하나뿐이라는 것과, 이 앱의 글꼴 배율 상한이 2.0이 아니라 **1.3**이라는 것(위 「함정」 절). ⚠️ 판을 스크롤 부모에 넣지 않았다(함정 44, 선례는 다시보기) — 판과 설명이 남은 높이를 **비율로** 나눈다. ⚠️ 뒤로가기가 **세 겹**이 됐고(단원 → 목록 → 허브 → 홈) 겹마다 `BackHandler` 하나씩 두되 **한 번에 하나만 컴포즈**되게 갈랐다 — 셸은 이 화면을 전혀 모른다(훅 예산 42/42 유지). ⚠️ 문구는 곁표로 뺐다(함정 61 — 생성자 여유는 여전히 0칸). `APP_IA_AND_UI_SPEC.md` §2.5 신설. 실기(Pixel 7 에뮬레이터) 확인: 여섯 단원 전부 · 한국어/영어 · 글꼴 기본/크게 · 뒤로가기 세 겹 | `3b4f564d` · `04045305` |
| 181 | **홈 화면 카드 메뉴 정사각형 아이콘** — 대국 하기(마지막 선택 AI 캐릭터, 기본값 수제자 반상)·대국 기록(9x9 실전 종국 미리보기)·학습 하기(정적 국면 + 돋보기 배지) 세 카드에 아이콘 슬롯을 추가했다. 이후 아이콘 크기를 150%→90%(75.6dp)→90%(68dp)로 재조정하고 좌측 여백을 극단적으로 좁히는 등 여러 차례 다듬었다 | `544b3be6` · `41d8a384` · `e83f3fc5` · `5f15eefd` · `928daa83` · `fbd2437c` · `8cdf5703` |
| 179 | **카메라로 바둑판 인식해 분석하기** — CameraX 프리뷰 + 4점 코너 터치 보정 + 온디바이스 돌 검출(`GridStoneDetector`) + 대화형 국면 보정 에디터(`BoardCorrectionEditor`) + KataGo JSON 정적 국면 분석(`initialStones`/`initialPlayer` 주입, `EngineSession.syncStaticPosition`) 전체 파이프라인 구현. `FeatureId.BoardScan` 신설로 기존 4개 프리미엄 기능과 같은 게이팅(구독/광고 1시간)을 받고, 홈 화면 노출은 설정 화면의 실험실(Labs) 토글 뒤에 숨긴다. U-47(돌 배치만)·U-48(온디바이스)·U-49(구독 전용)·U-50(보정 UI) 전부 해소 | `ca626973` · `44f3d714` |
| 182 | **홈 화면 로고 배지 — 프리미엄 차별화 + 구독 진입점** — 구독 중이면 로고 원 테두리·타이틀을 금색으로, 미구독이면 금색 점선 테두리 + "Premium?" 라벨(아이콘 우상단, 원 테두리 옆 이어짐)을 띄운다. 게이팅은 `isPurchased`만(광고 1시간 제외), 라벨·아이콘 둘 다 탭하면 기존 `PremiumSubscribeDialog`가 뜬다. 같은 날 메뉴 카드 아이콘도 90%로 줄이고 여백을 좌측 0dp·상하우측 4dp로 재조정(#181 연장). 실기(에뮬레이터) 확인 완료. ⚠️ **함정 63** 참고 | `1f0ddaa7` · `fbd2437c` |
| 174 | **구독 만료 자체 판정 — 신선도 시계를 중간 완료로 정리한다** — 24시간 재조회·72시간(3일) 강등 로직은 2026-09-18에 배선·실기(Pixel 8 에뮬레이터) 확인까지 끝났다. 오프라인에서 `queryPurchasesAsync`가 캐시된 `Purchased`를 돌려주는 경우의 실측만 안 됐는데, **이미 결제를 마친 유저의 뒷단 안전장치 성격**이라 그 구멍이 남아 있어도 감수하고 다른 항목을 먼저 처리하기로 2026-09-20 사용자가 정했다 — 그래서 중간 완료로 닫는다. 남은 실측·처방(`ConnectivityManager`)과 대국 중 강등 시나리오(U-46) 점검은 **#180**으로 분리 | `acfa6871` · `e6454eeb` · `187d9ff1` · `9cd20e3e` |
| 163 | **학습하기 허브 껍데기 + 뒤로가기** — `StudyScreen`을 ① 허브 ② 「유튜브 강좌 보기」로 갈랐다. 하위 분류 **4개**(U-16, 2026-09-20): 유튜브 강좌 보기(열림) / 바둑 규칙 배우기 / 바둑 기초 행마 / 기본 사활 공부하기 — 뒤의 셋은 **회색 + 「준비 중」 배지, 비활성**(`clickable`을 끄는 게 아니라 **아예 달지 않는다** — 리플 잔상·접근성 오독 방지). ⭐ **진짜 일이던 뒤로가기는 실기로 여섯 경로를 전부 밟았다**(Pixel 7 에뮬레이터): 유튜브 목록에서 시스템 뒤로가기 → **허브**(홈으로 안 튄다) · 상단 화살표 → 허브 · 허브에서 둘 다 → 홈 · 「준비 중」 행 탭 → 무반응 · 강좌 행 탭 → 유튜브 앱. ⚠️ **`GoCoachApp.kt`는 한 줄도 안 건드렸다**(훅 예산 42/42 유지) — 하위 분류는 화면이 소유하고 `StudyVideoListScreen`의 중첩 `BackHandler`가 잡는다(#156 선례). ⚠️ **새 함정 61을 여기서 팠다 — `UiStrings` 생성자의 여유가 정확히 0이다.** 문구 9줄을 생성자에 더했더니 **앱은 멀쩡히 컴파일되고 테스트만 570개 중 29개가** `ClassFormatError`로 죽었다. 범인은 `copy$default`(`1+필드245+비트마스크8+마커1 = 정확히 255칸`, JVM 한도) — **한 칸만 더해도 깨진다.** 곁표 `UiStringsStudyCategories.kt` + 멤버 함수로 뺐고 `StudyHubContractTest`가 칸수를 센다. ⚠️ **#164는 여기에 줄을 더하지 말 것** — 줄은 이미 있다. `StudyCategory.available`을 `true`로 돌리고 `StudyScreen`의 `when`에 화면을 잇는다. `APP_IA_AND_UI_SPEC.md` §2.4 신설 + §2.1의 낡은 "준비 중 토스트" 기술 정정 | `7cb36b75` |
| 151 | **(backlog #151 + 반응형 스코프 수정)** 대국 중 착수 평가(추천 수) 탐색이 대국 진행을 방해하지 않게 정책을 뒤집는다 — U-35(2026-09-18)가 "추천 수 보기"를 꺼도 사람 차례마다 강제로 엔진 후보수 탐색을 걸게 만들었는데, 그 탐색 자체가 대국 진행을 방해한다는 사용자 제보(2026-09-20)로 강제-OR을 걷어냈다. ⚠️ **새 백로그 항목 착수가 아니라 반응형 수정이다** — 사용자가 실기로 겪은 불편을 그 자리에서 되짚어 고쳤다. 자동 탐색은 이제 `topMovesEnabled`(추천 수 보기)·`showMoveReviewEnabled`(구독자 전용 착수 평가) 둘 중 하나를 **사용자가 명시적으로 켰을 때만** 돈다 — 둘 다 꺼져 있으면 전혀 안 돈다. ⭐ **리플레이의 큰 실수 표시는 그대로 유지된다** — 이미 공짜로 기록되는 형세 스냅샷(매 수 엔진 동기화의 부산물)의 앞뒤 점수차로 대국이 끝나 기록되는 시점에 계산한다(`deriveMoveReviewMarkersFromScoreSwing`, 새 엔진 호출 없음). ⚠️ `moveEvaluations`는 더 이상 호출부가 파라미터로 넘기지 않는다 — `runGameHistoryAppendIfCompleted`가 `scoreSnapshots`로부터 직접 계산해 **까먹을 수 없는 구조**로 바꿨다. U-35가 살던 `ReplayRecordingPolicy.kt`는 삭제 — 더 이상 아무것도 강제하지 않아 죽은 스위치를 남기지 않았다. 실기(Pixel 7 에뮬레이터) 확인 완료 | `ade68cb1` |
| 172 | **현 지점부터 커스텀 새 대국하기** — 다시보기의 수순 N에서 그 국면을 그대로 물려받아 새 대국을 시작한다(원본 리플레이는 읽기만, 결과는 완전히 새 기록). ⭐ **새 진입점을 파지 않았다** — 착수 전 조사는 `NewGameController`에 통로 하나가 빠졌다고 봤지만, 분기가 필요로 하는 것(수순이 실린 국면에서 시작·엔진 동기화·좌석 설정 복원·세션/매치 제너레이션·턴 시계 리셋)이 **「이어하기」(`SavedSessionController.restore`)에 이미 전부** 있어 `SavedGameSnapshot`을 지어 그 길에 태웠다. ⚠️ **끝난 국면에서는 비활성 + 사유 문구** — 다시보기는 마지막 수에서 열리므로(#156) 기권·종국 판에서는 그 자리가 가장 먼저 눌린다. ⚠️ **셸은 2줄만 늘었다**(훅 42/42 유지) — 덮어쓰기 경고 상태는 대국 기록 화면이 소유한다. ⚠️ **덮어쓰기 경고의 본문은 홈 것을 베끼지 않았다** — `overwriteWarningMessage`가 *"대국 설정으로 이동하시겠습니까?"* 로 끝나는데 분기는 그 화면을 거치지 않는다(함정 39). 제목만 공유한다. 착수 중 정한 셋: 버튼은 조작부 **맨 아래 단독 줄**(이동 버튼 옆이면 오작동) · 형세는 **분기점까지 상속**(복원 경로가 이미 그 필드를 받아 비용 0, 착수 평가는 이어하기와 같게 미상속) · 슬롯 충돌이 없으면 **확인 없이 바로 시작**(라벨이 "8수부터 새 대국"이라 이미 말한다) | `1b9e5574` |
| 156 | 대국 다시보기 화면 — 목록 행(`hasReplay`만) → 수순대로 되짚는 판(읽기 전용) + 큰 실수 구간(10집 이상 잃은 수, 칩으로 점프) + 형세 그래프(재지 않은 판은 "0"이 아니라 빈 상태를 정직하게 보여 준다). ⭐ **이 앱 첫 배너**를 여기 붙였다(2026-08-09 `PREMIUM_MODE.md`가 예약해 둔 자리, 구독자 제외는 `SubscriptionAwareBannerAd`가 관문 하나로). ⭐ **중첩 `BackHandler` 첫 사례** — 셸은 다시보기를 모른다(목적지가 아니라 대국 기록 화면의 하위 상태, 훅 예산 42/42). ⚠️ 세로 차례·수순 타이틀·조작부는 **사용자 피드백으로 두 번 다시 정했다**: 광고 → 큰 실수 → 사석·승률 → 판 → 조작부, 타이틀은 "17수"가 아니라 "17"(체크박스 "수순 표시"가 옆에), 하단은 이동 버튼 넷 + **예비 버튼 둘**("형세 보기"·"추천 수", 엔진이 리플레이에 개입할 날을 위해 항상 비활성으로 자리만). 목적문의 "그 자리의 추천수까지"는 **남겨 뒀다** — 지나간 국면을 엔진에 다시 묻는 일이라 성격이 달라 별도 착수가 맞다. ⚠️ **완결 처리 이후 한 번 더 손댔다**(2026-09-19) — 수순 슬라이더를 없애고, 그 역할(위치를 훑는 것)을 형세 그래프의 **기본 확장 상태**로 넘겼다(이동은 이제 버튼·실착 칩뿐) | `18bbe628` · `8f83ef44` · `67a677b2` · `1b299641` |

---

## 진행중

> 지금 손대고 있는 것과, **코드는 끝나고 사용자 행동만 남은** 것.

## 예정사항

> **순서가 곧 우선순위다**(규칙 ⑤). 위에서부터 집는다.
>
> ⚠️ **2026-09-18 사용자 리뷰로 범위가 크게 줄었다.** 캐릭터 개별 판매(#160·#161·#18)와 프로모션 코드(#166)는
> **폐기**했고, 결제는 **프리미엄 월간 구독 하나**로 좁혔다. 아래 「폐기」 절에 사유가 있다 — 지우지 않는다.
>
> ⚠️ **기준선은 v0.9.5다.** 그 이전에 번복된 수익화 논의(영구 구매 ↔ 구독 ↔ 캐릭터 판매)는 **참고하지 말 것.**
> 지금 유효한 기조는 #150 하나다.

### 우선순위 요약표

| # | 제목 | 모델/노력 | 이 자리인 이유 |
|---|---|---|---|
| 184 | 학습 콘텐츠 「기본 사활」 | Opus/최대 | **2026-09-22 사용자 승인 순서.** 성격이 ⓑ라 U-51 결정 뒤 |
| 170 | 로그인 부활 + 백업 동기화 | Opus/최대 | 사용자 결정으로 방향은 확정. 비용 대부분이 코드 밖 |
| 155 | 좌석 카드에 상대 캐릭터 얼굴 | Opus/높음 | **사용자 지시로 후순위** — 몰입도 고도화 |
| 167 | 상대 캐릭터 표정 변화 | Opus/중간 | 155 뒤. 자산이 병목 |
| 153 | 제스처 영역 회피 | Opus/높음 | **152의 진단이 지목했을 때만** 착수 |
| 168 | 전체 수순 번호 오버레이 | Opus/중간 | 156이 부족하다고 판정될 때만 |
| 171 | DID/VC 타당성 결정 문서 | Opus/중간 | 검토 성격. 답을 안 적으면 같은 질문이 다시 열린다 |
| 180 | 구독 오프라인 조회 실측 + 보정 | Sonnet/낮음 | **2026-09-20 #174에서 분리.** 실측 의존 — 사용자가 후순위로 지정, 기회가 생기면 |
| 120·121·122 | 앞 세대에서 넘어온 것 | — | 아래 별도 절 |

---

### 184. **학습 콘텐츠 — 「기본 사활」** (AI 모델: Opus, 노력정도: 최대) — **U-51 결정 뒤**

- ⚠️ **앞의 둘(#164·#183)과 같은 일이 아니다.** 사활은 읽어서가 아니라 **풀어서** 배운다 —
  ⓐ+로 만들면 반쪽이 된다. 제대로 하면 **문제 데이터 모델 · 정오 판정 · 진행도 저장소**가
  새로 생긴다(#164가 일부러 만들지 않은 셋).
- ⚠️ **저장소가 생기는 순간 두 규칙이 걸린다**: `UserPreferencesSnapshot`에 넣지 말 것(미배선
  필드가 조용히 초기화된다, 함정 2 · 메모리 `preferences-autosave-pattern`), 접두사는 `go_ai_coach_`.
  새 저장소는 `ReleaseResetCoordinator` 목록에 넣을지도 **판단해서 적을 것**(함정 6 — 진행도는
  권한이 아니므로 아마 넣지 않는다. 판단을 안 적으면 다음 사람이 다시 묻는다).
- ⚠️ **엔진은 필요 없다**(`BoardRules`가 순수 KMP). 진짜 난제는 **정답이 하나가 아니라는 것**이다 —
  사활 문제는 정답 수순이 여럿이고 오답 변화마다 응수가 필요하다. *"수순 하나를 정답으로 박는다"* 로
  시작하면 조금만 다르게 두어도 틀렸다고 말하는 화면이 된다.
- 사용자 결정: **U-51**(ⓐ+로 갈 것인가 ⓑ로 갈 것인가 — ⓑ면 위의 셋이 전부 따라온다).

### 170. **로그인 부활 + 백업 동기화** (AI 모델: Opus, 노력정도: 최대) — 169 뒤

- ⚠️ **#186을 먼저 읽을 것**(2026-09-22 신설) — 기기 교체 복구는 **서버 없이도 대부분 된다**.
  이 항목은 그것이 못 하는 것(백업을 끈 사용자 · 여러 기기 동시 · 크로스 플랫폼 · 서버가
  필요한 기능의 토대)을 위한 것이지, *"복구하려면 이것뿐"* 이 아니다. 둘의 비용 차가 크다.
- ⚠️ **로그인은 미완성이라 꺼진 게 아니라 완성된 채로 꺼졌다** — Google·이메일 로그인, 인앱 계정 삭제, 온보딩 4버튼,
  4개 언어 문구가 전부 살아 있고 실기 검증까지 끝났다. 끈 사유는 하나 — *"실질 기능 0 대비 정책/UX 비용만 있는 상태"*
  (`LOGIN_AND_ACCOUNT_SYSTEM.md:154`). **백업이 그 빠진 실질 기능이다.**
- ⚠️ **플래그를 켜도 온보딩은 안 뜬다** — `FirstRunGate`가 화면보다 먼저 `hasSeenOnboarding=true`를 박는다(#140).
  가르려면 새 스냅샷 필드 → **autosave 카나리아 배선**(함정 2) + `GuideProgressStore.arm()`이 같은 `if`에 얹혀 있어
  계약이 함께 깨진다.
- ⚠️ **릴리스 SHA-1 미등록** — `google-services.json`에 등록된 건 **디버그 키 하나뿐**이고 릴리스 업로드 키·
  Play 앱 서명 인증서가 없다. **릴리스 서명 빌드 실기 검증이 첫 관문**이다. ⚠️ json이 없는 기계는
  `R.string.default_web_client_id` 폴백이 0건이라 **컴파일 자체가 깨진다**.
- ⚠️ **저장소 포트 6종이 전부 동기**라 Firestore를 그 뒤에 못 끼운다 → 로컬 정본 + **옆의** 동기화 계층.
- ⚠️ **충돌 해소가 자료형마다 다르다** — 출석은 max, **소모품은 차감형이라 max 병합이 곧 재화 복제**
  (`ConsumableInventory.kt:14-18`), 캐릭터는 합집합.
- ⚠️⚠️ **저장소 안에 전면 개방 Firestore 규칙이 있다** — `scripts/remote-engine-mq-prototype/firestore.rules:8-15`가
  `allow read, write: if true`이고 옆 `firebase.json`이 그것을 가리킨다. **그 디렉터리에서 `firebase deploy`를 한 번
  돌리면 `project-baduk-hanpan` DB 전체가 공개된다.** 배포 대상에서 격리할 것.
- ⚠️ 계정 삭제는 **새 웹페이지가 의무가 아니다**(`mailto:`로 요건 충족, 이미 등록됨). 진짜 의무는
  `deleteAccount()`가 Auth 계정만 지우고 **Firestore 문서는 남긴다**는 것(`LOGIN_AND_ACCOUNT_SYSTEM.md:149`).
- 사용자 결정: **U-32**(충돌 규칙, 자료형별 표). 콘솔·방침 작업은 **사용자 몫**.

### 155. **대국 좌석 카드에 상대 캐릭터 얼굴** (AI 모델: Opus, 노력정도: 높음) — ⏸️ **후순위**

⏸️ **2026-09-18 사용자**: *"대국 진행 중의 몰입도를 높이는 고도화 아이디어 — 후순위로 미뤄도 된다."*

- 대국 화면에 캐릭터 그림이 **0건**이다. 자리는 `GameStatusPanel.kt`의 `PlayerSeatCard`(:282)·`CompactSeatCard`(:221),
  호출부 6곳. 그림 5장·아바타 컴포저블·`forPlayLevel`·CompositionLocal이 전부 이미 있다.
- ⚠️ **`WideGameLayoutContractTest`가 넓은 배치 두 함수를 `between(선언A, 선언B)` 텍스트 구간으로 자른다** —
  그 사이에 헬퍼를 선언하면 엉뚱한 구간을 검사하거나 터진다. `PlayControlsContractTest`는 인자 **순서**를 못박는다.
- ⚠️ **5장은 얼굴이 아니라 전신 일러스트다** — 22~40dp에서 원형 클립하면 판·주머니가 잘린 작은 전신이다.
- 사용자 결정: **U-10**(전신 축소 수용 vs 얼굴 크롭 5장 신규).

### 167. **상대 캐릭터 표정 변화** (AI 모델: Opus, 노력정도: 중간) — ⏸️ **후순위**, 155 뒤

- ⚠️ **`botAvatarRes`/`AllBotAvatarRes` 쌍에 넣으면 스플래시가 표정을 랜덤으로 띄운다.** `AppSplashContractTest`의
  `listed` 정규식이 `AllBotAvatarRes` **뒤 파일 끝까지**를 훑어 같은 파일 안엔 안전한 자리가 없다 — **새 파일**로.
- ⚠️ **표정 그림에는 그물이 없다** — `BotCharacterAvatarTest`는 `BotCharacterCatalog.all`만 순회하므로 카탈로그 밖
  변형은 384×384·VP8X 검사를 **하나도 안 받는다**. 파일명 규칙 기반 새 검사를 산출물에 포함할 것.
- ⚠️ **표정을 몰 신호 중 무료로 항상 도는 것은 차례·착수 발생·사석·승패뿐**이다(151의 층과 같은 제약).
- 사용자 결정: **U-26**(표정 자산을 만들 것인가·몇 종 — 원화는 `work/artwork/*_transparent.png` 5장뿐) · **U-27**(무엇을 '상황'으로).

### 153. **판이 화면 끝까지 갈 때 시스템 제스처 영역 회피** (AI 모델: Opus, 노력정도: 높음) — 152의 진단이 지목할 때만

> ⏸️ **#152가 지목하지 않았다 — 착수 보류.** 2026-09-18에 #152가 실기로 확인한 것은 **히트박스(ⓐ)뿐**이고,
> 그것은 허용오차를 반 칸으로 넓혀 해소됐다.
>
> ⚠️ **축 ⓑ(제스처 영역)는 아직 열려 있다 — 다만 "실기기 손가락 테스트" 항목으로만 남긴다**(2026-09-18 사용자).
> 갤럭시 S23에서 `navigation_mode=2`로 바꿔 좌우 끝 열을 두드렸을 때 **6/6 성공**했고 이 창의 좌우
> `systemGestures` 인셋도 `[0,0][0,0]`이었다. 그러나 **`adb shell input tap`은 주입 이벤트**라, 실제 손가락과
> 같은 경로로 시스템 제스처 감시를 지나는지 확신할 수 없다 — **이 결과로 ⓑ를 "없다"고 단정하지 말 것.**
> · **되살릴 조건**: 사람이 직접 손가락으로, 제스처 내비 기기에서, 좌우 **맨 끝 열**을 눌렀을 때
>   *"가늠돌이 떴다 사라진다"* 가 관찰되면 그때 ⓑ가 확증된다(#152가 정한 감별기).

- 자리는 `GamePlaySection.kt:1093-1104`(`expandBeyondScreenPadding`). 저장소 전체에 `systemGesture*`가 **0건**.
- ⚠️ **`BoardFitTest.kt:104-108`이 `"Modifier.expandBeyondScreenPadding()"`를 문자열 그대로 찾고 차례까지 본다** —
  인자를 붙이는 순간 **#139 얘기로 읽히는 실패 메시지**가 뜬다.
- ⚠️ `systemGestureExclusion()`은 변당 **200dp까지만** 존중되는데 판 높이는 ~390dp다 — **전부는 못 살린다.**
- 사용자 결정: **U-6**(귀를 살릴지 변 가운데를 살릴지) · **U-7**(새 설정이면 메뉴 12칸 차례의 어디에 — #149가 *"사용자가 정한 것"*).

### 168. **전체 수순 번호 오버레이** (AI 모델: Opus, 노력정도: 중간) — 156이 부족할 때만

- ⚠️ 156의 "한 단계"가 아니다. `currentMoveNumberAt`이 `gameState.stones`에서 파생되므로(`GoBoard.kt:877-884`)
  **잡힌 돌의 번호가 사라진다** — 종이 기보처럼 보이려면 데이터 소스를 `moves`로 바꾸고 같은 자리 재착수(패·되따냄)
  번호 중첩 규칙까지 정해야 한다.
- 사용자 결정: **U-28**(되짚기만으로 충분한가).

### 171. **로컬 키 기반 DID/VC 타당성 — 결정 문서** (AI 모델: Opus, 노력정도: 중간)

- ⚠️ **2026-09-22 사용자와 오간 문답으로 세 갈래가 갈렸다** — 결정 문서는 이 셋을 **따로** 적어야
  한다. 한 낱말로 *"무의미"* 라고 적으면 틀린다(실제로 한 번 그렇게 적었다가 정정했다).
  · **로그인 「대체」로는 못 쓴다.** Keystore 키는 **그 기기에만 있어** 기기를 바꾸면 함께 잃는다
    — 풀려는 문제(기기 교체 시 데이터 유실)를 정확히 못 푼다. 이 앱엔 이미 같은 성질의 로컬
    식별자가 있다(`DeviceIdentityStore`의 UUID = 「계정 없이 시작하기」의 실체).
  · **로그인 「강화」로는 성립한다 — 다만 패스키가 우월하다.** 기기 키로 소유를 증명하는 것은
    실제 보안 요소이고, 그것의 표준 구현이 **패스키(Credential Manager/FIDO2)**다. 이 앱은 이미
    Credential Manager를 쓰고 있고(Google 로그인), 패스키는 **기기 간 동기화가 된다** —
    did:key의 치명적 약점이 없다. did:key는 사실상 **동기화가 안 되는 패스키**다.
  · ⚠️ **그 전에 물을 것: 지금 무엇을 지키나.** 앱 계정 뒤에 있는 것은 출석 도장판·1회권·
    캐릭터이고 **구독은 Play가 따로 지킨다.** 금전 가치가 있는 자산이 계정 뒤에 없으면 2FA는
    비용(이해·복구 경로·지원 문의)만 남는다. **위협 모델을 먼저 적을 것.**
  · did:key **고유의** 값은 서버 없는 **제3자 검증**뿐이고, 그것이 값을 갖는 시나리오는 아래
    Stage F 하나다.
- 검토 성격이라 맨 뒤. 다만 **답을 적어 두지 않으면 반드시 다시 열린다** — 결론이 "하지 않는다"여도 지우지 말 것(#121 선례).
- ⚠️ 문서가 적어야 할 세 문장: ⓐ **검증자가 없으면 VC는 `claimedBots`와 구별되지 않는다** ⓑ 발급자가 앱 자신이면
  **하드웨어 키도 위조를 못 막는다**(Keystore는 키 유출을 막을 뿐) ⓒ 무르기 무제한·추천수 노출 때문에 이 앱의
  "이겼다"는 **실력의 증거가 아니다**.
- ⚠️ 소급 발급은 반쪽만 가능하다 — `PlayerSetupJsonCodec`에 스키마 버전이 없어 **5단계 재정의 이전 기록의 level 3은
  지금 다른 캐릭터**를 가리키고, `Resign`은 승패를 담지 않는다.
- 값을 갖는 유일한 실증 시나리오는 **Stage F 원격 엔진 보상 원장**(서로를 믿지 않는 피어의 기여 정산)이다 —
  그 트랙이 살아나면 성격이 '검토'에서 '선행'으로 바뀐다.
- 사용자 결정: **U-33**(제품 기능인가 PoC인가) · **U-34**(검증자를 만들 의사가 있는가).

---
### 180. **구독 오프라인 조회 실측 + 필요시 `ConnectivityManager` 보정** (AI 모델: Sonnet, 노력정도: 낮음) — ⏸️ 실측 기회가 생길 때까지 보류

✅ **2026-09-20 사용자 결정으로 #174에서 분리** — #174(신선도 시계: 24시간 재조회·72시간 강등)는 배선·실기
확인까지 끝나 중간 완료로 닫았다. 이미 결제를 마친 유저의 뒷단 안전장치 성격이라 아래 구멍이 남아 있어도
다른 항목을 먼저 처리한다.

- **전제**: 오프라인에서 `queryPurchasesAsync`가 캐시된 `Purchased`를 돌려주면 확인 시각이 계속 갱신돼
  72시간 강등이 영영 안 걸릴 수 있다 — 아직 **실측되지 않았다.** 판정법·처방 전문은
  `work/plans/PREMIUM_MODE.md`의 **「⛔ 아직 실측되지 않은 전제」** 절 — **그 절을 건드리지 말 것.**
- ⚠️ **에뮬레이터로는 확인할 수 없다**(구글 계정이 없어 결제 연결이 `BILLING_UNAVAILABLE`) · 사이드로드도 안 된다(함정 56).
  **라이선스 테스터 + Play 설치**가 유일한 길이다.
- 참이면 처방: 확인 "성공" 조건에 그 순간 실제 인터넷 연결 여부(`ConnectivityManager`)를 더한다.
- **함께 점검할 것**(U-46, #174에서 미해소로 넘어옴): 72시간 한도를 넘는 순간 사용자가 대국 중이면 그 판을
  끝까지 허용할지 즉시 잠글지 — 지금 `decideStaleSubscriptionDowngrade`는 대국 상태를 안 가리고 즉시 내린다.
- ⚠️ **결함으로 다시 제기하지 말 것** — 사용자가 후순위로 정한 상태다(2026-09-20). 실측할 기회가 생기면 그때 연다.

---
### 앞 세대에서 넘어온 것 — 위 개수에 세지 않는다

> 사용자 지시(2026-09-06): **미뤄 둔 항목도 지우지 말고 등록해 두고 순위만 뒤로 둔다.**

120. **AGP 9.0 이상으로 업그레이드** (AI 모델: Opus, 노력정도: 높음) [⚠️ **전제부터 다시 볼 것**]
    - ⚠️ **이 항목을 만든 근거가 사라졌다**(2026-09-06) — 812 업로드 뒤 콘솔 권장조치가 하나도 뜨지 않는다.
      #119를 고치자 AGP 9.0 줄까지 함께 사라진 것으로 보인다(같은 「R8 최적화」 아래 묶여 있었을 가능성).
      **착수 전 첫 질문은 "AGP 9.0이 아직도 필요한가"다.** 콘솔이 요구하지 않으면 Play 지적 대응이 아니라
      일반 툴체인 최신화이고, 급하지 않다. ⚠️ 다만 *"권장조치가 사라졌으니 최신이다"* 로 읽지 말 것 — 지금도 **8.13.2**다.
    - **착수 전 조사**: ⓐ AGP 9.0이 요구하는 Gradle 최소 버전 ⓑ KMP 플러그인·Compose 컴파일러 호환
      ⓒ 9.0에서 제거된 DSL이 이 저장소에 있는가(`buildConfig`·`namespace`·`useLegacyPackaging` 주변)
      ⓓ `libs.versions.toml` 한 줄로 끝나는지.
    - **부수 효과**: 9.0은 `android.r8.optimizedResourceShrinking`을 기본값으로 만든다 → #119가 넣은 줄을 **이 항목에서 지운다.**
    - ⚠️ **끝나면 §0 B-3을 다시 밟을 것** — R8이 함께 올라가 함정 1(enum 상수 이름 = 저장 포맷)이 다시 열린다.
      **컴파일 초록은 그 위험을 하나도 못 본다.**

121. **`middleware` 게이트웨이를 KMP 모듈로 분리** (AI 모델: Opus, 노력정도: 높음) [#117에서 이월]
    - 2026-09-06에 **#117로 한 번 종결됐던 항목**이다 — 참조 0인 3파일이라 죽은 코드로 지울 후보였는데,
      사용자 판단은 *"죽은 코드가 아니라 **모듈 분리를 위해 세워 둔 뼈대**이고 그 분리를 나중에 한다"* 였다.
    - ⚠️ **지우려는 유혹이 다시 올 것이다** — 죽은 코드를 훑는 스레드가 같은 3파일을 또 찾아낸다.
      **이 항목이 그 답이다: 지우지 않는다.**
    - ⚠️ 정말 지우게 된다면 `LayeringContractTest`의 **존재 단언을 같은 커밋에서 함께** 걷어낼 것.

122. **봉인 문서 다섯을 지울지 판단** (AI 모델: Sonnet, 노력정도: 낮음)
    - 저장소 문서 보존 정책은 *"완료되고 결론이 상위 문서에 흡수된 문서는 삭제한다, git 히스토리가 아카이브다"* 인데,
      봉인 백로그 다섯은 **아직 실제로 참조된다**(함정 1~50의 전문이 거기 있다).
    - **판단 시점을 "정식 출시 이후"로 잡아 둔 것이 이 항목이다.** ✅ 정식 출시는 2026-09-17에 됐다(905) —
      **이제 판단할 수 있다.**
    - 조건: 노하우와 결정사항이 상위 문서로 **놓치는 것 없이** 흡수됐는지 확인한 뒤에만 지운다.
      ⚠️ 함정 색인이 번호로 전문을 부르므로, 지우려면 **전문을 어디로 옮길지 먼저 정해야 한다.**

---
### ⛔ 폐기 — 되살리려면 여기 사유부터 뒤집을 것 (2026-09-18 사용자 결정)

> 지우지 않는다. 다음에 같은 제안이 올라올 때 **이 절이 답이다**(#121 선례).

- **#160 캐릭터 4종 판매 배선** · **#161 캐릭터 특전 재정의** · **#18 봇 캐릭터 개별 구매** — **폐기.**
  사유(사용자): *"광고·출석으로 이미 받을 수 있는데 추가 결제 구매까지 넣으면 복잡도가 증가한다.
  오히려 프리미엄 구독으로 해소하게 하면 모든 게 심플하다."*
  · ⭐ **부수 효과**: 캐릭터 특전(`isBotCharacterPerkActive`)은 **유료 캐릭터 구매 유도 장치**였으므로 목적이 사라졌다.
    지금 코드는 발동 조건이 *"상대가 `Purchase` 캐릭터인가"* 인데 **그런 캐릭터가 0종**이라 **이미 영구 휴면**이다 —
    건드리지 않으면 아무 일도 일어나지 않는다. `FEATURE_ACCESS_PRINCIPLES.md` 옛 8.3-2의 "매판 N회" 정책은
    **#150에서 걷어냈다**(지금은 8.2가 폐기 사유를 들고 있다)(팔지 않는 것의 특전을 남겨 두면 다음 스레드가 구현하려 든다).
  · ⚠️ 결제 **코드**는 지우지 않는다 — `AndroidBillingClient`가 구독과 공유하고, 되살릴 때 다시 쓴다.
- **#166 프로모션 코드(출석 보상 일괄 지급)** — **폐기.** 사유(사용자): *"로직을 건드리지 말자."*
  · 근거였던 것: 「회차 점프」가 `AttendanceCheckIn.kt:60-62`가 명시적으로 금지한 **이중 경로**이고,
    `copy(attendanceCount = 7)`은 오래 쓴 사용자의 출석일을 **되돌릴 수 없이 깎으며**, "+7 가산"은 28일차에 착지해
    **관장 천원이 공짜로 나간다.** 로컬만으로는 「1기기 1회」도 보장되지 않는다.
  · ⚠️ 출시 프로모션이 필요하면 **Play 콘솔의 구독 오퍼**로 한다(#159의 첫 달 900원) — 앱 로직을 건드리지 않는다.


---
### 사용자가 정해 줘야 하는 것

> ✅ **2026-09-18 리뷰로 해소된 것**: U-30(정체성 뒤집기 → **선택적 로그인으로 확정**, #169) ·
> U-14·U-15·U-22·U-25(캐릭터 판매 폐기로 소멸) · U-23·U-24(프로모션 코드 폐기로 소멸) ·
> U-2·U-3(구독 단일 3,900원으로 확정 — 상품 ID `premium_basic`/`monthly`).
>
> ✅ **2026-09-19 #172 착수 전 사전 정리로 해소된 것**: U-38~40(아래 표 참고 — #172 본문에 전문).
>
> ✅ **2026-09-22 #164 착수 전 결정**: U-17·U-18·U-19(아래 표 참고).

| 코드 | 항목 | 물어야 할 것 |
|---|---|---|
| ~~U-1·U-42~~ | 150 | ✅ **2026-09-18 전부 해소·실행됨** — 수익창출 열림 확인 · `premium_lifetime` 비활성화 완료 · `premium_basic`/`monthly`/3,900원/177개국 생성 완료 |
| **U-43** | 158 | **기본 요금제를 언제 활성화할 것인가** — 라이선스 테스터 실결제에는 활성이 필요하지만, 활성화하면 등재문과 다시 어긋난다. ⓐ 그때 등재문도 함께 고친다 / ⓑ 테스트 기간 배지 노출을 감수한다 — ✅ **2026-09-18 해소: 사용자가 기본 요금제를 활성화했다.** ⚠️ 그래서 지금은 사실상 ⓑ(테스트 기간 배지 노출 감수) 상태다 — **등재문 정정은 아직 남아 있고 #162의 몫**이다 |
| **U-35** | 151 | **무료 대국에도 손실집수를 계산해 저장할 것인가**(표시는 게이팅 유지). 안 하면 다시보기의 실착 지표가 사실상 구독자 전용이 된다 — ✅ **2026-09-18 해소 — 저장한다**(무료 대국도 계산·저장, 표시는 게이팅 유지) |
| U-4 | 151 | 대국 기록 보관 개수 상한 — ✅ **2026-09-18 해소 — 1,000개 AND 100MB 두 겹**(+100KB 초과 시 경고 방안 검토) |
| U-5 | 152 | 빗나간 탭을 무시할지 가장 가까운 교점에 놓을지 |
| U-9 | 154 | 꾹 누름 임계(확대창) 후에도 가늠돌을 띄울지 — ✅ **2026-09-18 해소**: 돋보기가 **뜨는 경우에만** 내린다(꺼져 있으면 유지) |
| **U-36** | 159 | **첫 달 900원을 낼 것인가 · 기간·조건.** ⚠️ 한국의 구독료 인상 opt-in 규정이 도입 할인의 정가 전환에 걸리는지 **확인 후** 설계 — ✅ **2026-09-18 해소: 첫 달 금액 할인은 하지 않는다.** *"하더라도 7일 무료체험 등을 할 예정이고, 당장은 고려 안 해도 됨"*(사용자) |
| ~~U-44~~ | 174 | ✅ **2026-09-18 구현으로 해소** — ⓑ(마지막 성공 조회 + 유예 한도) 채택, `SubscriptionFreshnessPolicy` 배선 |
| ~~U-45~~ | 174 | ✅ **72시간(3일) 유예로 확정** — 사용자 제안(3일)과 일치 |
| U-46 | 180 | **한도 초과 순간 대국 중이면** 어떻게 할 것인가 — 그 판은 끝까지 허용할지, 즉시 잠글지(지금 구현은 대국 상태를 안 가리고 즉시 내린다) — #174에서 #180으로 이월 |
| ~~U-11~13, U-37~~ | 156 | ✅ **2026-09-19 전부 해소** — 무료 / **배너는 다시보기 헤더 아래**(2026-08-09 예약분 집행, 구독자 제외) / 목록만 / 10집 고정 |
| ~~U-38~40~~ | 172 | ✅ **2026-09-19 전부 해소** — 무관한 다른 진행 중 대국만 기존 덮어쓰기 경고 재사용 / 설정은 그대로 물려받음(설정 화면 재진입 없음) / 원본과 구분 표시 불필요 |
| ~~U-16~~ | 163 | ✅ **2026-09-20 해소** — 하위 분류 **4개**: 유튜브 강좌 보기 / 바둑 규칙 배우기 / 바둑 기초 행마 / 기본 사활 공부하기. 미구현은 **회색 + 「준비 중」 배지**(비활성) |
| ~~U-17~19~~ | 164 | ✅ **2026-09-22 전부 해소** — **ⓐ+ 정적 문구 + 도해 넘김**(문제 풀이·정오 판정·진행도 저장소는 만들지 않는다) / **내가 네 언어 초안 → 사용자는 한국어만 검수** / **바이너리에 못박는다**(원격 배포는 열지 않는다 — 데이터 보안 양식까지 번진다, 함정 60). ⚠️ **다음 분류(기초 행마·기본 사활)에 그대로 물려주지 말 것** — 사활은 성격이 ⓑ라 U-17을 다시 물어야 한다 |
| ~~U-52~~ | 185 | ✅ **2026-09-22 해소 — 바꿨다**(「대국 시작」 → 「재 대국」). 둘은 같은 이벤트를 쏘고 그 라벨은 종국 후에만 보이며, 문구를 갈라 뒀던 사유 주석은 이미 끊어진 포인터였다 |
| ~~U-20·21~~ | 165 | ✅ **2026-09-22 해소** — **닉네임 첫 글자 + 색**(새 그림 0장·새 권한 0개) / **마이 페이지 안에서만**. ⚠️ 그래서 **닉네임이 지금은 아무 데도 안 쓰인다** — 같은 날 사용자가 그 점을 지적했다(U-53) |
| **U-53** | 186 | **닉네임을 어디에 쓸 것인가 — 아니면 쓸 데가 생길 때까지 감출 것인가.** 2026-09-22 사용자 지적: *"닉네임 쓸 일이 없어서 당장 노출하는 게 맞는가 의문"*. #165가 U-21을 「마이 페이지 안에서만」으로 고른 결과다 |
| ~~U-55~~ | 188·189 | ✅ **2026-09-22 착수 전 전부 해소** — 돋보기는 **기능 자체 삭제** / 가늠돌은 **50%만 위로**(터치 면적 기반은 범위 밖) / 설정 초기화는 **스키마 버전 + 바뀐 필드만** / 항목은 **둘로** 나눈다. **다음 스레드는 질문 없이 착수한다** |
| **U-51** | 184 | **사활을 ⓐ+(읽는 해설)로 갈 것인가 ⓑ(푸는 문제)로 갈 것인가.** ⚠️ #164의 U-17 답을 물려받지 말 것 — 거기는 규칙 해설이라 ⓐ+가 맞았지만, 사활은 읽기만 하면 반쪽이다. ⓑ면 문제 모델·정오 판정·진행도 저장소가 전부 따라온다 |
| **U-54** | 186 | **안드로이드 자동 백업을 되살릴 것인가** — 되살리면 형제 저장소(`rezen.dev`)의 개인정보처리방침 *"앱을 삭제하는 즉시 함께 파기됩니다"* 를 **먼저** 조건형으로 고쳐야 한다(이 저장소 테스트가 닿지 못하는 문장). / **대국 기보까지 백업할 것인가**(크기 판단) |
| U-31, **U-41** | 169 | 백업 범위(소모성 재화를 올릴지) / **안드로이드 자동 백업을 함께 살릴 것인가**(로그인 없는 사용자의 안전망) |
| U-32 | 170 | 충돌 해소 규칙, 자료형별 표 |
| U-10 | 155 | 전신 축소를 받아들일지, 얼굴 크롭 5장을 새로 뽑을지 |
| U-26·27 | 167 | 표정 자산을 만들 것인가·몇 종 / 무엇을 '상황'으로 볼 것인가 |
| U-6·7 | 153 | 귀를 살릴지 변 가운데를 살릴지 / 새 설정이면 메뉴 차례의 어디에 |
| U-28 | 168 | 되짚기만으로 충분한가 |
| U-33·34 | 171 | 제품 기능인가 기술 PoC인가 / 검증자를 만들 의사가 있는가 |
| **U-47~50** | 179 | 무엇을 인식하는가 / 온디바이스인가 클라우드인가 / 무료인가 구독 전용인가 / 보정 UI를 어디까지 |

**착수 전 반드시**: 결제 항목(#157~#162)에 손대기 전에 **#150의 「이미 끝낸 것」 네 줄을 읽을 것** —
콘솔 작업은 2026-09-18에 끝났고, 다시 하면 되돌리는 일이 된다.

---
## 관련 문서

- `260917-260919_SUBSCRIPTION_LAUNCH_AND_GAMEPLAY_FLOW.md` — 앞 세대(#150~#178). **봉인** · **함정 51~57 전문**
- `260919-260919_RELEASE_909_HANDOFF.md` — 909 출시 인수인계. **완결** — 그날의 순서와 틀렸던 전제가 남아 있다
- `260911-260916_GAMEPLAY_UX_AND_POLICY_COMPLIANCE.md` — 그 앞 세대(#138~#149). **봉인** · 함정 45~50 전문
- `260906-260911_RELEASE_AND_EARLY_FEEDBACK.md` — 그 앞 세대(#118~#137). **봉인** · ⭐ **함정 1~44·기조·문서 규칙 배경의 전문(부록 A~C)**
- `work/plans/GOOGLE_PLAY_LAUNCH_PLAN.md` — **출시 트랙의 진행 상태 정본**(§0). 콘솔 작업은 전부 그쪽이다(규칙 ④)
- `work/plans/FEATURE_ACCESS_PRINCIPLES.md` — **8장이 수익화 정본**(구독 하나). 보상·게이팅 설계의 전제
- `work/plans/PREMIUM_MODE.md` — 프리미엄/구독 동작 정본. #174의 미확정 전제가 그 문서에 있다
- `docs/spec/APP_IA_AND_UI_SPEC.md` — 화면 명세. #156·#163·#165가 여기에 자리를 만든다
