# 활성 백로그 (9세대) — 1.0 이후

> **이 파일 하나가 새 스레드의 진입점이다.** 아래 「신규 스레드 착수 프로토콜」이 전부다.
> 8세대까지는 `260919-260922_STUDY_CONTENT_AND_1_0_RELEASE.md`에 봉인돼 있다.

**세대 시작: 2026-09-23 · 기준선: 1.0.0(versionCode 10000) 프로덕션 라이브**

---

## 이 문서가 답하는 질문은 **하나**다

**"지금 무엇을 할 것인가."**

나머지 둘은 **다른 곳이 이미 답한다** — 이 구분이 9세대 개편의 전부다.

| 질문 | 답하는 곳 | 여기 쓰지 않는 이유 |
| --- | --- | --- |
| 무엇을 했는가 | **git 히스토리** (`git log --grep "backlog #NNN"`) | 8세대에서 이것이 **28KB, 파일의 31%**를 차지했다 |
| 왜 그렇게 했는가 | **코드 KDoc** — 이 저장소는 사유를 코드 옆에 두는 관행이 이미 강하다 | 같은 사유가 백로그에도 있으면 **둘이 어긋나는 날** 어느 쪽이 맞는지 알 수 없다 |
| 무엇을 밟게 될 것인가 | **여기 「함정 색인」** | 이것만은 과거가 아니라 **미래**라 백로그의 일이다 |

⚠️ **완료 줄은 「한 줄 + 커밋」이다. 250자를 넘기지 않는다.**
8세대에도 같은 규칙이 있었지만 **검사가 없어서** 한 줄이 1,500자까지 자랐다. 규칙이 틀린 게
아니라 규칙을 지키는지 아무도 안 봤다. 그래서 이번에는 **세는 법**을 함께 적는다:

```sh
python3 scripts/check-backlog-done-lines.py
```

**스레드를 닫기 전에 한 번 돌린다.**

⚠️ **처음에는 이 자리에 `awk` 한 줄을 적어 뒀다가 2026-09-23에 스크립트로 바꿨다.**
`awk`의 `length()`는 **바이트를 센다** — 한글은 1자가 3바이트라 250바이트 ≈ 83자가 되어
규칙이 뜻한 것보다 세 배 엄했고, 정규식이 「완료」 표뿐 아니라 「다음」의 **우선순위 요약표까지**
잡았다. **틀린 검사는 검사가 없는 것보다 나쁘다**(함정 24: 초록 ≠ 안전) — 세는 단위와 범위를
코드로 못박은 이유다.
⚠️ 길게 쓰고 싶어지면 그것은 **커밋 메시지에 쓸 말**이다 — 거기는 길이 제한이 없고, 그게 정본이다.

---

## 이 문서의 규칙

1. **순서가 곧 우선순위다.** 「다음」의 맨 위가 다음에 집을 것이다.
2. **완료는 한 줄 + 커밋.** 250자 이내(위 참조).
3. **함정 번호는 영구불변.** 죽은 함정도 번호를 재사용하지 않는다 — 옛 문서의 「함정 44」가
   다른 것을 가리키게 되는 순간 그 문서가 전부 거짓이 된다.
4. **성격이 다른 것을 한 절에 섞지 않는다.** 「다음」은 지금 집을 수 있는 것만이다
   (9세대 신설 — 사유는 바로 아래).
5. **미결 결정(U-번호)은 착수 **전**에 묻는다.** 절반 만들고 묻는 것이 가장 비싸다.

### ⚠️ 규칙 ④를 신설한 이유 — 8세대에서 실제로 걸린 것

8세대의 「예정사항」 10건 중 **4건은 집을 수 없는 것**이었다:
`153`·`168`은 *"다른 항목이 문제라고 지목할 때만"*, `180`은 *"실측 기회가 생길 때까지"*,
`171`은 **코드가 아니라 결정 문서**. 그런데 프로토콜은 *"예정사항의 첫 번째 항목을 맡으라"*
고 한다 — **조건부 항목이 첫 줄로 올라오는 순간 스레드는 못 움직이거나, 조건을 무시하고
착수한다.** 그래서 9세대는 셋을 갈랐다: **「다음」(집을 수 있다) · 「대기」(방아쇠가 있다) ·
「서 있는 답」(일이 아니라 답이다)**.

---

## 핵심 동작 기조

- **실기로 확인하지 않은 것은 "됐다"고 하지 않는다.** 테스트 초록은 안전이 아니다(함정 24).
- **사용자 지시와 저장소 관행이 부딪히면 그대로 실행하지도, 거부하지도 않는다** — 둘을 모두
  만족시키는 해석을 찾아 제안하고, 그 판단을 기록에 남긴다(#190의 금색·#192의 이모지가 그 예다).
- **없는 것을 문구로 약속하지 않는다**(함정 51). 앱에 있는 것만 스토어·웹에 적는다.
- **사용자가 정한 것을 스레드가 뒤집지 않는다.** 뒤집을 근거가 생기면 **묻는다.**

---

## 신규 스레드 착수 프로토콜

**고정 프롬프트**(그대로 복사):

> 이번 스레드에서는 `work/roadmap/260923-_ACTIVE_BACKLOG.md`를 읽고 **「다음」의 첫 번째 항목**을
> 맡아 주세요. 항목을 이해했으면 **「지금」으로 옮기고** 착수하시고, 결과물을 저와 논의한 뒤
> 제가 완료를 승인하면 문서를 완료로 갱신하고 스레드 업무가 끝났다고 알려 주세요.

**착수 직후 할 것 넷** — 순서가 있다.

1. **맡은 항목의 미결 U-번호를 확인한다.** 「사용자 결정 대기」 표에 있다. 모른 채 절반을
   만들고 묻는 것이 가장 비싸다 — **질문은 지금 한다.**
2. **항목 본문의 ⚠️를 전부 읽는다.** 거기 적힌 것은 대부분 **이미 한 번 밟은 것**이다.
3. **키워드가 걸리는 함정만 전문을 연다.** 60개를 다 읽지 않는다 — 색인은 그러라고 있다.
4. **`git log --grep "backlog #NNN"`으로 그 번호의 과거를 본다.** 완료 줄이 짧은 것은
   정보를 버려서가 아니라 **정본이 거기 있어서**다.

⚠️ **기기가 둘 이상 붙어 있을 수 있다**(에뮬레이터 + 실기 폰). `./gradlew installDebug`는
**붙어 있는 전부에 설치한다** — 반드시 `make install-dev TARGET=emu`를 쓸 것. 2026-09-22에
사용자 폰에 잘못 설치한 적이 있다.

⚠️ **작업 트리를 여러 세션이 공유한다.** `git checkout`으로 되돌리면 **남의 미커밋 작업이
날아간다.** 커밋 전에 `git status`로 내 것이 아닌 변경이 섞였는지 본다.

**검증 명령**(JDK 17 필요):

```sh
export JAVA_HOME=/Library/Java/JavaVirtualMachines/temurin-17.jdk/Contents/Home
./gradlew :shared:testDebugUnitTest :app-android:testDebugUnitTest
```

`:shared`를 건드렸으면 iOS 타깃도:
`./gradlew :shared:compileKotlinIosSimulatorArm64 -PenableIosTargets=true`

---

## ⚠️ 함정 색인 — 키워드가 지금 일에 걸릴 때만 전문을 연다

> **전문은 전부 `docs/spec/PITFALLS.md`에 있다**(#194로 한 파일에 모았다, 2026-09-23).
> 번호로 찾아 **그 번호만** 편다 — 84건을 처음부터 읽지 않는다.
>
> ⚠️ **전문과 색인이 어긋나면 색인이 옳다** — 색인은 계속 고쳐 왔고 전문은 각 세대가 쓴 시점에
> 동결됐다. **전문을 고치지 말 것**: 새로 알게 된 것은 색인에 ⚠️로 적고, 새 함정이면 다음 번호를 딴다.
> ⚠️ **번호는 영구불변이다** — 죽은 함정도 번호를 재사용하지 않는다. 옛 문서의 「함정 44」가
> 다른 것을 가리키게 되는 순간 그 문서가 전부 거짓이 된다.

**저장·상태**
- **1** enum 이름 = 저장 포맷 — 개명하면 기존 사용자 값이 조용히 기본값으로. R8은 AGP 기본 keep이 지킴, 검증은 `dexdump -d`
- **2** 설정 스냅샷 새 필드 — 자동저장이 새로 만들어 미배선 필드는 조용히 초기화. `current`에서 필요한 축만 덮어쓸 것
- **6** 정식 릴리즈 초기화 — `ReleaseResetCoordinator`가 권한 저장소 넷을 지운다. 새 권한 저장소는 목록에 추가, `CurrentResetGeneration` 함부로 올리지 말 것
- **12** 출석 시계 — `AppClock` 미사용(`System.currentTimeMillis` 직접). 날짜 조작은 `lastCheckInUtcDay` 되감기로만
- **13** 저장소 직접 쓰기 → 화면 상태 미갱신 — 지급 경로마다 되읽기 콜백. 출석 다이얼로그는 어떻게 닫아도 지급된다
- **14** 같은 권한에 지급 경로 둘 — 쉬운 쪽이 설계를 무력화. 정책을 옮길 땐 언급한 문서·주석도 grep, 판정 경로는 닫지 말 것
- **37** 백업 — ⚠️ **2026-09-22에 뒤집혔다**(#186): 제외 규칙 두 벌로 엔진 모델(94MB)을 빼 백업이 **된다**(약 2MB, 실측 복원 확인). `minSdk 26`이라 `fullBackupContent`+`dataExtractionRules` **둘 다** 필요. 문구·방침도 함께 조건형이 됐다(`LocalOnlyDataNoticeContractTest`)

- **69** 저장 스키마는 필드 추가만, 버전은 불변 — `SchemaVersion`을 한 번이라도 올리면 이어하기·종국 결과·출석 일수·보유 캐릭터 등 진행도가 통째로 초기화된다(JSON 블롭 스토어 9개 중 8개가 마이그레이션 경로 없음, 등호 비교로 버전을 가름). 필드 추가는 `optInt`/`optDouble` 기본값 흡수로만, 스키마 상수를 건드리는 커밋은 승인 대상. 같은 계보: 함정 1·55

**화면·레이아웃·문구**
- **3** 셸 예산 — 조이는 건 줄 수가 아니라 **상태 훅(여유 0)**. 새 상태는 그 상태를 쓰는 역할의 파일로. 숫자 정본은 `LayeringContractTest`
- **7** 다이얼로그 z순서 — 별도 윈도우라 선언 순서로 위아래가 안 정해진다. 명시적으로 게이트
- **9** 고정 dp 높이 금지 — 글자 상자가 폰트 배율에 잘린다. `heightIn(min)`·`IntrinsicSize`, 캐러셀은 계산, 원 안 글자는 dp로
- **10·10-2** 문구 그물 사각지대 — 리플렉션 그물은 String 필드만(`fun` 문구는 손 그물). 쓰임 확인은 이름으로 전 저장소 grep(`strings::`·문구가 문구를 부름·data class 4개 언어)
- **21** 폭 부족 — 레이아웃 비율 말고 **4개 언어 문구 길이**부터(CJK=2로 세기)
- **26** 앱 이름 — 번역되는 서술. `AppNameContractTest`: 런처 = 앱 안, 스토어 제목은 런처 이름으로 **시작**(⚠️ #137에서 "같다"→"시작한다"로 바뀜). 그물 밖이던 셋(`RuntimeAppName`·리포트 제목·클립보드 라벨)도 이제 한국어 런처 이름에 묶였다(⚠️ #137이 셋을 놓쳐 2026-09-11에 고치며 그물을 달았다)
- **38** 겹침 — z순서는 `MainActivity`의 `Box`(나중이 위). `layout(w,0)` 안 `Text` 미표시 → `onSizeChanged`+`offset`. 글꼴 배율은 1.0·1.3뿐
- **39** 안내 문구 — "무엇이 있다" 말고 "무엇을 하면 된다". 인용 라벨은 그 화면 글자 그대로, 상태로 바뀌는 라벨은 인용 금지
- **40** 조용한 기본값·반쪽 판정기·하나만 세는 게이트 — 폴백 기본값은 거짓말(`requireNotNull`), 한글 판정기는 섞인 영어를 못 봄, 팝업 게이트는 `GuideBlockingOverlays`
- **42** 비활성 표현 — 같은 배경이면 enabled가 글자 농도로만 보인다. 다른 축으로도, 화면 성격이 정해졌으면 조건 판정을 쓰지 말 것
- **43** 버튼과 응답이 다른 화면 — 옮길 땐 응답이 그려지는 곳도. `engineMessage`·`candidateText`는 화면에 안 보인다

- **45** 스크롤 부모 안에서 `min(가로,세로)`는 **늘 가로폭**이다 — `maxHeight`가 무한. 넓은 화면(폴드)에서만 터진다. `wm size 1812x2176`로 재현하고 끝나면 **`wm size reset`**. 스크롤 안에서 잰 내용 높이는 거짓(최소 높이가 넘어온다)
- **46** `rememberUpdatedState`는 **구조적** 동등성 — 지역 함수 참조(`::f`)는 새 클로저여도 `==`라 갱신이 걸러지고 **첫 컴포지션의 콜백이 계속 불린다**. 처방은 `referentialEqualityPolicy()` 또는 람다. `clickable`은 참조 비교라 멀쩡해 보인다

- **61** `UiStrings` 생성자 여유는 정확히 0칸 — 문구를 더하면 `compileDebugKotlin`은 통과하고 `data class`가 자동 생성하는 `copy$default`가 JVM 인자 한도(255칸)를 넘겨 단위 테스트만 무더기로 `ClassFormatError`. 새 문구는 생성자에 더하지 말고 곁표+멤버 함수로(`StudyHubContractTest`가 칸수를 잰다)
- **64** `tentativeMove`는 뜻이 「다음은 여기」 하나뿐 — 착수금지·패 자리를 가리키는 범용 마커로 돌려쓰면 판이 "놓을 수 있다"고 반대로 말한다(입문자용 화면이라 더 나쁘다). `GoRuleLessonsTest`가 모든 마커를 적법성으로 되묻는다
- **65** 글꼴 배율 상한은 2.0이 아니라 **1.3**(둘뿐) — 실기 확인은 설정 화면 「글꼴 크기 → 크게」로(시스템 설정 `font_scale`은 아무 일도 안 한다). 문단 높이는 **영어**에서 먼저 터진다(같은 내용을 더 긴 글자 수로 적음), 세로 공간 배분은 영어·1.3배로 실기 검증할 것
- **67** `GoCoachApp.kt`의 `wiringContext` `remember` 키 제거는 **하지 말 것** — 설계안 4개 전부가 같은 틀린 근거(*"64개 멤버가 전부 안정적이라 키가 필요 없다"*)로 제안했다. 지역 `val` 5개(`playerSetup`·`matchMode`·`searchTimeSettings`·`topMovesEnabled`·`shouldShowResumePrompt`)와 remember 금지가 명시된 `engineName`·`engineDiagnostic`이 첫 컴포지션 값에 영구히 얼어붙고, 네 안이 공통 제안한 회귀 테스트도 이 버그를 못 잡는다. **다음 스레드가 또 제안할 것이다** — 착수 전 ⓐ 위 8개 값을 람다/지연 읽기로 전환 ⓑ 64개 멤버 전수 감사(0건 확인), 순서를 반드시 지킬 것
- **74** 「훅 예산을 회수했다」는 실제로 상태 훅이 줄었는지 셀 것 — `GoCoachApp.kt`에서 지역 변수를 지워도 그 변수가 훅을 안 썼다면 줄어드는 건 줄수 예산(`lineBudget`)뿐, 상태 훅 예산(함정 3, 42/42)은 그대로다. 완료 기록에 어느 예산을 회수했는지 정확히 밝힐 것

**제스처·입력·기기**
- **17** 스크롤 안 길게 누르기 — 조용히 실패, 트리거는 탭 수·위치로. ⚠️ 전문의 *"`input swipe`는 MOVE를 안 만든다"* 는 2026-09-11 실측과 어긋난다(스와이프가 스크롤에 빼앗겼다 = MOVE가 있었다)
- **30** adb 입력 — ⚠️ **2026-09-11 실측(API 35 에뮬레이터): `input tap`이 Compose 버튼·반상에 모두 닿았다.** 안 닿는 기기면 `input swipe x y x y 120`. 좌표는 `uiautomator dump`의 bounds
- **41** 회전·크기 변화 — 세로 고정은 큰 화면(sw≥600dp, targetSdk 36)에서 무시, `configChanges` 필수. 프로세스 종료는 별개('이어 하기')
- **44** 판이 끌기를 가진다(의도) — 판에서 시작한 스와이프는 스크롤 안 됨, 빠른 튕김은 착수. 소비하면 Final 단계 검사 금지

**테스트·검증**
- **15** `@Test` 누락은 조용히 사라진다(`TestAnnotationContractTest`). shared만 바뀌면 app 테스트가 쉰다 → `--rerun-tasks`
- **16** 테스트 건수 — `build/test-results` 누적 XML을 세지 말 것. 비우고 한 번의 실행만
- **19** 아무도 컴파일 안 하는 소스셋 — `make test`에 `compileDebugAndroidTestKotlin`. 시그니처 바꿀 땐 세 트리
- **20** 저장소 밖 상태가 초록을 만든다 — "이름이 서로 맞나"를 계약으로(AGP는 `.gz`를 푼다). 산출물은 직접 열어 볼 것
- **22** 소스 계약은 식별자를 고정 — 이름을 바꾸기보다 되돌리는 게 싸다
- **23** 스텁 폴백 재현법 — 에셋 치우기·`make release` 금지·병합 산출물 삭제·`pm clear`·원복
- **24** 초록 ≠ 안전 — 검사가 그 표기를 실제로 보는지 일부러 깨서 확인
- **32** 미사용 import — 암묵 사용(`getValue` 등)과 KDoc `[Symbol]`도 사용으로 셀 것

- **47** `input tap`은 down→up이 몇 ms라 **"누르고 있는 동안"에 걸린 결함을 못 잡는다** — 지속시간이 얽히면 `input swipe x y x y <ms>`. tap으로 통과한 것은 통과가 아니라 **안 해 본 것**
- **48** 판 위를 픽셀로 잴 때 **격자선도 어둡다** — 빈 자리는 교차점 대각선 25% 지점으로 판정, 크기는 지름 대신 **넓이**로. 그래도 **절대 배율의 증거는 못 된다**
- **49** `screencap`으로 1초 미만 Compose 애니메이션은 **볼 수 없고**, 그 눈멂이 '측정값'처럼 보인다 — **On/Off A/B로 계측기부터 검증**할 것(#145가 그 수치를 적었다가 철회했다). `screenrecord`도 같은 경로
- **50** `pm clear`는 **엔진 모델까지 지운다**(앱 데이터 안) → 조용히 스텁 AI. 뒤에 **반드시 `make seed-engine TARGET=…`**. 엔진 `Unavailable` 팝업이 셸 게이트 **첫 항**이라 엉뚱한 검증까지 막는다

- **66** 표식으로 잘라 재는 소스 계약 — `substringAfter`는 표식을 못 찾으면 문자열 전체를 돌려줘, 이름을 바꾼 날 그물이 죽지 않고 **다른 것을 재기 시작한다**(초록인 채 아무것도 안 지킬 수 있다). 자르기 전에 `assertTrue(source.contains(marker))`로 표식 존재부터 단언할 것
- **68** 계약 테스트의 하드코딩 경로 — `RepoPaths`로 일부 해소(`7841ed37`)했지만 22개 테스트가 아직 상대경로. 워크트리가 여럿이면 실행 위치별로 다른 트리를 검사하거나, 파일을 옮기는 순간 `FileNotFoundException`으로 동시에 터진다(원인이 "경계가 깨졌다"가 아니라 "파일이 없다"로 보인다). 파일 이동 전 22개 흡수가 선행
- **70** 공용 인터페이스(`EngineCoreApi.syncStaticPosition` 등) 기본 구현 삭제는 순서를 뒤집을 것 — 공통 계약 테스트 스위트(Local/Remote/Stub 셋을 같은 시나리오로)를 **먼저** 만들고 삭제는 그 다음에. 먼저 지우면 흩어진 손 페이크 전부가 동시에 컴파일 에러가 된다
- **77** 공유 트리에서 **음성 대조(고의 파손)는 `--rerun-tasks` 없이 거짓 판독을 낸다** — Gradle이 소스 변경을 무시하고 컴파일을 `UP-TO-DATE`로 건너뛰어 "멀쩡한데 안 깨진다"가 나온다. 함정 24의 거울상(빨개져야 할 것이 안 빨개지는데 그게 안전으로 읽힌다). 로그에서 컴파일이 실제로 돌았는지 눈으로 확인할 것
- **78** 공유 트리에서 **`build/test-results`의 테스트 개수는 못 믿는다** — 다른 세션이 필터 걸린 테스트를 돌려 덮어쓴다. 개수 대조는 빌드 산출물이 아니라 **소스에서** 센다(`git ls-tree`로 두 리비전의 `@Test`를 세는 식)
- **81** 계기 테스트의 "갓 설치" 초기화 — `shared_prefs` **파일 삭제만으로는 안 된다**(안드로이드가 `SharedPreferences`를 프로세스 단위로 캐시해, 앞 테스트 설정을 그대로 물려받는다). 지우기 **전에** 같은 이름으로 열어 `clear().commit()`(`FreshAppState.resetToFreshInstallState()`). **단독 실행은 초록, 셋을 함께 돌리면 빨강**으로 나타나고 증상이 원인에서 멀다

**빌드·릴리스·콘솔**
- **4** R8 release — 스모크만으로 끝내지 말 것(대국·출석·1회권). ⚠️ *"release 실행 기록 없음"* 은 낡았다(813·901·902 실기, §0 B-3)
- **5** 산출물(AAB·스크린샷)은 화면 변경이 멎은 뒤에
- **11** 개발자 섹션 1차는 **release에도 실린다** — 게이트는 런타임 boolean(`DeveloperModeStore`, 버전 10탭). 권한을 만드는 버튼은 `BuildConfig.DEBUG`로 한 겹 더(→ 2차)
- **25** 빌드타입 블록에서 던지지 말 것 — 검사는 `packageRelease`/`packageReleaseBundle`에. `make`는 검사가 `bump-version`보다 앞
- **31** `src/friend/assets`는 지우지 말 것 — release AAB의 엔진(98MB, gitignore)
- **33** 16KB — ⓐ 우리 `.so` ELF 정렬 + ⓑ 패키징, ⓑ만으론 통과 못 함. 확인은 `PT_LOAD p_align`
- **34** Play "이전 출시 버전"은 **트랙별** — 번들 둘을 워크트리로 대조하기 전엔 원인을 말하지 말 것
- **35** `setContent` 앞에 SDK를 깨우지 말 것 — 기동 크래시(`StartupOrderContractTest`)
- **36** (참고) #123 헛다리 — EPIPE는 잡음, n=30은 표본 부족. 지난 크래시 원문은 `dumpsys dropbox --print data_app_crash`

- **51** 등재문이 앱보다 늦게 늙는다 — 그물(`AppNameContractTest`)이 **이름 하나만** 본다. 기능을 지우면 `[주요 기능]`·`[요금 안내]`·`[차별점]`을 **대괄호 표제로** grep
- **52** 거부문의 줄이 둘이면 정책도 둘 — Families(연령 선언으로 소멸)와 일반 Ads(살아남는다). AdMob 광고등급 기본값은 **MA**, 콘솔 값은 **심사관에게 안 보인다**(코드에도 박을 것). 이의신청은 집행당 1회
- **53** 16KB 지적은 *"프로덕션에 낡은 번들이 남았다"* 는 뜻일 수 있다 — 고치려 들기 전에 `PT_LOAD p_align`부터 읽을 것
- **54** 콘솔 폼을 프로그램으로 만지면 **화면만 바뀌고 모델은 안 바뀐다** — 실제 클릭 + 저장 뒤 재방문 확인. 「게시 개요」의 미제출 출시가 함께 나갈 수 있다
- **55** 저장소를 prefs → `filesDir`로 옮기면 `wipeToFreshInstall`이 **조용히 지나친다**(테스트는 전부 초록). 목록/본문을 가르고 스키마는 **키만** 더할 것
- **56** Play 앱 서명 앱은 **사이드로드로 결제를 검증할 수 없다** — 그런데 `queryPurchasesAsync`만 성공해서 속는다. 내부 테스트 업로드 → Play 설치가 유일한 길
- **57** 진단 로그를 보려고 개발자 모드를 켜면 **그 순간 검증 대상이 지워진다**(3시간마다 초기화). 실기 결제는 **UI로 판정**할 관찰 지점을 먼저 정할 것
- **58** IARC **새 설문지는 이전 답을 하나도 안 불러온다** — 전부 다시 채운다. 문항은 **앞 문항에 답해야 나타난다**
- **59** 스토어 애셋은 **파일명 순으로 안 들어간다**(업로드 완료 순). 한 장씩 넣을 것 + 라이브러리가 **내용 해시로 중복을 합친다**
- **60** 방침과 데이터 보안 양식이 **어긋나면 그 자체가 위반** — 한쪽만 고치지 말 것. AdMob이 무엇을 보내는지는 **Google이 발행한 매핑**을 따른다

- **75** iOS 컴파일 게이트는 옳지만 `make test`(유일한 릴리스 게이트)에 넣지 말 것 — 아무것도 출하하지 않는 타깃의 컴파일 실패로 안드로이드 릴리스가 막히면 안 된다. 별도 `make test-ios`로 분리, iOS가 실제로 출하하는 날 되돌릴 것
- **76** 죽은 스캔을 `require`로 되살리는 조치는 실행 즉시 초록이 아니라 위반이 쏟아질 각오를 하고 넣을 것 — 오래 검사되지 않던 구간의 실제 위반이 무더기로 드러날 수 있다. `make test`가 유일한 릴리스 게이트라 드러난 위반은 **같은 스레드가 끝까지 초록으로 만들고 닫아야** 한다(위반 0건은 보장이 아니라 그때의 결과일 뿐)
- **79** Lint `abortOnError=true`는 **새 Error만 막고 새 Warning은 통과시킨다**(`warningsAsErrors=false`이므로). 얻는 보장은 "새 Error 0"이지 "새 경고 0"이 아니다. 덤: `NewApi`는 인라인되는 상수 참조를 건너뛰어 음성 대조가 안 걸린다 — 실제 메서드 호출로 해야 한다
- **82** 파일을 옮기면 `lint-baseline.xml`의 **경로가 낡아** 기준선에 묻어 둔 경고가 **새 Warning으로 되살아난다** — 79 때문에 게이트는 초록. `RepoPaths`·FQN 등록부·이름 전수 grep이 **전부 못 보는** 경로 계약이다. 옮기는 커밋에서 `LintBaselineFixed`·새 Warning 건수를 전후 비교하고, 기준선을 재생성하지 말고 **경로만** 고칠 것

**광고·결제**
- **8** `AndroidBillingClient` 공유 — 상품 종류는 **호출부가 선언**(#136에서 기본값 제거)
- **28** UMP — 콘솔 메시지 게시 전엔 `canRequestAds()`가 늘 true. CMP 미비는 수익만 준다(급한 건 데이터 보안)
- **29** 동의 폼 실기 — `addTestDeviceHashedId` 필요, 거부해도 limited 광고는 정상, 테스트 폼은 우리 메시지가 아니다

- **63** 마이페이지 "구독 관리·해지"는 실제 Google Play 구독 관리 화면으로 딥링크 — 에뮬레이터도 로그인 계정이 실사용 개인 계정이면 실수로 진짜 구독을 건드릴 수 있다("에뮬레이터=안전"은 전제가 깨진다). 실기 확인은 앱 안 다이얼로그 버튼까지만, Play 스토어로 나가는 "구독 관리·해지" 버튼은 누르지 말 것. 기기 여럿이면 `adb -s <serial>` 필수(안 붙이면 `installDebug`가 연결된 기기 전부에 설치)

**문서·에셋**
- **18** 이 문서 스크립트 편집 — `---`로 항목 경계를 잡지 말 것(다음 항목 머리글로). 편집 뒤 출현 수를 세고 **읽은 뒤** 커밋, 편집·커밋을 한 호출에 묶지 말 것
- **27** 원형 아바타 — `_transparent` 원화에서 출발, 배경은 연결성으로 제거

**저장소·아키텍처**
- **62** 공유 작업 트리에서 `git checkout`으로 되돌리면 남의 미커밋 작업이 날아간다 — 증상은 내가 안 건드린 파일에서 난다(엉뚱한 `Unresolved reference` 등). 되돌리기 전 `git status --short`로 내가 안 건드린 `M` 파일이 떠 있는지 먼저 볼 것. 복구는 세션 jsonl(`~/.claude/projects/.../*.jsonl`)의 `Edit` 호출 `old_string`/`new_string`으로
- **71** 오퍼레이션 단위 `Mutex`만 넣으면 `forceReset`이 다른 모든 오퍼레이션을 자기 타임아웃까지 얼린다 — 단위 테스트로는 재현되지 않는 타이밍 경쟁(착수·무르기가 수십 초 얼어붙는다). `withLock` 대신 타임아웃/세대(generation) 기반 취소를 함께 넣을 것
- **72** 공유 작업 트리에서 대규모 개명·이동(`*Application.kt`→`*UseCase.kt` 등)은 순수 미학이라도 저장소 전체가 충돌하는 최대 충돌면을 만든다 — **채택하지 않는다.** 꼭 필요하면 계획에 `parallelizable: false`로 못박고 다른 스레드에 알린 뒤 단독 점유로만(함정 62와 같은 뿌리)
- **73** 죽은 코드를 릴리스 블로커로 착각하지 말 것 — 결함을 발견하면 먼저 "이걸 실제로 부르는 살아 있는 경로가 있는가"부터 grep. 프로덕션 소비자 0건이면 급하지 않다(단, 빌드 플래그로 막혀 있을 뿐인 잠재 결함과는 구분할 것 — 그건 미뤄도 되는 게 아니다)
- **80** `git add <경로>` 뒤의 `git commit -m`도 안전하지 않다 — `add`와 `commit` 사이에 다른 세션이 스테이징하면 삼켜진다(실제 35파일). **`git commit <경로> -m`** 으로 커밋할 것. ✅ **실증(2026-09-24)**: 트리에 242파일이 떠 있고 rename 7개가 staged된 상태에서 경로 지정 커밋이 **정확히 1파일만** 집어냈다 — `git add .` 였으면 진행 중이던 이동 전체를 삼켰다. ⚠️ **전문의 "인덱스는 작업 트리 전체가 공유한다"는 너무 넓다**(2026-09-24 교정): 인덱스는 **워크트리마다 별개**이고 이 함정은 **같은 워크트리를 쓰는 세션끼리만** 해당한다. `git worktree list`로 확인할 것 — `.claude/worktrees/*`·`.release-build`의 세션과는 인덱스를 공유하지 않는다. ⚠️ 남의 트리가 깨끗한지 **내 트리에서 `git status`로 재지 말 것** — `git -C <그 워크트리> status`로 트리를 지목해야 한다
- **83** `git mv` 뒤 경로 지정 커밋에 **새 경로만** 넣으면 옛 경로의 삭제가 빠져 **커밋 트리에 파일이 두 벌** 남는다 — 게이트는 작업 트리를 빌드하므로 **초록**(실제 `3bd2efe6`). pathspec에 **옛 경로도** 넣고, `git show --stat`에 `R`이 보이는지 확인할 것(`A`만 보이면 남은 것). 80과 한 쌍
- **84** spotless를 루트 `**`로 걸면 `.claude/worktrees`의 남의 워크트리까지 쓸어 담는다 — **모듈별 `src/**/*.kt`만**. `spotlessApply`는 **격리 워크트리에서만**(또는 `-PspotlessIdeHook=<파일>`로 파일 하나). `.editorconfig`의 `ktlint_standard = disabled` 한 줄이 규모를 좌우한다

### 이번 세대에 새로 생긴 함정 (58~) — 전문은 여기

58. **⚠️ Play Console IARC 「새 설문지」는 이전 답을 하나도 안 불러온다** (2026-09-19, #162)
    - 답 하나만 바꾸려 해도 **카테고리부터 전부 다시** 채워야 한다. 바꾸려던 항목만 보고 들어가면 나머지를 빈칸으로 제출한다.
    - ⚠️ **문항이 순차로 열린다** — 앞 문항에 답해야 다음 문항이 DOM에 나타난다. 참조(ref)를 미리 모아
      한 번에 클릭하면 **첫 클릭만 먹고 나머지는 조용히 버려진다.** 한 문항씩 답하고 눈으로 확인할 것.
    - ⚠️ **카테고리가 스토어 설정과 다를 수 있다** — 이 앱은 스토어가 `게임 > 보드`인데 IARC는
      `다른 모든 앱 유형`으로 받아 놨다. 바꾸면 설문 축이 통째로 달라져 **177개국 연령등급이 움직인다.**
      출시 직전에는 건드리지 않는 쪽이 옳다(2026-09-19 사용자 결정) — **정리할 거리로 남아 있다.**

59. **⚠️ 스토어 애셋은 파일명 순으로 들어가지 않는다 — 그리고 같은 그림은 하나로 합쳐진다** (2026-09-19, #162)
    - 8장을 한 번에 올리면 **업로드 완료 순**으로 들어간다. 실측 순서가 `03,04,05,02,06,01,08,07`이었다.
      **한 장씩 검색 → 선택 → 추가**해야 `01→08`이 된다.
    - ⚠️ **애셋 라이브러리는 내용 해시로 중복을 합친다.** 휴대전화/7인치/10인치 폴더의 파일이 바이트 단위로
      같으면 뒤의 업로드가 **통째로 무시된다**(*"애셋이 중복 삭제되었습니다"*). 태블릿 칸에도 **같은 애셋을
      그대로** 넣으면 된다 — 애셋 이름은 스토어에 보이지 않는다.
    - ⚠️ **휴대전화 스크린샷 상한은 8장이다**(10장 아님). 최소는 2장이라, 전량 교체할 때는 **지우고 나서 넣는다.**

60. **⚠️ 개인정보처리방침과 데이터 보안 양식은 한 몸이다 — 한쪽만 고치면 그 자체가 위반** (2026-09-19, #162)
    - 콘솔에 들어 있던 답이 **「아무것도 수집하지 않음」** 이었다. AdMob을 싣고 `AD_ID`를 선언하는 앱에서
      그것은 **과소 선언**이다 — 코드를 grep해서 보이는 것만 적으면 이렇게 된다.
    - **SDK가 무엇을 보내는지는 코드에 안 보인다.** Google이 발행하는
      [AdMob 매핑 안내](https://developers.google.com/admob/android/play-data-disclosure)를 **그대로** 따를 것:
      **IP 주소**(대략적 위치 추정) · **사용자 제품 상호작용** · **진단 정보** · **기기/계정 식별자** 넷이다.
    - ⚠️ **「금융 정보 = 아니오」는 전제에 기댄다** — 콘솔 도움말이 *"Google Play 결제 시스템을 활용하는 경우
      결제 서비스가 수집하는 데이터는 선언할 필요가 없다"* 고 **이름을 짚어** 적고 있다(조건 둘: 앱이 그 정보에
      액세스하지 않을 것 · 결제 서비스가 사용자로부터 직접 수집할 것). **구매 내역**은 *"기기에서 로컬로만
      처리되고 기기 밖으로 안 나간 데이터"* 예외에 걸린다.
    - **판정법**: 데이터 보안을 고쳤으면 **같은 날 방침도 연다.** 방침이 좁으면 넓히고, 우리 로그와 SDK 진단은
      **문단을 갈라 적을 것** — 한 문단에 섞으면 *"이 앱은 진단이 밖으로 안 나간다"* 로 읽힌다.

61. **⚠️ `UiStrings` 생성자의 여유는 **정확히 0칸**이다 — 문구를 더하면 앱은 컴파일되고 테스트만 무더기로 죽는다** (2026-09-20, #163)
    - `internal data class UiStrings(...)`에 문구 **9줄**을 더했더니 `:app-android:compileDebugKotlin`은 **통과**하고
      단위 테스트만 **570개 중 29개**가 `java.lang.ClassFormatError: Too many arguments in method signature`로 죽었다.
      실패 메시지가 *"문구를 너무 많이 더했다"* 고 말해 주지 않아 원인을 엉뚱한 데서 찾게 된다.
    - **범인은 생성자가 아니라 data class가 자동으로 만드는 `copy$default`다.** 그 정적 메서드는
      `인스턴스 1 + 필드 N + 기본값 비트마스크 ceil(N/32) + 마커 1` 칸을 쓴다 — 필드가 245개인 지금
      `1+245+8+1 = **정확히 255칸**`(JVM 한도)이다. **한 칸만 더해도 깨진다.**
    - **처방**: 새 문구는 생성자에 더하지 말고 **곁표 + 멤버 함수**로 뺀다(`UiStringsStudyCategories.kt`,
      선례 `UiStringsStudyVideos.kt`·`UiStringsBotCharacters.kt`). 함수는 이 산수에 끼지 않는다.
    - **그물**: `StudyHubContractTest.theUiStringsConstructorStaysUnderTheJvmArgumentLimit`이 칸수를 센다.

62. **⚠️ 이 작업 트리는 세션 여럿이 공유한다 — `git checkout`으로 되돌리면 남의 미커밋 작업이 날아간다** (2026-09-20, #163)
    - 실제로 당했다. `UiStrings.kt` + 4개 언어 파일을 `git checkout`으로 되돌렸는데, 그 직전 다른 세션이
      넣어 둔 `scoreLeadEstimateLabel`/`scoreLeadEvenLabel`(형세 보기 우세 레이블)이 함께 사라졌다.
    - ⚠️ **증상이 내 코드와 무관한 파일에서 난다** — `GamePlaySection.kt`의 `Unresolved reference`로 나타나
      원인을 내 쪽에서 찾으면 영원히 못 찾는다.
    - ⚠️ **세션 시작 시점의 `git status`가 "clean"인 것은 아무 보장도 아니다** — 남들이 그 뒤에 쓴다.
      되돌리기 전에 `git status --short`로 **내가 건드리지 않은 파일이 `M`으로 떠 있는지** 먼저 볼 것.
    - **복구 경로**: `~/.claude/projects/-Users-ryan9kim-worksoc-go-ai-coach/*.jsonl`에 각 세션의 `Edit` 호출이
      `old_string`/`new_string` 그대로 남아 있다. 최근 mtime의 jsonl을 `grep`해 그 편집을 다시 적용하면 된다
      (이번에 그렇게 온전히 복구했다).

63. **⚠️ 마이페이지의 "구독 관리·해지"는 실제 Google Play 구독관리 화면으로 딥링크된다 — 에뮬레이터라도
    로그인된 계정이 실제 개인 계정이면 실수로 진짜 구독을 건드릴 수 있다** (2026-09-21, #182)
    - `openSubscriptionManagement(context)`가 여는 화면은 **우리 앱 안이 아니다.** adb로 화면을 훑다가
      뒤로가기 좌표를 잘못 짚어 그 Play 스토어 화면 위에서 다른 곳을 탭했더니 **"Subscription will be
      canceled"** 토스트가 떴다 — 실제로 무엇이 취소됐는지 앱 쪽 로그로는 전혀 알 수 없다.
    - ⚠️ **에뮬레이터도 예외가 아니다.** 이 작업에 쓴 에뮬레이터에 로그인된 계정이 Play Points 2,429점의
      실사용 흔적이 있는 진짜 개인 Google 계정이었다 — "에뮬레이터니까 안전하다"는 전제가 깨진다.
    - **처방**: 프리미엄 배지·구독 진입점을 실기로 확인할 때는 다이얼로그의 "구독하기"/"취소" 버튼까지만
      누르고 멈출 것. "구독 관리·해지"(Play 스토어로 나가는 버튼)는 **누르지 않거나**, 누르더라도 그 뒤
      모든 좌표를 `uiautomator dump`로 정확히 구해서만 탭한다 — 감으로 좌표를 짚지 않는다.
    - ⚠️ **물리 기기가 함께 연결돼 있으면 `adb`가 대상을 못 정한다** — `adb devices -l`로 몇 대가 잡히는지
      먼저 확인하고, 여러 대면 모든 명령에 `-s <serial>`을 명시할 것(빠뜨리면 `gradlew installDebug` 같은
      태스크가 **연결된 기기 전부**에 설치한다 — 이번엔 다행히 그 폰도 이미 같은 디버그 빌드였어서 무해했다).

64. **⚠️ 읽기 전용 도해에서 `tentativeMove`를 "여기를 보세요" 마커로 돌려쓰지 말 것 — 뜻이 하나뿐이다** (2026-09-22, #164)

    `GoBoard`는 `tentativeMove`를 **`gameState.nextPlayer` 색의 깜빡이는 가늠돌**로 그린다
    (`GoBoard.kt:442`). 대국에서 그 뜻은 *"여기 둘까?"* 하나뿐인데, 도해에서 주의를 끄는
    범용 마커가 없다 보니 **아무 자리나 가리키는 데** 쓰고 싶어진다.
    - ⚠️ **착수금지·패 단원이 정확히 그 유혹에 걸린다.** 「여기엔 둘 수 없습니다」라고 적어
      놓고 그 자리에 가늠돌을 찍으면 **판은 "놓을 수 있다"고 말한다** — 글과 그림이 정반대다.
      입문자용 화면이라 더 나쁘다. 그 사람은 판을 믿지 글을 믿지 않는다.
    - **처방**: 뜻을 **하나로 못박고 그물을 단다.** `GoRuleStep.marker`의 뜻은 「다음은 여기」
      뿐이고, `GoRuleLessonsTest.everyMarkerPointsAtALegalMove`가 모든 마커를
      `LegalMoveGenerator.isLegalPlay`로 되묻는다. 못 둘 자리는 `marker = null`로 두고 문구가 맡는다.
    - ⚠️ 표현을 새로 만들고 싶더라도 **`GoBoard`를 건드리는 값이 비싸다** — 이 저장소에서 가장
      많이 불리는 컴포넌트다(호출부 7곳). 뜻을 좁히는 쪽이 싸다.

65. **⚠️ 이 앱의 글꼴 배율 상한은 2.0이 아니라 1.3이고, 레이아웃은 **영어**에서 먼저 터진다** (2026-09-22, #164)

    함정 9와 그 밖의 여러 주석이 *"글꼴 배율 2.0"* 을 말하지만 **이 앱은 시스템 배율을 따르지
    않는다**(`AppFontScalePolicy` — 2026-09-04 사용자 결정). 고를 수 있는 값은
    `AppFontScales = [1.0, 1.3]` **둘뿐**이다. `adb shell settings put system font_scale 2.0`은
    **아무 일도 하지 않는다** — 그걸 모르면 "2.0에서 확인했다"는 거짓 검증을 하게 된다.
    - **실기 방법**: 설정 화면의 「글꼴 크기 → 크게」. 시스템 설정이 아니다.
    - ⚠️ **네 언어 중 먼저 터지는 것은 영어다.** 함정 21이 *"CJK=2로 세라"* 고 하지만 그것은
      **한 줄 폭** 이야기이고, **문단 높이**는 다르다 — 같은 내용을 한국어보다 영어가 훨씬 긴
      글자 수로 적는다. #164의 가장 긴 본문은 한국어 220칸 대 **영어 257칸**이었고, 한국어
      1.3배는 멀쩡한데 **영어 1.3배에서 마지막 줄이 잘렸다.**
    - **처방**: 세로 공간을 비율로 나눌 때 **가장 긴 본문을 영어 · 1.3배로 실기에서 밟을 것.**
      하나가 전부를 대표한다(길이 순위는 소스에서 바로 셀 수 있다). 모자라면 **양보하는 쪽은
      그림이다** — 판은 작아져도 읽히고 글은 잘리면 못 읽는다.


66. **⚠️ 표식으로 잘라 재는 소스 계약은, 그 표식이 사라지면 조용히 파일 전체를 잰다** (2026-09-22, #183)

    `source.substringAfter("private fun …(")`로 한 함수만 떼어 재는 그물이 이 저장소에 여럿 있다.
    **`substringAfter`는 표식을 못 찾으면 문자열 전체를 돌려준다** — 이름을 바꾼 날, 그물은
    죽지 않고 **다른 것을 재기 시작한다.**
    - #183에서 `StudyRuleLessonScreen` → `StudyLessonScreen` 개명으로 실제로 걸렸다. 그때는
      **빨개져서** 알았지만(다른 함수의 `verticalScroll`을 재게 되어 실패), 순서가 반대였다면
      **초록인 채로** 아무것도 안 지키는 그물이 됐을 것이다. 함정 24의 *"초록 ≠ 안전"* 이
      계약 테스트 자신에게 적용되는 경우다.
    - **처방**: 자르기 전에 **표식의 존재를 먼저 단언한다.**
      `assertTrue("표식을 못 찾았다", source.contains(marker))` 한 줄이면 된다. 함정 22가
      *"이름을 바꾸기보다 되돌리는 게 싸다"* 고 말하는 이유가 여기에도 있다.

---

## 지금

> 손대고 있는 것. **비어 있는 것이 정상이다** — 여기 오래 머무는 항목은 쪼개야 한다는 신호다.

_(없음)_

---

## 다음 — 순서가 곧 우선순위다

> **여기 있는 것은 전부 「지금 집을 수 있는 것」이다.** 방아쇠를 기다리는 것은 아래 「대기」로,
> 일이 아니라 답인 것은 「서 있는 답」으로 간다(규칙 ④).

| # | 제목 | 모델/노력 | 이 자리인 이유 |
|---|---|---|---|
| 191 | 새 대국 준비 중 「무승부」 배지 | Sonnet/낮음 | 화면이 거짓을 말한다. 한 줄로 고쳐진다 |
| 184 | 학습 콘텐츠 「기본 사활」 | Opus/최대 | 사용자 승인 순서. ⚠️ **U-51을 먼저 물을 것** |
| 170 | 로그인 부활 + 백업 동기화 | Opus/최대 | 방향은 확정. 비용 대부분이 코드 밖 |
| 155 | 좌석 카드에 상대 캐릭터 얼굴 | Opus/높음 | 몰입도 고도화. 자산 판단(U-10)이 먼저 |
| 167 | 상대 캐릭터 표정 변화 | Opus/중간 | 155 뒤. 자산이 병목 |

### 191. **새 대국 준비 중에 결과 배지가 「무승부」라고 말한다** (Sonnet / 낮음)

🐛 2026-09-22 실기 발견(#190 검증 중). 1.0.0에는 넣지 않았다.

종료 화면에서 새 대국을 시작하면, 엔진이 깨어나는 동안 판에 *"새 대국을 위해 준비 중입니다"* 가
뜨는데 그 아래 배지가 **「무승부」**라고 적혀 있다. 헤더는 **`수순 0수`**다.

- ⭐ **원인은 #176이 이미 한 번 푼 함정이다.** `GoBoard.kt`가 그때의 실측을 주석에 남겼다 —
  `새 대국 준비 — busy=true ended=true passes=true ind=Preparing`. **준비 구간에도 `isGameEnded`가
  참인데** 배지는 그 플래그 하나만 보고 그려지고, 그 순간 판정도 기권 수도 없어 승자가 `null` →
  무승부로 읽힌다.
- **고치는 방향 둘 — 판단해서 적을 것.**
  ⓐ `gameState.moves.isEmpty()`면 그리지 않는다. ⭐ **끝난 판은 수가 반드시 하나 이상이다**
    (기권 1수·양통과 2수) — 0수인데 끝났다는 것이 이 과도 상태의 정의다.
  ⓑ `engineActivityIndicator == Preparing`이면 그리지 않는다. #176이 고른 결이고 뜻이 더 직접적이지만
    배지가 지금 안 받는 값이라 **인자를 하나 더 내려야 한다.**
- ⚠️ **`isGameEnded` 자체를 고치려는 유혹을 조심할 것** — 그 플래그는 종료 화면의 버튼 묶음
  (`재 대국`·`복기 하기`·`대국 설정`)도 쥐고 있고, 계가 오버레이가 **그 켜지는 순서에 기대고 있다**
  (*"종국 플래그는 계가가 끝난 뒤에 켜진다"*). 근원을 건드리면 셋이 함께 흔들린다 — **배지 쪽에서 막는다.**
- ⚠️ #190의 테두리는 여기서 **올바르게 동작한다**(승자가 없으니 테두리도 없다). 고칠 곳은
  배지가 **뜨는 조건**이지 색이 아니다.
- **검증**: 종료 → `재 대국` → 준비 구간에 배지가 **없는가** · 준비 후 정상 진행 ·
  **진짜 종국(기권·계가)에서는 여전히 뜨는가**(고치다 통째로 없애는 것이 이 항목의 실패 모드다).

### 184. **학습 콘텐츠 — 「기본 사활」** (Opus / 최대) — ⚠️ **U-51을 먼저 물을 것**

- ⚠️ **앞의 둘(#164·#183)과 같은 일이 아니다.** 사활은 읽어서가 아니라 **풀어서** 배운다 —
  ⓐ+(정적 문구 + 도해 넘김)로 만들면 반쪽이 된다. 제대로 하면 **문제 데이터 모델 · 정오 판정 ·
  진행도 저장소**가 새로 생긴다(#164가 일부러 만들지 않은 셋).
- ⚠️ **저장소가 생기는 순간 두 규칙이 걸린다**: `UserPreferencesSnapshot`에 넣지 말 것(미배선 필드가
  조용히 초기화된다, 함정 2) · 접두사는 `go_ai_coach_`. 새 저장소를 `ReleaseResetCoordinator` 목록에
  넣을지는 **판단해서 적을 것**(함정 6 — 진행도는 권한이 아니므로 아마 넣지 않는다. **판단을 안
  적으면 다음 사람이 다시 묻는다**).
- ⚠️ **엔진은 필요 없다**(`BoardRules`가 순수 KMP). 진짜 난제는 **정답이 하나가 아니라는 것**이다 —
  오답 변화마다 응수가 필요하고, *"수순 하나를 정답으로 박는다"* 로 시작하면 조금만 다르게 두어도
  틀렸다고 말하는 화면이 된다.
- **선행 결정**: **U-51**(ⓐ+로 갈 것인가 ⓑ로 갈 것인가 — ⓑ면 위의 셋이 전부 따라온다).

### 170. **로그인 부활 + 백업 동기화** (Opus / 최대)

- ⚠️ **#186을 먼저 읽을 것** — 기기 교체 복구는 **서버 없이도 대부분 된다**(1.0.0에서 실제로 동작).
  이 항목은 그것이 못 하는 것(백업을 끈 사용자 · 여러 기기 동시 · 크로스 플랫폼 · 서버가 필요한
  기능의 토대)을 위한 것이지 *"복구하려면 이것뿐"* 이 아니다. **둘의 비용 차가 크다.**
- ⚠️ **로그인은 미완성이라 꺼진 게 아니라 완성된 채로 꺼졌다** — Google·이메일 로그인, 인앱 계정 삭제,
  온보딩 4버튼, 4개 언어 문구가 전부 살아 있고 실기 검증까지 끝났다. 끈 사유는 하나 —
  *"실질 기능 0 대비 정책/UX 비용만 있는 상태"*(`LOGIN_AND_ACCOUNT_SYSTEM.md:154`).
  **백업이 그 빠진 실질 기능이다.**
- ⚠️ **플래그를 켜도 온보딩은 안 뜬다** — `FirstRunGate`가 화면보다 먼저 `hasSeenOnboarding=true`를
  박는다(#140). 가르려면 새 스냅샷 필드 → **autosave 카나리아 배선**(함정 2) + `GuideProgressStore.arm()`이
  같은 `if`에 얹혀 있어 계약이 함께 깨진다.
- ⚠️ **릴리스 SHA-1 미등록** — `google-services.json`에 등록된 건 **디버그 키 하나뿐**이다.
  **릴리스 서명 빌드 실기 검증이 첫 관문이다.** ⚠️ json이 없는 기계는 `R.string.default_web_client_id`
  폴백이 0건이라 **컴파일 자체가 깨진다.**
- ⚠️ **저장소 포트 6종이 전부 동기**라 Firestore를 그 뒤에 못 끼운다 → 로컬 정본 + **옆의** 동기화 계층.
- ⚠️ **충돌 해소가 자료형마다 다르다** — 출석은 max, **소모품은 차감형이라 max 병합이 곧 재화 복제**다.
- ⚠️ **방침·데이터 보안 양식을 함께 고쳐야 한다**(함정 60) — 지금 방침은 *"회원가입・로그인 기능이
  없으며"* 라고 적고 있고 **그것이 참인 동안만 유효하다.** 앱보다 방침이 늦으면 그 자체로 위반이다.
- **선행 결정**: **U-32**(충돌 해소 규칙, 자료형별 표).

### 155. **대국 좌석 카드에 상대 캐릭터 얼굴** (Opus / 높음)

- **선행 결정**: **U-10**(전신 축소를 받아들일지, 얼굴 크롭 5장을 새로 뽑을지).
- ⚠️ **#192가 홈 카드에 캐릭터를 이미 얹었다** — 거기서 쓴 `BotCharacterSquareIcon`은 원형 클립도
  잠금 회색조도 없는 **정사각 그대로**다. 좌석 카드는 크기가 훨씬 작아 **전신을 줄이면 얼굴이 안 보인다** —
  그것이 U-10이 묻는 것이다.

### 167. **상대 캐릭터 표정 변화** (Opus / 중간) — 155 뒤

- **선행 결정**: **U-26·27**(표정 자산을 만들 것인가·몇 종 / 무엇을 '상황'으로 볼 것인가).
- ⚠️ **자산이 병목이다.** 코드는 어렵지 않고, 그림이 없으면 아무것도 못 한다.

---

## 대기 — 방아쇠가 당겨질 때만 집는다

> ⚠️ **여기 있는 것을 「다음」으로 올리지 말 것.** 조건이 충족되지 않았는데 착수하면
> **필요 없는 일을 하거나, 조건을 무시한 채 만든다.** 방아쇠를 각 줄의 첫머리에 적는다.

- **153. 판이 화면 끝까지 갈 때 시스템 제스처 영역 회피** (Opus / 높음)
  **방아쇠: #152의 진단이 「제스처 영역이 원인」이라고 지목할 때.** 그 전에는 고칠 대상이 없다.
  · 선행 결정 **U-6·7**(귀를 살릴지 변 가운데를 살릴지 / 새 설정이면 메뉴 차례의 어디에).
- **168. 전체 수순 번호 오버레이** (Opus / 중간)
  **방아쇠: #156(다시보기)이 부족하다고 실제로 판정될 때.** 지금은 되짚기로 충분할 수 있다.
  · 선행 결정 **U-28**(되짚기만으로 충분한가).
- **180. 구독 오프라인 조회 실측 + 필요시 `ConnectivityManager` 보정** (Sonnet / 낮음)
  **방아쇠: 오프라인에서 구독이 잘못 판정되는 사례가 관측될 때.** 실측 없이 고치면 있지도 않은
  문제에 코드를 더한다.
  · 선행 결정 **U-46**(한도 초과 순간 **대국 중이면** 그 판은 끝까지 허용할지).
- **171. 로컬 키 기반 DID/VC 타당성 — 결정 문서** (Opus / 중간)
  **방아쇠: #170의 방향이 정해진 뒤.** ⚠️ **코드가 아니라 문서가 산출물이다.**
  · ⭐ **결론을 미리 적어 두면 안 되지만, 분석의 출발점은 이미 있다**: DID:key는 **기기에 묶인 키**라
    기기를 잃으면 함께 잃는다 → **로그인을 대체할 수 없다.** 강화는 가능하지만 **패스키가 엄격히
    더 낫다**(OS 제공 · 기기 간 동기화). 그래서 **위협 모델을 먼저 쓰는 것**이 이 문서의 첫 절이다 —
    이 앱 계정 뒤에 금전 가치가 있는 것이 **아직 없다.**
  · 선행 결정 **U-33·34**(제품 기능인가 기술 PoC인가 / 검증자를 만들 의사가 있는가).

---

## 서 있는 답 — 일이 아니다. 같은 제안이 올라올 때 **여기가 답이다**

> ⚠️ **지우지 않는다.** 8세대가 #121에서 배운 것이다 — 답을 지우면 같은 제안이 다시 올라오고,
> 그때마다 처음부터 논증해야 한다. **이 절은 그 논증을 한 번만 하게 해 준다.**

- **`middleware` 게이트웨이 3파일을 지우지 않는다**(구 #121). 참조 0이라 죽은 코드로 보이지만
  **모듈 분리를 위해 세워 둔 뼈대**이고, 그 분리를 나중에 한다(사용자 판단).
  ⚠️ **지우려는 유혹이 다시 온다** — 죽은 코드를 훑는 스레드가 같은 3파일을 또 찾아낸다.
  ⚠️ 정말 지우게 되면 `LayeringContractTest`의 **존재 단언을 같은 커밋에서 함께** 걷어낼 것.
- **캐릭터 개별 판매은 폐기다**(구 #160·#161·#18, 2026-09-18 사용자 결정).
  사유: *"광고·출석으로 이미 받을 수 있는데 추가 결제 구매까지 넣으면 복잡도가 증가한다.
  오히려 프리미엄 구독으로 해소하게 하면 모든 게 심플하다."*
  · ⭐ **부수 효과**: 캐릭터 특전(`isBotCharacterPerkActive`)은 유료 캐릭터 **구매 유도 장치**였으므로
    목적이 사라졌다. 발동 조건이 *"상대가 `Purchase` 캐릭터인가"* 인데 **그런 캐릭터가 0종**이라
    **이미 영구 휴면**이다 — 건드리지 않으면 아무 일도 일어나지 않는다.
  · ⚠️ 결제 **코드**는 지우지 않는다 — `AndroidBillingClient`가 구독과 공유한다.
  · ⚠️ **등재문에 캐릭터 가격을 적지 말 것.**
- **프로모션 코드(출석 보상 일괄 지급)는 폐기다**(구 #166). 사유(사용자): *"로직을 건드리지 말자."*
  · 근거: 「회차 점프」가 `AttendanceCheckIn.kt:60-62`가 명시적으로 금지한 **이중 경로**이고,
    `copy(attendanceCount = 7)`은 오래 쓴 사용자의 출석일을 **되돌릴 수 없이 깎으며**, "+7 가산"은
    28일차에 착지해 **관장 천원이 공짜로 나간다.** 로컬만으로는 「1기기 1회」도 보장되지 않는다.
  · ⚠️ 출시 프로모션이 필요하면 **Play 콘솔의 구독 오퍼**로 한다 — 앱 로직을 건드리지 않는다.
- **AGP 9.0 업그레이드는 근거가 사라진 상태다**(구 #120). 812 업로드 뒤 콘솔 권장조치가 하나도
  뜨지 않는다. **착수 전 첫 질문은 "AGP 9.0이 아직도 필요한가"다.** 콘솔이 요구하지 않으면 Play
  지적 대응이 아니라 일반 툴체인 최신화이고 급하지 않다.
  ⚠️ *"권장조치가 사라졌으니 최신이다"* 로 읽지 말 것 — 지금도 **8.13.2**다.
  ⚠️ 하게 되면 R8이 함께 올라가 **함정 1(enum 상수 이름 = 저장 포맷)이 다시 열린다.**
  **컴파일 초록은 그 위험을 하나도 못 본다.**

---

## 부채 — 이 문서 자신에 대한 것

- **봉인 문서 넷을 지울지 판단**(구 #122). 저장소 정책은 *"결론이 상위 문서에 흡수되면 삭제한다,
  git 히스토리가 아카이브다"* 이다.
  ✅ **막고 있던 것 하나는 치웠다**(#194, 2026-09-23) — 함정 전문 66건이 `docs/spec/PITFALLS.md`
  한 파일에 모였고 색인이 거기만 가리킨다. **이제 함정 때문에 못 지우는 상태는 아니다.**
  ⚠️ **그렇다고 바로 지울 일은 아니다.** 지우기 전에 **함정 말고 무엇이 더 남아 있는지** 한 번 더
  봐야 한다 — 완료 이력 상세·해소된 U-번호·세대별 결정 기록이 거기 있다(정본은 git이지만,
  *"git에 있으니 괜찮다"* 는 **확인하지 않고 하는 말**이 되기 쉽다).
  · 착수하면 **파일마다** ⓐ 남은 내용이 무엇인지 ⓑ 그것을 지금 참조하는 곳이 있는지
    (`grep -rn "파일명"`) ⓒ 흡수처가 어디인지를 적고 나서 지운다.

---

## 사용자 결정 대기 (U-번호)

> ⚠️ **미결만 남긴다.** 8세대는 해소된 것과 섞여 21행이었고, 취소선 없이 본문에 "✅ 해소"만 적힌
> 행도 있어 **한눈에 미결을 못 골랐다.** 해소된 U는 봉인 문서에 있다.
> ⚠️ **착수 전에 묻는다**(규칙 ⑤) — 절반 만들고 묻는 것이 가장 비싸다.

| U | 항목 | 물어야 할 것 |
| --- | --- | --- |
| U-51 | 184 | 사활을 **ⓐ+(읽는 학습)**로 갈 것인가 **ⓑ(푸는 학습)**로 갈 것인가 — ⓑ면 문제 모델·정오 판정·진행도 저장소가 전부 따라온다 |
| U-32 | 170 | 동기화 충돌 해소 규칙 — **자료형별 표**가 필요하다(출석은 max, 소모품은 max가 곧 재화 복제) |
| U-10 | 155 | 전신 축소를 받아들일지, **얼굴 크롭 5장**을 새로 뽑을지 |
| U-26·27 | 167 | 표정 자산을 만들 것인가·몇 종 / 무엇을 '상황'으로 볼 것인가 |
| U-6·7 | 153 | 귀를 살릴지 변 가운데를 살릴지 / 새 설정이면 메뉴 차례의 어디에 |
| U-28 | 168 | 되짚기만으로 충분한가 |
| U-46 | 180 | 한도 초과 순간 **대국 중이면** 그 판은 끝까지 허용할지 |
| U-33·34 | 171 | 제품 기능인가 기술 PoC인가 / 검증자를 만들 의사가 있는가 |
| U-5 | (보류) | 빗나간 탭을 무시할지 가장 가까운 교점에 놓을지 — ⚠️ #152가 판 경계를 고친 뒤 **증상이 재현되는지부터** 볼 것 |

---

## 완료 — 한 줄 + 커밋 (250자 이내)

> **9세대는 빈 표로 시작한다.** 8세대의 완료 31건은 봉인 문서에 있고,
> 그 전부의 정본은 **git 히스토리**다(`git log --grep "backlog #NNN"`).
> ⚠️ 길게 쓰고 싶으면 **커밋 메시지에 쓴다.** 여기는 250자다(맨 위 검사 명령 참조).

| # | 무엇을 했는가 | 커밋 |
| --- | --- | --- |
| 193 | **등재문을 1.0.0에 맞추고 영어 등재정보를 열었다** — 빠져 있던 다시보기·학습하기를 넣고, #169가 세 곳만 손대 살아남은 「계정 가입 없이」를 걷어냈다. ⭐ **본체는 그물** — 문구 셋만 보던 것을 **어근**으로 넓혔다(초록은 다 지워서가 아니라 그물이 몰라서였다). 영어 본문 3,991자 + 출시 노트 한·영 | `67e80ec9` · `94e872a6` · `d64e9605` · `8a5c1609` |
| 194 | **함정 전문 66건을 `docs/spec/PITFALLS.md` 하나로 모았다** — 봉인 문서 넷에 흩어져 그것들을 지울 수 없던 것을 풀었다. 손으로 옮기지 않고 잘라 붙인 뒤 **1~66 빠짐없음·글자 단위 동일**을 대조로 확인했고 원본은 그대로 뒀다 | `(아래)` |

---

## 관련 문서

| 무엇 | 어디 |
| --- | --- |
| 8세대(봉인) — 완료 이력·함정 58~66 전문·해소된 U | `260919-260922_STUDY_CONTENT_AND_1_0_RELEASE.md` |
| **리팩토링 진행** — 이 트랙의 일감·진척은 전부 저기 있다(번호 체계가 다르다) | `260923-_REFACTORING_BACKLOG.md` — 「완료 항목 → 진행 중 → 예정사항 → 사용자 결정 대기」 · 실기 일감은 번호를 골라 개별 세션으로 연다(고정 프롬프트 ①) · 착수 프로토콜과 함정 색인이 그 안에 있다 |
| **아키텍처 실측과 처방** — 계층 일감은 여기서 나온다 | `260923-_ARCHITECTURE_DIAGNOSIS_AND_REFACTORING.md` — §1 무엇이 실제로 깨져 있나 · §3 목표 아키텍처 · §4 착수 전 함정(→ `PITFALLS.md` 67~76) |
| **함정 전문 1~84** | `docs/spec/PITFALLS.md` — 번호로 찾아 그 번호만 편다 |
| 출시·콘솔 절차, 등재문, 체크리스트 | `work/plans/GOOGLE_PLAY_LAUNCH_PLAN.md` · `work/play-store-assets/store_listing.txt` |
| 화면 구조·기능 명세 | `docs/spec/APP_IA_AND_UI_SPEC.md` |
| 권한·게이팅 정책 | `docs/spec/FEATURE_ACCESS_PRINCIPLES.md` |
| 저장소 전체 안내 | `docs/HANDOVER.md` · `docs/DOCS_INDEX.md` |
