# docs/roadmap — 앱 고도화 계획과 진행 이력

앱을 **어떻게 키워 나갈지**를 담는 폴더다. 아이디어 → 착수 스펙 → 일감 백로그 → 완결까지의
흐름이 한 폴더 안에서 날짜순으로 읽히도록 정리했다.

버그 수정이나 리팩토링 기록이 아니라 **기능 고도화 트랙**이 여기 온다. 출시 절차는
`launch-plan/`, 화면 명세는 `APP_IA_AND_UI_SPEC.md`가 따로 담당한다.

## 파일 이름 규칙 — `시작일-완결일_이름.md`

날짜는 `YYMMDD` 6자리다. **가장 중요한 성질은 파일명 정렬이 곧 시간순이라는 것**이므로,
`8.23`처럼 0을 떼지 말 것 — `9.5`와 `10.1`이 섞이는 순간 정렬이 무너진다.

| 형태 | 뜻 | 예 |
| --- | --- | --- |
| `260823-260830_NAME.md` | 8/23에 시작해 8/30에 **완결**된 트랙 | 아래 킥오프 플랜·백로그 |
| `260830-_NAME.md` | 8/30에 시작해 **아직 진행 중**인 트랙(완결일 자리가 비어 있다) | 아래 출시 후 고도화 |

완결되면 뒷자리를 채워 이름만 바꾸면 된다. ⚠️ **이름을 바꿀 때는 참조를 함께 훑을 것** —
코드 주석이 이 문서들을 **파일명만으로** 가리키는 곳이 여럿 있다(2026-08-30 이동 때 8곳이었다).

```bash
grep -rn '260823-260830_OFFLINE' --include='*.kt' --include='*.md' .
```

## 지금 있는 문서 (이름순 = 시간순)

| 파일 | 상태 | 내용 |
| --- | --- | --- |
| `260823-_DAU_GROWTH_IDEAS.md` | 열림 | 일일 접속량(DAU) 증대 **아이디어 풀**. 상태(제안/논의중/채택/보류/기각) 표로 관리하며 계속 자란다 — 아래 트랙들이 여기서 갈라져 나왔다 |
| `260823-260830_OFFLINE_ENGAGEMENT_FEATURES_KICKOFF_PLAN.md` | 완결 | 위 아이디어 중 출석 보상·업적·대국 히스토리·봇 캐릭터를 "로그인 없이 로컬 전용(Phase 1)"으로 구체화한 **착수 스펙**. 구현 결정 각주가 여기 쌓여 있다 |
| `260823-260830_OFFLINE_ENGAGEMENT_FEATURES_BACKLOG.md` | 완결 | 위 스펙을 스레드 단위 일감으로 쪼갠 **진행 관리 백로그**. #1~#42 완료 이력과 "신규 스레드 착수 프로토콜" 원문이 남아 있다 |
| `260830-_POST_LAUNCH_ENHANCEMENTS.md` | **열림 — 새 일감은 여기로** | 출시를 막지 않아 뒤로 미룬 항목들. 위 백로그가 완결되면서 활성 일감 목록을 넘겨받았다 |

**새 일감은 `260830-_POST_LAUNCH_ENHANCEMENTS.md`에 붙인다.** 완결된 두 문서는 이력·근거
참조용이며 갱신하지 않는다.

## 이 폴더가 `docs/` 아래로 내려온 경위 (2026-08-30)

원래는 저장소 루트의 `engagement-growth/`였다. 다른 마스터플랜 폴더들과 같은 배치였고,
`DOCS_INDEX.md`는 그것을 **참조 안정성** 때문에 의도적으로 그렇게 뒀다고 적어 두었다 —
백로그가 자기 경로를 하드코딩한 고정 프롬프트를 담고 있어, 옮기면 이미 복사해 둔 그 프롬프트가
조용히 깨진다는 이유였다. 같은 문서가 **"진행 중인 트랙은 옮기지 않는다"** 는 조건도 함께
달아 두었다.

그 조건이 2026-08-30에 사라졌다 — 백로그가 완결됐다. 루트 폴더 수를 줄여 집중을 흐트러뜨리지
않겠다는 사용자 결정으로 여기로 내렸다(루트 15개 → 14개). 이동하면서 우려됐던 참조 파손은
실제로 존재했고(코드 주석 8곳 + 문서 7곳), **전부 새 이름으로 갱신했다.** 완결된 백로그의
고정 프롬프트에는 리다이렉트 문구를 달아 두었다.

⚠️ `docs/history/`와 혼동하지 말 것. 그쪽은 **완전히 종료된** 트랙의 보관처(`YYYY-MM-DD_<폴더명>`)이고,
이 폴더는 아직 살아 있는 고도화 트랙이 사는 곳이다.

## 관련 문서

- `DOCS_INDEX.md` — 저장소 전체 문서 지도. 이 폴더도 거기 등록돼 있다
- `launch-plan/README.md` — 초도 발행 체크리스트와 콘솔 작업 현황
- `feature-access-principles/README.md` — 로그인 없이/기기 로컬 저장 원칙. 보상·게이팅 설계의 전제
