# 출시 이후 고도화 항목 (2026-08-30 분리)

> **왜 갈라 놨는가**: `260823-260830_OFFLINE_ENGAGEMENT_FEATURES_BACKLOG.md`가 41개 항목까지
> 늘어나 "출시까지 남은 일"이 묻혔다. 출시를 막지 않는 항목은 전부 이리로 옮겼다.
>
> **그 문서는 2026-08-30에 완결됐다** — #40(스크린샷 재캡처 + 비공개 테스트 AAB v0.8.4)이
> 마지막 항목이었다. **새 일감은 이제 이 문서가 받는다.** 저쪽에 덧붙이지 말 것.
>
> 표기 규칙은 원 문서와 같다 — `(AI 모델: …, 노력정도: …)` + 상태, `⚠️`는 착수 전에 반드시
> 읽어야 할 함정. **목록 순서가 곧 실행 순서**다. 신규 스레드 착수 프로토콜(읽기 → 상태 전환 →
> 작업 → 사용자 승인 → 완료 처리)도 원 문서 것을 그대로 따른다.
>
> ⚠️ **#18·#26은 Play Console 게이트에 묶여 있다.** 대시보드 3항목(개인정보처리방침 URL·
> IARC 콘텐츠 등급·데이터 보안 양식)이 "수익 창출"의 선행 조건이라, 그것이 열리기 전에는
> 둘 다 실기 검증이 불가능하다. 나머지 셋(#39·#32·#33)은 콘솔과 무관하게 언제든 착수 가능하다.
>
> **기본 원칙은 "여기 있는 항목은 출시 전에 손대지 않는다"** 이고, 출시 후 우선순위는 다시 정한다.
> ⚠️ 다만 **예외가 이미 넷 있다** — #43~#46은 발행 당일 출시 전으로 당겨졌다(완료 사항 참고).
> 출시를 막지 않는다는 것이 "출시 전에 넣으면 안 된다"는 뜻은 아니다. 판단 기준은 **사용자가
> 잃는 것이 있는가**였다: #43·#44는 벌어서 가진 1회권이 사라지는 문제라 당겼고, #39는 난이도가
> 높아 그대로 뒤에 남겼다.

## 신규 스레드 착수 프로토콜

**매번 새 스레드에 넣는 고정 프롬프트** (그대로 복사해서 사용):

> 이번 스레드에서는 `docs/roadmap/260830-_POST_LAUNCH_ENHANCEMENTS.md` 이 파일의 내용을 파악하여 '예정사항'의 첫번째 항목을 파악하여 수행 담당해야합니다. 먼저 항목을 이해하셨다면 진행중으로 변경하고 작업 착수하시고, 결과물을 사용자와 논의 후 사용자가 완료 승인을 하면 문서에 완료 업데이트하고 당신의 스레드 업무가 종료되었다고 명시해주시면 됩니다.

절차(읽기 → 상태 전환 → 작업 → 막히면 확인 → 사용자 승인 후 완료 처리 → 다음 항목 안내)와
검증 명령(JDK 17, `./gradlew :shared:testDebugUnitTest :app-android:testDebugUnitTest`)은
완결된 백로그 `260823-260830_OFFLINE_ENGAGEMENT_FEATURES_BACKLOG.md`의 같은 절에 그대로 남아
있다 — **그 문서는 프로토콜 원문의 보관처이고, 일감 목록은 이 문서가 받는다.**

⚠️ **커밋 시점은 사용자가 정한다.** 스레드가 임의로 커밋하지 않는다.

---

## 완료 사항

> ✅ **넷 다 출시 전에 들어갔다(2026-08-30 사용자 승인).** 이 문서는 원래 "출시 후"를 담는
> 곳이지만, 이 넷은 발행 당일 출시 전으로 당겨졌다 — #43·#44는 **사용자가 벌어서 가진 1회권이
> 아무 대가 없이 사라지는** 문제였고(1회권은 광고 시청과 매일 출석으로 얻는 것이라 체감 가치가
> 있다), #45·#46은 몇 줄짜리 UX 정리라 같은 릴리즈에 태우는 편이 나았다.
>
> ⚠️ **비용은 코드가 아니라 스토어 자산 쪽에 있었다.** #43·#44는 컷을 안 바꿔 AAB만 다시
> 빌드하면 됐지만, #45·#46이 홈과 픽커를 바꾸면서 **스크린샷 5장 재캡처**가 따라왔다.
> 결과적으로 하루에 AAB가 803 → 804 → 805로 세 번 갈렸고 **앞의 둘은 콘솔에 올라가지 않았다.**
> 다음에도 같은 순서를 밟을 것 — **화면 변경이 멎은 뒤에 스크린샷과 AAB를 만든다.**

43. 빈 바둑판에서 형세 보기가 동작해 1회권만 소모된다 (AI 모델: Sonnet, 노력정도: 낮음) [완료]
    - 2026-08-30 사용자 제보. 아무도 착수하지 않은 판에서 형세 보기를 켜면 **덤만 반영된
      자명한 값**이 나오는데, 그 한 장이 그대로 차감된다. 정보량이 0인 곳에 표를 쓰게 두는 셈이다.
    - **원인은 가드 부재로 확인했다.** `presentation/GameScreenState.kt:314`의
      `canRequestEval = coachingGateOpen && (input.isEngineReady || matchMode == LocalTwoPlayer)` —
      **착수 수(moveCount)를 보는 조건이 없다.** 바로 아래 `canRequestTopMoves`는 "사람 차례에만"
      이라는 좌석 조건을 이미 갖고 있으므로, 같은 자리에 조건 하나를 더하는 모양이 된다.
    - 정할 것: **경계를 어디로 둘 것인가.** ⓐ `moves.isEmpty()`만 막을지, ⓑ 접바둑 배석만 있고
      실착이 없는 상태(수순 0수)까지 막을지. ⚠️ **ⓑ가 맞아 보인다** — 접바둑은 흑돌이 놓여 있어도
      `moveCount`가 0이라 판세가 정해진 배석 그대로이고, 판단할 것이 없기는 마찬가지다.
    - ⚠️ **버튼을 '비활성'으로 만들지, '눌리되 표를 안 쓰게' 할지도 정해야 한다.** 비활성 쪽이
      정직하지만, `coachingButtonEnabled(isFilled, canRequest)`가 `isFilled || canRequest`라
      **이미 켜져 있으면 끌 수 있게** 열어 두는 구조다 — 이 함수를 건드리면 "끄지 못하는 버튼"이
      생길 수 있으니 조건을 `canRequest` 쪽에만 추가할 것.
    - 추천 수도 같은 문제인지 확인할 것. `canRequestTopMoves`에도 수순 조건은 없다.

44. **같은 수순 안에서 껐다 켜면 1회권이 또 소모된다** (AI 모델: Opus, 노력정도: 중간) [완료]
    - 2026-08-30 사용자 제보. 형세 보기를 켜고(1장 소모) → 다시 눌러 끄고(무료, 의도된 동작) →
      **한 번 더 누르면 이미 값을 치른 그 수순인데 또 한 장이 나간다.** 추천 수도 같은 경로다.
    - **원인을 코드에서 확정했다 — 한 줄이다.** `ui/ConsumableUiState.kt`는 1회권 사용을
      `oneShots: Map<FeatureId, Int>`로 들고 있고 **그 Int가 "몇 수째에 켰는가"** 다
      (`markOneShot(featureId, moveCount)`). 표를 한 수에 묶어 두려고 존재하는 값이다.
      그런데 `ui/GamePlaySection.kt:394-398`의 끄는 분기가 `clearOneShot(featureId)`로
      **그 기록을 통째로 지운다.** 지워지고 나면 세 번째 탭에서 `isOneShotActive`가 `false`라
      `Locked` 분기로 떨어져 다시 차감된다.
      · 즉 **"켜져 있다"와 "이 수순에 값을 치렀다"가 같은 자료구조 하나에 얹혀 있는 것**이 원인이다.
        끄는 순간 앞의 뜻이 사라지면서 뒤의 뜻까지 같이 사라진다.
    - **선택지 둘 — 사용자 결정 필요.**
      · **ⓐ 지불 원장을 분리한다(권장).** `oneShots`를 "이 수순에 값을 치른 기능" 원장으로 남기고,
        보이는지 여부는 별도 집합으로 뺀다. `clearOneShot`은 표시만 끄고 원장은 건드리지 않으며,
        `featureGated`가 `원장[기능] == 현재 수순`이면 무료 통과시킨다. 다음 수가 놓이면
        `expireOneShotsAtMove`가 지금처럼 둘 다 정리한다. **기존 규칙("끄는 것은 무료")을 그대로
        지키면서 결함만 없앤다.** 이미 수순을 들고 있어서 새로 만들 상태가 없다 — 10줄 안팎이다.
      · **ⓑ 켜면 그 수순 동안 못 끄게 한다(사용자 제안).** 원천 차단이라 가장 단순하다.
        ⚠️ 다만 **형세 음영이 반상을 상당히 가린다**(스크린샷 03 참고). 끄지 못하면 돌 모양을
        다시 보려고 **수를 두는 수밖에 없어진다** — 판단하려고 켠 기능이 판단을 방해한다.
        게다가 "끄는 것은 무료"는 `GamePlaySection.kt:386`과 `ConsumableUiState.kt`에 근거까지
        적어 둔 **의도된 규칙**이라, 이 안은 그 결정을 되돌리는 쪽이다.
      · 사용자도 "커버하려다 로직이 복잡해질 수 있다"고 우려했는데, **실제로는 복잡해지지 않는다** —
        원장에 필요한 수순 값이 이미 그 맵에 들어 있기 때문이다. 그래서 ⓐ를 권한다.
    - ⚠️ **추천 수도 같은 `featureGated`를 지나므로 고치면 함께 낫는다**(사용자 3번 항목).
      따로 일감을 내지 않은 이유가 이것이다.
    - ⚠️ **회귀 그물을 반드시 같이 깔 것.** 이 결함은 "탭 → 탭 → 탭"이라는 **순서**에서만 드러나
      단발 테스트로는 안 잡힌다. 최소한 "같은 수순에서 3연속 탭 시 차감은 1회", "다음 수로
      넘어간 뒤 탭하면 다시 1회 차감" 두 가지를 고정할 것.
    - 참고: `presentation/GameScreenState.kt:290`에 **"9 → 8 → 7로 차감됐다"** 는 실기 확인 기록이
      이미 있다. 그때는 확인 팝업 없이 바로 쓰는 설계를 설명하는 맥락이었지만, **지금 보면
      이 결함의 흔적이기도 하다** — 같은 수순에서 연속으로 줄어드는 장면이다.

### 구현 결과 (2026-08-30, 승인 대기)

**#43** — `presentation/GameScreenState.kt`의 `coachingGateOpen`에 `input.gameState.moves.isNotEmpty()`
한 항을 더했다. 형세·추천 수가 **이미 공유하던 게이트**라 한 곳만 고치면 둘 다 닫힌다.
⚠️ 조건을 `canRequest` 쪽에만 넣었다 — `coachingButtonEnabled(isFilled, canRequest)`가
`isFilled || canRequest`이므로, 프리미엄 사용자가 형세를 켜 둔 채 새 대국에 들어가도 **끌 수는
있다.** 잠그면 빈 판에서 표시를 못 끄고 갇힌다.

**#44** — ⓐ안(지불 원장 분리)으로 갔다.
- `ui/ConsumableUiState.kt`에 순수 타입 `OneShotLedger(paidAtMove, hidden)`를 신설했다.
  `mark`는 원장에 적고 숨김을 풀며, `hide`는 **표시만** 끄고 원장은 남기며, `expireAtMove`는
  수순이 넘어간 항목만 양쪽에서 지운다. 컴포저블은 이 값을 `mutableStateOf`로 하나 들고만 있다.
  · ⚠️ **컴포저블에서 뺀 것이 이 작업의 핵심이다.** 결함이 "켜기 → 끄기 → 켜기"라는 **순서**에서만
    드러나는데, 전이가 `@Composable` 안에 있으면 JVM 테스트로 그 순서를 재현할 수 없다.
- `ConsumableUiState.isPaidForMove(featureId, moveCount)` 신설 — **현재 수순을 인자로 받아**
  비교한다. 만료가 `LaunchedEffect`라 한 프레임 늦게 도는 창에서 옛 항목이 무료로 새지 않는다.
- `ui/GamePlaySection.kt`의 `featureGated`에 "같은 수순이면 무료 통과" 분기를 넣고,
  잠금 테두리 판단도 `tapIsFree()`로 바꿨다 — 껐다고 금색 테두리가 돌아오면 "누르면 또 나간다"고
  **잘못 알리는** 셈이기 때문이다(실제로는 무료다).
- 추천 수도 같은 `featureGated`를 지나므로 함께 나았다(사용자 3번 항목).

**테스트**: `ConsumableUiStateTest`에 순서를 그대로 적은 회귀 3건(껐다 켜기 무과금 / 다음 수에서
만료 / 만료 대상만 보고), `GameScreenStateTest`에 #43용 2건(빈 판 차단 / 켜져 있으면 끌 수 있음).
⚠️ 기존 `topMovesClosesOnTheAiTurnBecauseItsGateChecksTheSeat`가 **빈 판을 좌석 검증에 쓰고 있어**
같이 고쳤다 — #43이 그 판을 막으면서 "좌석 때문에 갈린다"는 요지가 흐려졌다. 전제(한 수 둔 판)만
바꾸고 의도는 그대로 뒀다. 239건 전부 통과.

**실기 검증(에뮬레이터, 13x13 유저 대 유저)**:
| 상황 | 형세 1회권 |
| --- | --- |
| 수순 0수에서 버튼 탭 | **3 → 3** (버튼이 회색으로 잠겨 있고 표가 안 나간다) |
| 1수 진행 후 켜기·끄기·켜기·끄기·켜기(5탭) | **3 → 2** (한 장만) |
| 다음 수로 넘어감 | 2 (표시 자동 해제) |
| 새 수순에서 켜기 | **2 → 1** (다시 정상 과금) |

고치기 전이었다면 5탭 구간에서 3장이 나갔을 자리다.

---

45. 홈 화면 카드 순서를 **대국 하기 → 대국 기록 → 학습 하기**로 (AI 모델: Sonnet, 노력정도: 낮음) [완료]
    - 2026-08-30 사용자 지시. 지금은 대국 하기 → **학습 하기** → 대국 기록이다
      (`ui/GoCoachHomeScreen.kt:171·189·201`). 학습이 유튜브 링크 모음이라 앱 안에서 이어지는
      동선(두고 → 돌아보고)이 끊긴다는 판단으로 보인다.
    - 구현은 `MenuCard` 두 블록의 순서를 바꾸는 것이 전부다. ⚠️ 카드마다 색이 다르지 않고
      **첫 카드만 `primary`** 이므로(나머지는 `surfaceVariant`) 순서를 바꿔도 강조가 어긋나지 않는다.
    - ⚠️ **`01_home.png`이 달라진다**(아래 스크린샷 주의 참고).

46. 첫돌이(초보) 설명 문구 다듬기 — **첫돌이만** (AI 모델: Sonnet, 노력정도: 낮음) [완료]
    - 2026-08-30 사용자 지시. 현재
      `shared/.../botcharacter/BotCharacterCatalog.kt:43`: `"오늘 처음 돌을 잡은 입문생. 두는 곳마다 실수예요."`
      → 요청안: `"입문자를 위해 실수를 많이 유도한 캐릭터에요."` **나머지 4종은 그대로 둔다.**
    - ⚠️ **맞춤법을 확인할 것 — `캐릭터에요`가 아니라 `캐릭터예요`다.** 받침 없는 말 뒤에서는
      `이에요`가 `예요`로 줄기 때문이다. 바로 옆 기존 문구가 이미 `실수예요`로 맞게 쓰고 있어,
      한 화면에 두 표기가 나란히 놓이면 눈에 띈다. **스토어 스크린샷(05)에 그대로 찍히는 문장이다.**
    - ⚠️ **말투(register)가 다른 4종과 갈린다.** 기존 다섯 줄은 전부 **캐릭터를 사람처럼 소개**한다
      ("오늘 처음 돌을 잡은 입문생", "기본기를 익히는 중", "도장 최강"). 요청안은 **제작 의도를
      설명**하는 문장이라("…를 위해 …한 캐릭터") 첫돌이만 화자가 바뀐다. 의도가 명확해지는
      이점과 세계관이 깨지는 손실을 저울질할 것. 같은 뜻을 인물 화법으로 옮긴 대안 예:
      `"입문자와 두려고 일부러 자주 실수하는 상대예요."`
    - ⚠️ **이 문장은 지금 모든 언어에서 한국어로 보인다**(#32). 도메인 데이터라 번역을 거치지
      않기 때문이다 — 이 항목이 그 결함을 키우지는 않지만, #32를 할 때 이 문장도 같이 옮겨야 한다.
    - ⚠️ **`05_character_picker.png`이 달라진다**(아래 스크린샷 주의 참고).

### 구현 결과 (2026-08-30, 승인 대기)

**#45** — `ui/GoCoachHomeScreen.kt`에서 학습·기록 두 `MenuCard` 블록의 순서만 바꿨다. 강조는
어긋나지 않는다 — 첫 카드만 `primary`이고 나머지는 `surfaceVariant`라 순서와 무관하다.

**#46** — **대안 문구로 확정(2026-08-30 사용자 승인)**:
`"오늘 처음 돌을 잡은 입문생. 두는 곳마다 실수예요."` → `"입문자와 두려고 일부러 자주 실수하는 상대예요."`
요청 원안(`…한 캐릭터에요`)은 두 가지 때문에 쓰지 않았다 — ⓐ `캐릭터에요`가 아니라 `캐릭터예요`가
맞고(받침 없는 말 뒤 `이에요` → `예요`), ⓑ 나머지 4종이 전부 **캐릭터를 사람처럼 소개**하는데
그 문장만 제작 의도를 설명해 화자가 갈렸다. 대안은 같은 뜻(입문자용, 일부러 실수)을
인물 화법으로 옮기고 맞춤법도 맞다. **나머지 4종은 손대지 않았다.**
⚠️ 이 문장은 여전히 모든 언어에서 한국어로 보인다(#32) — 그 항목을 할 때 함께 옮길 것.

**검증**: 단위 테스트 239건 통과, `:shared:compileKotlinIosSimulatorArm64` 통과(도메인 데이터를
건드렸으므로 확인했다). 실기(에뮬레이터)에서 홈 카드가 대국 하기 → 대국 기록 → 학습 하기 순으로
그려지고, 상대 고르기에 새 문구가 나오는 것을 확인했다.

**스토어 자산**: 두 항목 모두 컷을 바꾸므로 **스크린샷 5장을 다시 찍고**(빌드 `v260830.1754`,
같은 캡처 조건: 13x13 접바둑 5점 21수, 03은 형세 ON) AAB를 다시 빌드했다. 한 세트가 같은
빌드에서 나와야 해서 01·05만이 아니라 5장 전부를 교체했다.

---

**산출물(#43·#44)**: (app-android) `presentation/GameScreenState.kt`(빈 판 게이트),
`ui/ConsumableUiState.kt`(`OneShotLedger` 신규 + `isPaidForMove`),
`ui/GamePlaySection.kt`(`featureGated` 무료 통과 분기, `tapIsFree` 잠금 테두리).
테스트 5건 신설(`ui/ConsumableUiStateTest.kt` 3건, `presentation/GameScreenStateTest.kt` 2건),
기존 `topMovesClosesOnTheAiTurnBecauseItsGateChecksTheSeat` 전제 수정 1건.

**산출물(#45·#46)**: (app-android) `ui/GoCoachHomeScreen.kt`(카드 순서).
(shared) `application/botcharacter/BotCharacterCatalog.kt`(첫돌이 설명 한 줄).
스토어 자산은 `design-handoff/export/2026-08-30-play-store-listing-and-screenshots/`
(`screenshots/01~05.png` 교체, `store_listing.txt` 3차 갱신 노트),
`dist/play-store-assets/` 업로드 사본 15장, `version.properties`(804→805),
`launch-plan/README.md` §0(업로드 대상·해시).

---

---

32. 봇 캐릭터의 **이름·설명이 한국어 하드코딩**이라 모든 외국어 사용자가 한글을 본다 (AI 모델: Opus, 노력정도: 중간) [완료]
    - #31 작업 중 발견(2026-08-30). #31이 `UiStrings`의 `copy()` 누락이었다면 이건 **도메인 데이터**가 애초에 한 언어뿐인 경우다.
    - `shared/.../botcharacter/BotCharacterCatalog.kt`의 캐릭터 5종이 `name`·`description`을 한국어 리터럴로 갖는다(`첫돌이`, `연습생 돌뫼`, `도장생 반상`, `사범 묘수`, `관장 천원` + 설명 5줄). `UiStrings`가 이 값을 4개 언어 문장에 그대로 끼워 넣으므로(`botCharacterLabel`, `botShardProgressLabel`, `botPurchasedToast`, 출석 보상 목록 등 8곳 이상) **영어 사용자도 한글 이름을 본다.** 실기 확인: 일본어 대국 설정에서 `관장 천원 (達人)`.
    - ⚠️ **번역을 `BotCharacterCatalog`에 넣지 마라 — 계층 위반이다.** 그 파일은 `shared`의 도메인이고 `UiLanguage`는 `app-android`의 UI 개념이다. `UiStrings` 쪽에 `botCharacterName(id)` / `botCharacterDescription(id)` 조회를 두고, 도메인의 `name`은 **디버그·로그용 식별자로 격하**하는 편이 맞는지부터 따져라.
    - ⚠️ #31이 넣은 회귀 그물은 이걸 **못 잡는다.** 한글이 `UiStrings` 필드가 아니라 도메인 데이터에서 흘러들기 때문이다. 고칠 때 그물을 어디까지 넓힐지 함께 정할 것.
    - 분량: 5종 × (이름 + 설명) × 3개 언어 = 30개 문자열. 캐릭터 이름은 말맛이 있는 고유명사라 **기계적 번역으로는 안 되고**(`첫돌이`, `사범 묘수`) 언어별로 새로 지어야 한다.

### 구현 결과 (2026-08-30, 승인 대기)

**도메인에서 표시 문구를 걷어냈다.** `BotCharacter`의 `name`·`description`을 **제거**하고,
네 언어 표를 UI 계층의 `ui/UiStringsBotCharacters.kt`로 옮겼다. 문서가 제안한 "도메인 name을
디버그용으로 격하"보다 한 걸음 더 갔는데, 격하만 하면 **같은 사실이 두 곳에 남아** 한쪽만
고쳐지는 위험이 그대로다 — 카탈로그 자신이 다른 대목에서 경계하던 바로 그 상황이다. 지금
도메인은 `BotCharacterId`와 획득 경로만 갖고, 그 id가 문구 표의 키다.

**고친 호출부는 아홉 곳이다**(`UiStrings.kt` 여덟 + `BotCharacterUiState.kt` 하나).
⚠️ 처음 조사에서 `character.name` 패턴만 찾아 **`botLevelClampedMessage`의 `from.name`/`to.name`을
놓쳤다** — 컴파일러가 잡았다. grep 패턴에 기대지 말고 필드를 지운 뒤 컴파일에 물어보는 편이 확실하다.

**출석 보상 목록에서는 한국어가 두 갈래로 새고 있었다.** 이름 말고 **티어명**도 도메인의
`PlayLevelSetting.tierLabel`(한국어 전용)을 그대로 쓰고 있었다 — `fastBeginnerTierLabel`이 이미
네 언어를 갖고 있는데 그쪽을 부르지 않았다. 그 한 줄을 고치면서 **도메인 레벨 라벨을 UI에
직접 쓰는 곳이 앱 전체에서 사라졌다**(확인: `tierLabel|displayLabel` grep 결과 0건).

**번역 방침**: 한국어 이름은 *직함 + 바둑 용어*다(도장생 `반상`, 사범 `묘수`, 관장 `천원`).
음역하면 말맛이 사라지므로 언어마다 다시 지었고, 직함이 서열을 드러내고 이름이 그 언어권에서
실제 쓰는 바둑 용어일 것 두 가지를 지켰다. 일·중은 한자어가 거의 그대로 대응하고(妙手/天元/盤面),
영어는 바둑 어휘를 골랐다 — `Tesuji`·`Tengen`·`Goban`, 그리고 `돌뫼`(돌무더기)는 돌을 쌓은
표식을 뜻하는 `Cairn`으로 옮겨 원뜻이 살아 있다.

**회귀 그물을 어디까지 넓혔나**(이 항목이 착수 전에 정하라고 한 것):
- #31의 그물은 `UiStrings`의 String **필드**를 리플렉션으로 훑는데, 새 문구는 **함수**로 나와
  시야에 안 들어온다. 그래서 `UiStringsBotCharacterTest`를 따로 세웠다 — 카탈로그 전 종 × 전 언어를
  훑어 ⓐ 표에 빠진 항목(=화면에 id가 그대로 뜨는 경우), ⓑ 비한국어에 남은 한글을 잡는다.
- 두 그물이 "한글"의 정의를 각자 갖지 않도록 판정기를 `test/.../HangulDetection.kt`로 빼서 공유한다.
- "한글만 아니면 통과"는 일본어 자리에 영어를 박아도 지나가므로, 이 항목을 발행하게 만든 그 줄
  (`관장 천원 (達人)`)과 1·5단계 이름은 네 언어 값까지 못 박았다.

**검증**: 단위 테스트 244건 통과(신설 5건), `:shared:compileKotlinIosSimulatorArm64` 통과.
실기(에뮬레이터)에서 일본어 픽커가 `館長 天元 (達人)`으로, 영어가 `Tengen the Master (Master)`로
나오고 **두 화면 모두 한글이 한 글자도 남지 않은 것**을 UI 덤프로 확인했다.

⚠️ **스토어 스크린샷은 한국어 세트라 영향이 없다** — 재캡처가 필요 없었다.
**다만 AAB는 다시 만들어야 했다**: 805는 이 수정보다 28분 먼저 빌드돼 있어 #32가 빠진 채
"업로드 대상"으로 표기돼 있었다. 번들의 dex를 열어 새 문자열이 없음을 확인하고 806을 새로
빌드했다(sha256 `2d435076…6f095f`). **코드가 멎은 뒤에 산출물을 만든다**는 규칙이 스크린샷뿐
아니라 AAB에도 똑같이 적용된다는 뜻이다.

**산출물**: (shared) `application/botcharacter/BotCharacter.kt`(필드 제거),
`BotCharacterCatalog.kt`(문구 인자 제거). (app-android) `ui/UiStringsBotCharacters.kt` 신규,
`ui/UiStrings.kt`(조회 두 개 + 호출부 여덟), `ui/BotCharacterUiState.kt`.
테스트: `ui/UiStringsBotCharacterTest.kt`·`ui/HangulDetection.kt` 신규,
`ui/UiStringsTest.kt`(판정기 공유), `commonTest`의 봇/출석 테스트 3개(단언을 id 기준으로 전환).

---

33. 학습 화면의 강좌 소개 3줄이 한국어 하드코딩 (AI 모델: Sonnet, 노력정도: 낮음) [완료]
    - #31 작업 중 발견(2026-08-30). `ui/StudyScreen.kt:59,64,69`의 `studyVideoEntries[].description`이 `UiStrings`를 거치지 않는다.
    - ⚠️ **단순 번역이 답인지가 먼저다.** 이 셋은 **한국어 유튜브 강의**(바둑에듀)라, 소개만 일본어로 바꾸면 못 알아들을 영상을 일본어로 소개하는 셈이 된다. 선택지: ⓐ 소개만 번역하고 "한국어 강의"임을 명시, ⓑ 언어별로 다른 영상을 고른다, ⓒ 비한국어에서는 학습 카드를 숨긴다. **제품 판단이 필요하다.**
    - 참고: 홈의 `homeStudySubtitle`("유튜브 기초 강좌로 바둑을 배워보세요")은 4개 언어가 이미 번역돼 있어, 카드를 눌러 들어가면 갑자기 한국어가 나오는 낙차가 있다.

### 구현 결과 (2026-08-30, 승인 대기)

**ⓐ안(번역하되 한국어 강의임을 밝힌다)으로 갔다(사용자 결정).** 근거는 유튜브 자동 번역
자막이다 — 자막을 켜면 따라갈 수 있으므로, 카드를 숨기거나(ⓒ) 새 영상을 찾을 때까지(ⓑ)
비워 두는 것보다 낫다는 판단이다.

⚠️ **다만 "간결하게"와 "정직하게"가 부딪히는 지점이 하나 있었다.** 자동 자막은 품질이
들쭉날쭉하고 영상에 따라 아예 없을 수도 있어, 들어가 보고서야 한국어인 걸 알게 두면 헛걸음이
된다. 그래서 **문장을 더하는 대신 채널명 괄호 안에 `· 한국어` 두 글자만 얹었다** —
`（バドゥクエデュ・韓国語）`. 한 줄 소개는 `maxLines = 2`라 길어지면 말줄임되므로 늘릴 여지가
없었다. **한국어 문구에는 붙이지 않았다** — 한국어 사용자에게 알릴 이유가 없고 오히려 되묻게 된다.

**구조는 #32와 같다.** `StudyVideoEntry`에서 `description`을 빼고 짧은 `id`(`rules`/`shapes`/
`life_and_death`)를 키로 삼아, 네 언어 표를 `ui/UiStringsStudyVideos.kt`에 두었다.
⚠️ URL을 키로 쓰지 않은 이유: 영상을 교체할 때 **URL과 썸네일만 갈아 끼우고 문구는 그대로 두는**
경우가 흔한데, URL이 키면 그때마다 네 언어를 다시 써야 한다.

썸네일의 `contentDescription`도 같은 문구를 쓰도록 함께 옮겼다 — 화면 낭독기에는 한글이 그대로
남아 있었다.

**회귀 그물**: 이 문구도 **함수**로 나와 #31의 필드 그물에는 안 보인다. `UiStringsTest`에
강좌 3편 × 비한국어 3언어를 훑는 검사를 더했고, 번역 누락·표 누락과 함께 **`· 한국어` 표기가
한국어 문구에까지 붙는 실수**도 같이 막는다.

**검증**: 단위 테스트 245건 통과, 실기(에뮬레이터)에서 일본어 학습 화면 세 줄이 전부
일본어로 나오고 **한글이 한 글자도 남지 않은 것**을 UI 덤프로 확인했다.

⚠️ **스토어 스크린샷에는 학습 화면이 없어 영향이 없다**(01~05는 홈·로비·대국·픽커다).
AAB는 이 수정까지 담아 **807**로 다시 빌드했다(sha256 `47340a95…7e7725d`) — 두 문자열이
dex에 실제로 들어갔는지 확인했다. 805에서 겪은 "낡은 산출물에 업로드 딱지" 사고를 반복하지
않으려고, 이번에는 **코드가 멎은 뒤에** 빌드했다.

**산출물**: (app-android) `ui/UiStringsStudyVideos.kt` 신규,
`ui/StudyScreen.kt`(엔트리 키 전환 + 소개·접근성 설명 조회), `ui/UiStrings.kt`(조회 하나),
`test/.../UiStringsTest.kt`(회귀 1건).

---

47. 내부 테스트 빌드에 **R8을 켜서** Play Console 난독화 파일 경고를 없앤다 (AI 모델: Sonnet, 노력정도: 중간) [완료]
    - 2026-08-30, 807 업로드 중 사용자 제보. 경고 전문: *"이 App Bundle 유형과 연결된 난독화
      파일이 없습니다. 난독화된 코드(R8/proguard)를 사용하는 경우 난독화 파일을 업로드하면
      비정상 종료 및 ANR을 더 쉽게 분석하고 디버그할 수 있습니다."*
    - ✅ **지금은 무시해도 된다 — 경고가 맞는 말이지만 이 빌드에는 해당 사항이 없다.**
      `playInternal`은 `friend`→`debug`에서 `initWith`하므로 **R8이 아예 돌지 않는다.** 난독화된
      것이 없으니 되돌릴 매핑 파일도 없고, 크래시 스택은 이미 원래 클래스·메서드 이름으로 온다.
      번들을 열어 확인한 사실:
      · 807에는 `BUNDLE-METADATA/com.android.tools/**d8**.json`이 있고 `proguard.map`이 **없다**.
      · `release` 타입으로 만든 번들에는 `**r8**.json` + `com.android.tools.build.obfuscation/proguard.map`이
        **둘 다 들어 있다** — 즉 R8만 켜면 매핑이 번들 안에 자동으로 동봉되고, **콘솔에 따로
        업로드할 필요가 없다.** 경고가 찾는 "연결된 파일"이 바로 그것이다.
    - **`release` 타입에는 이미 켜져 있다**(`isMinifyEnabled`·`isShrinkResources` = true,
      2026-08 App Bundle Explorer 경고 5건을 고칠 때 넣었다). 이 항목은 그 설정을
      **`playInternal`에도 적용하는 것**이다 — `build.gradle.kts`의 해당 블록에 두 줄 + `proguardFiles`.
    - **크기 이득이 작지 않다(실측)**: dex 총합이 `playInternal` **35.4MB(4개)** vs `release`
      **7.7MB(1개)**. ⚠️ 다만 두 번들이 같은 시점 코드가 아니고(release 쪽은 8/18 빌드),
      **AAB 전체 111MB 중 105MB가 `assets/katago/model.bin`** 이라 다운로드 크기 체감은 dex 절감분
      그대로가 아니다. 기대치를 부풀리지 말 것.
    - ⚠️ **켜는 순간 "테스트한 것"과 "올리는 것"이 갈린다.** `playInternal`이 debug 계열을
      물려받는 것은 실기로 검증한 그 코드를 그대로 올리려는 의도였다. R8은 리플렉션·직렬화
      경로를 조용히 지울 수 있으므로, 켠 뒤에는 **에뮬레이터 스모크만으로 끝내지 말고** 최소한
      대국 1판·출석 수령·1회권 사용·광고 시청까지 실기로 밟을 것.
    - ⚠️ **위험이 낮다고 볼 근거는 있다** — `proguard-rules.pro`가 3줄뿐이고(소스/라인 보존과
      애노테이션 유지) 앱 전용 keep 규칙이 하나도 없는데도 `release` 빌드가 성립해 왔다.
      **다만 그 release 빌드를 실기에서 돌려 본 기록은 없다**(업로드 채널이 계속 `playInternal`이었다).
      그러므로 "release가 되니까 괜찮다"로 넘기지 말 것.
    - 선택지: ⓐ `playInternal`에 R8을 켠다(경고 소멸 + 크기 절감, 위 검증 필요) /
      ⓑ 내부 테스트도 `bundle-aab`(release 타입)로 올린다 — 다만 그건 **실제 AdMob/결제 값**을
        쓰므로 테스트 광고로 도는 지금 채널 성격이 바뀐다 / ⓒ 그대로 두고 경고를 받아들인다.
      **ⓐ를 권한다** — ⓑ는 광고 정책 위험(자기 클릭)을 내부 테스트에 들이는 일이다.

### 구현 결과 (2026-08-30, **사용자 승인 완료**)

**ⓐ로 갔다.** `app-android/build.gradle.kts`의 `playInternal` 블록에 `isMinifyEnabled`·
`isShrinkResources` = true와 `proguardFiles`(release와 같은 두 줄)를 넣었다. **코드 변경은 이
13줄이 전부**이고 `proguard-rules.pro`는 한 줄도 건드리지 않았다.

**경고의 원인은 사라졌다 — 번들을 열어 확인했다.**
- `BUNDLE-METADATA/com.android.tools/` 아래가 `d8.json` → **`r8.json`** 으로 바뀌었다.
- `BUNDLE-METADATA/com.android.tools.build.obfuscation/proguard.map`이 **생겼다**(82.5MB).
  항목이 예측한 대로 **콘솔에 따로 올릴 필요가 없다** — 번들이 스스로 들고 간다.

**크기(같은 소스에서 R8만 껐다 켜서 실측 — 항목이 경계한 "다른 시점 코드" 비교가 아니다)**:

| | dex | APK | res 항목 | resources.arsc |
| --- | --- | --- | --- | --- |
| R8 off | 4개 · 35.4MB | 109.7MB | 513 | 1,007,720B |
| R8 on | **1개 · 7.8MB** | **100.3MB** | 418 | 636,324B |

⚠️ **dex는 27.6MB 줄었지만 APK는 9.4MB만 줄었다** — dex가 APK 안에서 압축돼 들어가기 때문이다.
항목의 "기대치를 부풀리지 말 것"이 맞았다. AAB는 116.8MB(dist의 807) → **113.0MB**, 82.5MB짜리
매핑을 새로 품고도 줄었다.

**⚠️ 착수 전 이 항목이 몰랐던 위험을 하나 찾았고, 확인 결과 문제가 없었다.**
이 앱은 **enum 상수 이름 그 자체가 저장 포맷**이다 — `.name`으로 쓰고 `enumValueOf`/`valueOf`로
읽으며(`persistence/` 8개 스토어 · 기록 24곳), 모르는 이름은 **조용히 기본값으로 떨어진다.**
R8이 이 이름을 바꾸면 **기존 사용자의 프리미엄·출석·설정·진행 중 대국이 전부 소리 없이 초기화**된다.
`PremiumSource.Purchase`를 개명하지 말라는 #26의 경고와 정확히 같은 함정인데, R8은 그것을
**전 enum에 한꺼번에** 저지를 수 있었다.
- **결론: 안전하다.** AGP 기본 `proguard-android-optimize.txt`의
  `-keepclassmembers enum * { values(); valueOf(String); }`가 이름을 지켜준다.
  dex를 `dexdump`로 뜯어 `<clinit>`의 문자열 상수를 직접 확인했다 — `FeatureId`(Undo/Eval/
  TopMoves/MoveReview), `PremiumSource`(None/AdGrant/Purchase), `Ruleset`(Chinese/Japanese +
  scoringLabel·katagoName), `PlayLevelGroup`(FastBeginner~Advanced), `GameSetupUxMode`,
  `GameHistoryResult` 전부 **원래 이름 그대로** 남아 있다. **그래서 keep 규칙을 추가하지 않았다.**
  · ⚠️ 검증 방법 주의: `strings | grep`로는 일부가 안 잡혀 잘못된 결론이 나왔다. **`dexdump -d`로
    `<clinit>`을 봐야 한다.** 다음에 이 확인을 반복할 사람을 위해 적어 둔다.

**R8이 위험할 만한 다른 축은 애초에 없었다(코드로 확인)**: `external fun`이 **하나도 없다** —
KataGo는 JNI가 아니라 `ProcessBuilder`로 띄우는 **별도 프로세스**라 R8과 무관하다. 직렬화
라이브러리(Gson/Moshi/kotlinx) 없음, `Class.forName`/`getIdentifier` 없음, 리플렉션은 로그
문구용 `this::class.simpleName` 한 곳뿐이다.

**단위 테스트**: shared 1122건 + app-android 1225건 = **2347건 전부 통과**(실패 0, 에러 0).

**실기 검증(에뮬레이터 Pixel, R8 APK 설치)** — 항목이 요구한 네 가지를 전부 밟았다.

| 확인 | 결과 |
| --- | --- |
| 앱 실행 | Displayed +505ms, `FATAL`/`ClassNotFound`/`NoSuchMethod`/`NoClassDefFound` **0건** |
| 대국 1판 | KataGo 프로세스 기동, AI 착수(3수), 실시간 형세 B+51.0 → B+49.1 |
| 출석 수령 | 1일차(무르기 무제한) · 2일차(1회권 30+30) 둘 다 수령 |
| 1회권 사용 | 형세 보기 **(30) → (29)**, 오버레이 렌더 |
| 광고 시청 | 리워드 전면 광고 재생 → **"Reward granted"** → 형세·추천 수가 `(⏱)`로 열림 |

⚠️ **저장 왕복을 콜드 재시작으로 따로 확인했다** — 위 enum 위험이 실제로는 안 터지는지 코드가
아니라 앱으로 본 것이다. 룰(집계가→면적계가)을 바꾸고 `force-stop` 후 재실행하니 **그대로 남아
있었고**, 출석 2일차 판정·소모품 30개·"이전 대국 이어하기(3수, 마지막 수 White L10)"가 전부
정확히 복원됐다. 덤으로 **#44 회귀도 확인**했다 — 같은 수순에서 5번 껐다 켜도 **29에서 안 움직인다.**

⚠️ **여전히 실기(실제 단말) 검증은 남아 있다.** 항목이 "에뮬레이터 스모크만으로 끝내지 말 것"이라고
못박았는데, **연결된 물리 단말이 없어 에뮬레이터까지만 했다.** 위 표를 실제 폰에서 한 번 더 밟아
주셔야 한다 — 특히 KataGo 네이티브 성능과 실제 광고 로딩은 에뮬레이터와 다를 수 있다.

⚠️ **업로드 산출물은 아직 안 바꿨다.** `version.properties`는 807 그대로이고
`dist/go-ai-coach-play-internal.aab`도 옛 번들 그대로다 — `make play-internal-aab`이 버전을
올리는데, 커밋·버전 시점은 사용자가 정한다는 규칙 때문에 손대지 않았다. 승인 후 그 명령을 돌리면
808로 올라가면서 R8 번들이 `dist/`에 들어간다.

**산출물**: `app-android/build.gradle.kts` 13줄(주석 8 + 설정 5). **다른 파일 변경 없음.**

---

48. 봇 캐릭터 **이미지 5종** 제작·배선 (AI 모델: Sonnet, 노력정도: 중간) [완료]
    - 2026-08-30 사용자 지시. **문제 인식**: 지금 캐릭터가 이름+한 줄 설명뿐이라 *"획득 가치가
      떨어져 보인다"*. #49·#50이 전부 이 자산 위에 서므로 **이것이 선행 항목이다.**
    - ✅ **배선 자리는 이미 있다** — `BotCharacter.avatarRef: String?`가 모델에 있고 5종 전부
      `null`이다(`BotCharacterCatalog.kt:138`). **타입을 새로 만들 필요가 없다.**
    - ⚠️ ~~지금 앱에 이미지 자산이 하나도 없다~~ → **정정(착수 후 확인)**: `drawable/`에
      `study_thumb_baduk_intro_1~3.jpg` 세 장이 이미 있다. 즉 **밀도 버킷 없는 단일 `drawable/`에
      래스터를 넣는 선례가 이미 있다** — 나중에 ⓐ(래스터)로 갈아탈 때 그 방식을 그대로 따르면 된다.
    - ✅ **크기 걱정은 안 해도 된다**: AAB 113MB 중 105MB가 `model.bin`이라 이미지 5장은 체감이 없다.
    - ⚠️ **흑백/블러 변형을 자산으로 따로 만들지 말 것(#50과 직결).** 컬러 원본 1장만 넣고
      흑백은 런타임 `ColorFilter`로 만드는 쪽을 권한다 — 변형까지 넣으면 장수가 2배가 되고
      두 자산이 어긋날 수 있다. ⚠️ 다만 **`Modifier.blur`는 API 31+이고 이 앱 `minSdk`는 26**이라
      (`build.gradle.kts:92`) 26~30에서는 블러가 조용히 무시된다. 흐림을 쓸 거면 대체 표현
      (채도 0 + 알파 낮춤)을 반드시 같이 정할 것.
    - ⚠️ **착수 시 정할 것 — 이미지를 무엇으로 만들 것인가.** 세 갈래이고 이후 작업이 달라진다.
      · **ⓐ 사용자가 외부 도구로 생성한 래스터(PNG/WebP) 5장을 준다** — 표현력이 가장 높다.
        스레드는 배치·밀도 전략·`avatarRef` 배선만 한다. ⚠️ 밀도 버킷 결정이 이때 따라온다.
      · **ⓑ 스레드가 벡터로 직접 작도한다**(SVG → `VectorDrawable`, 또는 Compose `Canvas` 직접
        그리기). ⚠️ **Claude는 AI 생성 래스터 이미지를 만들 수 없다** — 손으로 도형을 짜는 방식이라
        화풍은 단순한 스타일화에 그친다. 대신 **밀도 버킷이 불필요하고 용량이 사실상 0이며
        코드로 버전관리**된다. 조각 파이 클리핑(#50)도 벡터에서 그대로 된다.
      · **ⓒ 임시 플레이스홀더**(이니셜·실루엣)를 넣고 나중에 교체 — 사용자가 *"이미지는 나중에라도
        업데이트하면 된다"* 고 한 방침과 가장 잘 맞고, #49·#50을 즉시 진행시킨다.
      · ⚠️ 어느 쪽이든 **`avatarRef` 배선과 렌더 경로는 동일**하다. 즉 ⓒ로 시작해도 나중에 ⓐ로
        갈아끼우는 비용이 자산 교체뿐이다 — **먼저 그릇을 만들고 자산을 갈아끼우는 순서가 안전하다.**
    - ✅ **ⓑ(스레드가 벡터 작도)로 확정(2026-08-30 사용자 지시).** "그릇부터 채운다" —
      완성도를 미리 끌어올리기보다 렌더 경로를 세우고, 마음에 드는 래스터가 나오면 그때 ⓐ로 교체한다.

### 구현 결과 (2026-08-30, **사용자 승인 완료**)

**ⓑ로 갔다 — 벡터 5종을 손으로 작도했다.** 캐릭터 이름이 이미 강한 시각 은유를 갖고 있어
(돌 하나 → 돌무더기 → 반상 → 묘수 → 천원) 그대로 그림으로 옮겼다.

| | 캐릭터 | 그림 | 등급 신호 |
| --- | --- | --- | --- |
| 1단계 | 첫돌이 Pebble | 돌 하나 + 새싹, 동그란 입 | 연한 민트 원판 |
| 2단계 | 연습생 돌뫼 Cairn | 돌 세 개를 쌓고 초록 연습생 띠 | 민트 원판 |
| 3단계 | 도장생 반상 Goban | 반상이 곧 몸, 눈이 화점의 흑돌 | 반상 색 원판 |
| 4단계 | 사범 묘수 Tesuji | 금빛 머리띠 + 묘수의 반짝임 둘 | 따뜻한 금빛 원판 |
| 5단계 | 관장 천원 Tengen | 천원에서 뻗는 빛 8줄 + 금테 | 프리미엄 금색 원판 |

**규격을 코드가 아니라 테스트로 못박았다**: 96x96 뷰포트 · 원판 full-bleed(중심 48,48 반지름 48) ·
눈은 흰자+동공 2단. ⚠️ **다섯이 같은 규격이어야 캐러셀(#49)에서 카드 크기가 튀지 않는다** —
주석으로만 적은 약속은 안 지켜지므로 `allAvatarsShareTheSameViewport`가 그림을 직접 읽어 확인한다.

**배선**
- (shared) `BotCharacterCatalog`의 `avatarRef`를 `"bot_fast_beginner_$tier"`로 채웠다.
  ⚠️ 값이 `id`와 겹쳐 보이지만 **다른 축이다** — id는 저장 스키마라 고정이고 이쪽은 그림을
  갈아끼우면 바뀔 수 있다. 주석과 테스트 양쪽에 적어 뒀다.
- (app-android) `ui/BotCharacterAvatar.kt` 신규 — 문자열→리소스 변환의 **유일한 지점**.
  ⚠️ **`Resources.getIdentifier`를 쓰지 않았다.** 이름을 실행 중에 찾는 방식이라 #47에서 R8을
  켠 지금은 리소스가 지워졌는지 알 수 없다. 명시적 `when`이라 참조로 잡히고, 빠뜨리면 테스트가 잡는다.
- 렌더 두 곳: **픽커 줄**(40dp, 잠긴 건 흑백) + **좌석 버튼**(22dp, 픽커를 열지 않고도 상대를 알아보게).

**⚠️ 흐림(`Modifier.blur`)은 쓰지 않았다.** API 31+ 전용이라 `minSdk` 26인 이 앱에서는 26~30 기기에서
**조용히 무시된다**(착수 전 함정 그대로였다). 채도 0(흑백)만으로 구분한다 — 조각만큼 시계방향으로
컬러가 드러나는 연출은 #50의 몫이고, 여기서는 "가진 것/못 가진 것"만 가른다.

**테스트**: `BotCharacterAvatarTest` 4건 신설(모든 캐릭터에 `avatarRef`가 있다 / 그 이름의 그림
파일이 실재한다 / 다섯이 같은 뷰포트다 / `botAvatarRes`의 `when`에 빠진 것이 없다).
⚠️ **JVM 테스트라 `botAvatarRes`를 직접 부르지 않는다** — `R.drawable.*`이 없기 때문이다. 같은
사실을 파일 시스템과 소스 문자열로 확인해 계측 테스트 없이 목적을 달성했다.
⚠️ 기존 `BotCharacterCatalogTest`가 **`avatarRef == null`을 단언**하고 있어 함께 고쳤다("아직
플레이스홀더 단계라 전부 비어 있다") — 이 항목이 바꾸려던 바로 그 상태였다. 2351건 전부 통과.

**실기 검증(에뮬레이터, R8 `playInternal` 빌드)**
- 좌석 버튼에 22dp 얼굴이 붙고 레이아웃이 흔들리지 않는다.
- 픽커에서 **첫돌이만 컬러, 잠긴 넷은 흑백** — 사용자가 원한 "획득 가치"가 한눈에 드러난다.
- ✅ **R8 + 리소스 축소를 통과했다**: `aapt2 dump resources`로 APK 안에 5종이 그대로 있음을 확인.
  명시적 `when`을 택한 판단이 실물로 검증된 셈이다.

**크기**: 5장 합쳐 **13.8KB**(밀도 버킷 없음). AAB 113MB에 견주면 사실상 0이다.

⚠️ **앱은 라이트 테마 고정이다**(`AppColorScheme.kt`에 `darkColorScheme`도 `isSystemInDarkTheme`도
없다) — 다크 대응은 검증 대상이 아니었다. 다만 원판이 자체 배경을 갖는 규격이라 어떤 바탕에서도 읽힌다.

**산출물**: (shared) `BotCharacterCatalog.kt`, `BotCharacterCatalogTest.kt`.
(app-android) `res/drawable/bot_fast_beginner_1~5.xml` 신규, `ui/BotCharacterAvatar.kt` 신규,
`ui/BotCharacterUiState.kt`(픽커 줄을 가로 배치로), `ui/PlayerSetupPanel.kt`(좌석 버튼),
`test/.../BotCharacterAvatarTest.kt` 신규 4건.

---

49. AI 캐릭터 픽커를 **가로 캐러셀로** 교체 (AI 모델: Sonnet, 노력정도: 낮음~중간) [완료]
    - **2026-08-30 사용자 재설계로 범위가 크게 줄었다.** 처음 초안은 대국 설정 상단의 플레이어
      설정을 `[흑][백]` 단일 선택으로 갈아엎는 안이었는데, 그러면 **`2인 대국`과 `AI끼리 대국`으로
      들어갈 입구가 사라진다**(2×2 그리드가 네 조합을 표현하고 있었다). 사용자가 이를 확인하고
      **"2×2는 그대로 두고, AI 좌석 아래 캐릭터 이름 버튼이 여는 팝업만 가로 스크롤로"** 로 바꿨다.
      → **개편 범위가 화면 하나에서 다이얼로그 하나로 줄었다.**
    - ✅ **팝업은 이미 있다 — 신설이 아니다.** `ui/BotCharacterUiState.kt:150`의
      `BotCharacterPickerDialog`가 그것이다(2026-08-29 #10에서 단계 드롭다운을 이미 캐릭터
      픽커로 교체했다). **이미 되어 있는 것**: 잠긴 캐릭터 포함 5종 노출, 흐림 처리(alpha 0.5/0.8),
      해금 힌트 문구, 조각 수 표시, 광고 시청·구매 액션, 선택 반영.
      → **남은 일은 `text = { Column { roster.forEach { BotCharacterRow(...) } } }` 의 세로 나열을
      가로 캐러셀로 바꾸는 것 하나다.** 그래서 노력정도를 높음 → 낮음~중간으로 내렸다.
    - ✅ **`playLevel`이 좌석별이라 좋은 성질이 유지된다** — `SidePlayerSetup.playLevel`이므로
      AI끼리 붙일 때 흑 AI·백 AI에 **서로 다른 캐릭터**를 줄 수 있다. 픽커가 좌석에서 열리는
      지금 구조라 이게 공짜로 따라온다(전역 설정으로 바꿨다면 잃었을 성질이다).
    - ⚠️ **교환 조건 — 선택이 느려진다.** 지금은 5종이 한눈에 세로로 보여 바로 탭하지만,
      가로 캐러셀은 5단계까지 여러 번 스와이프해야 한다. 완화책 셋을 같이 넣을 것:
      ⓐ 열 때 **현재 선택된 캐릭터 위치에서 시작**, ⓑ 점 인디케이터 5개로 위치·전체 수 표시,
      ⓒ **카드 폭을 컨테이너의 ~60%로** 잡아 **좌우 카드가 균등하게 살짝 보이도록(peek)** 한다
        (2026-08-30 사용자 확정 — 초안의 80%에서 낮췄다. 좁으면 옆이 한 장만 보여 "다음이 더 있다"가
        덜 읽히고, 5종이 다 있다는 인상도 약해진다).
      · ⚠️ 60%면 카드가 좁아진다 — **이름+티어·조각 진행률·해금 힌트**가 한 카드 안에서 잘리지
        않는지 4개 언어 전부로 확인할 것(독일어 같은 긴 언어는 없지만 일본어·중국어는 줄바꿈이 다르다).
    - ⚠️ **이 픽커는 대국 설정 로비에서만 열리는 게 아니다.** `PlayerSetupPanel`이
      `GameSetupLobby.kt:135`와 **`GameMenuSection.kt:152`(인게임 햄버거 메뉴)** 양쪽에서 쓰인다 —
      바꾸면 대국 중 화면에서도 같이 바뀐다. 좁은 컨텍스트에서도 성립하는지 확인할 것.
    - ⚠️ **`AlertDialog`는 폭이 좁다.** peek 효과를 내려면 폭 제약을 풀어야 할 수 있다
      (`DialogProperties(usePlatformDefaultWidth = false)` 또는 커스텀 `Dialog`).
      ⚠️ 지금 `dismissOnClickOutside = false`로 **의도적으로** 잠가 뒀다(광고 흐름 보호) — 유지할 것.
    - ⚠️ **이미지 없이 먼저 하면 원래 동기의 절반만 달성된다.** "텍스트뿐이라 획득 가치가 떨어져
      보인다"가 출발점인데 #48 전에는 카드가 여전히 텍스트다. 다만 세로 한 줄보다 카드가 넓어
      조각 진행률·해금 힌트를 크게 보여줄 수 있어 그 자체로 소폭 개선이고, **#48·#50이 들어올
      그릇**이 된다. 기대치를 이 선에서 잡을 것.
    - ⚠️ 앱에 `HorizontalPager`/`LazyRow`/`horizontalScroll` 사용처가 **하나도 없다** — 첫 도입
      사례다(의존성 추가는 불필요, Compose foundation에 있다).

### 구현 결과 (2026-08-30, **사용자 승인 완료**)

예상대로 **다이얼로그 본문만 바뀌었다.** 2×2 좌석 그리드는 한 줄도 건드리지 않았고, 세로 나열
`Column`이 `HorizontalPager`로 바뀐 것이 전부다. 선택·광고·구매 분기는 원래 `when`을 그대로 옮겼다.

**완화책 셋 다 들어갔다**
- **ⓐ 현재 상대 자리에서 연다** — `initialPage = roster.indexOfFirst { it.id == selected?.id }`.
- **ⓑ 점 인디케이터 5개**(`PagerDots`) — 현재 위치가 초록, 나머지는 옅게. 전체 개수도 같이 읽힌다.
- **ⓒ 카드 60% + 좌우 균등 peek** — `contentPadding = PaddingValues(horizontal = maxWidth * 0.2f)`.
  가운데 페이지에서 양옆이 대칭으로 걸쳐 보이는 것을 실기로 확인했다.

**⚠️ 곁 카드 탭에 가드를 넣었다(초안에 없던 것).** 양옆은 20%만 보이므로 거기서 곧바로 선택·광고가
일어나면 **스치듯 닿은 손가락이 광고를 띄운다.** 가운데가 아닌 카드를 누르면 **행동하지 않고 그
카드로 넘어가기만 한다.** 실기로 확인했다 — 조각 경로(사범 묘수)의 곁 카드를 눌러도 광고가 뜨지
않고 스크롤만 됐다(`topResumedActivity`가 `MainActivity` 그대로).

**⚠️ 카드 높이를 232dp로 고정했다.** 설명 길이가 캐릭터마다·언어마다 달라서, 높이를 내용에 맡기면
넘길 때마다 다이얼로그가 출렁인다. 넘칠 때를 대비해 각 줄에 `maxLines`+말줄임을 걸었다.

**⚠️ `AlertDialog` 폭 제약을 풀었다** — `usePlatformDefaultWidth = false` + `fillMaxWidth(0.94f)`.
기본 폭으로는 60% 카드에 글이 뭉개진다. ✅ `dismissOnClickOutside = false`는 **그대로 뒀다**
(#20의 광고 흐름 보호 장치라 건드리면 안 된다).

**4개 언어 실기 확인(문서가 경계한 지점)** — 잘림 없음.
| 언어 | 최장 카드 | 결과 |
| --- | --- | --- |
| 한국어 | 사범 묘수 (고수) | 이름 1줄·설명 2줄·힌트 2줄 |
| English | Goban the Student (Intermediate) | 이름 **2줄**·설명 2줄·힌트 1줄 — 232dp 안에 여유 |
| 日本語 | 師範 妙手 (上級者) | 이름 1줄·설명 2줄·힌트 2줄 |
| 简体中文 | 馆长 天元 (大神) | 전부 1~2줄 |

**⚠️ 문서 정정 — 인게임 메뉴는 이 변경의 영향을 받지 않는다.** 착수 전 함정에 "`PlayerSetupPanel`이
`GameMenuSection.kt:152`에서도 쓰이니 좁은 컨텍스트를 확인하라"고 적었는데, **그 분기는 지금 도달
불가다**: `ExpandedGameMenuSection(showSettings: Boolean = false)`이고 유일한 호출부
(`GoCoachContent.kt:149`)가 그 값을 넘기지 않는다. 저장소 전체에서 `showSettings`는 선언과 자기
`if` 두 줄에만 나온다. 실기로도 인게임 ☰ 메뉴에는 플레이어 설정이 없었다(언어·토글·탐색 시간·진단뿐).
· 그래서 **픽커는 대국 설정 로비에서만 열린다.** 나중에 그 플래그를 켜더라도 다이얼로그는 화면
  단위 창이라 폭이 같아 그대로 성립한다.
· ⚠️ 이 죽은 분기 자체는 별도 정리 대상으로 넘겼다(#49 범위 밖).

**테스트**: 2351건 전부 통과(실패 0). ⚠️ **이 항목에는 회귀 테스트를 새로 넣지 않았다** — 바뀐 것이
전부 `@Composable` 레이아웃이라 JVM 단위 테스트로 고정할 수 있는 순수 로직이 없다. 대신 #48이
넣은 `allAvatarsShareTheSameViewport`가 "카드마다 그림 크기가 튀는" 회귀를 계속 막아 준다.

**실기 검증(에뮬레이터, R8 `playInternal`)**: 캐러셀 렌더 · 좌우 균등 peek · 점 인디케이터 이동 ·
곁 카드 탭이 스크롤로 처리됨 · 가운데 카드 탭으로 선택+닫힘 · 잠긴 카드가 흑백으로 보이고 해금
힌트가 그대로 뜸. AI끼리 대국(흑·백 둘 다 AI)도 그대로 동작한다 — 좌석 그리드를 안 건드린 덕이다.

**산출물**: (app-android) `ui/BotCharacterUiState.kt` — 다이얼로그 본문을 `HorizontalPager`로,
`BotCharacterRow` → `BotCharacterCard`(고정 높이·중앙 정렬·선택 테두리), `PagerDots` 신규.
**다른 파일 변경 없음.**

---

50. 조각 획득을 **시계방향 공개 연출**로 (AI 모델: Opus, 노력정도: 중간) [완료]
    - 2026-08-30 사용자 지시. 미획득 구간은 흑백·흐릿하게, 조각을 얻을 때마다 **시계방향으로
      해당 조각만 정상 이미지**가 드러난다.
    - ⚠️ **초안이 놓친 것 — 5종 중 조각 경로는 2종뿐이다.** `AdShards(required=5)`(2단계)와
      `AdShards(required=10)`(4단계)만 조각으로 열리고, **3·5단계는 출석 해금이라 부분 진행률이
      아예 없으며**(전부 아니면 전무) 1단계는 기본 해금이다. 즉 **이 연출이 적용될 카드는 2장뿐**이다.
      · 정할 것: 출석 캐릭터는 ⓐ 실루엣 + "N일차 출석 시 해금"으로 다르게 보여줄지,
        ⓑ 출석 캐릭터에도 조각 개념을 도입할지(보상 정책까지 건드리는 큰 변경).
    - 구현 힌트: 조각 = 원형 파이 섹터이므로 Canvas `clipPath`+`arcTo`로 컬러 원본을 부채꼴로
      잘라 흑백 위에 겹치면 된다. 5조각이면 72°, 10조각이면 36°.
    - ⚠️ #48의 블러 API 제약(31+ vs minSdk 26)이 여기서 그대로 드러난다.

### 구현 결과 (2026-08-30, **사용자 승인 완료**)

**ⓐ로 갔다 — 표현만 바꾸고 보상 정책은 건드리지 않았다.** 착수 전 갈림길이 ⓐ(출석 캐릭터를
다르게 보여준다) / ⓑ(출석 캐릭터에도 조각 개념 도입)였는데, ⓑ는 `AttendanceRewardPolicy`까지
바꾸는 일이라 이 항목의 노력정도(중간)를 훌쩍 넘는다. **ⓑ는 여전히 열려 있다** — 필요해지면
별도 항목으로 내면 된다.

**구현**: `BotCharacterAvatar`가 세 상태를 그린다.
| 상태 | 그림 |
| --- | --- |
| 보유 | 원래 색 그대로 |
| 잠김 · 조각 경로(2·4단계) | 흑백 위에 모은 조각만큼 **12시부터 시계방향**으로 원본이 드러남 |
| 잠김 · 출석 경로(3·5단계) | 통짜 흑백(부분 진행이라는 개념이 없다) |

**⚠️ 조각 경계선이 이 연출의 숨은 핵심이다(초안에 없던 것).** 파이만 그리면 **0조각 카드와 출석
잠금 카드가 똑같이 통짜 흑백**이라 구분이 안 된다. 조각 경계마다 **카드 바탕색으로 틈**을 내면
0조각이어도 *"다섯 쪽짜리인데 아직 하나도 없다"* 가 읽힌다. 선이 아니라 틈으로 보이게 바탕색을
쓴 것도 같은 이유다 — 사용자가 말한 "조각 난 효과"가 그것이다.

**⚠️ 흐림은 이번에도 안 썼다.** `Modifier.blur`가 API 31+ 전용이라 `minSdk` 26에서는 26~30 기기가
조용히 무시한다(#48과 같은 제약). 채도 0 + 조각 틈만으로 구분한다.

**테스트 4건 신설** — ⚠️ **틀리기 쉬운 곳은 그림이 아니라 각도 계산**이라 순수 함수
`shardSweepDegrees`를 떼어 고정했다. #49가 회귀 테스트를 못 남긴 것과 대비되는 부분이다.
- 0조각 → 0°, 5/5 → 360°
- 5조각이면 한 조각 72°, 10조각이면 36°(카탈로그의 두 경로가 정확히 그 둘이다)
- ⚠️ **범위를 벗어난 값을 잘라내는지**: 9/5 → 360°(한 바퀴 초과 방지), 음수 → 0°,
  `required = 0` → 0°(0으로 나누기 방지). 화면에서는 조용히 깨질 자리다.
- **조각 경로가 2종뿐이라는 사실 자체를 고정**했다(`listOf(5, 10)`) — 초안이 놓쳤던 사실이라,
  나중에 경로가 늘거나 줄면 이 테스트가 먼저 말해 준다.

2355건 전부 통과(실패 0).

**실기 검증(에뮬레이터, R8 `playInternal`)** — 광고를 실제로 한 번 시청해 조각을 얻어 확인했다.
| 상황 | 결과 |
| --- | --- |
| 연습생 돌뫼 0/5 | 통짜 흑백 + **경계선 5개**. 옆의 출석 잠금 카드(선 없는 흑백)와 확실히 구분된다 |
| 광고 1회 시청 → 1/5 | **12시~72° 한 조각만 컬러로 되살아남.** 나머지는 흑백, 힌트도 `(0/5)` → `(1/5)` |
| 출석 잠금(도장생 반상·관장 천원) | 경계선 없이 통짜 흑백 — 의도대로 다르게 보인다 |

⚠️ **파이 특성상 얼굴은 늦게 살아난다** — 캐릭터 얼굴이 원 중심에 있어서 조각을 여럿 모아야
얼굴이 컬러가 된다. 결함이 아니라 파이 공개의 성질이고, "모을수록 살아난다"는 체감에는 오히려
맞는다고 봤다. 다르게 원하시면 (예: 중심부터 바깥으로) 별도 항목이 필요하다.

**산출물**: (app-android) `ui/BotCharacterAvatar.kt`(`ShardReveal`·`shardSweepDegrees` 신규,
Canvas 파이 클리핑 + 조각 틈), `ui/BotCharacterUiState.kt`(카드에서 조각 진행도 전달),
`test/.../BotCharacterAvatarTest.kt` 4건 추가. **다른 파일 변경 없음.**

---

51. **첫 실행 랜딩(온보딩) 화면** 신설 (AI 모델: Opus, 노력정도: 높음) [완료]
    - 2026-08-30 사용자 지시. 첫 실행 시 스스로 초기 설정을 채우도록 돕는 화면. 구성은 홈 화면을
      거의 그대로 재활용 — 상단 가운데 환영 문구, 우상단 언어 설정, 하단 3버튼 자리에 질문 3개.
      · 질문1 **당신의 바둑 실력은?** → 초기 접바둑/레벨 설정 + 결과 안내 팝업
      · 질문2 **계가 방식은?** (집 계가 / 면적 계가)
      · 질문3 **자리만 비워 둔다**(사용자: 지금 떠오르는 질문 없음)
    - ⚠️ **이름 충돌 — 착수 전에 반드시 정할 것.** 초안의 5개 보기(초보/하수/중수/고수/초고수)는
      **이미 봇 5단계의 이름 그 자체다**(`UiStrings.kt:988 fastBeginnerTierLabel`, 4개 언어 전부).
      게다가 초안은 다섯 답 모두 **1단계=초보 캐릭터**를 붙인다 — *"초고수"라고 답한 사용자에게
      "초보와 두세요"* 라고 말하는 화면이 된다. **자기 실력 축과 봇 이름 축이 같은 단어를 쓴다.**
      · 권장: 자기평가 쪽 어휘를 바꾼다(예: "규칙만 알아요 / 몇 판 둬봤어요 / …" 같은 서술형).
    - ⚠️ **더 근본적인 문제 — 접바둑으로는 고수를 만족시킬 수 없다.** 다섯 답이 전부 1단계 봇인
      이유는 **신규 설치 시 그것만 해금돼 있어서**로 보이고 그 판단 자체는 옳다. 다만 1단계는
      *깊이가 얕아서* 약한 게 아니라 **일부러 실수를 유도하도록** 만든 캐릭터라, 돌을 3점 준다고
      잘 두게 되지 않는다. 초고수가 첫 판에서 시시함을 느끼고 이탈할 위험이 있다.
      · 선택지: ⓐ 고수/초고수 선택 시 상위 캐릭터를 **체험 1판 한시 해금** /
        ⓑ 문구로 기대치 관리("먼저 첫돌이와 가볍게 한 판") / ⓒ 접바둑 대신 **바둑판을 19x19로**.
    - ⚠️ **첫 실행 플래그가 앱에 아예 없다.** 원래 있던 `FirstLaunchRewardScreen`(#5)은 출석
      Claim 방식으로 바뀌며 사라졌다(`AttendanceRewardClaimDialog.kt:36` 주석). 새로 만들어야 한다.
      · ⚠️ **`UserPreferencesSnapshot`에 필드로 넣지 말 것을 권한다** — 자동저장이 스냅샷을
        매번 새로 조립해서, 안 엮인 필드가 조용히 초기화되는 알려진 함정이 있다.
        `UiLanguageStore`/`DeveloperModeStore`처럼 **작은 전용 스토어**가 안전하다.
    - ⚠️ **출석 팝업과 첫 실행이 겹친다.** 진짜 첫 실행이면 1일차 출석 Claim 다이얼로그가 홈에서
      뜨는데, 랜딩도 같은 순간을 원한다. **모달 두 개가 포개진다** — 랜딩 → 홈 → 출석 팝업 순서를
      명시적으로 정할 것.
    - ✅ 언어 설정은 새로 만들 것이 없다(`UiLanguageStore` + 홈/설정의 선택기 재사용). ⚠️ 다만
      **질문이 그려지기 전에 언어가 적용돼야** 외국어 사용자가 질문을 읽을 수 있다. 그리고
      스토어 기본값이 `null`이라 "기본 한글"이 아니라 **기기 로케일 추종**이 더 친절할 수 있다 —
      초안은 "기본 언어: 한글"이라 했는데 확인 필요.
    - 질문3 후보(참고): **바둑판 크기(9/13/19)**. 지금 기본이 13x13인데, 입문자 체감에는
      계가 방식보다 판 크기가 더 크게 작용한다.

### 구현 결과 (2026-08-31, **사용자 승인 완료**)

**착수 전 결정 세 가지(2026-08-31 사용자 확정)**
- **자기평가 어휘 = 등급어**(입문/초급/중급/상급/최상급). ⚠️ **전제가 뒤집혔다** — 착수 전에는
   "1단계는 일부러 실수하는 봇이라 고수에게 시시할 것"이라 적었는데, 사용자가 **1단계도 실기력은
   일반 중급자를 상회한다**고 정정했다(애초에 그래서 호선이 아니라 접바둑을 넣었다). 그래서
   서술형("처음 배워요") 대신 등급어를 쓰고, **문구가 상대를 얕잡아 말하지 않도록** 했다.
- **고수 첫 판 = 문구로 기대치 관리.** 위 정정 덕에 원래 걱정 자체가 대부분 사라졌다.
- **랜딩은 모두에게 한 번 띄우되 건너뛰기 제공.** 기존 사용자는 건너뛰면 설정이 보존된다.

**⚠️ 착수 전 서술 정정 — "첫 실행 플래그가 앱에 아예 없다"는 틀렸다.**
`UserPreferencesSnapshot.hasSeenOnboarding`과 `ScreenDestination.Onboarding`이 **이미 있었다.**
다만 그 온보딩은 **로그인 화면**이고 `FeatureFlags.isLoginEnabled = false`라 `initialDestination`이
곧바로 Home으로 빠져 **실제로는 한 번도 뜨지 않는 상태**였다. 그래서 플래그를 새로 만들지 않고
`hasSeenOnboarding`을 그대로 재사용했다 — 첫 실행 개념이 둘로 갈리지 않는다.

**⚠️ 이 항목에서 가장 값진 발견 — 랜딩은 게임 화면 "바깥"이어야 한다.**
처음에는 랜딩을 `ScreenDestination.Landing`으로 넣었고 **테스트도 전부 통과했는데, 실기에서
아무것도 반영되지 않았다**(최상급을 골랐는데 흑=유저·5점 그대로). 원인:
`GoCoachScreen`이 컴포지션되는 순간 `preferencesStore.load()`로 **랜딩 이전** 값을 읽어 게임
상태를 세우고, 자동저장 `LaunchedEffect`가 **첫 컴포지션에서 곧바로 돌면서** 그 옛 값을 되쓴다.
즉 랜딩의 저장이 같은 프레임 안에서 지워졌다.
· 해결: `LandingGate`를 `GoCoachApp`의 `Surface` 안, **`GoCoachScreen`보다 바깥**에 두어
  **랜딩이 끝난 뒤에야 `GoCoachScreen`이 처음 컴포지션되게** 했다. 그러면 그 `load()`가 랜딩이
  쓴 값을 읽고 자동저장도 같은 값을 되쓴다.
· ⚠️ **화면 전환 목적지로 되돌리지 말 것.** 단위 테스트로는 절대 안 잡히는 결함이다.
· 덤: **출석 팝업이 랜딩 위에 겹치던 문제**(착수 전 함정 그대로 실기에서 재현됐다)도 이 구조에서
  저절로 사라진다 — 그 팝업은 `GoCoachScreen` 안이라 랜딩 동안 아예 컴포지션되지 않는다.
  순서가 **랜딩 → 홈 → 출석 팝업**으로 정리되고 보상도 잃지 않는다(실기 확인).

**설정 매핑**(`applyLandingSetup`, 순수 함수)
| 답 | 접바둑 | 좌석 |
| --- | --- | --- |
| 입문 | 5점 | 사람이 흑 |
| 초급 | 3점 | 사람이 흑 |
| 중급 | 호선 | 사람이 흑 |
| 상급 | 2점 | **사람이 백**(AI가 돌을 받는다) |
| 최상급 | 3점 | **사람이 백** |

⚠️ `current`에서 출발해 **필요한 축만** 덮어쓴다 — 기본값으로 새 스냅샷을 만들면 판 크기·덤·표시
옵션이 조용히 초기화된다(`UserPreferencesAutosaveApplication`이 겪었던 사고와 같은 모양).

**테스트 13건 신설**, 2368건 전부 통과.
- `LandingSetupApplicationTest` 8건 — 매핑표, **좌석 뒤집힘**(빠지면 고수가 오히려 돌을 받는다),
  손대지 않은 설정 보존, 판 크기 상한 클램프.
- `UiStringsLandingTest` 5건 — 네 언어 누락 검사, **문구 숫자가 매핑표에서 나오는지**,
  그리고 ⚠️ **자기평가 보기가 봇 티어명과 한 낱말도 겹치지 않는지**(이번 결정을 코드로 못박은 것),
  **상대를 "쉬운/약한"으로 부르지 않는지**(네 언어 금칙어 검사).

**실기 검증(에뮬레이터, R8 `playInternal`, 데이터 삭제 후 진짜 첫 실행)**
| 확인 | 결과 |
| --- | --- |
| 최상급 → 설정 완료 | 대국 설정이 **흑=AI(첫돌이) · 백=유저 · 접바둑 3점**, 보드 미리보기에 흑돌 3개 |
| 건너뛰기 | 설정이 기본값 그대로(흑=유저·5점) — **덮어쓰지 않는다** |
| 재실행 | 곧바로 홈 — 랜딩이 다시 뜨지 않는다 |
| 출석 팝업 | 랜딩이 끝난 뒤에 뜬다(겹치지 않음), 1일차 보상 정상 수령 |
| 영어 전환 | 언어가 **질문보다 먼저** 적용되고 문구가 안 잘린다 |

⚠️ **`GoCoachApp.kt` 라인 예산을 866 → 870으로 올렸다**(4줄, `LandingGate` 래핑뿐).
상태 훅은 46 그대로다 — 화면 상태를 `LandingScreen.kt`가 들고 있어서다.

**남은 것**: 세 번째 질문 자리는 **비워 뒀다**(사용자: 지금 떠오르는 질문 없음).

**산출물**: (shared) `application/preferences/LandingSetupApplication.kt` 신규 + 테스트 8건.
(app-android) `ui/LandingScreen.kt` 신규(`LandingGate` 포함), `ui/UiStringsLanding.kt` 신규(4개 언어),
`ui/GoCoachApp.kt`(래핑 4줄), `test/.../UiStringsLandingTest.kt` 신규 5건,
`architecture/LayeringContractTest.kt`(예산 866→870).

---

52. 접바둑 기본값을 5점 → **호선**으로 (AI 모델: Sonnet, 노력정도: 낮음) [완료]
    - 2026-08-31 사용자 지시. 랜딩을 **건너뛴** 신규 사용자가 곧바로 5점 접바둑으로 시작한다.
      실력을 묻지도 않았는데 최대 접바둑을 얹는 셈이라 기본값으로 부적절하다.
    - ✅ **원인을 찾았다 — 그리고 이미 앞뒤가 안 맞는 상태다.**
      · `UserPreferencesSnapshot.handicapCount`의 기본값이 **`boardSize.maxHandicapCount`(13x13에서 5)**.
      · 그런데 `UserPreferencesStore`의 디코드 폴백은 **`json.optInt("handicapCount", 0)`** — **0**이다.
      → 즉 **"저장 파일이 아예 없으면 5, 키만 빠졌으면 0"** 이라는 서로 다른 두 기본값이 공존한다.
        이 항목은 그 불일치까지 같이 없앤다.
    - ⚠️ **건너뛰기 경로에 0을 박아 넣지 말 것.** 건너뛰기는 *"설정을 건드리지 않는다"* 가 설계
      의도다(#51, 기존 사용자 보호). 거기서 0을 쓰면 **이미 접바둑을 맞춰 둔 기존 사용자의 값이
      지워진다** — 건너뛰기를 만든 이유가 통째로 뒤집힌다.
      · 권장: **데이터 클래스 기본값 자체를 0으로 바꾼다.** 그러면 신규 설치는 호선으로 시작하고,
        기존 사용자는 저장된 값이 있어 아무 영향이 없으며, 위 불일치도 함께 해소된다.
    - ⚠️ 랜딩의 **입문(5점)·초급(3점)** 매핑은 그대로 둔다 — 그건 "실력을 물었을 때"의 답이고,
      이 항목이 바꾸는 것은 "묻지 않았을 때"의 값이다. 둘을 헷갈리지 말 것.
    - 회귀 그물: 기본 스냅샷이 호선인지 + 디코드 폴백과 값이 같은지 두 가지를 고정할 것.

### 구현 결과 (2026-08-31, **사용자 승인 완료**)

**권장안대로 갔다 — 데이터 클래스 기본값 한 줄.**
`UserPreferencesSnapshot.handicapCount`를 `boardSize.maxHandicapCount`(13x13에서 5) → **`0`**.

**⚠️ 건너뛰기 경로에는 손대지 않았다.** 거기에 0을 박아 넣으면 이미 접바둑을 맞춰 둔 기존
사용자의 값이 지워진다 — 건너뛰기를 만든 이유(#51의 기존 사용자 보호)가 통째로 뒤집힌다.
기본값만 바꾸면 **신규 설치는 호선으로 시작하고 기존 사용자는 저장된 값이 있어 무영향**이다.

**✅ 부수 효과로 기존 불일치가 사라졌다.** 데이터 클래스 기본값은 5인데 `UserPreferencesStore`의
디코드 폴백은 `optInt("handicapCount", 0)` = **0**이었다 — *"저장 파일이 아예 없으면 5, 키만
빠졌으면 0"* 이라는 두 기본값이 공존했다. 이제 둘 다 0이고, **어긋나면 테스트가 잡는다.**

**⚠️ 기존 테스트 하나가 옛 결정을 고정하고 있어 함께 고쳤다** —
`UserPreferencesApplicationTest.buildsInitialGameAndRuntimeFromSavedPreferences`가 2026-08-18의
"기본값 = 그 판의 최대 접바둑"을 단언했다(`handicapCount = 5`, `nextPlayer = White`). 이 항목이
되돌리는 바로 그 결정이라 **의도는 그대로 두고 값만** 갱신했다(`0`, `nextPlayer = Black` — 접바둑이
없으면 흑이 먼저 둔다). 깨진 테스트는 **전 2370건 중 이 하나뿐**이었다.

**테스트 2건 신설**(`UserPreferencesCodecTest`)
- `aFreshSnapshotStartsOnEvenTerms` — 새 스냅샷이 0이고, **19x19로 만들어도 0**임을 함께 고정한다
  (판 크기를 따라가던 옛 동작으로 되돌아가지 않게).
- `theDecodeFallbackMatchesTheSnapshotDefault` — ⚠️ 이 항목이 실제로 고친 불일치를 못박는다.
  `handicapCount` 키를 지운 JSON을 디코드해 **스냅샷 기본값과 같은지** 본다. 어긋나면 같은 신규
  사용자가 경로에 따라 다른 판으로 시작하는데, 화면에서는 조용히 지나간다.

2370건 전부 통과.

**실기 검증(에뮬레이터, R8 `playInternal`, 데이터 삭제 후 첫 실행)**
| 경로 | 결과 |
| --- | --- |
| 랜딩 **건너뛰기** | 대국 설정이 **접바둑 (호선)**, 보드 미리보기가 빈 판 |
| 랜딩 **입문** 선택 | **접바둑 (5점)** 그대로 — 랜딩 매핑은 안 깨졌다 |

⚠️ 랜딩의 입문(5점)·초급(3점) 매핑은 **의도적으로 그대로 뒀다** — 그건 "실력을 물었을 때"의
답이고, 이 항목이 바꾼 것은 "묻지 않았을 때"의 값이다.

**산출물**: (shared) `application/preferences/UserPreferencesSnapshot.kt`(기본값 한 줄 + 사유 주석),
`commonTest/.../UserPreferencesApplicationTest.kt`(옛 결정을 고정하던 단언 갱신).
(app-android) `test/.../persistence/UserPreferencesCodecTest.kt`(회귀 2건 신설).

---

55. **출석 보상 화면을 도장판 UX로** 고도화 (AI 모델: Opus, 노력정도: 높음) [완료]
    - 2026-08-31 사용자 지시. 지금은 받을 보상을 **글로만 나열하는 팝업**이라(`AttendanceRewardClaimDialog`)
      "무엇을 향해 모으는 중인지"가 안 보인다. **10회차 전체를 한눈에** 보여주고 출석한 칸에는
      도장이 찍히게 한다.
    - 레이아웃 방향(사용자 지정):
      · `[1일차][2일차][3일차][4일차][5일차][6일차]` — 한 행에 6칸, 출석 시 도장
      · `[7일차][14일차][21일차][28일차]` — 위 6칸과 **같은 전체 너비를 4칸이 쓴다**(주 단위라 더 크고 넓게)
      · `[실제 받은 보상 상세가 보여지는 공간]`
    - ⚠️ **착수 전에 보상표부터 확정해야 한다(사용자와 함께).** 아래는 코드에서 그대로 뽑은 **현재**
      상태이고, 그대로 그리면 UX가 어색해지는 지점이 셋 있다.

      | 회차 | 현재 보상 |
      | --- | --- |
      | 1일차 | 무르기 무제한(영구) |
      | 2일차 | 형세 1회권 30 · 추천 수 1회권 30 |
      | 3일차 | 광고 스킵권 3 |
      | 4일차 | **캐릭터 해금: 도장생 반상(3단계)** — 소모품 없음 |
      | 5일차 | 형세 30 · 추천 30 |
      | 6일차 | 광고 스킵권 5 |
      | 7일차 | 형세 50 · 추천 50 · 스킵 10 · **조각 1**(돌뫼) · **조각 1**(묘수) |
      | 14·21일차 | **7일차와 완전히 동일** |
      | 28일차 | 7일차 내용 + **캐릭터 해금: 관장 천원(5단계)** |
      | 35일차~ | ⚠️ **7의 배수로 무한 반복**(`isRewardedTier`: `tier in 1..7 || tier % 7 == 0`) |

    - ⚠️ **어색해지는 지점 셋 — 결정 필요**
      · **ⓐ 28일차가 끝이 아니다.** 정책은 35·42…로 무한히 이어지는데 화면은 10칸이다. 28 이후를
        ⓐ-1 "반복 주기"로 표시할지 / ⓐ-2 28을 종점으로 두고 이후 정책을 바꿀지 정해야 한다.
      · **ⓑ 칸별 무게가 고르지 않다.** 4일차는 캐릭터 하나뿐이라 6칸 그리드에서 유독 비고,
        7일차는 항목이 **다섯 개**라 한 칸에 안 들어간다. 넓은 4칸 레이아웃이 이걸 흡수하는지 확인.
      · **ⓒ 7·14·21일차가 똑같다.** 넓게 그리는 이유가 "주 단위라 보상이 크다"인데 셋이 동일하면
        "왜 넓지?"가 된다. **주차별 차등**을 두는 편이 이 UX 의도와 맞는다.
    - ⚠️ **조각은 주당 1개뿐이다.** 사범 묘수(10개 필요)를 출석만으로 열려면 **70일**이 걸린다.
      표에 "조각 1"이 작게 적히면 가치가 안 읽힌다 — 진행도(예: 3/10)를 같이 보여줄 것.
      #50이 만든 조각 공개 연출과 이어 붙이면 자연스럽다.
    - ⚠️ **밀린 회차는 한 팝업에 모아 한 번의 Claim으로 전부 지급된다**(기존 확정 동작).
      도장판으로 바꿔도 이 성질이 깨지면 안 된다 — 여러 칸이 한꺼번에 찍히는 경우를 그려야 한다.
    - ⚠️ **`AttendanceRewardClaimDialog`가 상태를 통째로 들고 있다**(`GoCoachApp` 훅 예산 절약용).
      화면이 커지면 그 파일이 비대해진다 — 순수 판정은 `AttendanceRewardPolicy` 쪽으로 밀 것.
    - 참고: 지급 자체는 **"화면에 들어오는 것"이 아니라 Claim 버튼**이 한다(킥오프 5.1절). 도장판을
      보여주는 것만으로 지급되면 안 된다.

    - ✅ **보상표 확정(2026-08-31, 사용자와 함께 검토 후)**

      | 회차 | 보상 |
      | --- | --- |
      | 1일차 | 형세 30 |
      | 2일차 | 추천 수 30 |
      | 3일차 | **무르기 무제한(영구)** |
      | 4일차 | 광고 스킵권 3 |
      | 5일차 | 조각 1 (연습생 돌뫼) |
      | 6일차 | 조각 1 (사범 묘수) |
      | **7일차** | **캐릭터: 도장생 반상** — 소모품 없음 |
      | 14·21일차 | 형세 50 · 추천 50 · 스킵 3 |
      | **28일차** | **캐릭터: 관장 천원** — 소모품 없음 |
      | 35일차~ (7의 배수) | 형세 50 · 추천 50 · 스킵 3 |

      **설계 의도(사용자)**: 형세·추천을 나눠 줘 각각을 인지시키고, **3일차까지 무르기가 유료임을
      겪게 한 뒤** 지급해 가치를 체감시킨다. 5·6일차 조각으로 *"캐릭터는 광고를 봐야 모인다"* 를
      알려 유입시킨다. 7·28일차는 캐릭터라 소모품을 얹지 않는다. 반복 보상은 **한 주가 풍족하면
      프리미엄 의미가 없으므로 갈증이 남게** 준다.
      · ✅ 6칸 행이 전부 소모품·기능, 4칸 행이 `[캐릭터][반복][반복][캐릭터]`가 되어 **양 끝이
        특별해지는** 구조가 된다. 도장판 UX와 잘 맞는다.
      · ✅ **"무르기가 유료"라는 학습이 실제로 일어난다** — 무르기는 1회권이 아예 없어서
        (소모품은 형세·추천·스킵 셋뿐) 1·2일차에 쓰려면 광고를 봐야 하고, 스킵권조차 4일차에
        처음 나온다.
      · ⚠️ **검토 중 나온 "35일차~ 반복에 조각 1개" 제안은 사용자가 기각했다**(2026-08-31):
        그 시점이면 광고로 이미 확보한 사용자가 많고, 간격이 너무 벌어져 경로 구실을 못 한다.
        **조각의 무광고 경로는 의도적으로 없다** — 5·6일차 1개씩이 전부이고, 돌뫼는 광고 4번,
        묘수는 광고 9번이 더 필요하다. 되살리자는 제안을 다시 올리지 말 것.

    - ⚠️ **보유 상한을 종류별로 나눈다(사용자 확정)**: **기본 99, 광고 스킵권만 9.**
      아이템이 쌓이면 가치가 희석되고 "모으는 재미"에 빠지므로, **어차피 또 받을 것이니 쓰자**는
      쪽으로 유도한다.

    - ⚠️ **착수 시 반드시 같이 고쳐야 할 것 둘(코드 대조로 확인)**
      · **ⓐ 지금 구조로는 14·21일차가 빈 칸이 된다.** `rewardsFor`가
        `contentTier = if (tier > 7) 7 else tier`로 접어서 **14·21·35…가 전부 7일차 내용을 쓴다.**
        확정표에서 7일차는 "캐릭터만"이므로, 그대로 두면 14일차에 **이미 가진 캐릭터만 나오고
        소모품이 안 나온다.** → **7·28은 특별(캐릭터) / 그 외 7의 배수는 반복 번들**로 분리해야
        한다. 표 수정이 아니라 접기 로직 재작성이다.
      · **ⓑ 상한 초과분이 조용히 사라진다.** `ConsumableInventory.withGranted`가 상한에서 잘라내고
        **버린다.** 스킵권 상한이 9면 4일차 3 + 14일차 3 + 21일차 3 = **정확히 9**라, 35일차부터
        매 반복마다 "광고 스킵권 3개"라고 안내해 놓고 **0개가 들어온다.** 상한이 99일 때는 거의
        드러나지 않던 결함이 9로 낮추는 순간 상시화된다 — 팝업에서 상한 도달 항목을 표시하거나
        목록에서 뺄 것.

    - ⚠️ **도장생 반상의 해금 티어를 코드에서 4 → 7로 옮겨야 한다**(`BotCharacterCatalog`).
      저장소는 캐릭터 id만 담으므로 기존 데이터는 안전하고, 이미 4일차에 받은 테스터는 그대로 보유한다.

### 구현 결과 (2026-08-31, **사용자 승인 완료**)

**확정표 그대로 들어갔고, 착수 전 경고한 두 결함도 함께 고쳤다.**

**ⓐ 정책 구조를 갈랐다.** 예전 `rewardsFor`는 `contentTier = if (tier > 7) 7 else tier`로 접어서
14·21·35…가 7일차 내용을 그대로 썼다. 확정표에서 7일차는 **캐릭터뿐**이라 그대로 뒀으면
**14일차에 이미 가진 캐릭터만 나오고 소모품이 통째로 사라질** 뻔했다. 이제 반복 회차 판정이
*"7의 배수이면서 **캐릭터가 걸리지 않은** 회차"* 다 — 7·28을 빼는 이 조건이 핵심이라, `isWeeklyRepeatTier`가
회차 번호를 직접 적지 않고 `BotCharacterCatalog.forAttendanceTier`에 물어본다(두 곳이 어긋나지 않게).

**ⓑ 상한 초과분을 숨기지 않는다.** `ConsumableInventory`에 `grantableAmount`/`isAtMaxStock`을 넣고,
청구 상세가 실제로 0개가 들어오는 항목에 **"보유 상한"** 을 붙인다. 이게 없으면 스킵권 9개인
사용자가 *"광고 스킵권 3개"* 를 보고 0개를 받는다 — 상한이 99일 때는 거의 안 드러나던 결함이
9로 낮추는 순간 **35일차부터 매 반복마다** 벌어진다.

**보유 상한을 종류별로 나눴다**: `ConsumableItem.maxStock`(기본 99, **광고 스킵권만 9**).
상수 하나가 아니라 카탈로그 데이터로 둬서, 다음에 종류가 늘어도 같은 자리에 적으면 된다.

**도장판 UI**: 위 6칸(1~6일차, 정사각) + 아래 4칸(7·14·21·28일차, 같은 전체 너비를 넷이 나눠 써
자연히 넓고 높다) + 상세 영역. 칸은 세 상태다 — **도장(✓)** · **청구 가능**(초록 테두리) · **예정**.
⚠️ **좁은 6칸은 짧은 표기**("형세 30"), **넓은 4칸은 전체 문구**를 쓴다 — 처음에는 6칸에도 전체
문구를 넣었더니 전부 말줄임으로 뭉개졌다(실기 확인). 4칸이 넓은 이유가 여기서 드러난다.

**⚠️ 28일차는 판의 끝이지 정책의 끝이 아니다.** 35·42…가 이어지므로 판은 10칸으로 고정하고
넘치는 회차를 `AttendanceBoard.beyondBoard`로 따로 들고, 화면은 "이후 7일마다 반복"으로 알린다.

**테스트**: `AttendanceBoardTest` 7건 신설(판 구성·세 상태·판 밖 회차·상한 보고). ⚠️ 기존
**15건이 옛 표를 고정하고 있어 전부 갱신**했다 — 값만 바꾼 것이 아니라 뜻이 뒤집힌 것도 있다:
- `theSeventhDayFeedsShardsSoTheAdPathIsNotTheOnlyWay` → **정반대로 뒤집었다.**
  이제 조각의 무광고 경로가 없으므로 `shardCharactersCannotBeFinishedByAttendanceAloneOnPurpose`가
  1~100회차를 훑어 *"출석으로 나오는 조각은 1개뿐"* 임을 고정한다. 되살리려면 그 결정부터 뒤집어야 한다.
- `multiplesOfSevenRepeatTheSeventhDayExactly` → `weeklyRepeatsSkipTheCharacterTiers`.
- 1일차 무르기를 단언하던 셋은 3일차로, 캐릭터 4일차를 단언하던 둘은 7일차로.
2369건 전부 통과.

**실기 검증(에뮬레이터, R8 `playInternal`, 데이터 삭제 + 날짜 이동)**
| 확인 | 결과 |
| --- | --- |
| 1·2일차 수령 후 재실행 | 두 칸에 **도장(✓)**, 3일차가 초록 테두리로 청구 가능 |
| 6칸 행 | 형세 30 · 추천 30 · 무르기 · 스킵 3 · 조각 1 · 조각 1 — 잘림 없음 |
| 4칸 행 | 7일차 캐릭터 · 14·21 반복 · 28일차 캐릭터 |
| 상세 영역 | 지금 받을 회차와 항목만 표시 |

⚠️ **남은 것 — 판은 "받을 것이 있을 때만" 보인다.** 이 화면은 Claim 팝업이라, 다 받고 나면 판을
볼 길이 없다. 사용자 요구가 *"10번의 보상을 한눈에"* 였으므로 **마이 페이지에 상시 진입점**을
두는 편이 맞아 보인다 — 범위를 넘는 판단이라 별도 항목으로 낼지 확인이 필요하다.

**산출물**: (shared) `attendance/AttendanceBoard.kt` 신규 + 테스트 7건,
`attendance/AttendanceRewardPolicy.kt`(표·접기 로직 재작성), `consumable/ConsumableItem.kt`(종류별 상한),
`consumable/ConsumableInventory.kt`(`grantableAmount`·`isAtMaxStock`),
`botcharacter/BotCharacterCatalog.kt`(반상 4→7일차), 기존 테스트 4파일 갱신.
(app-android) `ui/AttendanceRewardClaimDialog.kt`(도장판), `ui/UiStringsAttendanceBoard.kt` 신규(4개 언어).

---

56. 출석 도장판을 **마이 페이지에서 상시로** 볼 수 있게 (AI 모델: Sonnet, 노력정도: 낮음~중간) [완료]
    - 2026-08-31 발행. #55가 10칸 도장판을 만들었지만 그것이 **Claim 팝업 안에만 있다** —
      받을 것이 없으면 팝업이 아예 안 뜨므로, 다 받고 나면 **자기 진행도를 볼 길이 없다.**
      사용자 요구가 *"10번의 보상을 한눈에 볼 수 있게"* 였으므로 이 상태로는 절반만 달성이다.
    - ✅ **화면은 이미 순수 계층으로 분리돼 있다** — `buildAttendanceBoard(state, collection)`가
      판을 통째로 만들어 주고 지급은 하지 않는다. 마이 페이지에서 같은 함수를 부르면 된다.
    - ⚠️ **Claim 버튼을 같이 옮기지 말 것.** 지급은 팝업이 한다(킥오프 5.1절: "화면에 들어오는
      것만으로 지급되지 않는다"). 마이 페이지의 판은 **읽기 전용**이어야 한다 — 두 곳에서
      지급할 수 있으면 밀린 회차 계산이 두 경로로 갈린다.
    - 마이 페이지는 지금 1회권 잔량만 보여주는 얇은 화면이라(`보유 중인 1회권`) 판을 얹기 좋다.
    - ⚠️ 그 화면에서 판을 보는 동안 **당일 체크인이 일어나면 안 된다** — 체크인은 앱 시작 시
      한 번이라는 지금 구조를 건드리지 말고 저장된 상태만 읽을 것.

### 구현 결과 (2026-08-31, **사용자 승인 완료**)

**도장판 그림을 `ui/AttendanceBoardView.kt`로 분리**해 Claim 팝업과 마이 페이지가 **같은
컴포저블**을 쓰게 했다. 한쪽에만 고치면 같은 화면이 두 모양으로 갈리기 때문이다.
마이 페이지 쪽은 **읽기 전용**이다 — 체크인도 하지 않고(저장값만 읽는다) Claim 버튼도 없다.

**⚠️ 이 항목의 진짜 수확은 판이 아니라, 판이 드러낸 결함이다.**
마이 페이지에 판을 얹자마자 **"1일차 도장은 찍혔는데 1회권은 0개"** 라는 모순이 한 화면에
나란히 보였다. 재실행하면 30개가 나오니 저장은 정상이고, 화면이 낡은 값을 들고 있었다.
- **1차 원인**: `buildConsumableUiState`가 재고를 `remember { store.load() }`로 한 번만 읽고
  **`spend` 안에서만** 갱신한다. 출석 지급(`runAttendanceRewardGrant`)은 저장소에 **직접** 쓰므로
  화면은 그 사실을 영영 모른다. → `ConsumableUiState.refresh()`를 내고 지급 직후 부르게 했다.
- **⚠️ 그런데 그것만으로는 안 고쳐졌다.** `AttendanceRewardClaimDialog`가 **167행**에서 불리는데
  `LocalConsumableUiState`의 provider는 **783행**이라, 팝업이 읽은 것은 **기본값**(빈 상태,
  `refresh`가 no-op)이었다. 팝업을 provider 안으로 옮겨서야 실제로 동작했다.
  · 이 두 번째 원인은 **화면을 보지 않았으면 못 찾았을 것이다** — 코드상 `refresh()`는 분명히
    불리고 있었고 컴파일도 테스트도 통과했다. 실기에서 값이 안 변하는 것을 보고서야 provider
    위치를 의심하게 됐다.
  · 재발 방지로 그 자리에 경고 주석을 남기고 `LayeringContractTest` 라인 예산을 870 → 874로
    올렸다(늘어난 4줄이 그 주석이다).

**부수 효과**: 이 결함은 마이 페이지만의 것이 아니었다 — 대국 화면의 1회권 표시도 출석 지급
직후에는 낡은 값이었다. 같은 수정으로 함께 나았다.

**테스트**: 2369건 전부 통과. ⚠️ **이 항목에도 새 회귀 테스트를 넣지 못했다** — 결함이
"컴포지션 트리에서 provider보다 위에 있다"는 **배치 문제**라 JVM 단위 테스트로 재현할 수 없다.
대신 그 자리 주석과 라인 예산 이력으로 남겼다.

**실기 검증(에뮬레이터, R8, 데이터 삭제 후 첫 실행)**
| 확인 | 결과 |
| --- | --- |
| 마이 페이지 "출석 현황" | 10칸 판 + "28일차 이후에는 7일마다 반복" 안내 |
| 1일차 수령 직후(재실행 없이) | 도장 ✓ 와 **형세 30개**가 일치 — 고치기 전에는 0개였다 |
| 기존 "보유 중인 1회권" | 그대로 유지 |

**산출물**: (app-android) `ui/AttendanceBoardView.kt` 신규(팝업에서 분리),
`ui/MyPageScreen.kt`(읽기 전용 판), `ui/ConsumableUiState.kt`(`refresh` 통로),
`ui/AttendanceRewardClaimDialog.kt`(지급 후 갱신), `ui/GoCoachApp.kt`(팝업을 provider 안으로),
`ui/UiStringsAttendanceBoard.kt`(섹션 제목 4개 언어), `LayeringContractTest`(예산 870→874).

---

59. 첫돌이 소개 문구를 **실제 기력에 맞게** 고쳐 쓴다 (AI 모델: Sonnet, 노력정도: 낮음) [완료]
    - 2026-08-31 사용자 지시. 기존 문구는 *"입문자와 두려고 일부러 자주 실수하는 상대예요."* 였는데
      **1단계의 실기력은 일반 중급자를 상회한다**(사용자 확인 — 애초에 그래서 호선이 아니라
      접바둑 기능을 넣었다). 얕잡아 소개해 두면 첫 판에서 진 사용자가 속았다고 느낀다.
    - ⚠️ **한 앱 안에서 두 말이 공존하던 상태였다.** #51이 랜딩 문구에 *"상대를 얕잡아 말하지
      않는다"* 를 세우고 그걸 4개 언어 금칙어 테스트로 강제해 뒀는데, 정작 캐릭터 소개는 반대로
      말하고 있었다. 같은 축의 결함이라 이번에 맞췄다.
    - **새 문구(사용자 방향: "최적수를 두진 못하지만 수준 있는 캐릭터")**
      · 한국어 "최선의 수를 두지는 못하지만, 결코 만만치 않은 상대예요."
      · English "Doesn't always find the best move, but a capable opponent."
      · 日本語 「最善手とはいきませんが、決して侮れない相手です。」
      · 简体中文 "未必能下出最佳一手，但实力不容小觑。"
      · ✅ **5단계와 대비를 이루게 잡았다** — 관장 천원이 *"언제나 최선의 수만 둡니다"* 라, 같은
        낱말("최선의 수")을 쓰면 두 캐릭터가 한 축 위에 놓인다.
    - 회귀 그물 1건(`UiStringsBotCharacterTest.theEntryOpponentIsNeverIntroducedAsWeak`) —
      1단계 소개에 "일부러/자주 실수/on purpose/loose/わざと/故意" 같은 표현이 **네 언어 어디에도**
      들어가지 않는지 본다. 랜딩 쪽 그물과 짝을 이룬다.
    - ⚠️ **#46이 같은 문구를 이미 한 번 고쳤다**(2026-08-30). 그때는 맞춤법·말투를 다듬은 것이고
      **강도 서술은 그대로 뒀다** — 이번이 그 축을 바꾼 첫 수정이다.
    - 산출물: (app-android) `ui/UiStringsBotCharacters.kt`(4개 언어), `test/.../UiStringsBotCharacterTest.kt`(1건).

57. 출석 보상 팝업 **뷰티파이 고도화** (AI 모델: Opus, 노력정도: 중간) [완료]
    - 2026-08-31 사용자 지시. #55는 **구조**를 세웠고(6칸+4칸+상세, 세 상태), 이 항목이 그 위에
      시각적 완성도를 얹었다 — 착수 전에는 사각형과 체크 문자뿐이라 "보상을 받는 기쁨"이 없었다.
    - **한 일: 도장판을 글자판에서 그림판으로 다시 썼다.**
      · **보상 종류별 글리프 5종을 벡터로 작도**했다(#48과 같은 방식, 새 의존성 없음) —
        형세(반쪽 채운 원판) · 추천(네 갈래 반짝임) · 무르기(되돌리기 화살) · 스킵(▶|) ·
        착수 평가(막대 셋). 마지막 하나는 지금 표에 없지만 `FeatureId`가 네 갈래라 미리 채웠다.
      · **캐릭터 회차에 아바타를 얹었다**(로드맵이 ✅로 짚어 둔 항목). 7·28일차는 얼굴 + 이름,
        **5·6일차는 조각 공개 연출(#50)을 그대로 재사용**해 모은 만큼만 색이 돈다.
      · **도장을 찍어도 보상 그림을 지우지 않는다.** #55는 도장을 찍으면 내용을 감췄는데,
        그러면 다 채운 판이 **똑같은 체크 열 개**가 되어 무엇을 받아 왔는지가 통째로 사라졌다.
    - ⚠️ **글자를 버린 것이 이 항목의 본질이다.** 6칸 폭 제약(로드맵 경고 ①)과 4개 언어
      길이 차이(경고 ②)는 **같은 문제의 두 얼굴**이었다 — 한국어에 맞춰 짧은 표기를 만들어도
      `추천`/`Moves`/`推荐`가 서로 달라 어느 한 언어는 반드시 깨진다. **글리프에는 길이가 없어**
      네 언어가 같은 폭을 쓴다. 좁은 칸의 회차도 같은 이유로 **숫자만** 쓴다("第 6 天"은 45dp에서
      잘린다). 뜻은 `contentDescription`이 지킨다.
      · 그래서 `attendanceRewardShortLabelFor`(#55의 짧은 표기표)를 **걷어냈다.** 되살리지 말 것 —
        그것이 존재했던 이유를 글리프가 이미 해결한다.
    - **실기에서만 드러난 결함 셋**(전부 테스트는 통과하고 있었다):
      · ⓐ **인장을 칸 한가운데에 찍으니 22dp 글리프를 24dp 원이 덮어** 보상 그림이 안 보였다 —
        겹치기로 한 이유가 그 자리에서 무너졌다. 모서리(TopEnd)로 옮기고 크기를 줄였다.
      · ⓑ **조각 이음선이 흰 거미줄로 보였다.** `BotCharacterAvatar`가 이음선을 `surface`로
        고정하고 있었는데(픽커 카드 색), 도장판의 칸은 `surfaceVariant`/`primaryContainer`다.
        **`seamColor` 매개변수를 열어** 호출부가 자기 배경을 넘기게 했다.
      · ⓒ **개수가 없는 회차(무르기·조각)만 글리프가 아래로 처져** 여섯 칸 높이가 어긋났다.
        개수 줄을 **비운 채로 항상 남겨** 줄을 맞췄다.
    - ❌ **도장 애니메이션·순차 연출은 넣지 않았다 — 넣을 자리가 없다.** 팝업은 Claim하는 순간
      닫히므로(정책상 닫는 모든 경로가 지급이다) **도장이 찍히는 그 순간에 판이 사라진다.**
      마이 페이지 판은 읽기 전용이라 찍히는 사건 자체가 없다. 되살리려면 "받고도 팝업이
      잠시 남는다"는 정책 변경이 먼저이고, 그건 `docs/ATTENDANCE_REWARD_POLICY.md` 1장과 충돌한다.
    - **검증**: 네 언어를 **전부 실기에서** 봤다(한국어/中文/English/日本語 — 로드맵 경고 ②).
      가장 긴 조합인 English 캐릭터 이름("Goban the Student")이 두 줄에 말줄임 없이 들어간다.
      R8 `playInternal`에서 `aapt2 dump resources`로 **글리프 5종이 리소스 축소를 살아남는지**
      확인했다 — `rewardGlyphRes`가 명시적 `when`이라 참조가 잡힌다(#47 이후 필수 점검).
    - 회귀 그물 7건: `AttendanceBoardViewTest`(3) — ⚠️ **소모품 분기는 `else -> null`로 닫혀 있어
      컴파일러가 빠뜨림을 못 잡는다.** 카탈로그에 소모품을 더하면 그 칸만 조용히 비므로 그물을 걸었다.
      `UiStringsAttendanceBoardTest`(3) — 네 언어 완비 + **세 칸 상태가 소리로 구분되는지**.
      `AttendanceBoardTest.everyDailyCellCarriesExactlyOneReward`(1) — 좁은 칸이 보상을 **하나만**
      그린다는 화면의 전제를 순수 계층에서 고정한다.
    - 산출물: (res) `drawable/reward_{eval,top_moves,undo,ad_skip,move_review}.xml` 5종 신규.
      (app-android) `ui/AttendanceBoardView.kt` 전면 재작성, `ui/BotCharacterAvatar.kt`
      (`seamColor` 매개변수 + `shardRevealOf` 헬퍼 — 픽커의 사본을 여기로 모았다),
      `ui/UiStringsAttendanceBoard.kt`(도장 안내 4개 언어 추가, 짧은 표기표 제거),
      `ui/AttendanceRewardClaimDialog.kt`·`ui/MyPageScreen.kt`(수집 상태 배선),
      `ui/BotCharacterUiState.kt`(헬퍼 사용). 테스트 3파일.

54. **정식 출시(릴리즈) 최종 체크리스트 정리** (AI 모델: Opus, 노력정도: 중간) [완료]
    - 2026-08-31 사용자 지시. 흩어진 "정식 출시에 반드시 필요한 것"을 한 곳에 모았다.
      지시대로 **새 구현 없이 정리·점검만** 했고, #18·#26을 다시 발행하지 않고 참조만 했다.
    - **산출물은 새 문서가 아니라 `launch-plan/README.md` §0의 전면 재작성이다.**
      · 새 최상위 문서를 만들면 문서 구조 정책(최상위 **5개** 상한, 현재 15개)을 더 어긴다.
        그리고 이 체크리스트의 **원래 자리가 거기다** — 260817에 만든 §0이 이미 그 역할이었다.
      · ⚠️ **진짜 고친 것은 목록이 아니라 구조다.** §0은 *상태*와 *히스토리*가 한 덩어리였고,
        그래서 **v0.1.11 시점 상태가 v0.8.x까지 "현재"인 척** 남아 있었다. 이제 §0은 상태
        표 넷(A 콘솔·B 코드/자산·C 결정·D "갭 아님")만 담고, 날짜별 세션 로그는 아래
        "갱신 기록"으로 내렸다. **둘이 어긋나면 표가 옳다**고 문서가 스스로 밝힌다.
    - **점검에서 그림이 바뀐 것 넷** (전부 추정이 아니라 저장소·실기로 확인):
      · ⓐ ⚠️ **임계 경로가 이 항목이 적어 둔 것과 다르다.** 이 항목은 *"ⓑ·ⓒ가 콘솔 3종
        (개인정보처리방침·IARC·데이터 보안)에 막혀 있다"* 고 썼는데, 2026-08-30에 사용자가
        "수익 창출" 잠금을 따라가 보니 **앱 콘텐츠 패널이 아니라 프로덕션 액세스 체크리스트**가
        나왔고 막고 있던 것은 **비공개 테스트 12명×14일** 한 줄이었다(그때 11일차).
        **8/31 기준 12일차, ETA ~2026-09-02.** 설정할 것이 없는 순수 시간 게이트다.
        · 콘솔 3종이 *추가로* 막는지는 여전히 미확인이다 — 그 화면에 안 나왔다고 해결된 것으로
          넘겨짚지 말라고 §0에 못박았다. 어차피 셋 다 필요하므로 카운트다운 동안 처리하면 된다.
      · ⓑ **스크린샷은 다섯 장 중 두 장만 다시 찍으면 된다.** 이전 기록은 세트 전체가 낡은
        것처럼 읽혔는데, `aacda3c..HEAD`에서 **대국 화면 소스가 한 줄도 안 바뀌어** `03`·`04`는
        그대로 쓸 수 있고 `01`은 실기 비교에서 동일했다. 낡은 것은 `02`(#52로 접바둑 기본값이
        5점→호선, 미리보기 판의 흑돌 다섯 점이 사라졌다)와 `05`뿐이다.
      · ⓒ ⚠️ **`05_character_picker`는 낡은 정도가 아니라 지금 거짓을 광고한다.** 화면 자체가
        캐러셀로 대체된 것(#48~#50)에 더해, *"출석 4일차에 받을 수 있어요"* 는 **7일차**로
        바뀌었고(#55) 첫돌이 소개는 **#59가 금지한 바로 그 문장**이 박혀 있다.
      · ⓓ ⚠️ **이전 기록의 사실 오류 하나를 정정했다** — *"`01_home`은 카드가 3장인데 HEAD는
        마이 페이지까지 4장"* 은 **사실이 아니다.** 마이 페이지는 카드가 아니라 헤더 칩이고
        양쪽에 다 있다. 실기 비교로 확인했다.
    - **그 밖에 확정한 것**:
      · **AAB가 9개 커밋 뒤처져 있다** — `dist/go-ai-coach-play-internal.aab`는 8/30 19:12
        산출물(807)이라 #47·#48~#50·#51·#52·#55~#57·#59가 전부 빠져 있다. 재빌드 필요(§0 B-1).
      · **`store_listing.txt`의 "앱 내 결제 없음"은 지금 참이다** — 플래그 셋(`isLoginEnabled`·
        `isPurchaseEnabled`·`isBotCharacterPurchaseEnabled`)이 전부 `false`임을 확인했다.
        바꾸는 시점은 #18·#26 **배포 때**이지 지금이 아니다.
      · **`release` 빌드타입 실기 검증은 여전히 0회다**(§0 B-3). 정식 출시는 그 빌드로 나간다.
      · ⚠️ **캐릭터 상품 가격이 문서 안에서 갈려 있다** — #18의 제목·"남은 것" 줄은 **4,900원**,
        같은 항목의 "착수 전 확인" 줄은 **9,900원**. 콘솔 등록 전에 정할 것으로 §0 C-2에 올렸다.
      · `launch-plan` §4·§5에 남아 있던 *"`isPurchaseEnabled`를 언제 켤지"* 미정 항목은
        **#26(월 구독 전환)이 성격을 바꿔 놓은 상태**였다 — 그대로 켜는 선택지가 없어졌음을
        두 절에 적고 §0으로 이어 뒀다.
    - **새 일감을 만들지 않았다.** §0의 B표(AAB 재빌드·스크린샷 재캡처·`release` 실기)가 그
      셋의 트래커다 — 백로그에도 올리면 같은 일이 두 곳에서 갈린다. 출시 직전에 셋을 묶어
      한 항목으로 발행할지는 사용자 결정으로 남긴다.
    - 산출물: `launch-plan/README.md`(§0 전면 재작성 + §4·§5 교차 참조), `docs/DOCS_INDEX.md`
      (§0 설명 갱신). 코드 변경 없음.


## 진행 중

없음.


## 예정사항

> **#54가 2026-08-31에 끝나면서 출시 트랙의 정본이 `launch-plan/README.md` §0으로 옮겨갔다.**
> 남은 출시 작업(콘솔 폼·AAB 재빌드·스크린샷 2장·`release` 실기)은 **그 표가 추적한다** —
> 여기에 다시 적으면 두 곳이 갈린다. 이 목록에 남는 것은 **코드 일감**뿐이다.
> ⚠️ **#26·#18은 여전히 출시 조건이다** — 후순위는 *착수 순서*일 뿐이고, 둘 다 Play Console
> 수익 창출이 열려야 실기 검증이 된다(§0 A-7·A-8).

53. 설정 화면에서 **앱 업데이트 가능 여부 확인 + 업데이트 버튼** (AI 모델: Opus, 노력정도: 중간)
    - 2026-08-31 사용자 지시. 설정에 들어오면 업데이트가 있는지 확인하고, 버전 정보 패널에
      "앱 업데이트하러 가기" 같은 버튼을 띄운다.
    - ⚠️ **가장 큰 함정: 실기 검증이 사실상 불가능하다.** Play In-App Update
      (`com.google.android.play:app-update`, **신규 의존성**)는 **Play로 설치된 앱에서만** 동작한다 —
      `adb install`한 빌드나 사이드로드한 friend APK에서는 `AppUpdateManager`가 업데이트를 못 찾는다.
      지금까지 해 온 에뮬레이터 검증으로는 "버튼이 안 뜬다"까지만 확인되고, **뜨는 경우를 볼 수 없다.**
    - 선택지: ⓐ In-App Update API로 실제 확인 후에만 버튼 노출(정석, 검증 어려움) /
      ⓑ 확인 없이 항상 "스토어에서 확인" 링크(단순하지만 "체크 한 뒤"라는 요구 미충족) /
      ⓒ **ⓐ로 하되 확인 실패·미지원이면 ⓑ로 폴백**. **ⓒ를 권한다** — Play 설치본에서는 정확히
      동작하고, 그 밖(내부 테스트 사이드로드 포함)에서도 죽은 화면이 되지 않는다.
    - ⚠️ **버전 텍스트에는 이미 숨은 제스처가 있다** — `SettingsScreen.kt`의 그 텍스트를 **10번
      두드리면 개발자 모드가 켜진다.** 업데이트 버튼을 그 텍스트에 겹치거나 바로 옆에 붙이면
      오탭으로 개발자 모드가 켜진다. **별도 행으로 뺄 것.**
    - ⚠️ 업데이트 확인은 네트워크를 탄다 — 설정 화면 진입마다 동기적으로 막지 말고, 결과가 오면
      그때 버튼이 나타나는 형태로. 실패는 조용히 넘긴다(업데이트 확인 실패로 설정 화면이
      경고를 띄우면 소음이다).

39. 바로 착수 모드의 **돋보기 드래그 착수** (AI 모델: Opus, 노력정도: 최대) — **출시 후순위(사용자 승인)**
    - 2026-08-30 사용자 지시. **난이도가 높으면 출시 뒤로 미뤄도 된다고 명시하셨다.**
    - 바로 착수 모드는 탭이 곧 착수라 오착수 위험이 있다. 손가락을 **누른 채 이동**하다 떼는 순간 착수하고, 누르는 동안에는 터치점 기준 **5×5 셀 반경이 확대**돼 어디에 놓이는지 보이게 한다. 떼면 확대 화면도 사라진다.
    - ⚠️ 지금 `GoBoard`는 `detectTapGestures`뿐이라(`ui/GoBoard.kt:153`) 드래그 추적 자체가 없다. `awaitPointerEventScope`로 다시 짜야 하고, 확대 레이어는 보드 Canvas 위에 별도로 그려야 한다.
    - ⚠️ **대국 화면 루트에 `verticalScroll`이 있다**(`ui/GoCoachContent.kt:120`). 보드에서 세로 드래그를 소비하기 시작하면 스크롤과 제스처가 충돌한다 — 지금은 탭만 쓰므로 문제가 없지만(#30에서 확인), 이 항목이 그 균형을 깬다.
    - #36(햅틱)과 짝이지만 **분리해서 발행했다** — 햅틱은 몇 줄이고 이쪽은 입력 파이프라인 재작성이다.
    - ✅ **스크롤 충돌 해소 방식이 정해졌다(2026-08-30 사용자 결정, #47 스레드에서 확인).**
      **꾹 누름(약 150~200ms)을 트리거로 둔다** — 그 시간 안의 세로 드래그는 스크롤에 그대로
      넘기고, 임계를 넘겨 누르고 있을 때만 돋보기를 띄우고 드래그 착수로 전환한다.
      · **고른 이유**: 바로 착수 모드의 존재 이유 자체가 "탭이 곧 착수라 위험하다"이므로,
        **의도적인 꾹 누름**을 관문으로 두면 스크롤을 뺏지 않으면서 우발적 착수 방지라는
        원래 목적에도 맞는다. 대안이던 ⓐ(보드가 무조건 소비)는 화면의 큰 면적에서 세로
        스크롤을 죽이고, ⓒ(이동 거리 임계)는 초반 몇 픽셀 동안 돋보기가 안 떠 판정이 애매하다.
      · ⚠️ **누르고 있는 동안 착수가 확정되면 안 된다** — 확정은 오직 떼는 순간이다.
    - ⚠️ **문서 정정(2026-08-30, #47 스레드에서 코드 확인)**: 위 "#36(햅틱)과 짝"이라는 서술과
      달리 **#36은 이미 구현돼 있다** — `ui/GoBoard.kt`의 `detectTapGestures` 주석("손가락이
      닿는 순간 약하게 한 번 울린다(#36)")과 `uxOptions.isPlayHapticEnabled`가 그것이다.
      그래서 이 항목은 "햅틱을 새로 넣는" 일이 아니라 **기존 햅틱 발화 지점을 옮기는** 일을
      포함한다: 지금은 **닿는 순간** 울리는데, 드래그 착수에서는 **떼는 순간**(착수 확정)으로
      가야 한다. 꾹 누름 임계를 넘긴 시점에도 한 번 울려 "이제 드래그 모드다"를 알릴지는 착수 시 판단.
    - ⚠️ **`verticalScroll`은 한 곳이 아니라 두 곳이다** — `ui/GoCoachContent.kt`의 **120행과 146행**.
      위 함정 항목이 120행만 짚고 있어 덧붙인다.
    - ⚠️ **검증이 반쪽이 될 수밖에 없다(#47과 같은 구조).** `adb shell input motionevent
      DOWN/MOVE/UP`으로 드래그를 합성하면 "돋보기가 뜨고 뗀 자리에 정확히 놓인다"까지는
      기계로 증명되지만, **"오착수가 실제로 줄었는가 · 손맛이 괜찮은가"는 실제 손가락으로만**
      판단된다. 스레드는 거기까지 만들고, 수용 판단은 실기에서 사용자가 한다.

26. 프리미엄을 영구 구매에서 **월간 구독(3,900원)** 으로 전환 (AI 모델: Opus, 노력정도: 높음)
    - 참고: `feature-access-principles/README.md` 8장(이미 "프리미엄 = 월 구독"이 기조), `premium-mode/README.md`. **2026-08-30 사용자 확정** — 금액이 5,000~10,000원에서 **3,900원**으로 좁혀졌고, "기능이 늘면서 가치가 오르면 그때 가격도 조정" 방침.
    - **핵심 난점**: "구매 = 영구"가 타입 하나가 아니라 **상태·판정·복원·저장·문구 다섯 곳에 각각 하드코딩**돼 있다. `PremiumState.isActive`는 `Purchase -> true`라 만료를 표현할 수 없고, `PurchaseOutcome.Purchased`는 페이로드가 없어 유효기간을 실어 나를 수 없다.
    - ⚠️ **착수 순서를 지켜야 한다.** `runPremiumPurchaseApplication`은 "복원에서 미소유가 나와도 절대 강등하지 않는다"를 의도적 설계로 못박아 뒀다 — 영구 구매에서는 옳았지만 구독에서는 만료자가 영원히 프리미엄을 유지하는 결함이 된다. **그런데 그 강등을 그냥 켜면 안 된다**: `AndroidBillingClient`가 "조회 성공+미소유"와 "조회 실패"를 똑같이 `null`로 뭉개므로, 강등을 켜는 순간 **일시적 네트워크 오류가 유료 구독자의 접근권을 박탈한다.** 그 conflation을 쪼개는 것이 1번이다.
    - ⚠️ `AndroidBillingClient`는 프리미엄(SUBS 예정)과 봇 캐릭터(INAPP, #18)가 **공유**한다. ProductType을 클래스 안에서 전역 치환하면 캐릭터 구매가 조용히 깨진다 — 지금 그 플래그가 꺼져 있어 테스트로도 안 드러난다. 생성자 파라미터로 올릴 것.
    - ⚠️ `PremiumSource.Purchase`를 **개명하지 말 것.** 저장소 디코드가 모르는 이름을 소리 없이 `None`으로 떨어뜨려 기존 사용자의 프리미엄이 사라진다. 개명 없이 의미만 바꾸면 이 위험이 통째로 사라진다.
    - **버튼 표기(#24)**: 구독도 `(∞)` 유지를 권한다 — 체감이 무제한이고, `(⏱)`는 로비 카운트다운과 짝이라 구독에 주면 "1시간 남음"으로 오독된다. 다만 사용자 확정 사안이므로 착수 시 승인받을 것.
    - **신규 UI가 필요하다**: 구독 관리/해지 딥링크가 지금 앱에 **전혀 없다**(구글 정책 요구). 마이 페이지(#24로 신설됨)가 그 자리다.
    - **ⓐ는 해소됐다(2026-08-30 사용자 확인): 영구 프리미엄 보유자가 아무도 없다.** 지금까지 비공개 테스트만 했고, 설령 누가 갖고 있더라도 **비공개 테스트 중에 얻은 권한이라 실제 오픈 시 초기화까지 고려 대상**이라는 방침이다. → **기존 보유자 마이그레이션을 설계할 필요가 없다.** 이 항목에서 가장 까다로울 뻔했던 축이 통째로 사라진 셈이라, `PremiumSource.Purchase`의 의미를 "영구"에서 "구독 유효"로 **그대로 바꿔도 안전하다**(개명만 하지 않으면 된다).
      · ⚠️ 다만 **"초기화를 고려한다"와 "초기화한다"는 다르다.** 실제 오픈 시 테스트 기간 권한을 밀 것인지는 아직 확정이 아니다 — 밀기로 한다면 그 시점에 별도 일감이 필요하다(저장소의 `PremiumState`를 어떤 조건으로 리셋할지, 무르기 클레임 같은 **별개 축**은 남길지).
    - 착수 전 확인(사용자): **ⓑ Play Console 수익 창출 잠금**(개인정보처리방침·IARC·데이터 보안)이 풀려야 구독 SKU를 만들 수 있다 — 그 전에는 실기 검증 불가.
    - ⚠️ **스토어 등록정보는 배포 시점에 바꾼다.** `store_listing.txt`가 "앱 내 결제 없음"을 세 번 단언하는데, 코드가 머지돼도 플래그가 꺼져 있는 동안은 그 문장이 **참**이다. 미리 바꾸면 스토어가 거짓을 말하고, 배포 때 잊으면 정책 위반이 된다.

18. 봇 캐릭터 개별 구매 배선 — 5단계 관장 천원, **4,900원** 단발성 결제·영구 소유 (AI 모델: Opus, 노력정도: 높음)
    - **2026-08-30 이관.** 출시 백로그의 유일한 미완 항목이었지만 이번 비공개 테스트 릴리즈에는
      필요 없다 — 플래그가 꺼져 있고 카탈로그에 유료 캐릭터가 없어 배선 전체가 잠들어 있다.
      **코드는 이미 머지돼 있고, 남은 것은 아래 ⓐⓑⓒ 세 줄뿐이다.**
    - 참고: 7장 획득 경로 표. **Phase 1 범위 확장이다** — 7장이 원래 "범위 밖(설계만 고려)"으로 못박아 뒀던 항목인데 2026-08-24에 범위 안으로 들어왔다.
    - `BotUnlockSource.Purchase`를 신설한다(#8에서 "지금 만들지 않았다"고 미뤄 둔 타입 — sealed라 추가하면 기존 `when`이 컴파일 에러로 빠진 처리를 잡아준다). **비소모성 단발 결제라 이미 검증된 `premium_lifetime`의 `AndroidBillingClient`(INAPP) 패턴을 그대로 재사용할 수 있다.**
    - 착수 전 확인(사용자): **Play Console 신규 상품 등록은 사용자 몫**이다(상품 ID·가격 9,900원 설정, 라이선스 테스터). 등록 전에는 실기 검증이 불가능하므로 착수 시점에 등록 여부를 먼저 확인할 것.
    - **구현 완료(2026-08-29) — 다만 결제 자체는 검증 불가**:
      · 결제 배선: `AndroidBillingClient(activity, productId)`가 이미 상품 ID로 매개변수화돼 있어 그대로 재사용했다. 상품 ID는 `local.properties`의 **`billing.botCharacterProductId`** 키로 주입하며, 없으면 플레이스홀더로 폴백해 "상품 없음"으로 안전하게 실패한다(프리미엄 상품과 같은 패턴).
      · 플래그: **`FeatureFlags.isBotCharacterPurchaseEnabled`(기본 `false`)** 를 신설했다. `isPurchaseEnabled`와 **일부러 분리**했다 — 그건 프리미엄 영구 구매를 가리는데, 프리미엄은 월 구독으로 가기로 확정돼 있어 캐릭터를 팔자고 그것까지 되살릴 이유가 없다.
      · 특전: `AllowedVia.CharacterPerk` 신설 + `PremiumUiState.resolve` 한 곳에서 접었다. 8.3절이 걱정한 "호출부 전부가 바뀐다"는 일어나지 않았다 — 인게임 게이팅이 이미 그 람다 하나로 모여 있었다.
      · ⚠️ **1회권 차감 방지가 이 항목의 숨은 핵심이다.** 특전을 소모품 판정(`alreadyAllowedVia`)까지 흘려보내지 않으면, 특전으로 이미 열려 있는데 표가 조용히 줄어든다. 테스트로 고정했다.
      · 재설치 복원: 산 캐릭터가 재설치로 사라지지 않도록 시작 시 복원 조회를 넣었다(`buildBotCharacterUiState` 안 — `GoCoachApp.kt` 라인 예산을 쓰지 않으려는 이유도 있다).
    - **실기 검증(에뮬레이터)**: 관장 천원을 소유로 심고 프리미엄 없이 대국 → 형세 보기가 열리고 영역 오버레이가 렌더되며 **1회권 3장이 그대로**. 상대를 첫돌이로 바꾸면 잠금 테두리가 돌아온다(특전이 새지 않는다).
    - ⚠️ **결제 부분은 Play Console 게이트에 막혀 보류됐다(2026-08-29)**. 콘솔의 "수익 창출"이 **"대시보드에서 앱 설정을 완료하세요"** 로 막혀 상품 등록 자체가 불가능하다 — 비공개 테스트 중이라서가 아니라, **앱 설정 대시보드의 미완료 항목(개인정보처리방침 필드·IARC 콘텐츠 등급·데이터 보안 양식)이 수익 창출의 선행 조건**이기 때문으로 보인다. 판매자(결제 프로필) 설정이 별도로 필요할 가능성도 남아 있다. 그 셋은 이미 `launch-plan/README.md` §0의 남은 콘솔 작업과 같은 목록이라, 하나를 끝내면 둘이 같이 열린다.
    - **그래서 결제는 미루고 5단계를 출석 보상으로 돌린다(2026-08-29 사용자 결정)** — 별도 항목으로 발행. 이 항목의 **특전·결제 배선 코드는 지우지 않고 그대로 둔다**: 카탈로그에 `BotUnlockSource.Purchase` 캐릭터가 없어지면 `isBotCharacterPerkActive`가 항상 `false`라 조용히 잠들 뿐이고, 유료 상품을 다시 열 때 그대로 깨어난다.
    - 남은 것(콘솔이 열린 뒤): ⓐ 4,900원 상품 등록 + 라이선스 테스터, ⓑ 상품 ID를 `local.properties`의 `billing.botCharacterProductId`에 기입, ⓒ `isBotCharacterPurchaseEnabled`를 `true`로.
    - 산출물(승인 대기): (shared) `application/botcharacter/BotCharacterPerk.kt` 신규, `application/premium/FeatureAccessPolicy.kt`(`CharacterPerk`·`characterPerkActive`), `application/consumable/ConsumableSpendApplication.kt`(특전 반영). (app-android) `build.gradle.kts`·`ui/FeatureFlags.kt`·`ui/PremiumPurchaseGlue.kt`·`ui/PremiumUiState.kt`·`ui/BotCharacterUiState.kt`·`ui/PlayerSetupPanel.kt`·`ui/UiStrings.kt`·`ui/GoCoachApp.kt`. 테스트 9건 신설, `LayeringContractTest` 라인 예산 851→856(사유는 그 주석에).
    - ⚠️ **프리미엄 월 구독 전환과 혼동하지 말 것** — 그건 이 백로그 밖(`premium-mode` 트랙)이고, 이 항목은 캐릭터 1종을 파는 별개 상품이다.
    - **구매 특전도 이 항목의 몫이다(2026-08-29 추가)**: 구매자는 **그 캐릭터와 두는 동안** 형세 보기·추천 수를 무제한 쓴다. ⚠️ 지금의 `FeatureAccessPolicy`는 전역 판정이라 이걸 표현할 수 없다 — 착수 전 정해야 할 세 가지가 `feature-access-principles/README.md` 8.3절에 있다.

---


---


---


58. **문서 구조 재편 마무리** — 최상위 15→5, 중분류 8→5 (AI 모델: Sonnet, 노력정도: 중간)
    - 2026-08-31 발행. 구조 정책은 `docs/DOCS_INDEX.md`의 "문서 구조 정책" 절에 이미 세웠고,
      **1단계(로그 분리)만 끝났다**(`docs/measurements/`, 하위 폴더 9 → 8).
    - **목표 형태는 그 절의 "현재 상태"에 이미 적혀 있다** — 여기 옮겨 적지 말 것(두 곳이 어긋난다).
      요약하면 최상위는 `HANDOVER` + 요약 4개(`PRD`·`ARCHITECTURE`·`ENGINE`·`OPERATIONS`),
      나머지 딥다이브 7개는 하위로. 중분류는 `architecture`·`engine`·`measurements`·`roadmap`·`history`.
    - ⚠️ **로그 이동보다 훨씬 위험하다.** 옮길 대상이 **핵심 문서끼리 서로 인용하는 것들**이다
      (예: `ENGINE.md` ↔ `ENGINE_API_CALL_POLICY.md`, `OPERATIONS.md` ↔ `USER_OPTION_MANUAL.md`·
      `DIAGNOSTIC_EVENT_SCHEMA.md`·`SCORE_AND_ENDGAME_DECISION.md`). 로그는 아무도 인용하지 않는
      데이터라 쉬웠지만, 이쪽은 **상대 링크와 절 참조가 사방으로 얽혀 있다.**
    - ⚠️ **착수 순서를 지킬 것**(정책의 "폴더를 옮길 때" 조항):
      ① 옮길 문서 전부에 대해 `grep -rn "<파일명>" .`을 **문서·코드 주석·`Makefile`·`scripts/`** 에 돌린다
      (로그 이동에서 `Makefile` 4줄이, 2026-08-30 이동에서 코드 주석 8곳이 이 방법으로만 잡혔다).
      ② 옮긴다. ③ **상대 링크(`../`)를 다시 계산한다** — 깊이가 한 단 깊어지면 전부 어긋난다.
      ④ 문서가 가리키는 파일이 실재하는지 기계로 재확인한다.
    - ⚠️ **`DOCS_INDEX.md`의 표 셋을 함께 고쳐야 한다** — "문서/폴더 전체 지도", "최상위 핵심 문서",
      그리고 각 하위 폴더 절. 이 인덱스는 **표와 실제 파일이 어긋난 전력이 있다**(2026-08-29에
      3중 불일치 발견). 개수 표기까지 실제와 맞출 것.
    - ⚠️ **`HANDOVER.md`가 진입점이라 경로를 직접 안내한다** — 재편 후 그 안내가 낡으면 새로 온
      사람이 첫 화면에서 막힌다. 마지막에 반드시 확인.
    - 참고: `error-cases/`는 raw 로그가 아니라 **분석 문서 3건**이고 `SCORE_AND_ENDGAME_DECISION.md`가
      인용한다 — `measurements/`가 아니라 `engine/` 또는 `history/` 쪽이 맞다(착수 시 판단).

---

## 관련 문서

- `260823-260830_OFFLINE_ENGAGEMENT_FEATURES_BACKLOG.md` — 출시까지의 본 백로그(완료 이력 포함)
- `260823-260830_OFFLINE_ENGAGEMENT_FEATURES_KICKOFF_PLAN.md` — 설계/스펙 원본과 구현 결정 각주
