# 도메인 분리 리팩토링 상태 재확인 — 260806 13h04m

작성 시각: 2026-08-06 13:04 (KST)
선행 문서: `DOMAIN_SEPARATION_REFACTORING_KICKOFF_PLAN_260729_2315.md` (2026-07-29 23:15 착수, 2026-07-30 00:08 종료)

## 0. 이 문서를 쓰는 이유

같은 날(2026-08-06) 다른 세션에서 `application/session/GameSessionStateHolder.kt`의 `shared` 모듈 이전 범위를 검토하다가, 선행 문서(260729_2315)의 5절 상태표 중 M-01 행("`gameSessionStateHolderStaysPlatformFreeForSharedMove` 계약 테스트는 있음")만 보고 "이식성 검사가 그 클래스 1개에만 걸려 있다"고 잘못 판단해 사용자에게 그렇게 보고했다. 실제로는 같은 문서 2절(63번째 줄)에 `application/` 전체(123개 파일, 예외 1개)를 재귀 검사하는 `engineOperationApplicationPoliciesStayPortable` 테스트가 **이미** 존재한다고 정확히 기록되어 있었다 — 상태표 한 셀만 읽고 문서 본문을 안 읽은 게 원인이었다. 이 문서는 그 정정과, 8일이 지나며 실제로 달라진 항목(M-04)을 함께 남긴다.

## 1. M-01~M-08 재확인 (260729_2315 대비 변경분만)

| 항목 | 260729 시점 | 260806 재확인 | 변경 여부 |
| --- | --- | --- | --- |
| M-01 `GameSessionStateHolder` shared 이전 | 🟡 1단계만 완료 | 🟡 동일 — 물리 이전 여전히 미착수. **단, 이식성 가드레일은 260729 시점에도 이미 `application/` 전체(123개 파일, 예외 1개) 범위였다**(`engineOperationApplicationPoliciesStayPortable`, R6에서 강화된 `forbiddenReferenceOffenders` 사용, wildcard import/FQN 인라인 사용까지 탐지). 단일 클래스 전용 검사가 아니다. | 상태 불변, 문서 이해만 정정 |
| M-04 androidTest/Robolectric smoke coverage | 🟡 skeleton만 존재(`AppLaunchSmokeTest.kt` 1개, 24줄, `@Ignore`) | 🟡 **실제 동작하는 테스트 1개 추가됨**: `NewGameBoardTapSmokeTest.kt`(앱 실행 → 새 게임 시작 → 보드 탭 → 착수 반영, `emulator-5554`에서 3회 연속 통과 확인). 전체 목표 5개 경로 중 3개(앱 실행/새 게임/보드 탭) 커버. `AppLaunchSmokeTest.kt`는 여전히 `@Ignore` — 별개 관심사(실제 `MainActivity`/엔진 부트스트랩 대상)라 건드리지 않음. | **완료 진행분 갱신** |

M-02·M-03·M-05~M-08은 이번에 재검증하지 않았다 — 260729_2315 5절의 값을 그대로 신뢰한다. 이 항목들이 실제 의사결정 근거로 쓰일 세션은 코드를 직접 재확인할 것(3절 참고).

## 2. M-01/M-02 물리적 이전 보류 결정 — 독립적으로 재확인됨

260729_2315의 진행 로그(260730 00h08m)에 기록된 "iOS/KMP 착수가 실제로 필요해질 때까지 물리 이전 보류" 결정을, 오늘 별도 세션에서 그 기록을 모른 채로 다시 마주쳤다. 사용자가 iOS를 "언젠가 고려 중"(미확정)이라 답했고, 1절에서 정정한 대로 가드레일이 이미 `application/` 전체 범위로 작동 중임을 근거로 "지금 물리 이전을 미뤄도 커플링이 몰래 쌓이는 리스크는 없다"고 독립적으로 결론지었다 — **260730과 260806, 두 시점에서 별도로 같은 결정에 도달**한 셈이다. 다음 세션은 이 결정을 원점부터 다시 검토할 필요 없이, iOS/KMP가 실제로 확정되는 시점에만 재론하면 된다.

## 3. 다음 세션에게

- `docs/refactoring/DOMAIN_SEPARATION_*` 중 가장 나중 타임스탬프 문서(현재는 이 문서)를 먼저 찾을 것 — 그리고 **상태표 셀만 읽지 말고 해당 절 본문까지 읽을 것**. 이번 실수의 직접 원인이 그것이었다.
- M-02/M-03/M-05~M-08은 이 문서에서 재검증하지 않았다. 실제 의사결정에 걸려 있다면 코드에서 직접 재확인할 것 — 문서 상태표를 그대로 신뢰하지 말 것.
