`[기능명]_SPEC_BACKLOG.md`와 한 쌍(Twin)을 이루는 `[기능명]_SPEC_PLAN.md` (기획/설계 원본 문서)의 표준 템플릿과 **구현 결정 각주(Footnote) 작성 규칙**입니다.

이 문서는 AI 스레드가 **"왜(Why), 무엇을(What), 어떤 제약 하에(Constraints)"** 만들어야 하는지 파악하는 **단일 진실 공급원(Single Source of Truth)** 역할을 합니다.

---

# 1. 스펙 문서 운영 원칙 및 각주 작성 규칙

스펙 문서는 처음 작성된 기획으로 끝나지 않고 개발 과정에서 지속적으로 진화하는 살아있는 문서(Living Document)입니다.

### 핵심 운영 원칙

1. **절 번호의 안정성**: 백로그가 `참고: 스펙 3.2절` 형태로 가리키므로, 대규모 구조 변경이 아니면 기본 절 번호 체계를 유지합니다.
2. **단방향 지식 역류**: AI 스레드가 코드를 작성하며 내린 아키텍처 판단과 버린 대안은 백로그가 아니라 **반드시 스펙 문서의 해당 절 아래 각주로 역류**시킵니다.
3. **각주는 Append-Only (추가 전용)**: 이미 작성된 각주는 수정하거나 지우지 않고 시간 순으로 덧붙입니다.

---

### 📌 구현 결정 각주(Footnote) 작성 공식

각주는 단순한 작업 일지가 아닙니다. "다음 AI 스레드가 똑같은 비효율적 대안을 다시 검토하지 못하게 막는 방어벽"입니다.

```markdown
> 구현 결정(백로그 #N, YYYY-MM-DD):
> 1. [선택한 방식]: 실제로 채택한 인터페이스/로직/설계
> 2. [버린 대안과 이유]: 검토했으나 배제한 방식과 그 기술적/도메인적 사유 (★ 가장 중요)
> 3. [영향 및 후속 주의점]: 다음 일감(#M)이나 타 모듈이 주의해야 할 부수효과

```

#### 실제 각주 작성 예시

> **구현 결정 (백로그 #12, 2026-08-24)**:
> 1. **선택**: `decideConsumableSpend` 순수 함수를 신설하여 차감 우선순위를 격리함.
> 2. **버린 대안**: 기존 `FeatureAccessPolicy.resolve()` 함수 내부에 차감 로직을 합치려 했으나 기각함.
> * *기각 사유*: `resolve()`는 단순 화면 UI 렌더링 시에도 호출되는 무상태 조회 함수인데, 여기에 자원 소모를 넣으면 '화면을 열어보기만 했는데 재고가 닳는' 치명적 버그가 발생함.
> 
> 
> 3. **후속 영향**: 인벤토리 화면 팝업을 담당하는 #15 스레드는 반드시 `ConsumableInventoryCoordinator`를 통해서만 차감을 트리거할 것.
> 
> 

---

# 2. 이식용 표준 템플릿: `[기능명]_SPEC_PLAN.md`

새 트랙 시작 시 아래 템플릿을 복사하여 `[기능명]_SPEC_PLAN.md`로 저장하여 사용합니다.

```markdown
# [기능/트랙명] 개발 스펙 및 설계 계획서

- 상태: [초안 / 확정 / 구현중 / 완료]
- 연계 백로그: `[기능명]_SPEC_BACKLOG.md`
- 매크로 이슈: JIRA-XXXX
- 최종 갱신일: YYYY-MM-DD

---

## 1. 개요 및 목표 (Overview & Goals)

### 1.1 배경 및 목적
- 이 기능을 왜 만드는지, 비즈니스/사용자 관점의 핵심 가치를 2~3문장으로 기술합니다.

### 1.2 포함 범위 (In-Scope)
- 이번 트랙에서 반드시 구현해야 하는 핵심 요구사항 목록.

### 1.3 제외 범위 (Non-Goals / Out of Scope)
- 이번 트랙에서 **의도적으로 구현하지 않는 것** (AI의 오버엔지니어링 차단).

---

## 2. 공통 전제 및 시스템 제약 (Constraints)

- **런타임 / 빌드 환경**: 예: JDK 17, Node v20, Gradle 8.5
- **공통 아키텍처 원칙**: 예: Domain Layer는 Android Framework 의존성을 가지지 않는다.
- **핵심 용어 정의 (Glossary)**:
  - `용어 A`: 도메인 내 구체적 정의
  - `용어 B`: 도메인 내 구체적 정의

---

## 3. 데이터 모델 및 인터페이스 (Data & Domain Models)

### 3.1 핵심 엔티티 및 상태 정의
- 주요 모델의 필드 구조, 상태 전이 다이어그램(State Machine) 또는 Enum 정의.

```kotlin
// 예: 핵심 도메인 인터페이스 또는 데이터 클래스 정의
interface GameHistoryStorePort {
    fun appendHistory(entry: GameHistoryEntry): Result<Unit>
    fun getRecentHistories(limit: Int): List<GameHistoryEntry>
}

```

---

## 4. 기능별 상세 스펙 (Detailed Feature Specifications)

> 백로그 항목은 아래 4.x 절 번호를 직접 참조하여 일감을 수행합니다.

### 4.1 [세부 기능 A 명칭]

* **요구사항**: 비즈니스 룰, 엣지 케이스 처리 조건.
* **입출력 명세**: UI 액션, API 시그니처, 파라미터 제약.

> 구현 결정(백로그 #1, YYYY-MM-DD):
> 1. [선택한 구현 방식 요약]
> 2. [검토 후 버린 대안 및 기각 사유]
> 
> 

---

### 4.2 [세부 기능 B 명칭]

* **요구사항**:
* **예외 처리 정책**: 네트워크 실패, 권한 부재 시의 대체 동작(Fallback).

---

### 4.3 [세부 기능 C 명칭]

* **요구사항**:

---

## 5. 검증 및 테스트 계획 (Verification)

* **단위 테스트 기준**: 필수 검증 시나리오 및 엣지 케이스.
* **실행 명령**: `./gradlew test` (환경 함정 주의사항 명시)

```

---

# 3. SPEC_PLAN과 SPEC_BACKLOG의 유기적 상호작용 요약

두 문서는 아래와 같이 완벽히 결합되어 동작합니다:


```

[SPEC_PLAN.md]                                [SPEC_BACKLOG.md]
4.1절 (상세 스펙) ◄────────── 포인터 참조 ────────  #13 일감 ("참고: 스펙 4.1절")
│                                                  │
│                                             [AI 스레드 작업]
│                                                  │
4.1절 각주 역류 ◄────── 버린 대안 및 결정 기록 ────────────┘
("> 구현 결정(#13): ...")

```

1. **백로그(BACKLOG)**는 스펙의 내용을 복제하지 않고 오직 **절 번호(`참고: 스펙 4.1절`)**만 가리킵니다.
2. **AI 스레드**는 해당 절을 읽고 작업을 수행합니다.
3. 작업 중 발생한 의사결정과 버린 대안은 **스펙(PLAN)의 해당 절 밑에 각주**로 남겨져 영구적인 설계 히스토리가 됩니다.
4. 백로그의 일감에는 **"스펙 4.1절 각주 참고"**라는 한 줄 포인터만 남겨 문서를 슬림하게 유지합니다.

```
