`[기능명]_SPEC_BACKLOG.md`와 한 쌍을 이루는 `[기능명]_SPEC_PLAN.md`(기획/설계 원본 문서)의 표준 템플릿과 구현 결정 각주 작성 규칙입니다. 방법론 설명은 `docs/AI_COLLABORATION_BACKLOG_METHOD.md`를 보세요 — 이 파일은 그대로 복사해 쓰는 실행 파일입니다.

이 문서는 AI 스레드가 **"왜(Why), 무엇을(What), 어떤 제약 하에(Constraints)"** 만들어야 하는지 파악하는 단일 진실 공급원(Single Source of Truth) 역할을 합니다.

---

# 1. 스펙 문서 운영 원칙과 각주 작성 규칙

스펙 문서는 처음 작성된 기획으로 끝나지 않고 개발 과정에서 지속적으로 진화하는 살아있는 문서입니다.

### 핵심 운영 원칙

1. **절 번호의 안정성**: 백로그가 `참고: 스펙 3.2절` 형태로 가리키므로, 대규모 구조 변경이 아니면 기본 절 번호 체계를 유지합니다.
2. **단방향 지식 역류**: AI 스레드가 코드를 작성하며 내린 아키텍처 판단과 버린 대안은 백로그가 아니라 **반드시 스펙 문서의 해당 절 아래 각주로 역류**시킵니다.
3. **각주는 append-only, 본문은 필요하면 개정한다**: 이미 작성된 각주는 수정하거나 지우지 않고 시간 순으로 덧붙입니다 — 그게 "그때 왜 그렇게 판단했는가"의 히스토리이기 때문입니다. 다만 이건 스펙 **본문**(요구사항, 정책표 등)까지 절대 수정하지 말라는 뜻은 아닙니다 — 요구사항 자체가 바뀌면 본문은 새 내용으로 갱신하되, 옛 각주는 지우지 않고 "본문이 재확정됐다"는 새 각주를 덧붙여 왜 바뀌었는지를 남깁니다.

---

### 구현 결정 각주 작성 공식

각주는 단순한 작업 일지가 아닙니다. "다음 AI 스레드가 똑같은 비효율적 대안을 다시 검토하지 못하게 막는 방어벽"입니다.

```markdown
> 구현 결정(백로그 #N, YYYY-MM-DD):
> 1. [선택한 방식]: 실제로 채택한 인터페이스/로직/설계
> 2. [버린 대안과 이유]: 검토했으나 배제한 방식과 그 기술적/도메인적 사유 (★ 가장 중요)
> 3. [영향 및 후속 주의점]: 다음 일감(#M)이나 타 모듈이 주의해야 할 부수효과
```

이 3항목은 **고정된 서식이 아니라 점검 체크리스트**입니다 — 매번 정확히 세 줄을 채우라는 뜻이 아니라, 좋은 각주라면 이 세 가지를 놓치지 않는다는 뜻입니다. 배제한 대안이 여러 개면 항목을 늘리고, 후속 영향이 없으면 3번은 생략해도 됩니다.

#### 실제 각주 (2026-08-24, 이 저장소)

> **구현 결정 (백로그 #12, 2026-08-24)**:
> 1. **선택**: 소모품 차감 우선순위 판정을 `decideConsumableSpend` 순수 함수로 분리했다.
> 2. **버린 대안과 이유**: 기존 `FeatureAccessPolicy.resolve()` 내부에 차감 로직을 합치는 안을 검토했으나 기각했다 — `resolve()`는 부수효과 없는 조회 함수라 화면 렌더링 중에도 호출되는데, 여기에 자원 소모를 넣으면 "확인만 했는데 재고가 닳는" 구조가 된다.
> 3. **영향**: "이미 쓸 수 있는가" 판정은 기존 `resolve()`에 위임해, 프리미엄·영구 클레임 정책이 두 군데로 갈라지지 않게 했다. 실제 잠금 UI와의 조합은 후속 항목(#15)의 몫.

각주에 **실제로 존재하는 함수/클래스 이름만** 쓰세요. 아직 안 만든 클래스를 예시로 미리 지어내면, 다음 스레드가 그 클래스가 실제로 있다고 믿고 코드를 찾다가 시간을 씁니다.

---

# 2. 이식용 표준 템플릿: `[기능명]_SPEC_PLAN.md`

새 트랙 시작 시 아래 템플릿을 복사하여 `[기능명]_SPEC_PLAN.md`로 저장해 사용합니다.

```markdown
# [기능/트랙명] 개발 스펙 및 설계 계획서

- 상태: [초안 / 확정 / 구현중 / 완료]
- 연계 백로그: `[기능명]_SPEC_BACKLOG.md`
- (선택) 연계 이슈: <조직 트래커를 쓴다면 그 키>
- 최종 갱신일: YYYY-MM-DD

---

## 1. 개요 및 목표

### 1.1 배경 및 목적
- 이 기능을 왜 만드는지, 비즈니스/사용자 관점의 핵심 가치를 2~3문장으로 기술합니다.

### 1.2 포함 범위 (In-Scope)
- 이번 트랙에서 반드시 구현해야 하는 핵심 요구사항 목록.

### 1.3 제외 범위 (Non-Goals / Out of Scope)
- 이번 트랙에서 **의도적으로 구현하지 않는 것**(AI의 오버엔지니어링 차단).

---

## 2. 공통 전제 및 시스템 제약

- **런타임 / 빌드 환경**: 예: JDK 17, Node v20, Gradle 8.5 — 백로그의 "검증 명령"과 짝을 이루되, 여긴 "무엇을 검증해야 완결로 보는가"(수용 기준)를 적고 백로그 쪽엔 "그 검증을 돌리는 실제 명령"을 적습니다.
- **공통 아키텍처 원칙**: 예: Domain Layer는 플랫폼 프레임워크에 의존성을 가지지 않는다.
- **핵심 용어 정의 (Glossary)**:
  - `용어 A`: 도메인 내 구체적 정의
  - `용어 B`: 도메인 내 구체적 정의

---

## 3. 데이터 모델 및 인터페이스

### 3.1 핵심 엔티티 및 상태 정의
- 주요 모델의 필드 구조, 상태 전이, 또는 Enum 정의.

```kotlin
// 예: 이 저장소의 실제 인터페이스(그대로 복사하지 말고 이런 수준의 구체성으로 적으라는 예시)
interface GameHistoryStorePort {
    fun appendCompletedGame(entry: GameHistoryEntry)
    fun loadAll(): List<GameHistoryEntry>
}
```

---

## 4. 기능별 상세 스펙

> 백로그 항목은 아래 4.x 절 번호를 직접 참조하여 일감을 수행합니다.

### 4.1 [세부 기능 A 명칭]

* **요구사항**: 비즈니스 룰, 엣지 케이스 처리 조건.
* **입출력 명세**: UI 액션, API 시그니처, 파라미터 제약.

> 구현 결정(백로그 #1, YYYY-MM-DD):
> 1. [선택한 구현 방식 요약]
> 2. [검토 후 버린 대안 및 기각 사유]

---

### 4.2 [세부 기능 B 명칭]

* **요구사항**:
* **예외 처리 정책**: 실패 시의 대체 동작(Fallback).

---

## 5. 검증 계획(수용 기준)

* **완결로 볼 수 있는 조건**: 이 기능이 "됐다"고 말할 수 있는 구체적 시나리오·엣지 케이스.
* (백로그 쪽에 실제 실행 명령이 있습니다 — 여긴 무엇을 검증해야 하는지만 적습니다.)
```

---

# 3. SPEC_PLAN과 SPEC_BACKLOG의 관계

```
[SPEC_PLAN.md]                                [SPEC_BACKLOG.md]
4.1절 (상세 스펙) ◄────────── 포인터 참조 ────────  #13 일감 ("참고: 스펙 4.1절")
│                                                  │
│                                             [AI 스레드 작업]
│                                                  │
4.1절 각주 역류 ◄────── 버린 대안 및 결정 기록 ────────────┘
("> 구현 결정(#13): ...")
```

1. **백로그**는 스펙의 내용을 복제하지 않고 오직 **절 번호**(`참고: 스펙 4.1절`)만 가리킵니다.
2. **AI 스레드**는 해당 절을 읽고 작업을 수행합니다.
3. 작업 중 발생한 의사결정과 버린 대안은 **스펙의 해당 절 밑에 각주**로 남겨져 영구적인 설계 히스토리가 됩니다.
4. 백로그의 일감에는 "스펙 4.1절 각주 참고"라는 한 줄 포인터만 남겨 문서를 슬림하게 유지합니다.
