# 출시 과정과 초기 사용자 요구 (#118~#137) — 완결 · **봉인**

> **⚠️ 통째로 읽지 말 것.** 이 문서는 **완료 항목의 근거 보관처**다 — 필요한 번호만 찾아 볼 것.
>
> **⚠️ 새 일감은 여기에 붙이지 않는다 → `260911-_ACTIVE_BACKLOG.md`.**
> 활성 함정 목록·착수 프로토콜·핵심 동작 기조의 **살아 있는 사본도 그쪽**이다. 여기 있던 것은
> 옮겼다 — 두 벌로 두면 갈라진다.

## 이 문서가 담은 것 (2026-09-06 ~ 2026-09-11)

- **#118·#119 — Play 콘솔 지적 대응.** 16KB 페이지 크기 오류와 리소스 축소 권장을 닫아 **지적 0건**.
- **#123 — 간헐적 시작 크래시**(`onCreate` 줄 순서, 520회 실측).
- **#124~#129 — 첫인상 정비.** 원화·스플래시·앱 이름 "코치"·첫돌이 가이드·마이페이지 고지.
- **#130~#136 — 초기 사용 피드백.** 회전 이탈·손을 따라오는 착수·랜딩 3지선다·최초 실행 되돌리기·설정 IA
  통합과 대국 설정 잠금·벤치마크 응답·결제 상품 종류.
- **#137 — 앱 이름 `포켓 바둑 코치`**와 스토어 제목 분리, 릴리즈 **0.9.2/902**.
- ✅ **이 세대 안에서 정식 출시가 됐다** — 811(0.8.11)이 프로덕션 176개국. 902는 앱 정보 변경과 함께 심사 중
  (2026-09-11). ⚠️ 출시 실행 상태의 정본은 `GOOGLE_PLAY_LAUNCH_PLAN.md` §0이다.

## ⚠️ 봉인 시점에 남아 있던 미해결 사항

아래는 **완료되지 않은 채** 다음 세대로 넘어갔다 — `260911-_ACTIVE_BACKLOG.md`에서 이어진다.

| # | 무엇이 남았는가 |
| --- | --- |
| 87 | 스토어 등록정보 문구 — 코드는 끝났고 **콘솔 반영(심사)만 남았다** |
| 120 | AGP 9.0 — 콘솔 요구가 사라져 **전제부터 다시 볼 것** |
| 121 | `middleware` KMP 모듈 분리 — 출시 후 트랙 |
| 122 | 봉인 문서 넷 삭제 판단 — 출시 후 트랙 |
| 26 | 프리미엄 월 구독 전환 — ⓕ는 끝났고(#136) 나머지는 콘솔 수익창출 대기 |
| 18 | 봇 캐릭터 개별 구매 — 같은 이유로 대기 |

---

## 완료 이력 (#118~#137)

> **한 줄 + 커밋만 남긴다.** 상세가 필요하면 커밋을 볼 것.
> 여기 표가 부르는 **"함정 N"은 활성 백로그의 함정 절에 같은 번호로** 있다(번호를 바꾸지 않고 옮겼다).
> 순서는 **완료 순서**다(위가 최신).

| # | 무엇을 했는가 | 커밋 |
| --- | --- | --- |
| 137 | **앱 이름을 `포켓 바둑 코치`로 — 스토어 제목과 런처를 일부러 갈랐다.** 스토어는 부제까지 `포켓 바둑 코치 - 오프라인 AI 대국·복기`(24자), 런처·앱 안은 브랜드만. 발단은 **마켓 검색에 앱이 안 잡힌다**는 신고였고, 실측하니 스토어 제목이 아직 `바둑 AI`(#127 개명이 콘솔에 반영된 적 없음)라 **앱 이름을 정확히 쳐도 안 나왔다.** ⚠️ `AppNameContractTest`를 *"세 곳이 같다"* 에서 **"스토어 제목이 런처 이름으로 시작한다"** 로 바꿨다 — 브랜드 표류는 계속 막고 부제만 허용한다(양방향 변이 확인). 릴리즈 **0.9.2/902**, 스크린샷 **여섯 장 전부** 재캡처(호선·6수). ⚠️ **실제 기사 이름·`카타고`는 기각했다** — 전자는 퍼블리시티권(부정경쟁방지법 제2조 제1호 타목)과 Play 사칭 정책, 후자는 MIT가 이름 권리를 주지 않고 `AI 카타고 바둑`이 이미 선점. ⚠️ **어제 "811~901은 사용자에게 도달한 적 없다"고 적었는데 틀렸다** — 811이 프로덕션에 나가 있어 출시 노트를 811 기준으로 다시 썼다 | `4ae5c063` |
| 136 | **결제 호출부가 자기가 파는 것을 말하게 했다(#26 ⓕ 선반영).** `AndroidBillingClient`의 `productType`이 기본값 `INAPP`을 갖고 있었고 **`SUBS`를 넘기는 호출부가 하나도 없어** 구독은 조회조차 되지 않았다 — 대가는 오류가 아니라 **조용한 미소유**다. 프리미엄 둘은 `SUBS`, 캐릭터 둘은 `INAPP`를 호출부가 직접 선언하고 ⭐ **어댑터의 기본값을 지웠다**(남아 있는 한 다음 호출부가 또 빠뜨린다). ⚠️ 플래그가 꺼져 있어 **실행 테스트로는 영원히 안 드러나므로** 계약이 소스를 읽는다. 지금 넣어도 안전한 근거: 플래그 off · SKU 없음 · 영구 보유자 없음 | `8c11f521` |
| 135 | **'엔진 성능 측정'이 눌러도 아무 반응이 없던 이유 — 응답을 그리는 자리가 버튼과 다른 화면이었다.** 벤치마크의 출력 채널은 `engineMessage`·`candidateText` 둘뿐인데 **둘 다 화면에 닿지 않는다**(진단 리포트·런타임 로그 전용). 그래서 유일하게 보이는 것이 팝업인데, 그 팝업이 `GoCoachContent`, 즉 **`InGame` 가지 안에서만** 그려지고 있었다 — 버튼은 설정 화면(개발자 1차)에 있으니 **막힘·진행·성공·실패 넷 다** 침묵했다. ⭐ 셸이 **목적지와 무관하게 한 번** 그리도록 올렸다(`EngineBenchmarkOverlays`, 엔진 실패 알림 **다음** 자리). 막힘은 그동안 사유가 진단 문자열로만 흘러 어디에도 안 떴으므로 `EngineOperationGate.Block`에 **타입(`EngineOperationBlockReason`)을 더해** 4개 언어로 번역해 띄운다 — ⚠️ **기본값을 주지 않아** 새 사유를 더하는 사람이 번역을 빠뜨리면 컴파일이 막는다. 실기 전 과정 확인: 준비 전 **차단 팝업**(실제 상태로 재현) → 닫기 → 실행 → 진행 0/15→8/15 → 완료 팝업. 그물 3+3건(전부 변이 확인) | `41435e89` |
| 134 | **설정 IA 통합 — 두 화면이 같은 구성을 갖고, 대국 화면은 대국 설정을 조건 없이 잠근다.** ⓐ `AI 착수 지연` 기본값을 **즉시**로 내리고(이미 쓰던 기기는 저장값을 그대로 읽으므로 신규 설치부터) 그 조작 UI를 **개발자 1차로 이관** — AI끼리 두는 속도는 일반 설정에 있을 이유가 없다. ⓑ 두 설정 표면이 **서로 배타적**이었음을 확인하고(겹치는 것은 언어 하나뿐) 합쳤다 — 사용자 원칙은 *"메인에서 설정하고, 깜빡한 사람은 대국 중에도 고칠 수 있어야"*. ⓒ 네 칸(계가·덤·바둑판·접바둑)이 **비활성처럼 보이는데 둘은 열리던** 문제를 넷 다 잠그는 것으로 닫았다(함정 42). ⚠️ **대국 화면은 `isBoardSetupLockedDuringGame`을 더 이상 쓰지 않는다** — 그 화면에 있다는 것 자체가 대국 중이라 0수라도 잠근다. 옛 결정을 못박던 계약 `onlyTheTwoBoardShapeCellsAreGated`를 **뒤집었다** | `25159e09`·`b4f7398e`·`62cd599f` |
| 133 | **개발자 1차에 '앱 최초 실행 상태로 되돌리기'** — 대국·설정·언어·가이드 진행도까지 지우고 프로세스를 재시작해 **방금 설치한 것과 같은** 상태로 만든다(기기 식별자만 남는다). ⚠️ 확인 팝업 상태를 자기가 들고 있는 **별도 파일**이 됐다 — `SettingsScreen`·`DeveloperTestSection` 둘 다 훅 여유 0이라 인라인이 계약에서 막힌다 | `bd46a0d8` |
| 132 | **첫 실행 가이드의 수준 질문을 3지선다로** — 입문자(3점 접바둑)·중급자(호선)·상급자(AI가 2점 깔고 백 요청) 셋만 묻는다. 문구는 첫돌이 1인칭으로 다시 썼고, ⚠️ **한국어 조사 문제를 피하려 `"$opponent: …"` 콜론 형태**를 쓴다. ⚠️ **일본어만 `入門/中級/上級`(者 없음)** — `入門者/中級者/上級者`는 봇 등급 라벨과 충돌해 기존 그물이 잡았다 | `cde958ea` |
| 131 | **돋보기를 꺼도 돌이 손을 따라온다** — 예전에는 돋보기가 켜져 있을 때만 드래그로 두고, 꺼져 있으면 손을 뗄 때 착수됐다. 돋보기 토글의 뜻을 **"확대 창을 그릴지"** 로 좁히고 드래그 상태(`MagnifierDrag`→`PlayDrag`)는 **항상** 채운다 — 반상 고스트는 늘 그리고, 확대 말풍선만 토글을 본다 | `4c68e288` |
| 130 | **대국 중 회전하면 로비로 튕기던 크리티컬 결함 — 세로 고정 + `configChanges`.** 액티비티가 재생성되며 `currentDestination`(`remember`)이 초기값으로 돌아가 **대국이 통째로 사라졌다.** ⚠️ 세로 고정 한 줄로 끝나지 않는다 — targetSdk 36은 **큰 화면에서 그 값을 무시**하고, 글꼴 배율·다크 모드·멀티윈도우가 **같은 경로로 같은 사고**를 낸다(함정 41). 곁가지로 **후보수 문구**도 고쳤다(항상 5개가 아닌데 "5개"라고 말하고 있었다). ⭐ **회전을 놓친 것이 아쉬워 범주를 넓혀 훑었다** — 다크 모드·글꼴 배율·밀도·백그라운드 복귀·프로세스 강제 종료·화면 잠금·뒤로가기(대국/팝업)·빠른 연타(30→29, 이중 차감 없음)·팝업 중 구성 변경까지 **앱 결함 0건**. 유일한 결함은 **내 검증 도구**였다(덤프 실패를 삼키고 앞 덤프를 재사용해 거짓 통과 — 스크립트를 시끄럽게 고쳤다) | `2ba3fab8`·`12dbdfd5` |
| 128 | **첫돌이가 안내하는 앱 내 가이드 — 화면마다 흩뿌리지 않고 헬퍼로 분리.** 판정은 `shared`의 순수 함수(`autoPlayStep`), 진행도는 전용 스토어(`go_ai_coach_guide`), 배선은 화면이 한 줄씩 부르는 `GuideAnchor` — ⭐ **셸(`GoCoachApp.kt`)은 0줄**이고 그 사실이 계약이다(훅 42/42로 여유가 없어 provider 안은 기각). ①랜딩 좌우 장식 · ②출석 팝업+마이페이지 · ③홈 카드 우상단 말풍선(`ZeroSizeOverlay`로 0×0 보고 → 배율이 올라도 카드가 안 밀린다) · ④대국 설정 카드 · ⑤**대국 화면 버튼별 코치마크 넷**(손그림 동그라미 + 버튼 옆 말풍선, 누를 때마다 다음) + **마이페이지 가이드 다시보기**(전체화면 `Dialog`, 실물 컴포저블 조립). ⚠️ **실기에서 값을 치른 것 셋**: 기록을 `onDispose`에 걸어 ③이 *가려진 채 소진*됐고(→1.2초 표시 후 기록), 오버레이를 `Column` 앞에 둬 반상이 말풍선을 덮었고(→뒤로), 커스텀 `layout(w,0)` 안에서 글자만 안 그려졌다(→`onSizeChanged`+`offset`). 함정 38·39 참고. ⚠️ **곁가지로 입력 동작을 하나 넓혔다** — 돋보기 토글의 뜻을 *확대 창을 그릴지*로 좁혀 **끌어서 두기는 항상** 되게 했다(사용자 지시, 소스 계약 + 실기 확인). 사용자 판정으로 `그만 보기`와 `dismissed` 플래그는 **두었다가 걷어냈다**. 그물 6건 신설(전부 변이 확인). ⚠️ **완료 뒤 감사가 셋을 더 물어 왔다**(함정 40) — ③이 **출석 팝업 아닌 팝업**에 가려진 채 소진되는 길(→ `GuideBlockingOverlays`를 `*Dialog.kt` 11개 전부가 부른다), 폴백 기본값이 만드는 **거짓말**(→ `requireNotNull`), 일본어 ⑤에 박힌 영어 `good`(→ 반대 방향 그물). 마이페이지 배치도 사용자 지적으로 다시 잡았다(인사는 첫 절 제목 옆, 다시보기는 맨 아래). 그물 6건 추가 | `d9b7412f`·`cf5050e4`·`e95128ef`·`921a2a91`·`8348c7d0`·`a765d988`·`594bb789`·`457b160e`·`be100ce2`·`f0054998`·`58f546c3` |
| 129 | **마이페이지 소실 고지를 "이 버전의 한계"로 다시 씀** — 나열을 비열거형으로 열고 **무르기 무제한**을 이름으로 부르고(라벨은 이미 4개 언어에 있었다), 주어를 *"앱이 되돌려 드릴 방법이 없어요"* 로 좁혔다. ⭐ **그 좁힘이 이 항목의 핵심 판단이다** — `allowBackup="true"`인데 제외 규칙이 없어, 고지가 참인 이유는 설계가 아니라 **모델 93MB가 백업 쿼터 25MB를 넘겨 백업이 통째로 실패**하는 것뿐이다(재실측 `bmgr backupnow` → `Size quota exceeded`). ✅ #100이 남겼다던 트립와이어가 세대 인수에서 유실된 것을 찾아내 **테스트로** 복원(`LocalOnlyDataNoticeContractTest`, 함정 37번). 유료 문장은 사용자 요청대로 넣되 `isBotCharacterPurchaseEnabled` **하나로** 가렸다 — 구독 복원이 `INAPP`만 조회해 OR로 묶으면 #26이 켜는 날 거짓이 되기 때문(→ #26 ⓕ 신설, §0 B-4를 다섯 자리로 확장). 일본어 「〜たり」 비문과 낡은 예산 주석 셋도 함께 고쳤다 | `fd95130c` |
| 127 | **앱 이름에 "코치"/"Coach"를 다시 붙였다** — #97의 2026-09-05 보류를 뒤집는 사용자 결정(형세 보기·추천 수가 이미 코치 역할이라, 나중에 코칭 기능이 더 늘어난 뒤 바꾸는 게 더 부자연스럽다). 한/영/일/중 4개 언어 전부: `바둑 AI`→`바둑 AI 코치`, `Go AI`→`Go AI Coach`, `囲碁AI`→`囲碁AIコーチ`, `围棋AI`→`围棋AI教练`. `AppNameContractTest`가 묶는 세 곳(런처·앱 안 타이틀·스토어 등록정보)과, 함정 26이 미리 지목해 둔 그물 밖 세 곳(`RuntimeAppName`, `DebugReportSections`의 리포트 제목, `DebugReportBuilder`의 클립보드 라벨)까지 전부 함께 바꿨다. ✅ **스크린샷도 재캡처했다** — 이름 글자가 화면에 박히는 것은 `01_landing`·`02_home` 둘뿐이라(`03`~`06`은 무관), 그 둘만 다시 찍었다(0.82%·0.36% 픽셀 차이, 나머지는 커밋본과 동일 — 상세는 스크린샷 폴더 README의 「2026-09-06 밤 재캡처」). ⚠️ **남은 것 하나**: `rezen.dev` 방침 사이트는 아직 `바둑 AI`까지만 고쳐진 채 미커밋이라 한 번 더 고쳐야 한다 | `c4cd43f2`·(스크린샷 후속 커밋) |
| 126 | **스플래시 후보 9종 + 개발자 모드 미리보기, 그리고 ⑦ 확정.** 말로 조율하는 대신 다 그려 놓고 눈으로 골랐다 — 개발자 모드 1차의 `1`~`9`가 각각을 전체 화면으로 재생하고, **미리보기와 기동이 같은 재생기를 써** 고른 것이 곧 실리는 것이다. ①은 사용자 설계안(10%→100%, 0.03초마다 5%, 9시·12시·3시·6시·중앙), 최종 채택은 **⑦빠른 확산 → 겹쳐 소멸**(1.5초). ⚠️ **시작 간격≠장당 길이**를 표현하려 `burstAfter`를 갈랐고 **기본값을 두지 않았다** — 묶는 순간 ⑦이 사라진다. ⚠️ **예산이 시킨 설계**: `SettingsScreen`·`DeveloperTestSection` 둘 다 훅 여유 0이라 미리보기가 **자기 상태를 갖는 별도 파일**이 됐다. ⚠️ **남은 판단 하나 — 이 미리보기를 남길지 지울지**(1차는 release에도 실린다) | `5bd6c57`·`40557f0`·`3981e94`·`61210c5`·`31ed5df` |
| 125 | **앱 시작 스플래시 1초 — 캐릭터 5장이 부채로 펼쳐졌다 모인다.** `ui/AppSplash.kt` 신설, `setContent` **안**에서 홈 위에 얹는다 — ⭐ **홈을 뒤에 숨기지 않고 함께 컴포즈해** 그 1초가 *벌어 두는 시간*이 되게 했다. **새 의존성 0**(시스템 스플래시는 아이콘 하나만 그려 5장을 못 담고, 이음매는 `windowBackground`를 앱 바탕색에 맞춰 없앴다). ⚠️ **실기 프레임을 뽑기 전에는 부채가 너무 좁은 것도, 모인 뒤 200ms 죽는 것도 안 보였다.** ⚠️ 폭이 모자라면 양끝이 잘려 `BoxWithConstraints`로 조인다(360dp 실측). `AppSplashContractTest` 3건(변이 확인 — 하나는 처음에 **import 줄 때문에 헐거웠다**) | `b2055bf` |
| 124 | **캐릭터 아바타 5장을 새 투명 원화로 교체** — 실제로 달라지는 것은 **원화의 바닥 그림자(드롭 섀도)가 없어지는 것 하나**이고, 아바타는 `CircleShape`로 잘려 "바닥"이라는 맥락이 없으므로 없는 편이 맞다(사용자 확정). 내부 잠식 0, 157KB→126KB. ⚠️ **프레이밍 재조정이 작업의 절반이었다** — 그냥 384로 줄이면 원 안에서 캐릭터가 92%→74~80%로 작아지는데 **계약 테스트(384x384+알파)는 그대로 초록**이다. #86이 쓰던 *bbox crop → 여백 15px 캔버스에 가로중앙·하단 정렬 fit*을 복제했다. ⚠️ 착수 근거를 한 번 틀리게 적었다(*"어두운 테마 흰 얼룩"*) — **이 앱에 다크 테마가 없고**(`lightColorScheme` 하나뿐) 제거 픽셀은 **평균 밝기 96~135의 회색**이다. **앱이 실제로 쓰는 배경색 위에서 재지 않으면 이런 오독이 난다** | `8f7098e` |
| 123 | **간헐적 시작 크래시 — 원인은 `MainActivity.onCreate`의 줄 순서였다.** `AdsConsentManager.refresh`가 `setContent`보다 **앞**에 있어, UMP가 자기 백그라운드 스레드에서 WebView 프로바이더를 로드하며 앱의 `Resources`/`AssetManager`를 갈아끼우는 일이 **메인 스레드가 창의 decor를 인플레이트하는 순간**과 겹쳤다. 그 한 줄을 `setContent` 뒤로 옮겼다. **실측 520회**: 에뮬(playInternal) 13/160 → **0/120**, 실기 Galaxy S23(debug) **20/120 → 0/120**. `StartupOrderContractTest` 2건으로 순서를 소스 계약화(변이로 확인) | `2821c7b` |
| 119 | **최적화된 리소스 축소** — 콘솔이 읽는 값은 번들 `r8.json`의 `resourceOptimization.isOptimizedShrinkingEnabled`였고 811에서 `false`였다. `gradle.properties`에 `android.r8.optimizedResourceShrinking=true`. ✅ **812 업로드에서 권장조치가 사라진 것으로 확인**(2026-09-06 사용자). ⚠️ 리소스 축소가 공격적이 되므로 값을 치렀는지 확인했다 — 811을 복원해 대조하니 줄어든 117개가 **전부 안 쓰는 라이브러리 자산**이었고 실기에서 캐릭터 아바타·도장판이 그대로 그려졌다 | `e574700` |
| 118 | **16KB 메모리 페이지 크기** — Play가 811을 막았다. 원인은 번들의 `.so` 셋 중 **우리가 빌드하는 `libkatago.so` 하나**(정렬 4096, AndroidX 둘은 이미 16384)였고, cmake에 `-DANDROID_SUPPORT_FLEXIBLE_PAGE_SIZES=ON` 한 줄로 재링크했다. ✅ **812 업로드에서 오류가 사라진 것으로 확인**. ⚠️ NDK r28로 올려 해결하지 말 것 — 정렬 한 줄 때문에 컴파일러 메이저를 바꾸면 검증 범위가 통째로 넓어진다 | `e574700` |

> ⚠️ #85~#117의 상세는 봉인 문서 `260905-260906_LAUNCH_PREP_AND_CLEANUP.md`의 같은 번호에,
> #63~#84는 `260831-260905_DEV_TEST_SECTION_BUILDOUT.md`, #43~#62는 `260830-260831_POST_LAUNCH_ENHANCEMENTS.md`,
> #1~#42는 그 앞 세대 문서에 있다. **통째로 읽지 말고 번호로 찾을 것.**

---

## 이 문서를 쓰는 규칙

① **봉인 문서는 아무것도 받지 않는다.** 여기서 완료된 항목을 앞 세대 문서로 옮기지 말 것.
   상태 기준으로 파일을 가르면 항목이 진행될 때마다 왕복 이관이 생긴다 — 완료된 것은 **이 문서에
   한 줄로 남는다.**

② **완료 항목은 "한 줄 + 커밋 해시"로만 적는다.** 상세가 필요하면 커밋을 보면 된다.
   · ⚠️ **예외는 함정뿐이다.** 다음 작업이 밟게 될 함정은 활성 백로그의 「함정」 절로 끌어올린다 —
     그것만은 `git log`로 되찾을 수 없다. **그 살아 있는 사본은 이제 `260911-_ACTIVE_BACKLOG.md`에 있다.**

③ **살아 있는 항목의 상세는 줄이지 않는다.** 그 상세는 히스토리가 아니라 **다음 작업의 입력**이다.
   그래서 #87·#120·#121·#122·#26·#18은 전문 그대로 새 문서로 옮겼다.

④ **출시 실행 상태는 여기 적지 않는다.** 콘솔 폼·AAB·스크린샷·`release` 실기는
   `GOOGLE_PLAY_LAUNCH_PLAN.md` §0이 정본이다.

---

## 진행 중 · 예정사항 → 이관됨 (2026-09-11)

이 문서가 담고 있던 **진행 중·예정사항·함정 목록·착수 프로토콜·핵심 동작 기조·부록**은
`260911-_ACTIVE_BACKLOG.md`로 옮겼다. **여기 사본을 되살리지 말 것 — 두 벌로 두면 갈라진다.**

가른 기준은 **날짜와 트랙의 성격 전환**이다(2026-09-11 사용자 지시). 이 문서는 *"출시 과정 자체와 등록
초기 사용자가 요구하는 것"* 을 받는 트랙이었고, 그 사이 정식 출시가 됐다. 새 문서는 **앱 마켓 등록 후
사용자 피드백으로 앱 안의 기능을 고도화**하는 트랙이다.

⚠️ **완료 항목을 이 문서로 되돌리지 말 것**(2026-08-31 규칙). 봉인 시점 그대로 두고, 새 문서에서 완료된
것은 새 문서에 한 줄로 남는다.

---

## 관련 문서

- `260911-_ACTIVE_BACKLOG.md` — **다음 세대 활성 백로그.** 살아 있는 함정·프로토콜·기조가 그쪽에 있다
- `260905-260906_LAUNCH_PREP_AND_CLEANUP.md` — 앞 세대(#85~#117). **봉인**
- `260831-260905_DEV_TEST_SECTION_BUILDOUT.md` — 그 앞 세대(#63~#84). **봉인**
- `260830-260831_POST_LAUNCH_ENHANCEMENTS.md` — 그 앞 세대(#43~#62). **봉인**
- `260823-260830_OFFLINE_ENGAGEMENT_FEATURES_BACKLOG.md` — 1세대(#1~#42). 완결
- `GOOGLE_PLAY_LAUNCH_PLAN.md` — §0이 **출시 트랙의 정본**
