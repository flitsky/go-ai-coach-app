# 활성 일감 백로그 (2026-09-05 개설)

> **다음 스레드는 이 문서만 읽으면 된다.** 완료된 항목의 상세는 앞 세대 문서들에 그대로 있고,
> 여기에는 **한 줄 + 커밋 해시**로만 남긴다. 아래 "이 문서를 쓰는 규칙"이 그 이유와 지켜야 할
> 선을 적어 뒀다.

## 서두 — 착수 전에 알아야 할 배경

1. **이 트랙의 계보(4세대)**: `260823-260830_OFFLINE_ENGAGEMENT_FEATURES_BACKLOG.md`(#1~#42)
   → `260830-260831_POST_LAUNCH_ENHANCEMENTS.md`(#43~#62) →
   `260831-260905_DEV_TEST_SECTION_BUILDOUT.md`(#63~#84) → **이 문서**(#85~).
   설계 원본과 구현 결정 각주는 `260823-260830_OFFLINE_ENGAGEMENT_FEATURES_KICKOFF_PLAN.md`에 쌓인다.
   · ⚠️ **앞 세대 문서를 통째로 읽지 말 것** — 필요한 항목만 번호로 찾아 볼 것.
2. **출시 트랙의 정본은 여기가 아니다.** 콘솔 작업·AAB·스크린샷·`release` 실기는
   `launch-plan/README.md` §0의 표가 추적한다. **여기에 다시 적으면 두 곳이 갈린다** —
   이 문서에 남는 것은 **코드 일감**뿐이다.
3. **⚠️ AAB 810은 낡았다 — 올릴 것과 테스트한 것이 어긋나 있다.** #65~#84가 전부 코드 변경이라
   810에는 그 어느 것도 없다. 사용자 피드백이 닫히면 **B-1을 다시 돌려야** 짝이 맞는다
   (`launch-plan/README.md` §0 규칙 2번). ⚠️ `bump-version`이 빌드마다 `VERSION_CODE`를 1 올리고
   Play는 번호를 재사용하지 않으므로 **시험 삼아 반복 빌드하지 말 것.**
   · 스크린샷은 살 가능성이 있다 — 판정은 그때 컷을 다시 대 보고 한다.
4. **비공개 테스트 시간 게이트는 지났다.** ⚠️ **다만 콘솔에서 실제로 풀렸는지는 확인되지 않았다** —
   사용자가 "프로덕션 액세스" 체크리스트를 봐야 확정된다. 풀렸다면 #26·#18의 "검증 불가" 사유가
   사라지므로 **그 둘을 다시 꺼낼지부터 판단할 것**(근거: §0 A-1·A-7·A-8).
5. ✅ **개발자 테스트 섹션 배치(#63~#84)는 2026-09-05에 닫혔다.** 설정 화면의 개발자 섹션이
   테스트 기능의 집합소가 됐고, 그 과정에서 실제 기능 둘(**캐릭터 획득 이펙트 팝업**, **저장되는
   글꼴 배율**)과 버그 여섯이 함께 처리됐다.
   · 섹션은 **2단**이다. 1차는 **버전 텍스트 10탭**으로 열리고 **release에도 실린다.**
     2차는 **'빌드 정보' 행 10탭**(#84)으로 열리고 **debug 전용 + 세션 한정**이다.
   · ⚠️ **권한을 만들거나 지우는 컨트롤은 반드시 2차**다 — #77의 기준은 라벨이 아니라
     **무엇을 저장하는가**다. 1차에 하나라도 섞이면 **무료 획득 경로가 그대로 출시된다.**
   · ⚠️ **2차 진입을 길게 누르기로 되돌리지 말 것**(#84가 두 번 제보된 뒤 걷어냈다. 함정 17번).
6. **⚠️ 지금 이어서 할 것은 #76이다** — 조사와 반증이 이미 끝나 계획이 항목에 적혀 있다.
   그 다음은 **#72·#75**인데 둘 다 **사용자 결정을 기다리는 상태**다.
7. **#26·#18 두 항목은 파킹 상태다.** #26은 착수 순서 1·2번만 끝내고 멈췄고(`34d74e3`), #18은
   결제 코드가 이미 머지돼 있다 — ⚠️ 다만 **콘솔 작업 세 줄로 끝나지 않는다**(획득 모델이 무료
   경로와 구매를 **동시에** 표현하지 못한다). **둘 다 "미완"이 아니라 "검증 불가로 대기"** 이며,
   이것은 사용자 결정이다 — 미검증 결제 코드를 쌓지 않는 것이 이 두 항목의 위험 관리다.
8. **커밋 시점은 사용자가 정한다.** 스레드가 임의로 커밋하지 않는다.
9. **테스트는 1003건이다**(shared 606 · app-android 352 · engine-android 45). ⚠️ **건수를 셀 때
   함정 16번을 볼 것** — 누적된 결과 XML을 합산하면 2배 이상으로 부풀려진다(실제로 이 백로그에
   여러 번 잘못 적혔다).

---

## 신규 스레드 착수 프로토콜

**매번 새 스레드에 넣는 고정 프롬프트** (그대로 복사해서 사용):

> 이번 스레드에서는 `docs/work/roadmap/260905-_ACTIVE_BACKLOG.md` 이 파일의 내용을 파악하여 '예정사항'의 첫번째 항목을 파악하여 수행 담당해야합니다. 먼저 항목을 이해하셨다면 진행중으로 변경하고 작업 착수하시고, 결과물을 사용자와 논의 후 사용자가 완료 승인을 하면 문서에 완료 업데이트하고 당신의 스레드 업무가 종료되었다고 명시해주시면 됩니다.

이 프롬프트를 받은 스레드가 따라야 할 순서:

1. **읽기** — 이 문서 전체 + `260823-260830_OFFLINE_ENGAGEMENT_FEATURES_KICKOFF_PLAN.md`의
   **2장(현재 조건)·3장(아키텍처 제약)** 은 모든 항목의 공통 전제이므로 항상 같이 읽고, 거기에
   더해 맡을 항목이 가리키는 절을 읽는다. 이 셋을 읽지 않고 바로 코드부터 쓰지 않는다.
   ⚠️ **봉인 문서를 통째로 읽지 말 것** — 필요한 항목만 아래 완료 이력 표의 번호로 찾아 본다.
2. **상태 전환** — 예정사항의 **첫 번째** 항목을 진행 중 섹션으로 옮기고 `[진행중]`을 붙인다.
   번호·제목·AI 모델·노력정도는 그대로 유지한다.
3. **작업** — 그 항목의 스펙대로 구현한다. 범위 확장이나 다음 항목 선착수는 하지 않는다.
4. **막히면** — 스펙에 없는 정보는 먼저 코드를 직접 확인해 스스로 판단한다. 그래도 답이 안 나오는
   **제품/설계 결정 사항**만 추측하지 말고 사용자에게 확인한다. 완료 처리는 반드시 사용자 승인
   이후에만 한다.
5. **완료 처리** — 사용자가 승인하면 그 항목을 **완료 이력 표에 한 줄로** 옮긴다(전문을 이 문서에
   남기지 않는다 — 아래 규칙 ② 참고). 이후 **"이 스레드의 업무가 종료되었습니다"라고 명시**한다.
6. **다음 스레드 안내** — 다음 예정사항 항목 번호만 짧게 언급하고 마친다. 사용자가 "이어서 진행"을
   지시하면 같은 스레드에서 다음 항목을 이어받되, 2~5단계를 항목마다 처음부터 다시 밟는다 —
   특히 **항목별 "착수 전 확인"과 완료 승인은 건너뛰지 않는다.**

**참고사항**

- `AI 모델` 표기는 스레드를 어떤 모델로 실행할지 사용자가 참고하는 값이다 — 스레드가 스스로
  모델을 바꾸는 동작이 아니다.
- 동시에 여러 스레드를 이 백로그에 붙이지 않는다 — 순차 실행을 전제로 설계됐다.
- **검증 명령**: 이 저장소는 **JDK 17**이 필요하다 — 시스템 기본 JDK로 돌리면 Gradle이 `25` 한 줄만
  뱉고 즉시 실패해 원인을 찾기 어렵다.
  `export JAVA_HOME=/Library/Java/JavaVirtualMachines/temurin-17.jdk/Contents/Home` 후
  `./gradlew :shared:testDebugUnitTest :app-android:testDebugUnitTest`.
  `shared/`를 건드렸다면 `./gradlew :shared:compileKotlinIosSimulatorArm64 -PenableIosTargets=true`
  까지 확인한다(commonMain 플랫폼 독립성).
- 이 백로그 갱신과 `DOCS_INDEX.md` 갱신은 별개다 — 그 문서는 새 파일이 생기거나 문서 구조 자체가
  바뀔 때만 갱신한다.
- 저장소 관행은 `main` 직접 커밋이며 메시지는 `feat(scope): ... (backlog #N)` 형식이다.

---

---

## ⚠️ 다음 작업이 반드시 알아야 할 함정

> 봉인 문서의 완료 항목들이 **값을 치르고 알아낸 것** 가운데, 아래 예정사항과 임박한 출시 작업이
> **직접 밟게 되는 것만** 끌어올렸다. 나머지는 봉인 문서의 해당 항목에 그대로 있다.

1. **enum 상수 이름 그 자체가 저장 포맷이다** (#47에서 확인). `.name`으로 쓰고 `enumValueOf`/
   `valueOf`로 읽으며(`persistence/` 8개 스토어 · 기록 24곳), **모르는 이름은 조용히 기본값으로
   떨어진다.** 이름이 바뀌면 기존 사용자의 프리미엄·출석·설정·진행 중 대국이 **소리 없이
   초기화된다.** → #26의 "`PremiumSource.Purchase`를 개명하지 말 것"이 바로 이 함정이다.
   · R8은 이 이름을 전 enum에 한꺼번에 저지를 수 있었으나 **안전하다**고 확인됐다 — AGP 기본
     `proguard-android-optimize.txt`의 `-keepclassmembers enum * { values(); valueOf(String); }`가
     지켜준다. 그래서 keep 규칙을 추가하지 않았다.
   · ⚠️ **검증 방법 주의**: `strings | grep`으로는 일부가 안 잡혀 **잘못된 결론이 나온다.**
     `dexdump -d`로 `<clinit>`의 문자열 상수를 봐야 한다.
2. **`UserPreferencesSnapshot`에 새 필드를 넣을 때는 자동저장 경로를 함께 배선할 것** (#51에서
   확인). 자동저장이 스냅샷을 **처음부터 다시 만들기** 때문에, 배선하지 않은 필드는 다음 저장에서
   조용히 기본값으로 되돌아간다. `current`에서 출발해 **필요한 축만** 덮어쓴다.
   → #26 ⓐ(구독 유효기간을 저장에 표현)가 정확히 이 경로다.
3. **⚠️ `GoCoachApp.kt`의 예산은 둘이고, 지금 **여유가 정확히 0**이다** (2026-09-03 실측).
   `LayeringContractTest.kt:1481-1482`가 `lineBudget = 880` / `stateHookBudget = 46`을 못박고 있고
   실제 파일이 **880줄 / 46훅**이다 — 비교가 엄격 부등호라 **881줄이나 47훅이 되는 순간 깨진다.**
   · ⚠️ **이 줄은 "현재 870"이라고 적혀 있었다** — #51 시점의 값이고, 그 뒤 #63(초기화 안내)까지
     들어와 880이 됐다. **숫자를 문서에서 읽지 말고 `wc -l`로 확인할 것.**
   · **그래서 새 다이얼로그를 셸에 배선하면 그 자리에서 깨진다.** #69의 처방은 `GoCoachApp.kt`를
     아예 건드리지 않고 `buildBotCharacterUiState`(`BotCharacterUiState.kt:113`) 안에서 emit하는
     것이고, #71은 시그널 object를 `AttendanceRewardClaimDialog.kt` 안에 두는 것이다.
   · 올릴 때는 **사유를 그 주석에 적는다.** #26 ⓒ(구독 관리 UI)도 이 예산에 부딪힌다.
4. **R8을 켠 뒤로 "테스트한 것"과 "올리는 것"이 갈렸다** (#47). `playInternal`이 이제 R8을 돌리므로
   에뮬레이터 스모크만으로 끝내지 말고 **대국 1판·출석 수령·1회권 사용·광고 시청**까지 밟을 것.
   ⚠️ **`release` 빌드타입으로 앱을 돌려 본 기록은 아직 없다**(`launch-plan/README.md` §0 B-3).
5. **산출물(AAB·스크린샷)은 화면 변경이 멎은 뒤에 만든다.** 2026-08-30 하루에 AAB가 803→807로
   다섯 번 갈렸고 앞의 넷은 콘솔에 올라간 적이 없다. 2026-08-31에는 #39가 스크린샷 판정을 하루
   안에 두 번 뒤집었다. 상세 규칙은 `launch-plan/README.md` §0의 "이 목록을 쓰는 규칙 둘".
6. **⚠️ 정식 릴리즈 초기화가 이제 존재한다**(#63) — `ReleaseResetCoordinator`가 앱 시작 시
   **권한 저장소 넷**(출석·컬렉션·소모품·프리미엄)을 지운다.
   · ⚠️ **권한 저장소를 새로 만들면 그 목록에 함께 넣을 것.** 안 넣으면 다음 초기화 때 그것만
     살아남아, 다른 셋과 어긋난 상태가 된다(#26의 구독 유효기간 저장이 바로 그 후보다).
   · ⚠️ **`ReleaseResetPolicy.CurrentResetGeneration`을 무심코 올리지 말 것** — 올리는 순간
     **전 사용자의 권한이 한 번 더 밀린다.** 올릴 때는 그 상수의 목록에 사유를 한 줄 남긴다.
   · ✅ 덕분에 **출시 시점 사용자에게는 마이그레이션할 옛 저장분이 없다** — #18의 획득 방법
     분리 저장처럼 새 필드를 넣을 때 기존 값 호환을 걱정하지 않아도 된다.
7. **⚠️ Compose 다이얼로그는 각자 별도 윈도우다 — 선언 순서로 z축이 정해지지 않는다.**
   나중에 선언한 쪽이 위로 오지 않는다(#63에서 실기로 확인: 출석 팝업이 초기화 안내를 가렸다).
   두 팝업의 순서가 중요하면 **명시적으로 게이트**할 것 — 순서에 기대면 조용히 뒤집힌다.
8. **`AndroidBillingClient`는 프리미엄과 봇 캐릭터가 공유한다.** #26이 `productType`을 생성자
   파라미터로 올려 뒀다 — 클래스 안에서 전역 치환하면 #18의 캐릭터 구매가 조용히 깨지고,
   그 플래그가 꺼져 있어 실행 테스트로도 드러나지 않는다.
9. **⚠️ 글자가 든 상자에 고정 `dp` 높이를 주지 말 것** (#64에서 **네 자리**를 한꺼번에 밟았다).
   글자만 폰트 배율을 따라 커지고 상자는 그대로여서, 배율 1.3배부터 아랫줄이 반토막 난다.
   **일반 단위 테스트로는 영원히 안 잡힌다** — 값은 전부 정상이고 측정 결과만 틀리기 때문이다.
   넷 다 사용자가 눈으로 찾아냈다. → #26 ⓒ(구독 관리 UI)가 화면을 새로 만들 때 그대로 밟는다.
   · **형제끼리 맞춰야 하면** `Row(Modifier.height(IntrinsicSize.Min))` + 자식 `fillMaxHeight()`,
     높이는 `heightIn(min = …)`으로 **바닥값**만 준다(도장판이 이 처방을 쓴다).
   · ⚠️ **캐러셀(`HorizontalPager`) 안에서는 그 처방이 안 통한다** — 페이지를 **한 장씩만** 재므로
     다른 카드의 높이를 알 방법이 아예 없다. 거기서는 배율에서 **계산**하고(`× fontScale`),
     **덜 중요한 줄에만 `weight`** 를 걸어 양보시킨다(`Column`은 가중치 없는 자식을 먼저 잰다).
   · ⚠️ **원 안에 갇힌 글자는 반대로 `dp`에 묶어야 한다.** 인장 `✓`를 원과 함께 키웠더니 32dp 원이
     20dp 글리프를 덮어 **보상 그림이 통째로 가려졌다**(#57 KDoc이 경고한 그 사고). `dp.toSp()`로
     표시를 묶고 원은 그대로 두는 것이 답이다. ⚠️ 이때 `lineHeight`도 같이 묶지 않으면 글자
     상자만 커져 잘림이 그대로 돌아온다.
   · ✅ 세 자리는 이제 `FontScaleLayoutContractTest`가 소스 계약으로 지킨다(변이 5건으로 확인).
10. **⚠️ `UiStrings`의 자동 그물은 String *필드*만 본다 — 함수로 만든 문구는 사각지대다.**
   `UiStringsTest`의 리플렉션 그물은 백킹 필드를 훑으므로 `fun ...(): String` 형태의 문구를
   **아예 못 본다**(`UiStringsBotCharacterTest`가 캐릭터 이름·설명에 손으로 그물을 단 이유다).
   #64에서 확인: `botUnlockHint`는 그물이 **하나도 없었고**, 그 사이 *"7일차 출석에서 조각을
   얻는다"* 는 **사실이 아닌 문구**가 네 언어에 오래 살아 있었다(확정표는 5·6일차에 1개씩이고
   그 회차는 반복되지 않는다). → 문구를 **함수로** 새로 만들면 그 자리에 그물을 **손으로** 달 것.
   `UiStringsBotUnlockHintTest`가 형식의 본보기다 — 문구만 보지 말고 **문구가 말하는 사실을
   도메인에 되물어** 함께 고정한다.
10-2. **⚠️ 문구가 쓰이는지 확인할 때 `strings.<이름>`으로만 찾으면 세 가지를 놓친다**
    (2026-09-05, #76 조사에서 **제가 실제로 셋 다 놓쳤다**).
    · ⓐ **`UiStrings*.kt` 파일 자체를 검색에서 제외**하면 **문구가 문구를 쓰는 것**을 못 본다 —
      `compactKomiLabel`은 `"$komi (${komiValueLabel(v)})"`(`UiStrings.kt:1002`)이고,
      `gameModeLabel`은 `handicapLabel(count)`(`:882`)을 부른다. 즉 **화면에서 안 쓰여도 살아 있다.**
    · ⓑ **메서드 참조 `strings::이름`** 은 `strings.이름` 패턴에 걸리지 않는다 —
      `CompactScoringAndBoardSettingsPanel.kt:69`가 `optionLabel = strings::komiValueLabel`이다.
    · ⓒ **`UiStrings`는 `data class`다.** `val`을 지우면 **주생성자와 생성된 `copy()`에서 함께
      사라져** 4개 언어 파일이 전부 컴파일 실패한다(`UiStringsKo.kt`는 생성자,
      나머지 셋은 `UiStringsKorean.copy(`). 선언 한 줄만 지우는 계획은 **컴파일되지 않는다.**
    · **처방**: 이름만으로 저장소 전체를 훑을 것 — `grep -rn '\b<이름>\b' app-android/src`
      (제외 없이). 그 다음 ⓒ를 위해 4개 언어 파일의 같은 이름 줄을 함께 센다.
      ⚠️ 함정 10번과 겹쳐 읽을 것 — `fun` 형태 문구는 리플렉션 그물에도 안 잡히므로,
      **잘못 지워도 테스트가 조용히 통과한다.**

11. **⚠️ 개발자 테스트 섹션은 `release` 빌드에도 그대로 실린다** (2026-09-03 확인).
   게이트는 빌드 타입이 아니라 **런타임 boolean 하나**다 — `DeveloperModeStore`
   (`go_ai_coach_developer_mode`)이고 버전 텍스트 **10탭**으로 켜진다
   (`SettingsScreen.kt:66·363-379·404`). KDoc이 `BuildConfig.DEBUG` 게이팅을 **의도적으로 제거했다**고
   적고 있으므로 그 결정을 되돌리지 말 것.
   · ⚠️ **그래서 여기 붙는 컨트롤은 곧 출시 빌드의 기능이다.** #70·#71이 붙이려는 것은 조각·출석
     조작이라 **캐릭터·무르기 영구 해금·1회권을 무료로 찍어내는 경로**가 된다 — 기존 프리미엄
     토글보다 악용 가치가 크다. 막으려면 **그 버튼 하나만** `BuildConfig.DEBUG`로 한 겹 더 감싼다.
12. **⚠️ 출석 경로는 `AppClock`을 쓰지 않는다 — 시계를 주입할 시임이 없다** (2026-09-03 확인).
   `AppClock.kt:24`는 포트가 아니라 **최상위 함수**이고, 출석의 벽시계 진입점 **두 곳 모두**
   app-android에서 `System.currentTimeMillis()`를 직접 읽는다
   (`AttendanceCheckInCoordinator.kt:34`, `AttendanceRewardClaimDialog.kt:82`).
   · **그래서 "날짜를 앞으로 돌린다"는 저장된 `lastCheckInUtcDay`를 되감는 방식만 성립한다.**
     전역 오프셋은 프리미엄 만료·대국 타이머·기록 타임스탬프까지 오염시킨다(직접 호출 20곳).
   · ⚠️ `shared` commonMain에는 bare `System.`이 금지돼 있으므로(함정 아래 `LayeringContractTest`)
     되감기 함수를 shared에 두면서 시간을 읽으려 들지 말 것 — **되감기는 시간을 읽을 필요가 없다.**
13. **⚠️ 저장소에 직접 쓰는 경로는 인메모리 상태를 갱신하지 않는다** (2026-09-03, #65의 본체).
   출석 Claim은 `AttendanceRewardClaimDialog.kt:104-118`에서 저장소에 직접 쓰는데
   `GoCoachApp.kt:186`의 `premiumState`는 **앱 실행당 한 번만 로드**된다 — 갱신 통로가 없어
   **3일차 무르기 보상을 받아도 그 세션 내내 Locked로 판정된다.** 재고에는 이미 같은 처방이 있다
   (`consumables.refresh()`) — 새 지급 경로를 만들 때마다 **화면 상태를 되읽는 콜백을 함께 배선할 것.**
   · ⚠️ 그리고 이 다이얼로그는 `onDismissRequest = onClaim`이라 **어떻게 닫아도 지급된다** —
     콜백을 confirmButton에만 걸면 뒤로가기 경로에서 조용히 누락된다.
15. **⚠️ `@Test`가 빠진 테스트 함수는 조용히 사라진다 — 빌드는 초록이다** (2026-09-03, #68에서 8건 발견).
   `AttendanceRewardGrantTest`의 여덟 함수가 한 번도 돌지 않은 채 남아 있었다. 컴파일도 되고
   빌드도 통과하며 **테스트 수만 조용히 줄어든다.** 그중에 #66의 그랜드파더링 판정과, 주석이
   *"가장 조용히 깨질 뻔한 곳"* 이라고 적어 둔 회차 테스트가 있었다.
   · ✅ **이제 `TestAnnotationContractTest`가 막는다**(shared 공용 테스트 + app-android 단위 테스트).
   · ⚠️ **그 그물에 사각지대가 있다**: `shared` 테스트만 바뀐 빌드에서는 Gradle이
     `:app-android:testDebugUnitTest`를 up-to-date로 건너뛰어 **그물도 함께 쉰다.** 테스트를
     새로 넣은 뒤에는 **테스트 수가 실제로 늘었는지** 확인하거나 `--rerun-tasks`를 붙일 것.
16. **⚠️ 테스트 건수를 `build/test-results`의 XML을 훑어 세지 말 것** (2026-09-04 적발).
    그 폴더는 **지우지 않으면 계속 쌓인다** — 태스크 변종(`testDebugUnitTest`/`testReleaseUnitTest`)과
    이전 실행의 결과가 함께 남아, `shared`가 두 번 세어지고 낡은 클래스도 함께 세어진다.
    그렇게 나온 **"2838건"이 이 문서에 여러 번 적혔고, 실측은 1001건이었다**(소스의 `@Test`
    개수와 일치: shared 606 · app-android 350 · engine-android 45).
    · **처방**: 세기 전에 `find . -path "*/test-results/*" -name "*.xml" -delete`로 비우고, 한 번의
      실행 결과만 센다. ⚠️ 숫자가 갑자기 **줄어든 것을 회귀로 읽지 말 것** — 앞 세대 문서
      (`260830-260831_POST_LAUNCH_ENHANCEMENTS.md`)의 2347~2370건도 같은 방식으로 나온 값이라
      **비교 대상이 아니다.** 테스트가 삭제된 것이 아니다.

18. **⚠️ 이 문서를 스크립트로 편집할 때 `---`를 항목 경계로 쓰지 말 것**
    (2026-09-05, **내가 실제로 내용을 날렸다**).
    항목 사이에 `---`가 **항상 있는 것이 아니다.** `s.index("\n---\n", start)`로 끝을 잡으면
    구분선이 없는 항목에서는 **다음 항목들을 통째로 삼킨다** — 그렇게 `#75`의 개정본과 `#26`의
    스펙 **62줄이 커밋에 실려 사라졌다**(`db943f8`). 다행히 git에서 되살렸다.
    · **처방**: 경계는 **다음 항목의 머리글**로 잡는다 — `s.index("26. 프리미엄을", start)`.
    · **처방 2**: 편집 뒤 **항목 출현 수를 세어 확인할 것.** `s.count("\n26. ")` 같은 한 줄이면
      충분하다.
    · ⚠️ **세는 것만으로는 부족하다 — 세고 나서 *읽어야* 한다.** 2026-09-05에 같은 실수를 한 번 더
      했다: 세는 줄은 넣어 뒀는데 **그 출력을 보기 전에 커밋까지 한 번에 실행해** `85. 출현 0회`가
      찍힌 것을 커밋 뒤에 알았다(항목이 통째로 안 들어갔다). **편집과 커밋을 한 호출에 묶지 말 것.**
    · ⚠️ **줄 수만 보는 것으로는 부족하다** — 항목을 지우고 다른 것을 더하면 총량이 비슷해진다.

17. **⚠️ 스크롤 안에서 길게 누르기를 트리거로 쓰지 말 것 — 조용히 실패한다**
    (2026-09-04·05, #77이 넣고 #84가 걷어냈다. **사용자가 두 번 제보했다**).
    `waitForUpOrCancellation()`은 다른 핸들러가 제스처를 가져가면 **정해진 시간 전에 `null`을
    돌려준다.** 그러면 홀드도 탭도 아니어서 **어느 분기에도 걸리지 않고 아무 일도 일어나지 않는다** —
    실패에 소리가 없다.
    · ⚠️ **`awaitFirstDown(...).consume()`은 충분한 처방이 아니다.** 첫 접촉만 막고 이후 MOVE
      경쟁은 막지 못한다. **그것을 지키던 제 계약 테스트는 초록인 채로 실기에서 계속 샜다** —
      *"잘못된 처방을 지키는 그물"* 의 실례다.
    · ⚠️ **에뮬레이터로는 재현되지 않는다** — `adb input swipe`는 DOWN/UP만 보내고 MOVE를 만들지
      않아 스크롤이 개입할 여지가 없다.
    · **처방**: **탭 수**로 바꾼다(탭은 터치 슬롭을 넘기 전에 끝나 이 경쟁을 아예 겪지 않는다).
      은닉이 필요하면 **위치**로 한다 — #84는 진입점을 *"1차 섹션 안에만 존재하는 행"* 에 둬서
      *"1차를 먼저 켜야 한다"* 를 런타임 검사 없이 **구조로** 성립시켰다.

14. **⚠️ 같은 엔타이틀먼트에 지급 경로가 둘이면, 쉬운 쪽이 설계를 무력화한다** (2026-09-03, #66의 교훈).
   무르기 영구 해금은 **3일차 출석 보상**인데(`AttendanceRewardPolicy.kt:42`, #55) 인게임 프로모션
   클레임(`FeatureAccessPolicy.kt:98` → `GamePlaySection.kt:467·496`)이 **1일차부터 아무 때나** 같은
   것을 지급한다. 두 경로 다 "확정 설계"로 문서에 적혀 있었고, **회차를 옮긴 커밋이 다른 쪽을 건드리지
   않아** 두 스레드를 지나며 살아남았다.
   · **교훈은 코드가 아니라 문서 쪽이다.** 낡은 서술이 코드의 근거로 계속 인용된 것이 원인이므로,
     정책을 옮길 때는 **그 정책을 언급한 문서·주석을 함께 grep해 정정한다**(#67이 그 몫이다).
   · ⚠️ **지급 경로를 닫을 때 판정 경로를 함께 닫지 말 것** — `FeatureAccessPolicy.kt:68`의
     `if (featureId in state.claimedFeatures)`를 지우면 이미 획득한 사용자가 전원 잠긴다.

---

## 진행 중

85. **돋보기 다듬기 — 창 크기·확대 배율을 사용자 설정으로** (AI 모델: Opus, 노력정도: 중간) [진행중]
    - **2026-09-05 실기 피드백**: *"너무 좁은 영역만 보여줘서 돋보기로 실제 착수를 하기 다소 어렵다."*
    - **⚠️ 원인이 둘이고 방향이 반대다.** 보이는 칸 수 ≈ **창 크기 ÷ 확대 배율**이라, 창만 키우고
      배율을 그대로 두면 효과가 줄고 배율만 낮추면 창이 그대로다. 그래서 **손잡이를 둘로 나눴다** —
      한쪽만 만지면 서로 상쇄된다(그 상쇄를 막는 것이 `MagnifierSettingsTest`의 첫 단언이다).
    - **기본값: 창 1.2배 · 배율 1.5배.** 이전(창 1.0 · 배율 2.0) 대비 **보이는 칸 수 1.6배**.
      사용자 지시(*"영역을 1.2배로 키우고 확대 비율은 조금 낮게"*)를 그대로 옮긴 값이다.
    - **선택지**: 창 = `기본`/`×1.2`, 배율 = `판 그대로`/`120%`/`150%`.
      · ⚠️ **배율 `1.0`을 "확대 없음"이 아니라 "판 그대로"로 적었다.** 숫자로 적으면 *"아무 효과
        없는 설정"* 으로 읽히는데, 확대가 없어도 **손가락이 가린 자리를 위에 띄워 준다** —
        #39가 이 기능을 만든 이유가 오착수 방지다. 문구 계약으로 못박았다.
    - **⚠️ 자리를 대국 중 메뉴(`KaTrainUxMenuPanel`)로 골랐다.** 사용자의 목적이 *"최적이 뭔지
      체크하면서 값 조정"* 이라 **판을 보면서 즉시 바꿔 보지 못하면 비교 자체가 안 된다.**
      이 메뉴는 대국 중에 열리고 `ChangeUxOptions`가 곧바로 반영한다.
      · 드롭다운이 아니라 **칩을 나란히** 뒀다 — 선택지가 늘 보여야 한 번의 탭으로 옮겨 다닌다.
    - **⚠️ 지름 상한에도 창 배수를 곱해야 한다.** 19줄 판에서는 **상한 쪽이 걸리므로**(칸이 작아
      첫 항이 훨씬 크다) 상한을 그대로 두면 **창이 전혀 커지지 않는다.** 계산으로 확인하고 고쳤다.
    - **⚠️ 이 항목이 만든 여유 감소 — 다음에 창을 더 키우려면 여기부터 볼 것.**
      창이 커질수록 손가락 **위에 들어갈 자리가 없어져 아래로 뒤집히는 영역이 넓어진다.**
      #39가 0.42로 잡았을 때 **판 한가운데서도 뒤집혔다.** 지금 최대는 0.32 × 1.2 = **0.384**로
      그 선 아래지만 여유가 줄었다. 테스트가 0.40을 문턱으로 잡고 있다.
    - **⚠️ 라인 예산을 880 → 882로 올렸다**(`LayeringContractTest`). 새로 저장되는 UX 옵션은
      셸의 오토세이브 요청 조립부를 지날 수밖에 없다(#39가 같은 이유로 +1했다). 사유를 그 자리에 적었다.
    - **가드**: `MagnifierSettingsTest` 5건, `UiStringsMagnifierTest` 3건(**`fun` 문구라 리플렉션
      그물 사각지대**, 함정 10번), `BoardMagnifierTest` 재작성 2건 + 목적 단언 1건.
      · ⚠️ 기존 테스트 둘이 상수(`2f`, `0.34`)를 박아 두고 있어 **설정화와 함께 뜻을 잃었다** —
        지우지 않고 **원래 의도**(배율이 판 크기에 흔들리지 않는다 / 창이 뒤집힘 문턱에 가깝지 않다)
        로 다시 썼다.
    - **실기 확인**: 메뉴에 두 줄이 뜨고, 칩을 누르면 저장이 즉시 바뀌며(`1.2`→`1.0`, `1.5`→`1.0`),
      **재시작 후에도 유지**된다. 돋보기를 실제로 띄워 창이 넓어진 것을 눈으로 확인했다.
    - 테스트 **1024건 전부 통과**, iOS 컴파일 확인.

---

## 예정사항

> **목록 순서가 곧 실행 순서다.** 표기 규칙은 앞 세대와 같다: `(AI 모델: …, 노력정도: …)` + 상태,
> `⚠️`는 착수 전에 반드시 읽어야 할 함정.
>
> ⚠️ **지금 남은 것은 #26·#18 둘뿐이고, 둘 다 콘솔 게이트에 막혀 있다.** 남은 조건은 콘솔에서
> 프로덕션 액세스가 실제로 풀렸는지 **사용자 확인**이다 — 그 전에는 결제 코드를 쌓지 않는다.
> · #74 ⓐ(구매 복원 웰컴 메시지)도 같은 게이트에 걸려 있다(진행 중 항목 참고).
>
> ⚠️ **#72는 폐기됐다**(2026-09-05 사용자 판단). #73·#75·#76은 완료됐다.

---

### 2026-09-03 발주의 잔여 — 개발자 테스트 섹션 배치에서 남은 넷

> **배치 본체(#65~#84)는 닫혔다**(봉인 문서 `260831-260905_DEV_TEST_SECTION_BUILDOUT.md`).
> 여기 남은 넷은 **성격이 서로 다르다** — #76만 지금 착수할 수 있고, 나머지 셋은 기다리는 중이다.




26. 프리미엄을 영구 구매에서 **월간 구독(3,900원)** 으로 전환 (AI 모델: Opus, 노력정도: 높음) [부분 착수]
    - ✅ **구독이 파는 것이 확정됐다(2026-09-01 사용자) — 셋이다.** ⓐ **모든 캐릭터 선택 가능**,
      ⓑ **대국 중** 프리미엄 기능 **무제한**(지금은 형세·추천 둘, **더 늘어날 여지 있음**),
      ⓒ **대국 종료 후** 프리미엄 기능(⚠️ **미구현·추가 예정** — *손실이 컸던 구간 짚어주기*,
      *그 구간부터 다시 둬보기*).
      · ⚠️ **ⓒ가 구독만의 축이라 값을 지킨다.** 캐릭터 보유 특전은 **대국 "중"** 에만, 그것도
        **매판 N회 한도**로 붙는다(2026-09-01 확정, #18 ⓑ). 특전을 넓힐 때 이 경계를 허물지 말 것 —
        허무는 순간 구독을 살 이유가 ⓐ 하나로 줄어든다.
      · ⚠️ **ⓒ는 아직 일감이 없다.** 8장이 *"대국 종료 후 분석"* 을 프리미엄 축으로 예고해 뒀을 뿐
        구현 항목이 없다 — 착수하려면 별도 항목을 발행할 것. 이 항목(#26)은 **결제 전환**이 범위다.
    - 참고: `feature-access-principles/README.md` 8장(이미 "프리미엄 = 월 구독"이 기조), `premium-mode/README.md`. **2026-08-30 사용자 확정** — 금액이 5,000~10,000원에서 **3,900원**으로 좁혀졌고, "기능이 늘면서 가치가 오르면 그때 가격도 조정" 방침.
    - **핵심 난점**: "구매 = 영구"가 타입 하나가 아니라 **상태·판정·복원·저장·문구 다섯 곳에 각각 하드코딩**돼 있다. `PremiumState.isActive`는 `Purchase -> true`라 만료를 표현할 수 없고, `PurchaseOutcome.Purchased`는 페이로드가 없어 유효기간을 실어 나를 수 없다.
    - ⚠️ **착수 순서를 지켜야 한다.** `runPremiumPurchaseApplication`은 "복원에서 미소유가 나와도 절대 강등하지 않는다"를 의도적 설계로 못박아 뒀다 — 영구 구매에서는 옳았지만 구독에서는 만료자가 영원히 프리미엄을 유지하는 결함이 된다. **그런데 그 강등을 그냥 켜면 안 된다**: `AndroidBillingClient`가 "조회 성공+미소유"와 "조회 실패"를 똑같이 `null`로 뭉개므로, 강등을 켜는 순간 **일시적 네트워크 오류가 유료 구독자의 접근권을 박탈한다.** 그 conflation을 쪼개는 것이 1번이다.
    - ⚠️ `AndroidBillingClient`는 프리미엄(SUBS 예정)과 봇 캐릭터(INAPP, #18)가 **공유**한다. ProductType을 클래스 안에서 전역 치환하면 캐릭터 구매가 조용히 깨진다 — 지금 그 플래그가 꺼져 있어 테스트로도 안 드러난다. 생성자 파라미터로 올릴 것.
    - ⚠️ `PremiumSource.Purchase`를 **개명하지 말 것.** 저장소 디코드가 모르는 이름을 소리 없이 `None`으로 떨어뜨려 기존 사용자의 프리미엄이 사라진다. 개명 없이 의미만 바꾸면 이 위험이 통째로 사라진다.
    - **버튼 표기(#24)**: 구독도 `(∞)` 유지를 권한다 — 체감이 무제한이고, `(⏱)`는 로비 카운트다운과 짝이라 구독에 주면 "1시간 남음"으로 오독된다. 다만 사용자 확정 사안이므로 착수 시 승인받을 것.
    - **신규 UI가 필요하다**: 구독 관리/해지 딥링크가 지금 앱에 **전혀 없다**(구글 정책 요구). 마이 페이지(#24로 신설됨)가 그 자리다.
    - **ⓐ는 해소됐다(2026-08-30 사용자 확인): 영구 프리미엄 보유자가 아무도 없다.** 지금까지 비공개 테스트만 했고, 설령 누가 갖고 있더라도 **비공개 테스트 중에 얻은 권한이라 실제 오픈 시 초기화까지 고려 대상**이라는 방침이다. → **기존 보유자 마이그레이션을 설계할 필요가 없다.** 이 항목에서 가장 까다로울 뻔했던 축이 통째로 사라진 셈이라, `PremiumSource.Purchase`의 의미를 "영구"에서 "구독 유효"로 **그대로 바꿔도 안전하다**(개명만 하지 않으면 된다).
      · ⚠️ 다만 **"초기화를 고려한다"와 "초기화한다"는 다르다.** 실제 오픈 시 테스트 기간 권한을 밀 것인지는 아직 확정이 아니다 — 밀기로 한다면 그 시점에 별도 일감이 필요하다(저장소의 `PremiumState`를 어떤 조건으로 리셋할지, 무르기 클레임 같은 **별개 축**은 남길지).
    - 착수 전 확인(사용자): **ⓑ Play Console 수익 창출 잠금**(개인정보처리방침·IARC·데이터 보안)이 풀려야 구독 SKU를 만들 수 있다 — 그 전에는 실기 검증 불가.
    - ⚠️ **스토어 등록정보는 배포 시점에 바꾼다.** `store_listing.txt`가 "앱 내 결제 없음"을 세 번 단언하는데, 코드가 머지돼도 플래그가 꺼져 있는 동안은 그 문장이 **참**이다. 미리 바꾸면 스토어가 거짓을 말하고, 배포 때 잊으면 정책 위반이 된다.
    - **✅ 2026-08-31 부분 착수 — 착수 순서 1·2번을 끝내고 나머지는 의도적으로 파킹했다.**
      · **파킹 사유(사용자 결정)**: 남은 부분은 **결제 상태를 만지는 가장 위험한 코드**인데
        Play Console 수익 창출이 잠겨 있어 **한 줄도 실기 검증이 안 된다.** 카운트다운 완료 예정이
        ~2026-09-02(`launch-plan/README.md` §0 A-1)이므로, **코드를 쓰면서 바로 검증할 수 있는
        그때 이어 잡는다.** 미검증 결제 코드를 쌓아 두지 않는 것이 이 항목의 위험 관리다.
      · **1번 — conflation을 쪼갰다.** `queryOwnedPurchaseOnce`가 *"Play가 미소유라고 답함"* 과
        *"조회가 실패함"* 을 **둘 다 `null`로** 반환하고 있었고, 그것이 `NotFound`로 접혔다.
        `OwnershipQuery`(Owned/NotOwned/Unknown) 세 갈래 + 새 사유
        `PurchaseFailureReason.OwnershipUnknown`으로 갈랐다.
        · ✅ **오늘 당장 고친 결함이 하나 있다**: 조회가 실패해도 진단 로그가
          `premium_purchase_restore_not_found`("소유한 구매 없음")라고 **거짓을 기록**하고 있었다.
          이제 `restore_unverified`로 갈리고 심각도도 Info/Warning으로 나뉜다.
        · **강등의 유일한 관문을 함수로 만들었다** — `isAuthoritativeNotOwned(trigger)`는
          `Restore × NotFound` 한 조합에서만 참이다. 지금은 로그 심각도를 가르는 데 쓰이고,
          강등 로직이 들어올 때 **반드시 이 함수를 지나야 한다.**
      · **2번 — `ProductType`을 생성자로 올렸다.** 어댑터를 프리미엄(SUBS 예정)과 캐릭터 구매
        (INAPP, #18)가 **공유**하므로, 클래스 안에서 상수로 치환하면 캐릭터 구매가 조용히 깨진다
        (그 플래그가 꺼져 있어 실행 테스트로도 안 드러난다).
        · ⚠️ **파 보니 함정이 하나 더 있었다**: **구독은 오퍼 토큰이 `subscriptionOfferDetails`에
          있고 `oneTimePurchaseOfferDetails`는 항상 `null`이다.** 종류만 바꾸고 그 자리를 두면
          구독이 **"상품 없음"으로 조용히 실패**하는데, 사용자에게 보이는 문구는 "지금은 구매할 수
          없어요"라 원인이 드러나지도 않는다. 고르는 규칙을 순수 함수(`billingOfferToken`)로
          떼어 테스트로 고정했다 — 실기로 못 밟는 지금 이것이 유일한 장치다.
      · **✅ 버튼 표기(#24)는 `(∞)` 유지로 확정됐다(2026-08-31 사용자 결정).** 구독이 유효한
        동안은 체감이 무제한이고, `(⏱)`는 로비 카운트다운(광고 1시간)과 짝이라 구독에 주면
        **"1시간 남음"으로 오독된다.** 더 이상 미정이 아니다.
        · ✅ **2026-09-03에 그 이유가 한 줄로 정리됐다**: `∞`가 말하는 축은 **횟수**이지 기간이
          아니다 — 그래서 구독이어도 `(∞)`가 맞고, 유효기간은 **버튼이 말할 일이 아니다.**
          `UiStrings.featureButtonLabel`의 KDoc에 박아 뒀다.
        · ⚠️ **그래서 판정도 "영구히 샀는가"가 아니라 "지금 유효한가"여야 한다** — 아래 ⓐ가 그 축이다.
      · **남은 것(콘솔이 열린 뒤)**: ⓐ 유효기간을 `PremiumState`·`PurchaseOutcome`·저장에 표현,
        ⓑ `isAuthoritativeNotOwned`를 관문으로 강등 활성화, ⓒ 구독 관리/해지 딥링크 UI(마이 페이지),
        ⓓ 콘솔 구독 SKU 등록, ⓔ 배포 시점에 `store_listing.txt`의 "앱 내 결제 없음" 수정.
      · 산출물: (shared) `application/premium/PurchasePort.kt`(`OwnershipUnknown` 신설),
        `application/premium/PremiumPurchaseApplication.kt`(진단 분기 + `isAuthoritativeNotOwned`).
        (app-android) `ui/AndroidBillingClient.kt`(조회 세 갈래 + `productType` 매개변수 +
        `billingOfferToken`). 테스트: `PremiumPurchaseApplicationTest`(2건 신설),
        `AndroidBillingClientTest`(3건 신설).

---

---

18. 봇 캐릭터 개별 구매 배선 — 5단계 관장 천원, **4,900원** 단발성 결제·영구 소유 (AI 모델: Opus, 노력정도: 높음)
    - ✅ **가격은 4,900원으로 확정됐다(2026-09-01 사용자 재확인).** ⚠️ **미정이었던 적이 없다** —
      킥오프 플랜의 구현 결정 각주가 이미 *"가격은 4,900원으로 확정(9,900원에서 인하, 사용자 결정)"*
      (백로그 #18, 2026-08-29)이라고 적고 있었고 코드 주석도 4,900원이었다. 갈려 있던 것은 결정이
      아니라 **전파**였고, 이 항목에 남아 있던 9,900원 표기를 그날 정리했다.
    - ✅ **판매 모델 확정(2026-09-01 사용자)** — **구매는 무료 경로를 대체하지 않고 병렬로 붙는다.**
      출석 보상 캐릭터도, 광고 조각 캐릭터도 **유료로 함께 판다**(기다리기·광고 보기 싫은 사용자용).
      순수 유료 **전용** 캐릭터는 나중에 새로 추가한다. → **팔 대상은 첫돌이를 뺀 네 종 전부**다.
      · ⚠️ **이 항목은 2026-09-01에 한 번 틀리게 적혔다** — *"팔 캐릭터가 없으니 천원을 되돌리거나
        유료 전용을 새로 만들어야 한다"* 는 이지선다로 썼는데, 그건 `unlockSource`가 단일 값이라는
        **코드 제약을 정책으로 착각한 것**이다. 사용자 정책은 둘 다이고 병렬이다.
    - ⚠️ **그래서 막는 것은 "팔 게 없다"가 아니라 "모델이 두 축을 표현하지 못한다"다.** 착수 전에
      `feature-access-principles/README.md` **8.3-2**를 반드시 읽을 것. 셋이 걸린다:
      · **ⓐ `BotCharacter.unlockSource`가 단일 값**이고 `Purchase` KDoc이 *"구매로만 열리는 캐릭터"* 라
        못박고 있다 — 그대로 팔면 출석·광고 경로가 사라진다. 무료 경로 × 구매로 축을 갈라야 한다.
      · **ⓑ ✅ 특전이 확정됐다(2026-09-01 사용자) — 무제한이 아니라 매판 한도다.**
        **첫돌이를 제외한 캐릭터를 1종 이상 보유하면 매 판 N회(현재 3)** 프리미엄 기능을
        **1회권보다 먼저** 쓴다. **획득 방법(유료·광고·출석)은 가리지 않는다.**
        · ⚠️ 같은 날 나왔던 중간안(*"보유한 모두에게 무제한"*)은 **폐기됐다** — 돌뫼가 광고 5회로
          열려 **광고 5회 = 영구 무제한**이 되어 월 3,900원 구독과 겹쳤다. 매판 한도가 그걸 닫는다.
        · ✅ 첫돌이는 `Default`라 `claimedBots`에 안 들어가 자연히 빠진다. **판정을 `isClaimed`
          기준으로 둘 것 — `isAvailable`로 바꾸면 기본 상대에 특전이 붙는다.**
        · ⚠️ **판정 축이 바뀐다** — 지금 `isBotCharacterPerkActive`는 *"상대가 내가 산 캐릭터인가"* 를
          묻는데, 새 조건은 **보유 여부**라 상대와 무관하다.
        · ⚠️ **매판 잔여를 세션 스냅샷에 저장할 것 — 안 하면 앱 재시작이 무한 리필이 된다.**
          ✅ 그 코덱은 `optInt(키, 기본값)` 방식이라 **스키마를 안 올리고 필드를 더할 수 있다**(확인함).
          ⚠️ 번호를 올리면 **진행 중 대국이 통째로 날아간다.**
        · ⚠️ **`alreadyAllowedVia`가 지금은 특전을 보고 차감을 건너뛴다** — 새 정책에서는
          **건너뛰는 대신 잔여를 1 깎아야** "우선 소진"이 성립한다.
        · ⚠️ **#44의 지불 원장(`OneShotLedger`)을 특전 잔여도 지나게 할 것** — 아니면
          *"켜기 → 끄기 → 켜기"* 에 3회가 순식간에 닳는다.
        · ⚠️ **N=3은 "현재 구상"이다**(사용자 명시). 상수 하나로 두고 문서와 코드가 같은 값을 볼 것.
        · **미확정 둘**: 여러 종 보유 시 한도가 늘어나는지(정액 3으로 해석함), 그 캐릭터와 둘 때만인지
          아무 대국에서나인지(보유 기준이라 아무 대국으로 해석함). 상세는 8.3-2.
      · **ⓑ' ✅ 획득 방법은 분리 저장한다(2026-09-01 사용자).** 특전이 이제 획득 방법을 **가리지
        않지만** 저장은 한다 — *"무료 보상인지 유료 구매인지에 따라 나중에 정책을 추가할 여지"* 다.
        **특전과 독립적인 이유이므로, 특전이 이 값을 안 본다고 저장을 지우지 말 것.**
      · **ⓒ ⚠️ 유령 보상.** `isAlreadySatisfied`가 조각만 걸러내고 캐릭터 지급은 안 걸러서, 이미
        보유한 캐릭터 회차에서 팝업만 뜨고 아무 일도 안 일어난다. 지금은 도달 불가능하지만
        **판매를 여는 순간 도달 가능해지고**, 7일차는 캐릭터뿐이라 통째로 빈손이 된다.
        해법은 *"이미 보유한 캐릭터 보상은 동등 가치 소모품으로 대체"* 를 **일반 규칙**으로 두는 것.
    - **2026-08-30 이관.** 출시 백로그의 유일한 미완 항목이었지만 이번 비공개 테스트 릴리즈에는
      필요 없다 — 플래그가 꺼져 있고 카탈로그에 유료 캐릭터가 없어 배선 전체가 잠들어 있다.
      **코드는 이미 머지돼 있다.** ⚠️ 이 줄은 2026-08-30에 *"남은 것은 아래 ⓐⓑⓒ 세 줄뿐"* 이라고
      적었지만 **그때도 틀린 말이었다** — #23이 2026-08-29에 5단계를 유료에서 출석으로 옮긴 뒤로
      `unlockSource`가 단일 값이라 **그 캐릭터를 "출석으로도 열리고 살 수도 있다"로 표현할 방법이
      없었고**, `canPurchase`가 누구에게도 참이 되지 않았다. 지금 남은 것은 **ⓐ~ⓓ 네 줄**이고,
      실제로 막고 있는 것은 **ⓓ**다(아래 참고).
    - 참고: 7장 획득 경로 표. **Phase 1 범위 확장이다** — 7장이 원래 "범위 밖(설계만 고려)"으로 못박아 뒀던 항목인데 2026-08-24에 범위 안으로 들어왔다.
    - `BotUnlockSource.Purchase`를 신설한다(#8에서 "지금 만들지 않았다"고 미뤄 둔 타입 — sealed라 추가하면 기존 `when`이 컴파일 에러로 빠진 처리를 잡아준다). **비소모성 단발 결제라 이미 검증된 `premium_lifetime`의 `AndroidBillingClient`(INAPP) 패턴을 그대로 재사용할 수 있다.**
    - 착수 전 확인(사용자): **Play Console 신규 상품 등록은 사용자 몫**이다(상품 ID·가격 **4,900원** 설정, 라이선스 테스터). 등록 전에는 실기 검증이 불가능하므로 착수 시점에 등록 여부를 먼저 확인할 것.
    - **구현 완료(2026-08-29) — 다만 결제 자체는 검증 불가**:
      · 결제 배선: `AndroidBillingClient(activity, productId)`가 이미 상품 ID로 매개변수화돼 있어 그대로 재사용했다. 상품 ID는 `local.properties`의 **`billing.botCharacterProductId`** 키로 주입하며, 없으면 플레이스홀더로 폴백해 "상품 없음"으로 안전하게 실패한다(프리미엄 상품과 같은 패턴).
      · 플래그: **`FeatureFlags.isBotCharacterPurchaseEnabled`(기본 `false`)** 를 신설했다. `isPurchaseEnabled`와 **일부러 분리**했다 — 그건 프리미엄 영구 구매를 가리는데, 프리미엄은 월 구독으로 가기로 확정돼 있어 캐릭터를 팔자고 그것까지 되살릴 이유가 없다.
      · 특전: `AllowedVia.CharacterPerk` 신설 + `PremiumUiState.resolve` 한 곳에서 접었다. 8.3절이 걱정한 "호출부 전부가 바뀐다"는 일어나지 않았다 — 인게임 게이팅이 이미 그 람다 하나로 모여 있었다.
      · ⚠️ **1회권 차감 방지가 이 항목의 숨은 핵심이다.** 특전을 소모품 판정(`alreadyAllowedVia`)까지 흘려보내지 않으면, 특전으로 이미 열려 있는데 표가 조용히 줄어든다. 테스트로 고정했다.
      · 재설치 복원: 산 캐릭터가 재설치로 사라지지 않도록 시작 시 복원 조회를 넣었다(`buildBotCharacterUiState` 안 — `GoCoachApp.kt` 라인 예산을 쓰지 않으려는 이유도 있다).
    - **실기 검증(에뮬레이터)**: 관장 천원을 소유로 심고 프리미엄 없이 대국 → 형세 보기가 열리고 영역 오버레이가 렌더되며 **1회권 3장이 그대로**. 상대를 첫돌이로 바꾸면 잠금 테두리가 돌아온다(특전이 새지 않는다).
    - ⚠️ **결제 부분은 Play Console 게이트에 막혀 보류됐다(2026-08-29)**. 콘솔의 "수익 창출"이 **"대시보드에서 앱 설정을 완료하세요"** 로 막혀 상품 등록 자체가 불가능하다 — 비공개 테스트 중이라서가 아니라, **앱 설정 대시보드의 미완료 항목(개인정보처리방침 필드·IARC 콘텐츠 등급·데이터 보안 양식)이 수익 창출의 선행 조건**이기 때문으로 보인다. 판매자(결제 프로필) 설정이 별도로 필요할 가능성도 남아 있다. 그 셋은 이미 `launch-plan/README.md` §0의 남은 콘솔 작업과 같은 목록이라, 하나를 끝내면 둘이 같이 열린다.
    - **그래서 결제는 미루고 5단계를 출석 보상으로 돌린다(2026-08-29 사용자 결정)** — 별도 항목으로 발행. 이 항목의 **특전·결제 배선 코드는 지우지 않고 그대로 둔다**: 카탈로그에 `BotUnlockSource.Purchase` 캐릭터가 없어지면 `isBotCharacterPerkActive`가 항상 `false`라 조용히 잠들 뿐이고, 유료 상품을 다시 열 때 그대로 깨어난다.
    - 남은 것(콘솔이 열린 뒤): ⓐ 4,900원 상품 등록 + 라이선스 테스터, ⓑ 상품 ID를 `local.properties`의 `billing.botCharacterProductId`에 기입, ⓒ `isBotCharacterPurchaseEnabled`를 `true`로, **ⓓ 획득 모델을 두 축으로 가르고(무료 경로 × 구매) 위 ⓑ·ⓒ를 함께 처리할 것** — 이게 빠지면 ⓐ~ⓒ를 다 해도 아무 일도 안 일어난다.
    - 산출물(승인 대기): (shared) `application/botcharacter/BotCharacterPerk.kt` 신규, `application/premium/FeatureAccessPolicy.kt`(`CharacterPerk`·`characterPerkActive`), `application/consumable/ConsumableSpendApplication.kt`(특전 반영). (app-android) `build.gradle.kts`·`ui/FeatureFlags.kt`·`ui/PremiumPurchaseGlue.kt`·`ui/PremiumUiState.kt`·`ui/BotCharacterUiState.kt`·`ui/PlayerSetupPanel.kt`·`ui/UiStrings.kt`·`ui/GoCoachApp.kt`. 테스트 9건 신설, `LayeringContractTest` 라인 예산 851→856(사유는 그 주석에).
    - ⚠️ **프리미엄 월 구독 전환과 혼동하지 말 것** — 그건 이 백로그 밖(`premium-mode` 트랙)이고, 이 항목은 캐릭터 1종을 파는 별개 상품이다.
    - ⚠️ **아래 줄은 2026-09-01에 대체됐다 — 위 ⓑ가 정본이다**(무제한 → 매판 N회, 상대 기준 → 보유 기준).
    - ~~**구매 특전도 이 항목의 몫이다(2026-08-29 추가)**~~: 구매자는 **그 캐릭터와 두는 동안** 형세 보기·추천 수를 무제한 쓴다. ⚠️ 지금의 `FeatureAccessPolicy`는 전역 판정이라 이걸 표현할 수 없다 — 착수 전 정해야 할 세 가지가 `feature-access-principles/README.md` 8.3절에 있다.

---

## 완료 이력 (한 줄 + 커밋)

> **상세는 여기 두지 않는다.** #63~#84의 착수 전 함정·구현 결정·실기 검증표는 봉인 문서
> `260831-260905_DEV_TEST_SECTION_BUILDOUT.md`의 같은 번호에 그대로 있고, #43~#62는
> `260830-260831_POST_LAUNCH_ENHANCEMENTS.md`, #1~#42는 그 앞 세대 문서에 있다.
> 필요한 항목만 번호로 찾아 볼 것 — 통째로 읽지 말 것.
>
> 이 표의 순서는 **완료 순서**다(위가 먼저).

| # | 무엇을 했는가 | 커밋 |
| --- | --- | --- |
| 74 ⓒ | 마이 페이지에 소실 정책 고지(*"앱을 지우면 사라져요"*, 4개 언어). ⓐⓑ는 구매 게이트 대기 | `8432e66` |
| 75 | 진행 중 대국에서 판 크기·접바둑 잠금 — 저장된 대국까지 봐야 재시작 우회가 막힌다 | `8432e66` |
| 72 | *(폐기)* 다음 캐릭터 회차 건너뛰기 — **사용자 판단: 불필요**(2026-09-05). *"28번 누르기 정도는 직접 해야 함"* | — |
| 76 | 사문화된 `showSettings` 경로와 심플 레이아웃 패널 삭제(순삭 292줄). 고아 문구는 여섯이 아니라 **셋**이었다 | `f714f60` |

---

## 이 문서를 쓰는 규칙

> 이 넷은 앞 세대 문서가 1,501줄(그중 **91%가 완료 항목**)까지 자라 정작 읽어야 할 84줄이 맨 아래
> 깔린 사고에서 나왔다. 규칙 ②가 그 재발을 막는 장치다.

① **봉인 문서는 아무것도 받지 않는다.** 여기서 완료된 항목을 앞 세대 문서로 옮기지 말 것.
   상태 기준으로 파일을 가르면 항목이 진행될 때마다 왕복 이관이 생긴다 — 완료된 것은 **이 문서에
   한 줄로 남는다.**

② **완료 항목은 "한 줄 + 커밋 해시"로만 적는다.** 구현 결과 전문을 이 문서에 남기지 않는다.
   앞 세대에서 완료 항목 하나가 50~100줄을 차지했고, 그중 **산출물 파일 목록 94줄**은
   `git log --stat`의 열등한 복사본이었다. 상세가 필요하면 커밋을 보면 된다.
   · ⚠️ **예외는 함정뿐이다.** 다음 작업이 밟게 될 함정은 위 "⚠️ 다음 작업이 반드시 알아야 할
     함정" 절로 끌어올린다 — 그것만은 `git log`로 되찾을 수 없다.

③ **살아 있는 항목의 상세는 줄이지 않는다.** 예정사항·진행 중 항목은 전문을 유지한다. 그 상세는
   히스토리가 아니라 **다음 작업의 입력**이다.

④ **출시 실행 상태는 여기 적지 않는다.** 콘솔 폼·AAB·스크린샷·`release` 실기는
   `launch-plan/README.md` §0이 정본이다. 여기에는 코드 일감만 온다.

### 봉인 문서를 언제 지울 것인가 — 아직 아니다 (2026-08-31 판단)

**지우지 않았다.** 저장소의 문서 보존 정책(`DOCS_INDEX.md` "문서 보존 정책")은 *"완료되고 결론이
상위 문서에 흡수된 문서는 그냥 삭제한다, git 히스토리가 아카이브다"* 이지만, 그 정책의 전제는
**"실제로 다시 읽히는 일 없이 쌓여만 있었다"** 이다. 봉인 문서는 그 전제를 지금 만족하지 않는다:

- **#47의 R8 / enum 저장 포맷 검증**은 #26(결제 상태 저장)과 §0 B-3(`release` 실기)이 직접 밟는다.
- **#48~#52·#55~#57·#59의 화면 변경 상세**는 §0 B-2(스크린샷 4장 재캡처)가 "어느 컷이 왜 낡았나"를
  판정한 근거다. §0의 흡수본은 *낡았다*까지만 말하고 **무엇이 어떻게 바뀌었는지는 원본에만 있다.**
- 문서가 **어제 만들어졌고 이틀 뒤 작업이 직접 참조한다.**

**삭제 판단은 출시 이후로 미룬다.** 조건은 사용자가 제시한 그대로다 — 상위 문서(`launch-plan`·
`DOCS_INDEX`·`ENGINE.md`·스펙 문서)로 **노하우와 결정사항이 놓치는 것 없이 흡수된 상태를 확인한
뒤에** 지운다. `launch-plan/`은 출시와 함께 역할이 끝나므로(`DOCS_INDEX.md`) 그때가 자연스러운
점검 시점이다.

---

---

## 관련 문서

- `260830-260831_POST_LAUNCH_ENHANCEMENTS.md` — 앞 세대(#43~#62). **봉인** — 완료 항목의 상세 근거
- `260823-260830_OFFLINE_ENGAGEMENT_FEATURES_BACKLOG.md` — 그 앞 세대(#1~#42). 완결
- `260823-260830_OFFLINE_ENGAGEMENT_FEATURES_KICKOFF_PLAN.md` — 설계/스펙 원본과 구현 결정 각주.
  **2장·3장은 모든 항목의 공통 전제**
- `launch-plan/README.md` — §0이 **출시 트랙의 정본**(콘솔·자산·빌드 현황)
- `feature-access-principles/README.md` — 8장이 "프리미엄 = 월 구독" 기조. #26의 전제
- `premium-mode/README.md` — 프리미엄 기능 매트릭스
- `260823-_DAU_GROWTH_IDEAS.md` — 아이디어 풀. 새 트랙이 여기서 갈라져 나온다
