<?xml version="1.0" encoding="utf-8"?>
<!--
  ⚠️ 이 파일은 **런처 라벨 전용**이다(백로그 #97·#103). 앱 안의 문구는 전부
  `UiStrings.kt`(+ Ko/En/Ja/Zh)가 갖는다 — 두 체계를 섞지 말 것.

  왜 여기에만 안드로이드 리소스를 쓰는가: **런처 라벨은 앱이 실행되기 전에 시스템이 읽으므로**
  `UiStrings`로는 표현할 수 없다. 그것이 이 파일의 유일한 존재 이유다.

  ⚠️ **런처 라벨은 앱 내 언어 설정을 따르지 않고 시스템 로케일을 따른다.** 앱 안에서 일본어를
  골라도 아이콘 이름은 기기 언어를 따라간다 — 안드로이드 리소스의 동작이고 `UiLanguageStore`는
  여기에 관여하지 않는다.

  ⚠️ **여기에 문자열을 더하지 말 것.** 예전에는 엔진 준비 화면의 두 줄(`preparing_engine`,
  `engine_copy_notice`)도 여기 있었다 — 그 화면이 `ProvideUiLanguage` **바깥**에서 그려져
  `LocalUiStrings`에 닿지 못했기 때문이다. #101 ③에서 준비 화면 자체를 없애(홈이 곧 랜딩) 두 줄이
  고아가 됐고 #103에서 지웠다. 앱이 그린 화면이 이 파일을 필요로 한다면 그것은 번역이 필요하다는
  신호가 아니라 **`ProvideUiLanguage` 밖에서 그리고 있다는 신호**다 — 화면을 안으로 옮길 것.
-->
<resources>
    <!--
      앱 이름. ⚠️ **스토어 등록정보의 이름과 같아야 한다**(2026-09-05 사용자 결정) —
      정본은 추적되는 `design-handoff/export/`의 `store_listing.txt`이고(`dist/`는 gitignore 대상),
      지금 값은 "포켓 바둑 코치"다(2026-09-11). ⚠️ **스토어 제목과 일부러 다르다** — 스토어는 부제까지 붙은 "포켓 바둑 코치 - 오프라인 AI 대국·복기"이고, 런처에는 브랜드만 남긴다. `AppNameContractTest`가 "스토어 제목이 이 이름으로 시작하는가"로 둘을 묶는다(같은 값을 요구하지 않는다).
      ⚠️ **"코치"가 다시 들어갔다(2026-09-06)** — 형세 보기·추천 수가 이미 코치 역할을 하고
      있어, 실제 코칭 기능이 더 늘어난 뒤에 이름을 바꾸는 쪽이 오히려 부자연스럽다는 사용자
      판단이다. 정식 출시 시점에 미리 반영한다.
      ⚠️ **브랜드가 아니라 서술이라 언어마다 번역한다** — `values-en/ja/zh`에도 있다.
      앱 안 타이틀(`UiStrings.appTitle`)과 같은 값을 유지할 것.
    -->
    <string name="app_name">포켓 바둑 코치</string>
</resources>
